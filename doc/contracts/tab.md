# Component: tab
tier: T1
category: navigation
shared-css-file: src/main/resources/web/js/zul/tab/css/tabbox.css
siblings: [tabbox, tabpanel]
preview: http://localhost:8080/tabbox.zul

## References
See tabbox contract.

## DOM key selectors
```
.z-tab / .z-tab-content / .z-tab-text / .z-tab-button
.z-tab-selected / -disabled
```

## Expected values
Tab-scoped subset of tabbox checks (c1–c6).

## States to evaluate
- [ ] default, hover, selected, disabled
