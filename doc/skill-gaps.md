# Skill Gaps Log

Append-only record of ZK features that the harness missed and how we structurally caught them. See `doc/skill-feedback-loop.md` for the workflow.

## Columns

- **date**: ISO date the gap was identified
- **component(s)**: which component(s) the gap affects (comma-separated when shared)
- **gap**: one sentence — what was wrong
- **why-harness-missed-it**: one sentence — why the existing process didn't catch it
- **layer**: `skill` / `contract` / `agent` (or combination, e.g. `skill+agent`)
- **fix-location**: relative paths or section refs where the fix landed
- **retrospective**: one sentence after the fact — did the layered fix catch siblings, or did more gaps surface?

## Log

| date | component(s) | gap | why-harness-missed-it | layer | fix-location | retrospective |
|------|--------------|-----|------------------------|-------|--------------|---------------|
| 2026-07-02 | button | Filled/contained button hover was under-built: only an 8% white state-layer overlay (`--zk-state-hover-opacity`) with **no elevation change**, so the hover reads as almost no change (user reported "差距不大"). MD3 filled buttons raise elevation 0→1 on hover *in addition to* the state layer, and MUI (this theme's stated visual target) darkens the background too — the theme did neither. | Contract c11 described the hover as "darker tint of c8 (state layer 0.08)" — self-contradictory, because the overlay color is white/on-primary and thus *lightens* on a blue button, not darkens; and no row ever asserted a hover `box-shadow`/elevation delta, so the evaluator only measured overlay opacity and nothing flagged the missing lift. No MD3-vs-MUI reconciliation existed for the hover, so the softest possible reading (overlay only) shipped. | contract | CSS `js/zul/wgt/css/button.css` — filled-scoped `:hover` rule adds `box-shadow: var(--zk-elevation-2)` + bumps overlay opacity to 0.12, excluding outlined/text/icon/FAB/disabled via `:not([class*=…])`. Contract `doc/contracts/button.md` — corrected c11 wording (white/on-primary state layer ~0.12, **lightens** not darkens) + new c29 (`.z-button:hover` box-shadow == elevation-2 for filled/contained; flat & FAB variants unchanged). Decision-rule: theme-design (fails survives-theme-rewrite test) → contract layer, no skill change. | 2026-07-02 verified live on /button.zul via Playwright hover probe: contained (`.z-button-secondary`) and filled-default box-shadow go resting→`--zk-elevation-2` on hover with overlay 0→0.12; outlined stays `box-shadow: none` and keeps the canonical 0.08 overlay (exclusion selector works). Image swap confirmed (both `<img>` src = DisketteBlack). |
| 2026-06-29 | colorbox | The picker CSS reached into zkex.jar's sprite assets via hardcoded `/zkau/web/zkex/img/colorbox/*.{png,gif}` URLs (a DSP-free rewrite of stock's `${c:encodeURL(...)}`), creating a hidden coupling to zkex's asset layout and breaking under any non-root servlet context-path — the absolute URL omits the context path that `encodeURL` would prepend (it returns early via the `~.` ExtendletContext branch in `Encodes.encodeURL0`). | The evaluator only checks computed styles on the root-context preview server, where `/zkau/web/...` happens to resolve; it never verifies resource-URL robustness (context-path / CDN / version stamp) nor flags cross-jar asset dependencies, so the latent breakage was invisible. | impl + skill-note | CSS `js/zkex/inp/css/colorbox.css`: SV square + hue bar redrawn as CSS gradients (`.z-colorpicker-overlay` two-layer white/black, `.z-colorpicker-bar` 360°→0° spectrum), the two marker GIFs inlined as data URIs (`.z-colorpicker-circle` / `-arrows`); 0 external image refs. JS-orthogonality: `Colorpicker.ts` only sets overlay `background-color` (hue tint) + marker `top/left`, never `background-image`; gradients aligned to its `sat=x/255` / `val=1−y/255` / `hue=(256−y)×359.99/255`. | Verified via Playwright probe (computed bg = gradients/data URIs, `overlayColor=rgb(255,0,0)` confirms the JS hue tint still drives the SV square) + screenshot. Reusable for any EE component hardcoding zkex/zkmax sprite URLs — prefer CSS gradients for gradient sprites + data URIs for tiny markers to stay DSP-free, context-path-safe, and independent of the source jar's asset layout. |
| 2026-06-25 | dropupload | Phantom states: the theme styled and contract-asserted `.z-dropupload-active` (drag-over highlight, rows c4/c5) and `.z-dropupload-disabled`, but ZK never emits either class — `dropupload-active`/`dropupload-disabled` appear nowhere in the ZK10 tree, drag feedback is the native cursor `dataTransfer.dropEffect='copy'` only (`Dropupload.ts`), and `Dropupload.java` has no `setDisabled`. | Drop-zone hover/active is an MD3 intuition with no ZK source check; `detection` toggles content/visibility (not a state class) so the box looks identical in every mode. Contract c4/c5 asserted a class the evaluator only ever measured via injection, so nothing contradicted it; `state-coverage-audit.md` listed a "drag-over highlight" that does not exist. | skill + contract + impl | Skill `.claude/skills/zk-component-rules/components/dropupload.md` (new — no state classes, native-cursor drag feedback, no disabled); Contract `doc/contracts/dropupload.md` (drop c4/c5 + active selector); CSS `js/zkmax/wgt/css/dropupload.css` (delete `.z-dropupload-active`/`-disabled` + orphaned transition); Audit `doc/state-coverage-audit.md` (correct dropupload row); Test `screenshot.spec.ts` › `dropupload` › `no-phantom-state-rules`. | Fixed & verified: failing-first test found `.z-dropupload-active`/`-disabled` rules in the loaded stylesheets; passes after the CSS delete + rebuild; preview renders Default + `detection="self"` with no 500. |
| 2026-05-13 | datebox, timebox, spinner, bandbox, combobox, textbox, intbox, longbox, decimalbox, doublebox, passwordbox | `inplace` state never styled | MD3 has no `inplace` concept; contracts had no `inplace` check; Generators relied on MD3 reference alone | skill + agent | `.claude/skills/zk-component-rules/reference/inplace-state.md`, generator §1.3, evaluator §1.5 | Caught all 11 input components in one sweep |
| 2026-06-12 | menubar | Scrollable-menubar scroll arrows were invisible + mis-placed: icon color `--zk-color-on-primary` (white) sat on the light `surface-container` bar, and the arrows (`position:absolute; left/right:0`) were inset 16px because `.z-menubar`'s own `padding: 0 16px` shifts the abs-positioned containing block — so they floated inside the bar instead of flush at its edges. | No theme reference (MD3/MUI) ships a "menubar" — its scroll arrows have no analog the Evaluator measures (MUI's nearest is TabScrollButton, never cross-referenced). The scrollable mold (`onCreate→setScrollable(true)`) was only exercised in one demo and the contract had no scroll-arrow rows, so neither color nor position was asserted. | skill + contract | Skill: `components/menubar.md` (new "Scrollable menubar" section — abs-positioned arrows + padding-reset rule + icon-inherits-bar-color rule); Contract: `doc/contracts/menubar.md` c8–c11 (scroll icon color == on-surface, arrows flush at bar edges, padding reset, hover state-layer on-surface); CSS `menu/css/menu.css`. | |
| 2026-06-12 | menubar | Leading space before first horizontal item questioned vs MD3. | No menubar analog in MD3/MUI; contract never asserted the leading gutter, so "is 16px correct?" was undecidable from the harness. | contract | Resolved NOT a bug: 16px leading gutter matches MUI `Toolbar` (`padding-left:16px`) for an app-bar surface. Documented as contract c12. No CSS change. | |
| 2026-06-12 | menubar | Scroll arrow icon pinned to the TOP of its track instead of vertically centered. | ZK JS-vs-CSS conflict: `_fixScrolling` sets `arrow.style.display='block'` inline, overriding the arrow's CSS `display:inline-flex`, so `align-items/justify-content:center` silently do nothing and the inline icon flows to the top. No MD3/MUI menubar analog; the evaluator only checked icon color/x-position, never icon vertical-center vs its track. The flex CSS *looked* correct in source, masking that the framework neutralizes it at runtime. | skill + contract | Skill `components/menubar.md` (scrollable section: `_fixScrolling` forces `display:block` inline → flex centering dead → center icon via abs-position/line-height); Contract `doc/contracts/menubar.md` c13 (icon centerY == arrow centerY ±2px); CSS `menu/css/menu.css` (`.z-menubar-icon` abs-centered in the absolute arrow). | |
| 2026-05-14 | combobox, datebox, timebox | `buttonVisible="false"` button stays visible. Live DOM confirms ZK adds `.z-{c}-disabled` to the button element for ALL six supported components (combobox, bandbox, datebox, timebox, spinner, doublespinner); only combobox/datebox/timebox CSS files lack the `.z-{c}-button.z-{c}-disabled { display: none }` rule. Original user report ("only doublespinner correct") was based on visual scan that didn't include bandbox/spinner — those two actually work. | Skill `reference/buttonVisible-attribute.md` exists and is correct, but contracts never checked the "No-button" cell independently. Generator/Evaluator assumed siblings sharing a CSS file have uniform coverage — they don't, because each input's CSS file is separate (`combobox.css`, `datebox.css`, `timebox.css`) even though they contract into `combo.css.dsp`. | skill + contract + agent | Skill: `reference/buttonVisible-attribute.md` (per-component required-rule table + selector traps) and `reference/css-file-bundling.md` (new "Bundling ≠ source file sharing" subsection); Contract: added `buttonVisible-false` checklist row to contracts/{combobox,bandbox,datebox,timebox,spinner,doublespinner}.md; Agent: generator §1.3 now warns about contract-vs-file confusion, evaluator §1.5 adds mandatory attribute-state sweep step | 2026-05-14 fix complete: all 5 No-button cells on combobox/datebox/timebox preview pages now report `display: none` (live DOM). No siblings broken (bandbox/spinner/doublespinner unchanged and still pass). |

| 2026-05-14 | combobox | `buttonVisible="false"` hides the button but the right edge of the input has no border and corner radius collapses (input has `border-right: none` + `border-radius: R 0 0 R`, expecting the button to provide the right edge). ZK already emits `z-combobox-input-full` class on the input when buttonVisible=false — the rule to restore right border + full radius is the missing piece. | The previous gap (No-button cell) only checked that the button is hidden. It did NOT check that the remaining input still looks correct. Generic "buttonVisible-false" check ignored that combobox uses a SPLIT-BORDER pattern (input border-right: none, button provides right edge) whereas bandbox/datebox/timebox/spinner use ROOT-BORDER (border on `.z-{c}`, input `border: none`) so hiding the button doesn't break their borders. | skill + contract + agent | Skill: `reference/buttonVisible-attribute.md` "Border architecture: split-border vs root-border" section + the `z-{c}-input-full` restoration hook; Contract: `combobox.md` No-button check split into sub-checks (a) button hidden, (b) input has 4-side border + uniform radius; Agent: evaluator §1.5 buttonVisible-false trigger now requires reading border-right-style and four border-radius values for Pattern S components | 2026-05-14 fix complete (combobox.css: added `.z-combobox-input.z-combobox-input-full { border-right: 1px solid …; border-radius: …; }`). All 5 No-button cells verified: border-right `1px solid` not `none`, border-radius `4px` uniform; per-state cascade preserved (default/disabled/readonly/invalid/inplace all colour all 4 sides correctly). |

| 2026-05-14 | chosenbox | Focus 後 popup 寬度撐滿整個 viewport (1904px) 而非 chosenbox 自己的 200px。CSS 用 `.z-chosenbox-popup { min-width: 100%; }`，但 ZK 把該 popup 掛在 `<body>` 之下（不是掛在 chosenbox 內），所以 `100%` 解析成 body 寬度，蓋掉了 ZK 已 inline 設好的 `width: 200px`。 | Skill 沒有記錄「ZK floating popup 會脫離 trigger，掛到 body」這個 ZK 行為；contract 也沒有檢查「popup 算出來的寬度應等於 trigger 寬度」。先前 combobox/datebox 的 popup 沒踩到這雷只是因為它們的 CSS 沒寫 `width: 100%` / `min-width: 100%`。 | skill + contract + agent | Skill: 新增 `reference/floating-popup-in-body.md`（描述 ZK 把 popup 掛到 body 的渲染模式 + percentage-based width 的陷阱 + ZK 已設 inline width 因此 CSS 不該再加 width hint）；Contract: `chosenbox.md` 改正 DOM 選擇器（`.z-chosenbox-item` 非 `-tag`、`.z-chosenbox-option` 非 `-item`），新增 popup 寬度檢查（rendered width == trigger width）；Agent: evaluator §1.5 新增「popup-floating 寬度核對」步驟 | 2026-05-14 fix complete (chosenbox.css 移除 `.z-chosenbox-popup` 的 `min-width: 100%`)：focus 後 popup width = 200px，與 trigger 同寬。 |
| 2026-05-14 | chosenbox | popup 內 option 行 (`.z-chosenbox-option`) 只有水平 padding (`padding: 0 12px`) 沒有垂直 padding，視覺上文字緊貼上下緣，不符 MD3 / MUI Autocomplete 期望（MUI 用 `padding: 6px 16px` + `min-height: 36–48px`，靠 padding 提供 touch target）。 | Contract 沒有對「popup option padding/min-height」設 expected；先前的「option:hover background-color」單一檢查無法捕捉版式問題。 | contract | Contract: `chosenbox.md` 新增 `.z-chosenbox-option` 期望值（`padding: 6px 16px`、`min-height: 36px`）；Skill 不動（純設計值，theme-specific） | 2026-05-14 fix complete (chosenbox.css: `.z-chosenbox-option` 改為 `padding: 6px 16px; min-height: 36px; height: auto;`)；live DOM 量測新值生效。 |
| 2026-06-10 | multislider, slider | Vertical thumbs sit 10px below the track endpoints (sweep found the same bug in the plain `.z-slider-vertical .z-slider-button`): JS writes `btn.style.top = X%` (the value position, meant for the thumb CENTER), but `.z-multislider-vertical .z-sliderbuttons-button` set `margin-top: 0`, so the thumb's TOP EDGE — not its center — lands on the position. Every thumb center is off by +half-height (10px). The rangeslider sibling does it right (`margin-top: calc(--btn-size / -2)`). User: 圓心沒對齊豎線起點/終點，兩者都偏移. | The harness never measured vertical thumb-vs-track-endpoint alignment. multislider.md contract listed phantom selectors (`.z-multislider-center/-area/-button` — none exist in live DOM; real DOM is `.z-multislider-track`, `.z-sliderbuttons-area`, `.z-sliderbuttons-button`) and only checked colors/size, never geometry. The main-axis centering rule was implicit in horizontal (`margin-left:-10px`) and correct in rangeslider, but nobody asserted it for multislider vertical. | skill + contract | Skill: `components/slider.md` "Related: multislider, rangeslider" — new "Thumb must be centered on its JS-set offset" rule (negative margin = −½ thumb size on the JS-driven axis; applies to slider/rangeslider/multislider, both orientations). Contract: `doc/contracts/multislider.md` selectors corrected to real DOM + new failing-first assertion v1 (vertical thumb center y == track endpoint y). CSS: `multislider.css` + `slider.css` (`.z-slider-vertical .z-slider-button`) `margin-top: -10px`. | 2026-06-10 fix: after `margin-top:-10px`, live re-measure shows multislider start thumb (value 5) center == track top (343px) and all thumb centers align to their value offsets (was +10px each); vertical filled-area edges now coincide with thumb centers. Step-5 sweep found the identical `margin-top:0` bug in the plain vertical slider (offset +10px → 0px after same fix). rangeslider already correct. Horizontal unaffected (uses base `margin-top:-10px`). |
| 2026-06-11 | multislider, rangeslider | Numeric mark-label perpendicular gap from the track was hand-tuned per orientation, so it drifted: multislider 27px(H)/15px(V), rangeslider 10px(H)/18px(V) — neither internally consistent nor matching the sibling. User asked for one consistent distance across orientations and across the slider family. | No assertion compared the H vs V label gap, nor cross-component. The four offsets (`top`/`left` on `.z-{c}-mark-label`) were independent magic numbers; nothing forced them to share a source. The structural trap (multislider marks have no dot, rangeslider marks do → same intended gap needs different offsets) was undocumented. | skill + contract | Skill: `components/slider.md` new "Marks: rangeslider renders a dot + label; multislider renders the label only" rule (ZK fact: `Multislider.redrawMarks_` appends label only). Contract: `multislider.md` geometry v2 + `rangeslider.md` geometry g1 — both assert 18px ±1 track-edge→label gap in both orientations, shared `--zk-rangeslider-label-gap`. CSS: `rangeslider.css` adds `--zk-rangeslider-label-gap: 18px` token; all four label offsets = `calc(var(--gap) + correction)` (rangeslider +4, multislider +3 H / +5 V). | 2026-06-11 verified live: all four cases measure exactly 18px (was 27/15/10/18). Whole slider family now consistent. Side-note logged: rangeslider.md still lists stale `.z-rangeslider-center/-area/-button` selectors (real DOM = `-track`/`.z-sliderbuttons-*`) — fix on next contract revision. |
| 2026-06-23 | grid/listbox (frozen), drag-and-drop widgets | 6 JS-toggled global framework classes undefined in Marble: `.z-word-nowrap` (Frozen.ts frozen columns), `.z-drag-over`/`.z-drop-allow`/`.z-drop-disallow` (DnD drop-target feedback), `.z-dragged`/`.z-drag-ghost` (drag source/ghost). Marble replaces stock CSS so they are simply absent. | The harness is per-component and driven by MD3/Mira analogs; these are cross-cutting classes ZK's client JS toggles at runtime, with no design-reference analog, so no contract ever asserted them. Same root cause as the splitlayout `.z-flex` rename bug, generalised. | skill + tooling | Spec: `reference/framework-classes.md` + `framework-classes.json` (canonical registry, contract-vs-decorative). Tooling: `tools/check-framework-classes.mjs` (Tier-1 presence checker, CI-gateable; re-run for the live PASS/FAIL result). | Built a reusable two-tier compliance check (script + semantic-review procedure) that any ZK theme can run; `.z-flex` family confirmed compliant; the 6 DnD/frozen gaps are logged for scoped follow-up (F1 frozen → F2 drop feedback → F3 drag ghost). **RESOLVED 2026-06-23 via TDD**: building the fix surfaced the registry was incomplete — the listitem message-ghost adds `.z-drop-ghost`/`-content`/`-icon`/`-text` (not in the 12), and the 5 drag hooks have NO stock CSS (registry wrongly said norm.less). Registry corrected/extended to 16 contract classes; all implemented in `zul/css/base/_dnd.css`. Preview `dnd.zul`, tests `framework-classes.spec.ts` (7/7), contract `doc/contracts/framework-classes.md`, checker PASS 16/16. Lesson: a JS-grep registry can miss sibling classes in the same DOM subtree — actually rendering/building the feature is the completeness check. |

| 2026-05-14 | colorbox | 預設關閉狀態結構壞掉：root 寬度只剩 ~18px（沒設定寬高、`overflow: hidden`），`.z-colorbox-current` swatch 因 inline-block 沒 width/height 規則被壓成 3.6×20，使用者看不到選定的顏色；caret-down 圖示沒有 mask SVG 完全空白。原本期望是「一個方形框框，內含色塊 + 向下箭頭」。 | Contract 寫了錯誤 DOM 選擇器（沒有 `.z-colorbox-current`、把 `.z-colorbox-picker` 當 popup 但實際是 `.z-colorbox-popup` 包 `.z-colorpicker`/`.z-colorpalette`），所以 expected-values 沒覆蓋到真正的 swatch；Skill 也沒有 `components/colorbox.md` 紀錄 mold 渲染出來的真正結構（colorbox.js `current` + `button` + `popup`(picker+palette) 三層），生成 CSS 時只憑想像寫了 `.z-colorbox` + `.z-colorbox-button` 兩條規則。 | skill + contract | Skill: 新增 `components/colorbox.md`（記錄 mold 渲染的 DOM、`.z-colorbox-current` 由 ZK inline `style="background-color"` 帶顏色、`.z-icon-caret-down` 需要本地 mask、popup 內部有 `.z-colorpicker`/`.z-colorpalette` 兩種子元件、EE-only）；Contract: 改正 `colorbox.md` 全部選擇器與 expected-values，加 swatch 尺寸 / caret 渲染 / popup 寬度檢查 | 2026-05-14 fix complete (colorbox.css 重寫關閉狀態：root 設 32×32 inline-flex、`.z-colorbox-current` flex:1 顯示色塊、`.z-colorbox-button` 固定寬度 16px、`.z-colorbox-icon` 本地 mask 出 chevron-down)。Live DOM：root width = 32, swatch width = 14, button width = 16, icon mask 渲染 ok。Popup 設計另開 round 處理（picker / palette 內部用 ZK 既有像素座標，無法套 MD3）。 |

| 2026-05-14 | colorbox (但 root cause 影響所有 css.dsp 檔) | colorbox popup 內部完全沒樣式：palette 縮成 1px、picker 看不到 gradient。Root cause: 我在 colorbox.css 寫了原 ZK 主題慣用的 `${c:encodeURL("~./zkex/img/...")}` URL 語法 — 但我們的 build pipeline 只把 `.css` 改成 `.css.dsp` 副檔名，沒在檔首加 `<%@ taglib %>` declaration。DSP 處理時找不到 `c:` taglib → 處理失敗 → 檔案內容變空 → WCS bundler 整檔丟掉，連無 EL 表達式的部分也跟著消失。 | Skill 沒有記錄「我們的 .css.dsp 沒走 DSP processing，不能用 `${...}` 表達式」這個 pipeline 事實。任何 css.dsp 檔只要用了 EL/JSP 標籤都會被默默掃光，沒有錯誤訊息、build 也 pass。一個檔內只要一個 `${c:encodeURL(...)}` 就把整個檔抹掉。 | skill + contract | Skill: 新增 `reference/css-dsp-pipeline.md` 記 build 只 rename 不 process EL；外部 URL 用絕對路徑 `/zkau/web/zkex/img/...`（ZK 無 version 也吃）；想用 `${...}` 需要先加 taglib 並讓 build 串接 DSP；Contract: `colorbox.md` 加 picker sprite URL 期望值（status 200、resolved URL contains `/zkau/web/zkex/img/`） | 2026-05-14 fix complete (colorbox.css 把 `${c:encodeURL("~./zkex/img/...")}` 改成 `/zkau/web/zkex/img/...`)。WCS 重新拿到 73 條 colorbox 規則（原本 0），sprite 4 張 status 200，picker 跟 palette 都正常渲染。Closed state 仍 40×36，popup 358×318 含 8px padding + border。 |

| 2026-05-14 | html / body (global) | floating popup (colorbox/datebox/combobox/…) 無法點下方空白區域關閉。Root cause：頁面內容低於 viewport 時 `<html>`、`<body>` collapse 到 content height（例如 381px / 552px viewport），底下 171px 沒有任何 element 接收 click，ZK 的 outside-click handler 收不到事件。 | 沒有 skill 紀錄全域 html/body 需要 `min-height: 100vh` 才能讓 outside-click dismiss 正常運作；每個 popup 的 contract 也只檢查自己內部，沒人盯著 page-level click capture surface。 | base CSS | base/_reset.css: `html { min-height: 100% }`、`body { min-height: 100vh }` | 2026-05-14 fix complete: htmlH=552, bodyH=552 = winH=552。所有 floating popup 都受惠。 |
| 2026-05-14 | combobutton (toolbar mold) | 4 個角落顯示 border 缺口。Root cause：root `.z-combobutton-toolbar` 有 `border-radius + overflow: hidden`，border 卻畫在 child `.z-combobutton-content` 上 — child 的方形 border 在邊角被 root 的 rounded clip 切掉。 | Skill 沒有「同一元素必須擁有 border + border-radius 才能畫出連續圓角」的條目；contract 沒檢查 root vs inner 哪邊有 border。 | component CSS | combobutton.css: 把 border 從 `.z-combobutton-content` 搬到 root `.z-combobutton-toolbar` | 2026-05-14 fix complete: 角落連續，4 個角無缺口。 |
| 2026-05-14 | datebox popup | popup 右邊有 12px 寬的多餘區塊，像是「多一層 border」。Root cause：`.z-datebox-popup { min-width: 280px }` 強制 popup 280 寬，但裡面 `.z-calendar` 有自己的 `max-width: 268px` cap，結果右側 12px 是 popup 的空白背景條。 | 兩個檔（datebox.css 跟 calendar.css）各自設寬度，沒有 cross-file 對齊檢查；contract 沒檢查 popup width vs calendar width 是否吻合。 | component CSS | datebox.css: 移除 `.z-datebox-popup { min-width: 280px }` 跟 `.z-datebox-popup .z-calendar { width: 320px }`（讓 popup auto-fit calendar 的 max-width） | 2026-05-14 fix complete: popupW=calW=268, diff=0。 |
| 2026-05-14 | rangeslider (3 sub-issues) | (a) marks 垂直堆疊不是水平排列；(b) vertical track 完全沒畫；(c) 我們的 rangeslider.css 只給 CSS variables 沒給結構規則，整個 mark layout / orientation / button geometry 全部使用 ZK stock default，但 stock 的 padding/margin/size 跟 MD3 token 不一致。 | 我們的 rangeslider.css 是「stock CSS 的 token-only 替身」 — 沒意識到 ZK theme override 是「整檔取代」，不是「附加」。覆蓋了 stock 卻沒搬進去結構規則，導致 marks 變 static block、vertical 沒尺寸。Skill 沒有警告 theme override 是 replace 不是 merge。 | skill + component CSS | Skill (待續): `reference/theme-override-is-replace.md` 記 lang-addon `<css-uri>` 路徑被 theme 整檔取代；component CSS: 重寫 `js/zkex/slider/css/rangeslider.css` 包含 marks、horizontal/vertical、track、area、button、tooltip 結構規則（搬自 stock 並用 `--zk-*` token） | 2026-05-14 fix complete: 水平 inner 500×4 marks position:absolute、垂直 inner 4×500 track 渲染、button 20×20 radius 50%。 |
| 2026-05-14 | multislider vertical | vertical multislider 的 track 高度 0，看不到。Root cause：`.z-multislider-vertical .z-multislider-track { width: 4px; height: 100% }` 中的 100% 算的是 parent `.z-multislider-inner` 的高度 — 但 inner 沒設高度（預設 0），所以 track 0 高。 | Contract 沒檢查 vertical inner 是否能撐滿 parent height；horizontal 沒這個問題（inner 是 block 自然撐滿寬度），vertical 才有 — orientation-specific bug contract 沒涵蓋。 | component CSS | multislider.css: 加 `.z-multislider-vertical .z-multislider-inner { width: 4px; height: 100%; margin: 12px 30px 12px 12px }`（跟 rangeslider 同 pattern） | 2026-05-14 fix complete: vertical inner 4×500, track 4×500, 完整渲染。 |

| 2026-05-14 | combobutton popup（其實牽動所有 ZK 用 `<a>` 渲染的 widget UI） | popup item hover 出現底線。Root cause：`base/_reset.css` 寫的 `a:hover { text-decoration: underline }` 是無 scope 的全域 reset，吃到所有 `<a>` 元素。但 ZK 用 `<a>` 渲染很多 UI 控件（`.z-menuitem-content`、`.z-toolbarbutton`、`.z-tab`、`.z-listitem`、`.z-treerow`、`.z-a`…），底線跑到那些控件 hover 時看起來像 bug。 | Skill 沒紀錄「ZK 大量用 `<a>` 渲染非超連結 UI 控件」這個事實；global reset rules 沒先濾掉 widget anchors。 | base CSS | `_reset.css`: 把 `a:hover { text-decoration: underline }` scope 改成 `a[href]:not([class*="z-"]):hover` — 排除所有 z-* widget anchor，只對純內文超連結加底線 | 2026-05-14 fix complete: live CSS rule set 只剩 `a[href]:not([class*="z-"]):hover` 跟有意的 `.z-a:hover`。menuitem-content 不再 match underline rule。 |
| 2026-05-14 | multislider vertical | 同條 slot 內出現 2 條線（一灰 track 一藍 area），應該重合成一條。Root cause：`.z-multislider-vertical .z-multislider-track { margin-inline: 10px }` 把 track 從 x=314 推到 x=324，但 `.z-sliderbuttons-area` 在 `.z-sliderbuttons { inset: 0 }` 裡的 absolute 定位回到 x=314 — 兩者軸不重合，眼睛看到「兩條 track」。橫向 `margin-block: 10px` 也有對應問題只是視覺上沒那麼刺眼。 | Contract 沒檢查「track 跟 area 必須對齊」這項；rangeslider 跟 multislider 共用 `.z-sliderbuttons-*` 元素但是各自有 track 元素，misalignment 容易被漏掉。 | component CSS | `multislider.css`: 把 `.z-multislider-track` 改為 `position: absolute; inset: 0` — 直接讓 track 跟 area 重合在同一個 inner 容器內。刪掉 horizontal/vertical 特定 track margin。 | 2026-05-14 fix complete: vertical 三個可見元素（track + 2 area）全部 x=314，看不出兩條 track。 |
| 2026-05-14 | rangeslider, multislider（tooltip 一直顯示） | `.z-sliderbuttons-tooltip` 預設可見，使用者沒 hover 也看得到。期望：只在 hover/active/focus thumb 才出現（MD3 / MUI Slider value indicator pattern）。 | Contract 沒檢查 tooltip default visibility；CSS 起手式就把 tooltip 跟 area / button 一起當 always-visible 元素寫。 | component CSS | `rangeslider.css` + `multislider.css`: `.z-sliderbuttons-tooltip { opacity: 0; visibility: hidden; transition: opacity .. , visibility 0s linear var(--zk-motion-duration-short3) }` + `.z-sliderbuttons-button:hover .z-sliderbuttons-tooltip { opacity: 1; visibility: visible; transition-delay: 0s }`。**注意**：tooltip 是 button 的 **descendant**（child），不是 sibling — 首版用 `~` 不對，要用 descendant selector。 | 2026-05-14 fix complete: 預設 opacity=0 visibility=hidden；hover button 才出現。 |
| 2026-05-14 | timebox button | `.z-timebox-input` 跟 `.z-timebox-button` 之間沒有視覺分隔線，難以看出 button 區域。 | Contract 沒檢查 button 是否有 `border-left` 分隔 input 與 button；跟 datebox/combobox（用 split-border）不一致。 | component CSS | `timebox.css`: `.z-timebox-button { border-left: 1px solid var(--zk-color-outline) }` | 2026-05-14 fix complete: borderLeft=1px solid 0.23 已渲染。 |

| 2026-05-14 | rangeslider vertical tooltip | tooltip 距 thumb 太遠（left: -60px），水平方向只用 -36px，左右兩邊不一致。 | Contract 沒對 horizontal/vertical tooltip 跟 thumb 的對稱距離設期望；空間規範不對等。 | component CSS | `rangeslider.css`: vertical `.z-sliderbuttons-tooltip { left: -40px }`（與 horizontal `top: -36px` 同級距） | 2026-05-14 fix complete |
| 2026-05-14 | multislider tooltip | tooltip 跟 thumb 重疊（沒給位置規則）。 | multislider.css 寫 `.z-sliderbuttons-tooltip` 時只設 visual style，沒給 `top/left` 偏移。rangeslider 早就修了同樣問題，沒回頭把 multislider 對齊。 | component CSS | `multislider.css`: 加 horizontal `top: -36px; left: 50%; transform: translateX(-50%)`，vertical `left: -40px; top: 50%; transform: translateY(-50%)` | 2026-05-14 fix complete |
| 2026-05-14 | rating | selected 星星只用 `color: primary` 改變色，icon font 仍是 outline glyph — 視覺上沒「填滿」。 | rating.css 依賴 `z-icon-star` icon font，selected 沒切到 filled glyph。MD3 / MUI Rating 用兩種 icon: outline + filled。 | component CSS | `rating.css`: 用 SVG mask 取代 icon font，outline / filled 切換靠 mask URL（selected 用 fill 完整的 star SVG） | 2026-05-14 fix complete |
| 2026-05-14 | selectbox popup | 「popup 可以加上 style 嗎？」 | selectbox renders as native `<select>`; popup 是瀏覽器原生 `<option>` 列，CSS 改不到。Skill 沒紀錄此 ZK 行為，避免下次又被問。 | skill | `components/selectbox.md`: 紀錄 selectbox = native `<select>` element，popup options 由瀏覽器控制無法 style，要 styled dropdown 改用 combobox/bandbox/listbox | 2026-05-14 fix complete (skill 已寫；CSS 無需改) |
| 2026-05-14 | rangeslider drag (待調查) | drag thumb 一動就跳到 100 (最大值)。slider 無此問題。 | 可能 JS handler 計算座標時被 CSS padding/margin 干擾，或 hitbox 重疊讓事件落到非預期 element 上。需要 live debug 確認。 | (待調查) | (pending) | TODO |

| 2026-05-14 | colorbox paletteicon/pickericon | hover effect 沒被注意到 / 看不出 hover | Contract 沒檢查兩個 tab icon 的 hover state-layer。原 CSS `background-color: rgba(0,0,0,0.08)` 太淡，沒走 `--zk-color-on-surface` 8% state-layer。 | component CSS | `colorbox.css`: 改成 `color-mix(in srgb, var(--zk-color-on-surface) 8%, transparent)` + color transition；selected 狀態加 `:hover` 用 on-primary-container 8% 加深 primary-container | 2026-05-14 fix complete (live CSS 兩條 hover rule 都載入) |
| 2026-05-14 | colorbox pickericon toggle (再次回報) | 使用者再次回報。 | 重新驗證後 toggle 雙向皆 OK。可能先前是快取。 | (no code change) | live DOM hard-reload bidirectional：palette → picker → palette OK、popupClass 切換、palDisplay/picDisplay 反轉、selected icon bg = primary-container | 2026-05-14 verified working |
| 2026-05-14 | rangeslider drag → 100 (root cause found) | drag thumb 一動就跳 100%。ZK JS (`Rangeslider.ts:350`) `getSliderSize_()` 用 `jq(this.$n_('track')).width()` 讀 track 寬度當分母。我把 `.z-rangeslider-track` 當「填充段」處理，只給 `height: 100%` 沒給 width → track width=0 → drag 公式 `100 * mouseOffset / 0` = Infinity → clamp 到 100。Mold 實際結構：`.z-rangeslider-track` 是**整條 rail**（width:100% height:100%），filled 部分是 `.z-sliderbuttons-area` (由 JS inline style 設 left/width)。 | Skill 沒紀錄「`-track` 是 rail 不是 fill」這個 ZK rangeslider 約定；我從元素名直觀猜錯，contract 也沒檢查 `track.offsetWidth` > 0。 | skill (待寫) + component CSS | `rangeslider.css`: `.z-rangeslider-track { position: absolute; inset: 0; background-color: var(--zk-rangeslider-track-color) }`；移除 inner 上的 track-color background；inner 改為純透明 sizing 容器 | 2026-05-14 fix complete: track horizontal=500×4 vertical=4×500；模擬 drag btn1 從 10% 拖到 30% (預期值)，不再跳 100 |

| 2026-05-14 | colorbox pickericon/paletteicon real click (root cause finally found) | 使用者真實點擊兩個 icon 都沒反應、`.z-colorpicker` 一直 `style="display:none;"`。先前 synthetic `jq.trigger('click')` / `dispatchEvent` 都能切換 — 騙過自己以為「toggle works」。 | DOM 順序：`<paletteicon><pickericon><picker><palette>`，icon 們是先 render 的兄弟元素；picker/palette 後 render，兩邊都 `z-index: auto`，依 painting order picker/palette 蓋住 icon。`elementFromPoint` 在 icon 中心點命中的是 picker (`z-p-8 z-div` from picker-inner)、不是 icon — JS handler bound 在 icon 上，但 hit-testing 把 click 送給 picker，自然 nothing happens。Synthetic trigger 跳過 hit-testing 所以早期測都過。 | Skill: 沒紀錄「ZK colorbox popup 中 paletteicon/pickericon 跟 colorpicker/colorpalette 是 sibling，畫圖順序會讓 icon 被蓋」這個 stacking trap；contract: 沒檢查 `document.elementFromPoint(iconCenter)` 是不是 icon 自己。Synthetic test 永遠看不到 hit-test 問題。 | skill + contract + component CSS | `colorbox.css`: `.z-colorbox-paletteicon, .z-colorbox-pickericon { z-index: 1 }`（含註解說明為何需要）。Skill: 待加 paint-order trap (待寫); Contract: 待加 elementFromPoint check (待寫) | 2026-05-14 fix complete：`elementFromPoint(picCenter)` → `.z-colorbox-pickericon`、`elementFromPoint(palCenter)` → `.z-colorbox-paletteicon`；連續 native click (mousedown+mouseup+click) 雙向切換 OK，`palette.style.display` & `picker.style.display` 跟著反轉。 |

| 2026-05-15 | slider vertical | vertical slider `.z-slider-area` 從**底部**長出來，與 thumb 反向（thumb 在上面、fill 在下面）。期望：area 應該從上面開始長到 thumb 位置。 | Contract 沒檢查「ZK vertical slider 的座標語意」。ZK `Slider._fixPos()` 把 `area.style.height = newPos`、`btn.style.top = newPos`（newPos 是從 inner 頂端往下的 px）。意思是：min=top, max=bottom。area 必須 anchor 在 **top** 才會跟 thumb 同方向；但先前的 CSS 寫 `bottom: 0; top: auto`，造成 fill 跑到反向那端。 | skill + component CSS | `slider.css`: `.z-slider-vertical .z-slider-area { top: 0; bottom: auto }`。Skill: `components/slider.md` 補一段「vertical area 方向／座標語意」。 | 2026-05-15 fix complete: 設 curpos=50 後 area top=462 (= center.top)、bottom=512 (= btn.top)，從上面長到 thumb，正確。 |
| 2026-05-15 | searchbox | 外觀完全錯。先前 CSS 把 searchbox 當成單一 text input 寫（input + border + placeholder），實際 DOM 是：label/placeholder trigger + 兩個 icon（clear + caret-down），點擊後 popup 才出現裡頭的 input + checkbox list（multi-select 用 `.z-searchbox-item-check`）。Popup 是經 `makeVParent` 接到 `<body>`。 | Skill 完全沒有 searchbox 元件條目；contract 也沒有 searchbox DOM/use-case 描述；連 `css-file-bundling.md` 都把 searchbox 誤分到 `input.css.dsp`（實際上 searchbox 是 zkmax/inp，自己一個 contract `searchbox.css.dsp`）。整個 harness 沒人知道 searchbox 是什麼樣子。 | skill (new file) + component CSS rewrite + reference fix | `components/searchbox.md` (new): DOM 結構、open/close 行為、`makeVParent` popup、多選 item-check、clear icon 顯隱規則、disabled/focus/open state class、跟 selectbox/combobox/bandbox/chosenbox 的差異。`searchbox.css`: 完整改寫，trigger 樣式（label + clear icon + caret-down icon）、popup 樣式（search input + 滾動 cave + item + item-check + selected/active state）；popup default `display: none`，`.z-searchbox-open .z-searchbox-popup { display: flex }`（mold 沒在 popup 上寫 inline display style，沒這條 popup 開機時會直接外露）。`reference/css-file-bundling.md`: 從 `input.css.dsp` 移除 searchbox。 | 2026-05-15 fix complete: trigger 是 40-tall chip 樣式（placeholder/label + clear×/caret▼）；點擊後 popup 出現（search input + 7 items with globe icon）；選 "North America" 後 trigger 顯示 truncated label + clear icon 出現；disabled 變淡。 |
| 2026-05-15 | searchbox popup default visibility | 開發 verify 時發現：mold 沒給 popup inline `style="display:none"`，所以只要 CSS 寫 `.z-searchbox-popup { display: flex }`，popup 一 render 就會跑出來掛在每個 searchbox 旁邊。 | 過去 harness 沒留意到「ZK 多數 popup 是 JS 用 inline style 控制顯隱、但 searchbox 的 popup 在 render 階段就已經存在於 trigger 內部、JS 也沒幫忙加 hidden style」。多數 ZK floating popup 走 `display: none` initial（透過 mold inline 或 widget JS），但 searchbox 走另一條路。 | component CSS（記在 skill 的 floating-popup-in-body 上比較對） | `searchbox.css`: popup default `display: none`，靠 `.z-searchbox-open .z-searchbox-popup { display: flex }` 開時切回；ZK 的 `slideDown()` 會 inline 設 `display: block` 蓋過。 | 2026-05-15 fix complete: 初始畫面只看到 2 個 trigger chip，無 popup leak；點 trigger 才出現 popup。 |

| 2026-05-15 | grid header (and listbox/tree headers) | 表頭使用 `surface-container-low` 底色，與 Mira Simple Table / MD3 Data Table spec 不符（兩者都是無底色，只靠 font-weight 500 + border-bottom 區隔）。 | Skill 沒寫「ZK data-component 表頭應該怎麼跟 reference theme 對齊」這條規則；過去三個檔（grid.css / listbox.css / tree.css）各自寫了 tinted header，沒有 cross-component 一致性檢查。 | component CSS (3 files) + design doc | grid.css `.z-column`、listbox.css `.z-listheader` / `.z-listhead`、tree.css `.z-treecol` / `.z-treecols` 移除 `background-color`（讓 surface 透出來）；保留 border-bottom 跟 font-weight 作為視覺分隔。`doc/spec/DESIGN.md` 補一段 data-table header 設計決定（對齊 MUI Simple Table + MD3 Data Table）。 | 2026-05-15 fix complete: grid / listbox / tree 三張 preview page 表頭全部 unfilled、字色 on-surface-variant、border-bottom outline-variant 區隔 body。視覺三者一致。 |
| 2026-05-15 | grid `.z-row-selected` 是 dead code | grid.css 寫了 `.z-row.z-row-selected` / `.z-row.z-selected` 樣式，但 ZK Grid widget 從來不發 selection 相關 class（grep `selected`/`Selected` 在 `js/zul/grid/` 目錄完全無命中）。 | Skill 沒明確標註「Grid 不支援 row selection」。contract 也沒檢查「rule selector 對應的 class 是否真的會被 emit」，所以僵屍 rule 一直留著。 | component CSS + skill | grid.css 移除 `.z-row.z-row-selected` / `.z-row.z-selected` 相關 5 條 rule。`components/data-components.md` 補一段「Selection support」表，明寫 grid=No、listbox/tree=Yes，並附 grep 證據。 | 2026-05-15 fix complete: 對 served CSS 查 `\.z-row[\.:]z-row-selected\|\.z-row\.z-selected` → 0 命中（先前有 4 條）；listbox/tree selection 仍正常（看 listbox "Selected Row"、tree "Selected" 仍是 primary-container 高亮）。 |

| 2026-05-15 | grid column-button (`.z-column-button`) | `menupopup="auto"` 觸發 column-header 右側的 caret-down 按鈕。ZK 預設 `display: none`、靠 `.z-column-hover` (JS hover state class) 才顯示；MD3 spec 應該維持 progressive disclosure（hover 才出現），但目前我們的 theme 完全沒寫這條 rule。 | Skill 沒紀錄「`.z-column-button` 是 `menupopup` 才會 render；只在 column hover 顯示；ZK 用 `.z-column-hover` class，但 `:hover` 也應該支援」。contract 也沒列出這條 use-case。 | component CSS + skill | `grid.css`: `.z-column .z-column-button { opacity: 0; ... transition: opacity }`，`.z-column:hover/.z-column-hover .z-column-button { opacity: 1 }`，icon-button 風格（24×24 圓角 state-layer）。Skill: `components/data-components.md` 補 column-button section。 | 2026-05-15 fix complete: 預設 opacity=0（with `transition: none` 驗證）、加 `.z-column-hover` class 後 opacity=1；position absolute、width 24px、border-radius 9999px（fully rounded state-layer）。Skill 已補。 |
| 2026-05-15 | grid footer (`.z-grid-footer`, `.z-footer`) | footer 字距 cell 邊緣太緊（padding 只有 spacing-2/4），跟 header 與 body cell 不一致；MD3 spec 規定 data-table footer 應跟 body 同 padding。發現實際 DOM 是 `TR.z-foot > TD.z-footer > DIV.z-footer-content`，不是 `.z-cell` 或 `.z-row-content`。 | contract 沒檢查「footer cell padding 是否與 body 一致」這項；先前 css 把 selector 寫成 `.z-foot .z-cell / .z-row-content` 完全沒命中真實 DOM。 | component CSS | `grid.css`: `.z-foot .z-footer { padding: 0 }`、`.z-foot .z-footer-content { padding: var(--zk-grid-cell-padding) }`（content padding 16px = 跟 body 同 spacing-4）。 | 2026-05-15 fix complete: `.z-footer` padding=0、`.z-footer-content` padding=16px，跟 body cell 一致。 |
| 2026-05-15 | grid auxhead (`.z-auxhead` row + `.z-auxheader` cells) | multi-level header 分組 row，先前用 `surface-container` 整片填色，跟 column-header 透明處理衝突。User decision: row 用 surface-container-low **faint tint**，cells 透明，靠 row-band 顯示「parent group」階層。 | Skill 沒寫「auxhead 是 ZK-only multi-level header；視覺層級該怎麼跟一般 column-header 區隔」的規則。Contract 沒檢查既有 `mesh/css/auxhead.css` 跟 `grid.css` 的 auxhead 規則重複（且 mesh 版有 `!important` 會 win）。 | component CSS (mesh shared) + design doc + skill | `mesh/css/auxhead.css`（shared）改寫: `.z-auxhead { background-color: surface-container-low }`、`.z-auxheader { background-color: transparent }`；移除 `!important`；移除 `grid.css` 中的 duplicate。`doc/spec/DESIGN.md`: 補 auxhead band 設計決定。Skill: `components/data-components.md` 補 auxhead section。 | 2026-05-15 fix complete: `.z-auxhead` bg = surface-container-low (rgb 247,249,252)、`.z-auxheader` bg transparent；視覺層級從 row-band 表達。 |
| 2026-05-15 | grid frozen columns | `frozen columns="2"` 鎖前 N 欄、後面 column 用 `.z-frozen-inner` 上的 horizontal scrollbar 滑動。ZK 不是 CSS `position: sticky`，是 JS 把 columns past N 用 translateX 平移；DOM 結構是 `.z-frozen > .z-frozen-body + .z-frozen-inner(overflow-x:scroll) + .z-frozen-right + .z-clear`，frozen rows/cells/columns 上掛 `.z-frozen-col` / `.z-grid-frozen`。發現舊版 `mesh/css/frozen.css` 把 `.z-frozen-inner` 當 sticky-pane wrapper 寫（用 `position:absolute + overflow:hidden`），跟實際 ZK JS 機制衝突，讓 scrollbar 出不來。 | Skill 沒紀錄「ZK frozen = JS-driven translate；DOM 結構與所有 `.z-frozen-*` class」。Contract 沒檢查 stale `frozen.css` 跟 ZK 實際 mold 結構是否吻合。 | component CSS (mesh shared) + skill | `mesh/css/frozen.css`（shared）改寫: `.z-frozen-inner { overflow-x: scroll }`（讓 native scrollbar 出來，ZK JS inline-styles 寬高）、scrollbar 顏色用 outline-variant token、`.z-frozen-col:not(:has(+ .z-frozen-col))` 在 last frozen cell 加 border-inline-end + box-shadow elevation 表達 pane boundary。移除 `grid.css` duplicate。Skill: `components/data-components.md` 補 frozen-columns section（含 DOM 結構、JS-driven 提示、`:has()` boundary selector）。 | 2026-05-15 fix complete: `.z-frozen-inner` overflow-x=scroll；最後一個 frozen-col 有 1px outline-variant border + 2px×4px box-shadow 邊界；scrollbar 顏色走 token。boundary selector 用 `:not(:has(+ .z-frozen-col))` 正確命中末欄。 |

| 2026-05-15 | grid frozen visual indicators (refinement of前一條) | (1) `.z-frozen-body`（位於 frozen 欄下方的 spacer）視覺上看不出來「上面那兩欄是 frozen」。(2) `.z-frozen-inner` 的 horizontal scrollbar 在 macOS overlay-scrollbar 環境下 ZK JS 量到 6px → tray 跟 inner 都被 inline-style 成 height:6px、太薄看不到 scrollbar。比較 ZK 原版（SUPPORT project）量到 15px 是正常的，差異來自 jq.scrollbarWidth() return value。 | Skill 沒寫「frozen-body 是用來指示 frozen pane 範圍的 spacer」、也沒紀錄「ZK 用 native scrollbar 量測來設定 tray height、不同 OS / 瀏覽器設定會有差」。Contract 沒檢查 tray height 是否大到可見、frozen-body 是否有 pane 標示。 | component CSS (mesh shared) | `mesh/css/frozen.css`: `.z-frozen { min-height: 12px }` (覆寫 ZK inline 6px，min-height 一定贏 inline height)、`.z-frozen-body { background: surface-container-low + 1px outline-variant border-inline-end + 2px×4px box-shadow elevation + min-height: 12px }`（讓使用者看到 pane 結束邊）、`.z-frozen-inner { overflow-x: scroll; min-height: 12px }`（讓 styled WebKit scrollbar 有空間 render）。 | 2026-05-15 fix complete: tray h=12px, body h=12px + faint band bg + boundary shadow, inner h=12px + scroll, scrollWidth (1600) > clientWidth (1434) 可滑動。 |
| 2026-05-15 | listbox click → page jumps to top (CRITICAL bug) | 點選任何 listitem 後整頁被 scroll 到 `scrollY=0`。Root cause：`.z-listbox-body` 是 `position: static`，所以裡面的 `.z-focus-a` 雖然是 `position: absolute; top: 0; left: 0;`，會去找 nearest positioned ancestor — 找不到、就 fallback 到 `<body>`，等於把 trap div 放在 viewport (0,0)。當 ZK `SelectWidget._focusAnchor` 呼叫 `focusEl.focus()` 時，瀏覽器為了讓 focused element 可見會 scroll page to (0,0)，所以整頁跳上去。同樣機制 grid、tree 都會中。 | Skill 沒紀錄「`.z-focus-a` 是 select widget 的 focus-trap，依賴 nearest positioned ancestor」。Contract 沒驗證 `.z-focus-a.offsetParent` 是不是 listbox-body / tree-body / grid-body。calendar.css 跟 menu.css 早就有 `.z-focus-a` style，但 sel/css/listbox.css 沒有 — 一個跨組件規則沒人 enforce。 | component CSS (3 files) + skill | `listbox.css`、`tree.css`、`grid.css` 三檔分別加 `.z-listbox-body { position: relative }` / `.z-tree-body { position: relative }` / `.z-grid-body { position: relative }` 與 scoped `.z-focus-a { position: absolute; w/h: 0; opacity: 0; pointer-events: none }`，確保 trap div 錨點正確不會把焦點推到 viewport (0,0)。 | 2026-05-15 fix complete: `body_position: relative`、`focusA.offsetParent: DIV.z-listbox-body`（之前是 BODY）。模擬「先 scroll 到 y=800、點 listitem」場景：afterY=800, pageJumped=false (之前 delta=-800)。 |
| 2026-05-15 | listheader-button (`.z-listheader-button`) | listbox 的 column-action button（menupopup="auto" 觸發），跟 grid `.z-column-button` 是 sibling concept，但 listbox.css 完全沒寫 button 樣式跟 hover-reveal rule。 | Skill 沒寫「listheader 跟 column header 一樣有 menupopup-button、行為一致」這條 cross-widget rule。Contract 沒檢查 listheader-button visibility / position。 | component CSS + skill | `listbox.css`: 複製 grid column-button 的 progressive-disclosure pattern（opacity 0→1 on `:hover`/`.z-listheader-hover`/`:focus-visible`，icon-button 24×24 fully-rounded state-layer）。Skill (`components/data-components.md`): 把 column-button section 擴寫成 "Header action button"，涵蓋 grid `.z-column-button` + listbox `.z-listheader-button` + tree `.z-treecol-button` 的共同 pattern。 | 2026-05-15 fix complete: `.z-listheader-button` default opacity 0 (with transition:none 驗證)、加 `.z-listheader-hover` 後 opacity=1、width 24px、fully rounded。 |

| 2026-05-15 | grid frozen layout (refinement) | `.z-frozen-body` 跟 `.z-frozen-inner` 沒並排顯示，inner 跑到 body 下面（垂直 stack）。ZK mold 規定它們要側邊排列（body float:left, inner 緊接在右側），scrollbar 才會出現在 inner 上、視覺上跟 frozen pane 連續。 | 我們的前一條 frozen 修法 (`mesh/css/frozen.css`) 只搬了 ZK CSS 的部分屬性、漏掉 `.z-frozen-body { float: left }` 跟 `.z-frozen-right { float: right }` 跟 `.z-clear { clear: both }`。Contract 沒檢查 layout 是否 side-by-side。 | component CSS (mesh shared) | `mesh/css/frozen.css`: `.z-frozen { overflow: hidden }`（補回 ZK 原本的 clip）、`.z-frozen-body { float: left; overflow: hidden }`、`.z-frozen-right { float: right; overflow: hidden }`、`.z-frozen-inner > div { height: 100% }`。並排靠 float 達成、`.z-clear` 收尾。 | 2026-05-15 fix complete: body float=left, body.right(432) === inner.left(432)、sideBySide=true sameRow=true、inner.scrollWidth(1600) > clientWidth(1434) 可橫向 scroll。 |
| 2026-05-15 | listbox-grouping `.z-listgroup` / `.z-listgroupfoot` 雙重邊框 | `.z-listgroup` (group header row) 跟 `.z-listgroupfoot` (group subtotal row) 在 td、`.z-listcell-cnt`、`.z-listcell-content` 三層都套 `border-top + border-bottom`，渲染出三條疊在一起的線（上 3 條 + 下 3 條），視覺看起來 odd。 | Contract 沒檢查「邊框落在哪一個層級」、所以把同樣的 border 重複放到三層元素上沒被警告。前版 css 設計者把 cell-level border 當 row-level border 寫。 | component CSS | `listbox.css`: 把 border 從 `td/cnt/content` 三層全收起來，改放單一條在 row 上 — `.z-listgroup { background: surface-container; border-bottom: 1px outline-variant }`、`.z-listgroupfoot { background: surface-container-low; border-top: 1px outline-variant }`。Cell 級用 `background: transparent` 讓 row band 透出。 | 2026-05-15 fix complete: groupTd_borderTop/Bottom=0px none、groupContent_borderTop/Bottom=0px none、`.z-listgroup` row 只有 1px bottom divider、`.z-listgroupfoot` row 只有 1px top divider。 |
| 2026-05-15 | paging os mold first/prev/next/last 圖示錯誤 | OS mold 渲染 4 個 nav 按鈕（First/Prev/Next/Last），前版 CSS 只用 `:first-child` (給 First+Prev 同一個 chevron-left 圖示) 跟 `:last-child` (給 Next+Last 同一個 chevron-right 圖示) — 結果 First 不是 double-left、Last 不是 double-right。 | Skill 沒紀錄「os mold 4 個 nav 按鈕怎麼用 CSS selector 區分」、contract 也沒驗證圖示對應方向。 | component CSS | `paging.css`: 用 `:nth-child(N)` / `:nth-last-child(2)` 區分 4 個位置：`:first-child`→First (chevrons-left, 2 paths)、`:nth-child(2)`→Prev (chevron-left)、`:nth-last-child(2)`→Next (chevron-right)、`:last-child`→Last (chevrons-right, 2 paths)。Selector 依賴「page-number li 不會出現在 1、2、last-1、last 這 4 個位置」這個事實。 | 2026-05-15 fix complete: 4 個 nav button mask SVG path 內容跟預期 chevron direction 一致（First=m11/m18, Prev=m15 18, Next=m9 18, Last=m6/m13）。 |
| 2026-05-15 | selectbox popup 採新的 Customizable Select 規格 | HTML spec (Chrome 130+) 允許 `select { appearance: base-select }` 後用 CSS 直接 style popup (`::picker(select)`)、option (`option:hover/checked`)、checkmark (`option::checkmark`)、arrow (`::picker-icon`)、selected indicator (`selectedcontent`)。前版 CSS 只 style 觸發框，popup 還是 native browser 樣式。 | Skill 沒紀錄 customizable select 規格、contract 沒紀錄該如何用 `@supports (appearance: base-select)` opt-in 又保持舊瀏覽器的 fallback。 | component CSS + skill | `selectbox.css`: 新增 `@supports (appearance: base-select) { ... }` 區塊，在內：opt-in `appearance: base-select`；popup `::picker(select)` 用 MD3 elevation-3 + surface-container + shape-medium；option flex layout + state-layer hover/active/checked（8%/12% on-surface 跟 secondary-container 對選中）；`option::checkmark` 用 SVG mask 渲染；`::picker-icon` 用 on-surface-variant、open 時 rotate 180deg；`selectedcontent` styling。 | 2026-05-15 fix complete: CSS.supports('appearance', 'base-select')=true、sel_appearance=base-select、option display=flex padding=8px 12px borderRadius=8px cursor=pointer 全部 cascade。Popup 顯示需 user gesture，UI 上點開即驗證。 |

<!-- New rows go below. Do not edit past rows. -->

| 2026-05-22 | panel | no-title panel (panels 4–6 in "Without Toolbar" section) 頂端出現一條可見的 minus-icon 橫條 (`.z-panel-drag-button`)。ZK mold 在無 `title=` 且無 `<caption>` 子元件時，渲染 `.z-panel-drag-button` 取代 `.z-panel-head`；該元素是純 JS drag-wire anchor，DK 主題用 `display: none` 隱藏它。Marble theme 的 panel.css 沒有這條規則，導致橫條可見。ZK 在相同條件下也會對 root 加上 `.z-panel-noheader` class（來自 `Panel.domClass_()` in `Panel.ts`）。 | Skill `components/panel.md` DOM 結構錯誤（缺 `.z-panel-drag-button` 分支）且沒有記錄「永遠 `display: none`」的要求；contract `panel.md` 沒有 no-title panel 的 c8 check；CSS 起手式沒參照 DK 基準。 | skill + contract | Skill: `components/panel.md` 補正完整 DOM 結構、新增 `z-panel-drag-button` 段落說明 render 條件與 `display: none` 要求、補 `z-panel-noheader` state class；Contract: `panel.md` 新增 c8（`display: none`）及 no-title states-to-evaluate 項目；CSS: `panel.css` 把 drag-button 規則改為 `display: none`（移除先前的 `cursor/padding/opacity`）。 | 補完 skill + contract + css 三層後，panel 的 no-title 情形完整記錄；drag-button 在 3 個 no-title panel 的 computed display 均為 none，offsetWidth/Height = 0。 |

| 2026-05-15 | listbox-grouping (`.z-listgroup-inner`) | group header row 的 td `colspan="5"` 沒有跨欄、只占第一欄寬度（1318/1838px）— 視覺上 group title 跟一般 cell 一樣窄，破壞 grouping 視覺結構。Root cause：先前 CSS 在 `td.z-listgroup-inner` 設 `display: flex` (為了用 flexbox 排 icon + text)，但 `display: flex` 會關掉 `display: table-cell`，HTML table layout 直接 ignore 該 td 的 `colspan` 屬性 — cell 退回單一 column 寬。 | Skill 沒紀錄「給 `<td>` 設 `display: flex/grid` 會 break colspan」這條 HTML table 規則；contract 也沒檢查 group row td.width === row.width。對 grid `.z-grid-foot` colspan 也可能有同樣陷阱。 | skill + component CSS | `listbox.css`: `td.z-listgroup-inner { display: table-cell }` 還回 table cell 行為；flex 排版搬到 `.z-listgroup-inner > .z-listcell-content { display: flex; align-items: center; gap }`。Skill (`components/data-components.md`): 新增 "colspan + display:flex" trap section。 | 2026-05-15 fix complete: td_display=table-cell, td.width(1837) === row.width(1837), spansAll=true。 |
| 2026-05-15 | listbox-header / listheader 長文字 + hover-button 重疊 | menupopup 或 sortable 的 `.z-listheader` 內部會藏一個 hover-revealed `.z-listheader-button` (`opacity: 0` → 1 on hover)。先前 header rule 用 `white-space: nowrap` + `overflow: hidden` + `ellipsis`，當文字長時被切掉；hover 出現 button 又跟文字重疊在右邊區域。 | Skill 沒寫「listheader 含 button 時要 reserve padding-right 給 button slot；header 文字應允許 wrap」這條設計規則。Contract 沒檢查文字行為 (nowrap vs wrap) 跟 button overlap area。 | component CSS + skill | `listbox.css`: 改 `.z-listheader { white-space: normal; overflow-wrap: anywhere }` 允許長文字 wrap；新增 `.z-listheader:has(.z-listheader-button) { padding-right: calc(spacing-1 + 24px + spacing-2) }`（=36px）reserve button slot — 注意 ZK 把 button 放在 `.z-listheader-content` **裡**，`:has()` 用 descendant 不能用 `>` 直接子代。Skill 補「header text wrap + button slot reservation」說明。 | 2026-05-15 fix complete: whiteSpace=normal, paddingRight=36px 在全部 5 個 header 上, 短文字行還是單行不撐高、長文字 wrap 2 行；button hover 時不再蓋文字。 |
| 2026-05-15 | listbox-header / checkbox-only column 圖示右邊被切 | `<listheader checkable="true" width="40px">` 用 `.z-listheader-checkable` span 包 16×16 check icon。先前 header rule padding 16px 四邊 → 40 - 16 - 16 = 8px 可用區，16px icon 超出右邊被 `overflow: hidden` 切掉。 | Contract 沒檢查「窄 checkbox column 的 padding 是否塞得下 icon」。先前的 width 規則只設了 `.z-listheader-checkable` (span 自己) 沒處理 outer `.z-listheader` cell padding。 | component CSS | `listbox.css`: `.z-listheader:has(.z-listheader-checkable) { padding-left: spacing-1; padding-right: spacing-1 }`（4px 兩側）讓 40px cell 留 32px content 區 — 16px icon 完整顯示，跟 listcell 同步。 | 2026-05-15 fix complete: checkbox-cell padding 16px 4px, headerW=40, iconW=16, iconLeft=881, headerRight=905 → 右邊還有 8px 餘裕，icon 完整不再被切。 |
| 2026-05-15 | selectbox `::picker(select):not(:open)` 永遠 match → popup 永遠隱形 | Customizable Select spec：`:open` pseudo-class 屬於 `<select>` element，**不**屬於 `::picker` pseudo-element。先前的 CSS 用 `.z-selectbox::picker(select):not(:open) { opacity: 0 }` — 但 picker 本身不會有 `:open` 狀態，`:not(:open)` 永遠成立，所以 opacity 永遠 0、popup 開了也看不見。 | Skill (selectbox.md Customizable Select 段) 沒紀錄 `:open` 套用對象是 select（originating element）不是 picker。新規格陷阱沒人示警。 | component CSS + skill | `selectbox.css`: 刪掉 broken `.z-selectbox::picker(select):not(:open)` rule；改用 `.z-selectbox:open::picker(select) { opacity: 1; transform: 0 }` 把開啟態加在 select 側 (`:open` chained on select, then `::picker(select)`)。base `::picker(select)` 設 opacity 0 + translateY(-4) 為起始/關閉態，加 `transition-behavior: allow-discrete` + `@starting-style` block 讓 top-layer fade-in/out 動畫順暢。Skill (`components/selectbox.md`): 補一段「`:open` lives on the select, not the picker」trap 與正確 selector pattern。 | 2026-05-15 fix complete: rules dump 顯示新規則 `.z-selectbox:open::picker` + `@starting-style { .z-selectbox:open::p }` 已 parse；舊的永真 `:not(:open)` 完全移除。popup 可用 user click 開啟驗證視覺（CSS 結構正確）。 |
| 2026-05-15 | menubar 全部 menuitem 文字看不見 | `.z-menubar { background-color: transparent; color: on-primary }` + `.z-menu-content / .z-menuitem-content { color: on-primary }` — `on-primary` 是白色，但 root 沒有 primary 底色 (transparent)，外層 page 又是 surface (`rgb(247,249,252)`)，白字落在白底上完全看不見。先前的設計顯然假設外層會包 primary container，但 preview / 多數實際 use case 都沒包。 | Skill 沒紀錄「ZK menubar 預設 surface tone 應該是什麼」這條 MD3 設計決定；contract 沒做 menubar contrast (text vs bg) 檢查。先前撰寫者把 menubar 當 top-app-bar primary variant 寫，但 spec 預設應該是 surface variant。 | component CSS | `menu.css`: `.z-menubar { background-color: surface-container; color: on-surface }`，`.z-menu-content` / `.z-menubar .z-menuitem-content` 兩處 `color: on-primary → on-surface`，state-layer overlay `background-color: on-primary → on-surface`。MD3 navigation/top-app-bar surface variant pattern。 | 2026-05-15 fix complete: bar_bg=rgb(240,244,250) (surface-container), bar_color/content_color=rgba(0,0,0,0.87) (on-surface)，對比強烈、所有 menuitem label 清晰可讀。 |
| 2026-05-15 | listbox-header / checkbox cell 圖示右邊被切 (cell-side) | 與 Round N 的 header-side 同源 bug 的鏡像：header 的 `.z-listheader:has(.z-listheader-checkable)` rule 把 padding 從 16/16 改成 4/4；但 cell 的 `.z-listcell-content` 仍是 `padding: 16px`，inline `.z-listitem-checkable` span (40px 寬) 從 content 邊界推到右邊外溢，icon (16px) 落在 `L=893, R=909`，被 `.z-listcell` 的 `overflow: hidden` 在 R=905 切掉 4px。先前只修了 header 側，cell 側遺漏。 | Contract 沒檢查「header 與 cell 同欄 padding 對稱」這條 cross-element rule。Round N 只測 `header.right ≈ icon.right` 沒測 cell；rule 應加 `cell.right ≈ cell-icon.right`。 | component CSS | `listbox.css`: 新增 `.z-listcell:has(.z-listitem-checkable) > .z-listcell-content { padding-left: spacing-1; padding-right: spacing-1 }`，鏡像 header 已有 4/4 padding 規則。 | 2026-05-15 fix complete: cellContent padding 16px 4px, iconLeft=881, iconRight=897, cellRight=905，左右各 8px 餘裕，icon 完整無切。 |
| 2026-05-15 | listbox mold="paging" bottom 分頁列右靠不置中 | `.z-listbox-paging-bottom` (paging 在底部的 wrapper) 設 `justify-content: flex-end`，分頁 nav 被推到右邊。 paging 在頂部時 wrapper 是 `.z-listbox-paging-top` (本檔無 rule)，fallback 到 `.z-paging { justify-content: center }` 預設，所以頂部置中。視覺上 top 跟 bottom 行為不一致。 | Skill 沒寫「listbox/grid paging 視覺定位應該一致」這條規則；contract 沒檢查 top 跟 bottom 分頁列的對齊方向是否相同。grid 沒 override 所以兩端皆置中、listbox 卻 override 成 flex-end。 | component CSS | `listbox.css`: `.z-listbox-paging-bottom { justify-content: flex-end → center }`，與 `.z-listbox-paging-top` 對稱、與 grid/tree paging 一致。 | 2026-05-15 fix complete: bottom_wrapper L=865 R=1263 W=398, inner paging L=937 W=310 → 中心 1092 == wrapper 中心 1064 ±28（內部還有 paging 自己 padding 偏移、視覺已置中對齊）；先前 inner 靠右 R=1247 距離 wrapper R=1263 只剩 16px。 |
| 2026-05-15 | popup 底色 5/2 分歧 (selectbox + searchbox 用 surface-container；combobox/bandbox/datebox/chosenbox/colorbox 用 surface) | 7 個 popup 元件中 2 個吃 `--zk-color-surface-container`、其餘 5 個吃 `--zk-color-surface`。多數派採用 `surface`，使用者選擇統一向多數派靠攏（即修 selectbox+searchbox）。MD3 spec Menu Container 規定 `surface-container`，但 marble 主題已先決定全部 popup 用 `surface` (與 input/wrapper 同色，視覺 cohesive)。 | Skill 沒寫「全部 floating popup 應該用同一個 surface token」的規則；contract 沒做跨元件 popup bg 比對。每個 popup 元件獨立寫 CSS，沒人 cross-check token usage。 | component CSS + skill | `selectbox.css` `.z-selectbox::picker(select) { background-color: surface-container → surface }`、`searchbox.css` `.z-searchbox-popup { background-color: surface-container → surface }`。Skill (`reference/popup-tokens.md`，待寫): 全部 floating popup 統一 `background-color: var(--zk-color-surface)`。 | 2026-05-15 fix complete: 兩個檔已改完；build 後 7 個 popup 元件統一 surface。 |
| 2026-05-15 | searchbox popup 可能向上展開 (JS flip 行為) | `Searchbox.ts _repositionPopup()` 動態檢查 viewport space — 不夠下方空間時把 `vPosition` 從 `'after'` flip 成 `'before'`，所以 popup 跳到 trigger 上面。其他 popup（combobox/bandbox/datebox）大多 anchor `after_start`，**但 combobox 也有 `before_start` flip 邏輯** (Combobox.ts:183)，並非真的「永遠下展」。使用者只是觀察到 searchbox flip 較頻繁（popup 較高、bottom space 容易不夠）。 | Skill 沒寫「ZK popup 都有 JS flip 邏輯」這條 cross-component 事實；contract 沒測 flip 行為。使用者期望「永遠向下展」需要 JS override，CSS 無法控制。 | (待 JS) | (CSS 無解；待 follow-up round 處理 JS override) | TODO: 若使用者堅持永遠向下，需新增 marble theme JS layer override `Searchbox.prototype._repositionPopup` 強制 `vPosition='after'`；目前 round 僅修 bg 顏色。 |

| 2026-05-20 | tabbox (Horizontal/Bottom with Toolbar) | 啟用 `.z-tabbox-scroll` 後 4 個 scroll arrow 變成 flex-column 子節點各佔一行，把 `.z-tabpanels` 擠到看不見、自身產生 vertical scrollbar；user 在 `usecase/index.zul#tabbox` 看到 arrow 圖示突兀地顯示在 tabpanel 下方且內容被切。 | (1) Skill `tabbox.md` 只在 Notes 提到 ZK 會 inline 設 `style.right={toolbarWidth}px` 在 right-scroll button 上（暗示 absolute），但沒有把「scroll button 必須 `position: absolute`」寫成 composition invariant；(2) Contract c22-c30 只規範 display/width/height/color/font-size，沒 c22a/b/c/d 規範 position / 邊界 anchor / 給 strip 預留 40px hit-area；(3) Generator iter 7 完全按 contract 期望值字面實作，沒參考 contract HTML 那邊已有 `position: absolute`（contract HTML 不是 generator 輸入）；(4) Evaluator 只跑 spec-conformance 量測，全部 PASS 但實際 layout 已塌陷 — spec ≠ layout。 | skill + contract + agent | Skill (`.claude/skills/zk-component-rules/components/tabbox.md`): 新增 `## Layout invariants` 章節明列 scroll button `position: absolute`、strip `padding-{x,y}: 40px` 預留 hit area、root 不能直接用 `flex-direction: column` 不搭配 absolute scroll；Contract (`doc/contracts/tabbox.md`): 新增 c22a (position absolute)、c22b (directional edge anchor)、c22c (horizontal strip padding `0 40px` 在 `.z-tabbox-scroll`)、c22d (vertical strip padding `40px 0`)；Agent (`.claude/agents/zk-theme-evaluator.md`): 新增 §3c "Layout-regression sweep" workflow — 每個 component instance 跑 children-fit / no-content-overflow / absolute-overlay / hidden-but-still-occupying 4 條跨元件 layout 檢查，failure 進 failing-set 為 `lr-N`；Component CSS (`tabbox.css`): 4 個 scroll button 改 `position: absolute` + 邊界 anchor + 加 `.z-tabbox-scroll.z-tabbox-{top,bottom} > .z-tabs { padding: 0 40px }` 及 vertical 對稱規則。 | 2026-05-20 iter 9 fix complete: 19 個 tabbox 實例全部 `lr-* failures = 0`、tabpanels overflow 消失、Horizontal/Bottom with Toolbar 已正確 overlay。Layout-regression sweep 從此可在任何元件捕捉 spec-passing 但 layout-broken 的同類錯誤。 |

| 2026-05-20 | tabbox (Horizontal with Toolbar — follow-up) | iter-9 fix 把 scroll button `position: absolute` 加好之後 user 仍在 `usecase/index.zul#tabbox` 看到 `Horizontal with Toolbar` 區段的 scroll arrows 顯示且 panel 偏小；live DOM 顯示 toolbar 在 flex-column 第 2 列獨佔一整列，把 .z-tabs 擠到很窄 (88px)，而且 `.z-tabs` 套 `padding: 0 40px` 又進一步壓縮 ZK 計算的 inline width，造成 tabs 顯示空間 = 8px 級的破碎。 | (1) Skill 完全沒記載 `Tabs._scrollcheck` 的核心算式 `tabs.style.width = tbx.offsetWidth − toolbar.offsetWidth − btnsize` 隱含「toolbar 必須 `position: absolute` 才能不重複扣寬度」；(2) Skill 也未說 ZK `Toolbar.domClass_` 在 parent 是 Tabbox 時會自動補 `.z-toolbar-tabs` context class，iceblue/MUI 都把這個 class 設成 `position: absolute; right: 0; top: 0`；(3) Contract c22c/c22d 用 `padding` 而非 `margin`，padding 在 content-box 模型下會與 ZK 的 inline `width` 加乘 → 壓縮可視 strip；(4) Evaluator §3c layout sweep 不檢查 toolbar 在 tabbox 內是否 absolute，也不檢查 `.z-tabbox-scroll > .z-tabs` 的 padding 為 0；spec 全綠仍 layout 壞掉。 | skill + contract + agent | Skill (`.claude/skills/zk-component-rules/components/tabbox.md` `## Layout invariants`): 新增三條 invariant — (a) `.z-toolbar.z-toolbar-tabs` 必為 `position: absolute`、(b) `.z-tabs` 在 scroll mode 必須用 `margin` 不可用 `padding`、(c) flex-column root 允許但前提是 scroll-buttons + toolbar 全部 absolute；Skill (`toolbar.md`) 新增 `## Context class: .z-toolbar-tabs` 章節說明 ZK 在 Tabbox parent 下自動補 context class；Contract (`doc/contracts/tabbox.md`) c22c/c22d 把 `padding` 改成 `margin`、新增 c22e (toolbar absolute)、c22f (root overflow:hidden)；Agent (`.claude/agents/zk-theme-evaluator.md` §3c): 新增第 5 點「Toolbar-in-container check」量測 `.z-toolbar-tabs` 必須 absolute、第 6 點「In-flow-overlay-margin check」確認 `.z-tabbox-scroll > .z-tabs` `padding=0` 且 `margin` 為預期值；Component CSS (`tabbox.css`): 把 `padding: 0 40px` 改 `margin: 0 40px`、新增 `.z-tabbox > .z-toolbar.z-toolbar-tabs { position: absolute; top:0; right:0; }` 及 bottom 變體、root 加 `overflow: hidden`。 | 2026-05-20 follow-up fix complete: tabbox idx=1 (Horizontal with Toolbar) live DOM 顯示 toolbar `position:absolute`、`.z-tabs` `margin:0 40px`、panel `h=sh=151` 無 overflow；所有 9 個 tabbox 實例 layout sweep 通過。Scroll arrows 仍可見（5 個 closable MD3 tabs 真實 overflow 在 88px 可視 strip 內），屬正確 ZK 行為；test ZUL 可改寬 tabbox 或減少 tabs 數量以視覺消除。 |

| 2026-05-21 | tabbox vertical (Vertical Left) | `.z-tabbox-up-scroll` 與 `.z-tabbox-down-scroll` 寬度 80px，而 `.z-tabs` / `.z-tab` 都是 120px，視覺上 scroll 圖示比 tab 短一截。原因：使用者在 ZUL 寫 `<tabs width="80px">`，ZK 把 80 設成 `.z-tabs.style.width`；我們的 CSS `.z-tabbox-vertical .z-tabs { min-width: 120px }` 把 tabs 撐到 120px，但 `Tabs._scrollcheck` (line 425) `u.style.width = d.style.width = tabs.style.width` 把 80 propagate 給 scroll buttons，這條 inline 80 沒有 CSS min-width 兜底所以按鈕真的渲染成 80。 | (1) Skill 沒提到 ZK 把 `.z-tabs.style.width` 複製給 up/down scroll button 的事實；(2) Contract 沒檢測「相依 inline width」這條 cross-element 規則；(3) Evaluator §3c 4 條 layout-regression 不會比對「兩個元素的 inline width 應該一致或都被 CSS 兜底」。 | skill + contract + agent | Skill (`tabbox.md` Layout invariants): 新增「Vertical-orient scroll buttons inherit `.z-tabs` inline width」條款，要求 vertical scroll buttons 必須複製相同 min-width；Contract (`doc/contracts/tabbox.md`): 新增 c22g `.z-tabbox-scroll > .z-tabbox-{up,down}-scroll { min-width: 120px }`；Evaluator §3c: 新增 check 7「Inherited-inline-width check」量測 ZK runtime 已知會跨元素複製 inline width 的情境；Component CSS (`tabbox.css`): vertical scroll buttons block 加 `min-width: 120px`。 | 2026-05-21 fix complete: vertical scroll buttons offsetWidth=120 等於 .z-tabs offsetWidth、等於 .z-tab offsetWidth。 |

| 2026-05-21 | tabbox accordion (Accordion Mold) | `height="300px" mold="accordion"` 五個 section 的情形下，被選 tab 的 `.z-tabpanel-content` 高度幾乎為 0（只剩 padding 32px），內容文字完全不可見。同樣的 ZUL 在 usecase2 (沒設 height) 是正常。原因鏈：(a) base `.z-tabpanel { min-height: 80px }` 未在 accordion override，5 個 section 變成 5×80=400px 已超過 tabbox 300px；(b) 即使 reset 了 min-height，base `.z-tab-content { padding: 12px 16px }` 與 accordion `.z-tab` padding 12px 疊加，header 變 68px、5×68=340 仍超過 300；(c) `Tabpanel._fixPanelHgh` (Tabpanel.ts line 166-171) 用 `tbx.offsetHeight - Σ(sibling offsetHeight) - tab.offsetHeight` 算 cave 高度，算出負數就 `jq.px0` clamp 到 0，把 inline `style.height: 0px` 寫進去。 | (1) Skill 完全沒提 `Tabpanel._fixPanelHgh` 這條算式以及它對 accordion section 高度的隱含 budget；(2) Skill 也沒警告 base `.z-tabpanel { min-height }` 與 base `.z-tab-content { padding }` 會「貫穿」accordion override 造成 budget 爆掉；(3) Contract 沒檢測 accordion section 與 header 的高度上限；(4) Evaluator §3c layout sweep 不檢查「inline style.height === 0px 但 scrollHeight > 0」這種 animation-clamped 狀態，spec 全綠仍會看不到內容。 | skill + contract + agent | Skill (`tabbox.md`): 新增「Accordion-mold section min-height MUST be reset to 0」與「Accordion `.z-tab-content` MUST reset its base padding to 0」兩條 invariant，並把 `_fixPanelHgh` 算式寫進來；Contract (`doc/contracts/tabbox.md`): 新增 c22h `.z-tabbox-accordion .z-tabpanel { min-height: 0 }`、c22i `.z-tabbox-accordion .z-tab-content { padding: 0 }`；Evaluator §3c: 新增 check 8「Animation-stuck-on-zero check」量測 `cave.style.height === '0px' && cave.scrollHeight > 0`；Component CSS (`tabbox.css`): accordion `.z-tabpanel` 加 `min-height: 0`、accordion `.z-tab-content` 加 `padding: 0`、accordion `.z-tab` padding 從 12/16 改 8/16 + min-height 從 52 改 48（aligned to MD3 48px 標準）。 | 2026-05-21 fix complete: tabbox 標準頁 height=300 + 5 sections，每個 header 48px、總高 48+4×49=244、剩餘 56px 給選中 cave、內容文字可見。 |


| 2026-05-21 | tabbox (Vertical Right — orient=right 回歸) | `usecase/index.zul#tabbox` 的 Vertical Right 區段 `z-tabbox-up-scroll` / `z-tabbox-down-scroll` 顯示在 tabbox 的左邊，而 tabs 在右邊；scroll icon 沒跟 tabs 同側、視覺斷裂。User 報告「這個問題我之前應該回報過」表示先前修過後又被改壞，要求「想一個方法避免再次造成錯誤」。Root cause：CSS 把 4 個 directional scroll button 寫成 4 條 single-line rule，其中 `.z-tabbox-up-scroll { left: 0 }`、`.z-tabbox-down-scroll { left: 0 }` 是 horizontal 邊界共用 default，對 `.z-tabbox-left`/`.z-tabbox-vertical` 正確（tabs 在左），但 `.z-tabbox-right` 沒有 override 還是繼承 `left: 0`，按鈕落到左邊。 | (1) Skill 沒有把「vertical scroll button 必須與 tabs 同側」寫成 layout invariant — 上次修完後跟著前後 round 一起 refactor，這條約束沒寫進來、後續 reformat 時不知不覺改回對稱的 `left: 0`；(2) Contract 沒有特別 c22 row 規範 `.z-tabbox-right` 的 scroll-button 位置；(3) Evaluator §3c 8 條 layout-regression 不檢查「scroll button 是否與對應的 tabs 在同一側」這條方向性 invariant、spec 全綠仍然錯位。 | skill + contract + agent | Skill (`tabbox.md` Layout invariants): 新增「Vertical scroll-button horizontal anchor MUST follow the tabs' side」條款，明寫 `.z-tabbox-left`/`.z-tabbox-vertical` 用 `left: 0`、`.z-tabbox-right` 需 explicit override `left: auto; right: 0`，並警告「regression-prone — 重構共用 directional rules 時容易遺失」；Contract (`doc/contracts/tabbox.md`): 新增 c22j `.z-tabbox-right > .z-tabbox-{up,down}-scroll { left: auto; right: 0 }`；Evaluator §3c: 新增 check 9「Scroll-button-side-mirror check」量測 vertical orient 中 scroll button `offsetLeft` 是否落在 tabs 同側（left/right half）；Component CSS (`tabbox.css`): 在 4 條 directional rule 後緊接著加 `.z-tabbox-right > .z-tabbox-up-scroll, .z-tabbox-right > .z-tabbox-down-scroll { left: auto; right: 0 }` 並寫 comment 警示這條 override 不能被「合併簡化」掉。 | 2026-05-21 fix complete (待驗證): CSS override 已加；skill / contract / evaluator 三層都記錄了 invariant + automated check，下次有人 refactor 共用 directional rule 時，evaluator check 9 會立刻 FAIL。 |

| 2026-05-21 | tabbox accordion (Accordion Mold border-radius mismatch) | `.z-tabbox-accordion` 設 `border-radius: var(--zk-shape-corner-medium)`，但其下的 children（headers `.z-tab`、`.z-tabpanel-content` cave）都是方角；rounded parent + 方角 children 在四個角落露出 children 的 sharp 邊。一般 fix 是 `overflow: hidden` 把 children 剪到圓角範圍內，但 accordion 必須避用 `overflow: hidden`（會干擾 `jq.slideDown` 對 cave 的 height 量測）。User 引用 MD style 表示「children 也不應該有 border-radius」、要求把 parent 拿掉 border-radius 走 flat 設計。 | (1) Skill / contract 沒有寫「accordion mold MUST NOT use border-radius」這條結構性禁止；(2) Skill 沒提「rounded parent + square children + 無 overflow 剪裁 = corners 漏出」這條跨組件 CSS 陷阱；(3) Evaluator §3c 8 條 layout-regression 沒檢查「parent 有 border-radius 但 overflow ≠ hidden 且 children 方角」這個 visual-defect pattern。 | skill + contract + agent | Skill (`tabbox.md`): 新增「Accordion mold MUST NOT set `border-radius` on `.z-tabbox-accordion`」invariant，附帶解釋為何 `overflow: hidden` 不能用；Contract (`doc/contracts/tabbox.md`): c10 由 `--zk-shape-corner-medium` 改為 `0`、新增 c22k 描述禁止 border-radius；Evaluator §3c: 新增 check 10「Parent-radius-without-clip check」量測 `border-radius > 0` 且 `overflow ≠ hidden` 且 first/last children 在對應角為方角時 FAIL；Component CSS (`tabbox.css`): 移除 `.z-tabbox-accordion` 的 `border-radius` 並加 inline comment 說明禁止改動。 | 2026-05-21 fix complete (待驗證): CSS 已移除 border-radius；skill / contract / evaluator 三層都記錄。新的 check 10 是通用規則，未來任何 component 若把 rounded parent 配方角 children 但無剪裁，自動會被 evaluator 標出。 |

| 2026-05-25 | dropupload | Preview ZUL used `disabled="true"` on `<dropupload>` but `Dropupload` extends `XulElement` (Java) / `zul.Widget` (TS) — neither base class provides `setDisabled`; no `disabled` property exists anywhere in Dropupload.java or Dropupload.ts. Attribute is silently ignored at runtime; the "disabled" dropupload behaves identically to the default one. | Preview ZUL was written assuming dropupload behaves like an input component (which inherits disabled from XulElement); no skill entry existed documenting that dropupload has NO disabled state. | skill | Skill: note in `components/dropupload.md` that `disabled` is unsupported; preview ZUL `dropupload.zul` simplified to single default state only. | — |

| 2026-05-25 | fileupload | Preview ZUL used `multiple="true"` on `<fileupload>` but `Fileupload` extends `Button` (Java + TS) and Button has no `multiple` Java property — multi-file upload is controlled via the `upload` attribute string (e.g. `upload="true,multiple=true"` on a regular `<button>`), not a standalone `multiple` attribute. Attribute silently ignored. | Preview ZUL was written assuming `fileupload` has a first-class `multiple` attribute like HTML `<input type="file" multiple>`; no skill entry existed clarifying that fileupload is simply a Button with pre-wired upload and exposes no `multiple` property. | skill | Skill: note in `components/fileupload.md` that `multiple` attribute is not supported; preview ZUL `fileupload.zul` — removed the "Multiple" row. | — |

| 2026-05-25 | stepbar | js-source drift detected — `Stepbar.ts` was updated by commit `009fb2b6c` ("ZK-5597: vertical stepbar") after the contract hash was recorded. The stored `js-source-hash` in `doc/contracts/stepbar.md` is `b09571285d7b559ed2be0d3045df17fa0c8179b667bb59c37dab6e5f9ed2aea9`; recomputed hash is `6549c549e6302649c9f1305f836f45283fa3c640fdf4118d909f1f3b718df3d7`. The vertical stepbar feature may introduce new DOM structure or state classes not reflected in the current contract or skill. | The js-source-hash check caught this correctly; no measurement was taken. The gap is that the contract needs re-authoring against the updated Stepbar.ts before evaluation can resume. | js-drift | `doc/contracts/stepbar.md` — re-run `zk-spec-author stepbar` to update hash and revise any expected-values or state-matrix rows affected by ZK-5597. |
| 2026-05-25 | organigram | js-source drift detected — contract hash `6604d5a4bb62757bb1a786388da924ed80f3f8c874c7d631b3493fc59dd6b15c` does not match recomputed hash `be8916eebd1c58fae43660c7791b47c27f27127a97eac9fc5bb75d2fe055a05f` for the four source files (Organigram.ts, Orgchildren.ts, Orgitem.ts, Orgnode.ts) under ZK10/zkcml/zkmax. The contract's structural assertions and state-class documentation may be stale relative to the current ZK source. | §0b js-source-hash drift detection gate fired; measurement refused | js-drift | doc/contracts/organigram.md |

| 2026-05-28 | navbar, menubar | Both navigation components were `background-color: transparent` ("inherit from container"), which is a deviation from MD3 spec. MD3 Navigation Drawer = `surface-container-low`, MD3 Top App Bar / Menu Bar = `surface-container`. Standalone use (no surrounding tinted container) produced floating-text look indistinguishable from page surface; nesting inside a tinted container also looked off because the tone hierarchy was missing. | The original DESIGN choice was theme-author preference ("avoid card-in-card"), not measured against MD3 spec. No contract assertion existed for either widget's background — both contracts left `c1` as `transparent OR surface`, so the evaluator never pushed the implementation either way. | contract + component CSS + design doc | CSS: `nav.css` `.z-navbar { background-color: var(--zk-color-surface-container-low) }`; `menu.css` `.z-menubar { background-color: var(--zk-color-surface-container) }`. Contract: `menubar.md` c1 to `var(--zk-color-surface-container)`; `navbar.md` c1 to `var(--zk-color-surface-container-low)` (pending). DESIGN.md §3: add navigation-surface tone mapping table. | Pending evaluator pass — expected: standalone navbar/menubar reads as navigation chrome with subtle tonal step above page surface. Composition rule (override to `transparent` when nested in tinted region) recorded in CSS comments. |

| 2026-05-28 | datebox (timezone selector) | The `<select>` inside `.z-datebox-timezone` styled only the closed-state trigger (border/padding/arrow); the popup opened with native browser styling — no MD3 surface, elevation, options padding, hover state, checkmark, or animated picker arrow. Inconsistent with `.z-selectbox` which already uses the Customizable Select spec (Chrome 130+) for popup styling. | The contract for datebox covered the date picker popup (`.z-datebox-popup .z-calendar`, timebox section) but had no assertion for the timezone `<select>` inside the popup — that sub-element was treated as out-of-scope. No cross-component rule said "every native `<select>` in the theme must adopt the Customizable Select treatment." | component CSS + skill | CSS: `datebox.css` adds `@supports (appearance: base-select)` block mirroring `selectbox.css`: opt-in `appearance: base-select`, `::picker(select)` popup with `--zk-elevation-3` + `surface`, MD3 option states, `::checkmark` SVG mask, animated `::picker-icon`. Skill (TODO `reference/customizable-select.md`): record the cross-component rule that every theme-styled `<select>` must adopt Customizable Select with the shared popup token set. | Pending — verify in Chrome 130+ that timezone popup matches selectbox popup visually; older browsers retain the existing border/arrow fallback. |

| 2026-05-28 | borderlayout | Visual design too close to default ZK theme: 8px gray opaque splitter slab, 16×56 outlined pill with grip-dots at 0.7 opacity, surface-with-border collapse button. User reported "似乎不符合 material design" and "z-west-icon z-icon-ellipsis-v should be visible." | No contract assertion captured "design must visually differ from default ZK theme"; no Mira/MUI reference exists for the splitter-button pattern so the harness had no anchor to push against. Grip icon visibility (opacity 0.7 + 12px size + on-surface-variant color) was the contract's intent but produced near-invisible glyphs against the surface-container-highest fill that the redesign needed. | contract + component CSS + design doc | CSS: `borderlayout.css` — splitter bar transparent at idle, primary-tinted on hover (6px hit-area); collapse button is now Material filled-tonal (surface-container-highest fill, no border, elevation-2 → elevation-3 lift), enlarged to 20×64 / 64×20, grip icons at 14px opacity 1 (caret 12px opacity 0.85). Contract: c12-c30 updated to new values; design contract narrative rewritten. | Pending evaluator pass — expected: borderlayout reads as Material filled-tonal collapse pills with hover-only resize highlight, clearly distinct from default ZK gray-slab design. |

| 2026-05-28 | vlayout | `.z-vlayout` had redundant `display: flex; flex-direction: column; align-items: stretch` — but `.z-vlayout-inner` is a `<div>`, so block flow already stacks children vertically with full width. User verified removing the flex rules produces no visual change. | No skill rule encoded "if the inner wrapper is a block-level `<div>`, do not add flex to the parent." Generator wrote flex defensively without checking the wrapper's display mode. | skill + component CSS | CSS: `layout.css` simplified `.z-vlayout` to `box-sizing: border-box`; removed `width:100%` from inner (block default). Skill (TODO `reference/layout-wrapper-defaults.md`): record "ZK *-inner wrappers are always block-level; do not stack flex/grid unless the children need flex alignment beyond block defaults." | Removing redundant flex is a CSS-minimum win. No regressions expected since block stacking is identical visually. |

| 2026-05-28 | cardlayout + (cross-component default spacing) | "cardlayout 跟三個 button 之間的空隙非常小" — by default cardlayout-then-button has zero margin (both are inline-flow), forcing the user to wrap in `<vlayout spacing="…">` or add sclass spacing. User goal: "預設擺下去就會好看，不需要一直增加各種 CSS class" (ZK users are back-end engineers, don't want to spend time on front-end layout). | No theme-wide rule for "default vertical rhythm between adjacent block widgets." Every component fixes its own internal spacing but the cross-widget margin space was left unaddressed. The harness has no contract checking "two siblings drop side-by-side and look acceptable." | base CSS (utilities) + design doc | CSS: `_utilities.css` adds (1) adjacent-sibling rule `widget-A + widget-B { margin-top: var(--zk-spacing-3) }` for all common block-widget pair combinations, (2) `.z-button + .z-button { margin-left: var(--zk-spacing-2) }` for inline button rhythm, (3) `.z-stack` / `.z-stack-{sm,md,lg,0}` Lobotomized-Owl utility, (4) `.z-cluster` / `.z-cluster-{sm,md,lg}` flex-wrap utility. DESIGN.md (TODO): add "Default rhythm" section explaining adjacent-sibling pattern. | Pending verification in browser. Users can opt out with `.z-stack-0` or explicit `margin-top: 0`. Sweep planned: confirm no regressions on existing usecase pages that already wrap children in vlayout. |


| 2026-05-27 | tbeditor | The contenteditable canvas (`.z-tbeditor-editor`) shows the browser's default blue focus ring (`outline: rgb(0,95,204) auto 1px`) spanning the whole editor when focused. Theme never reset it. | tbeditor is a Trumbowyg-wrapped addon with no MD3/Mira analog; harness never measured the contenteditable's `:focus` outline. | component CSS | `tbeditor.css`: `.z-tbeditor-editor { outline: none }` + `.z-tbeditor-box:focus-within { border-color: primary }` (focus moves to the wrapper). | 2026-05-27 fix verified live: focused editor `outline-style: none`; box border turns primary on focus-within. |
| 2026-05-27 | tbeditor | Formatting dropdown rendered full-viewport-wide (1777px) with 155px-tall buttons. Two causes: (a) `.z-tbeditor-dropdown` had no width and buttons no display/height, so each format option inherited the editor's block sizing; (b) the dropdown's `<svg><use href="#…">` icons have no intrinsic size and defaulted to 300×150, clipped to black bars. | tbeditor dropdown is a Trumbowyg construct with no analog; harness never checked dropdown geometry or that sprite-`<use>` SVGs need explicit width/height. | component CSS | `tbeditor.css`: `.z-tbeditor-dropdown { width: 200px }`, buttons `display:block; height:36px; line-height:36px; font-size; overflow:hidden`, and `.z-tbeditor-dropdown button svg { width:18px; height:18px }`. Matches ZK default theme (`tbeditor.less` lines 200–227). | 2026-05-27 fix verified live: dropdown 200px wide, 36px rows, Paragraph/Quote/Header 1–4 readable. |
| 2026-05-27 | tbeditor | Fullscreen toolbar button did nothing. The widget JS correctly toggles `.z-tbeditor-fullscreen` on the box and `.z-tbeditor-body-fullscreen` on `<body>`, but the theme's only fullscreen rule set `box-shadow` — no positioning, so the editor never expanded. | ZK-portable fact never recorded: the fullscreen class is inert without theme-supplied `position:fixed` layout. Harness measured no fullscreen state. | skill + component CSS | `tbeditor.css`: `body.z-tbeditor-body-fullscreen { overflow:hidden }`, `.z-tbeditor-box.z-tbeditor-fullscreen { position:fixed; inset:0; width/height:100%; z-index:99999; border:none }`, editor `flex:1`. (Skill note candidate: `components/tbeditor.md` — fullscreen class needs theme positioning.) | 2026-05-27 fix verified live: clicking fullscreen makes box `position:fixed`, fills viewport (1904×918), body locked. |
| 2026-05-27 | signature | The 3 toolbar buttons (undo/save/clear) showed text labels only — no icons. The widget JS emits `<i class="z-signature-tool-button-undo/save/clear z-signature-tool-button-icon">` with NO `z-icon-*` class and NO content, so the theme's generic icon machinery (`[class^="z-icon-"]::before`) never applied and nothing rendered. | ZK-portable fact never recorded: signature's tool-button icons use bespoke `z-signature-tool-button-{undo,save,clear}` classes (not `z-icon-*`); the theme must supply the mask itself. Default ZK theme used FA glyphs (`\f112/\f0c7/\f00d`). | skill + component CSS | `signature.css`: added `.z-signature-tool-button-icon::before` mask rule + `--_icon` Lucide URIs (undo-2/save/eraser) on the three classes; hid `.z-signature-tool-button-label`; buttons made 32×32 icon-only. (Skill note candidate: `components/signature.md` — tool-button icon classes are not `z-icon-*`.) | 2026-05-27 fix verified live: 3 icon-only 32×32 buttons render undo/save/eraser; labels `display:none`. |
| 2026-05-27 | (icon map) pdfviewer | `z-icon-rotate-right` rendered blank — `rotate-right` was absent from `FA_TO_LUCIDE` in `scripts/build-css.js` (only `rotate-left` existed), so no `--_icon` was emitted. | The FA→Lucide alias map is hand-maintained; no check ensures FA names used by addon widget JS (pdfviewer toolbar) are all mapped. | agent/build | `scripts/build-css.js`: added `'rotate-right': 'rotate-cw'`. Emits `.z-icon-rotate-right` in norm.css.dsp. | 2026-05-27 fix verified live: rotate-right `::before` mask-image present. |
| 2026-05-27 | stepbar | Each step title overlapped the connector line into the next step — the connector `::before` had `margin-left: -12px` (negative), pulling it ~12px back over the previous step's title. | Theme value bug (negative margin); contract/skill don't assert connector-vs-title spacing. | component CSS | `stepbar.css`: connector `margin: 0 var(--zk-spacing-3)` (symmetric positive) — removes the negative left margin. | 2026-05-27 fix verified live: connector now starts +12px after the title; no overlap across all 3 steps. |
| 2026-05-27 | tabbox | With scroll arrows + a toolbar + closable tabs, the right-scroll arrow overlapped the last visible tab (e.g. "Notifications"/"Appearance"). ZK reserves arrow space via `_getArrowSize()` (`Tabs.ts:241`) = `left.offsetWidth + right.offsetWidth`, but its measurement lagged our rendered 40px arrows (it reserved only ~60px total), and the right arrow is positioned at `right: toolbarWidth` (`Tabs.ts:211`), landing it ~20px over the last tab. | ZK-portable fact never recorded: scroll-arrow space reservation depends on a JS measurement that can disagree with the rendered arrow width, so margin-based reservation alone can't guarantee clearance. | skill + component CSS | `tabbox.css`: scroll arrows given opaque `background-color: var(--zk-color-surface)` (MUI pattern — partially-scrolled tab slides cleanly *under* the button); hover mix changed to layer over surface not transparent. (Skill note candidate: `components/tabbox.md` — arrow reservation is measurement-dependent; use opaque arrows.) | 2026-05-27 fix verified live: right arrow bg opaque white; tab no longer bleeds through. |
| 2026-05-27 | pdfviewer | User question: is the page off-center in fullscreen the intended design? | Native fullscreen could not be triggered under browser automation ("Permissions check failed"), so no measurement in fullscreen. | (no code change) | Centering is `.z-pdfviewer-page { margin: 0 auto }` — mode-independent; `.z-pdfviewer:fullscreen` only sets width/height/border-radius and never disables it. Normal mode verified centered (~618px each side). It only appears off-center when the page is zoomed wider than the viewport (then it scrolls — expected). | 2026-05-27 no change needed; centering is by design. |
| 2026-05-27 | organigram | `.z-orgnode::after` (the parent drop-out line) was drawn for *every* node, so (a) leaf nodes dangled a stray 12px stub below them, and (b) multi-child parents drew a stub *below* the horizontal bus — because the bus sits flush against the parent node's bottom (zero gap), so the bus + per-child drop-ins already connect everything and the parent's own drop-out is redundant and overshoots. | Skill's connector invariant described `.z-orgnode::after` as "the drop into its own nested orgchildren (if any)" but never recorded that the bus abuts the parent node bottom — so the drop-out is only needed for the bus-less only-child case and must be suppressed for multi-child and leaf nodes. | skill + component CSS | `organigram.css`: scoped the rule to `.z-orgitem:has(> .z-orgchildren > .z-orgitem:only-child) > .z-orgnode::after`; `components/organigram.md` connector invariant updated to state the only-child condition. | 2026-05-27 fix verified live: 0/14 nodes draw the drop-out (dataset is all leaf + multi-child); bus still connects multi-child generations. |
| 2026-05-27 | tbeditor | A stray bordered box appeared under the toolbar — the raw `<textarea>` (`.z-tbeditor-textarea`, Trumbowyg's HTML-view source) had no theme rule, so the browser's native textarea border (1px #767676) rendered. | The skill ALREADY mandates this (`tbeditor.md` lines 22-23, 84-86: editor-visible collapses textarea, editor-hidden swaps visibility) — but the harness has no MD3/Mira analog for Trumbowyg's dual views, so the addon's CSS was never run through the generator/evaluator and shipped without the rule. **Implementation gap, not a rules gap.** | implementation (CSS) | `tbeditor.css`: reset textarea (`border:none`), collapse it in WYSIWYG via `.z-tbeditor-editor-visible .z-tbeditor-textarea {height:1px;opacity:0;padding:0}`, and hide the editor in HTML view via `.z-tbeditor-editor-hidden .z-tbeditor-editor {display:none}`. No skill change — `tbeditor.md` was already correct. | 2026-05-27 fix verified live: textarea h:1, opacity:0, border:0 — stray box gone; link+formatting unaffected. |
| 2026-05-27 | tbeditor | Formatting/link dropdowns floated to the page's top-left instead of attaching under their toolbar button. Trumbowyg sets inline `position:absolute; top=buttonPaneHeight; left=btn.offsetLeft-box.offsetLeft` on the dropdown — coordinates relative to `.z-tbeditor-box` — but the theme's `.z-tbeditor-box` was `position:static`, so the dropdown's offset parent was `<body>` and the coords landed near the viewport origin. | The skill ALREADY states this (`tbeditor.md` line 54: "theme must not change `position` — Trumbowyg absolutely positions overlay/modal relative to the box"; line 75: dropdown appears directly below its button). The theme's flex rewrite of `.z-tbeditor-box` dropped `position:relative`, violating the existing rule. **Implementation gap, not a rules gap.** | implementation (CSS) | `tbeditor.css`: added `position:relative` to `.z-tbeditor-box`. No skill change — `tbeditor.md` line 54 already covers it. | 2026-05-27 fix verified live: dropdown offsetParent is `.z-tbeditor-box`, left=button-left, top=button-bottom for both formatting and link. |
| 2026-05-27 | tbeditor | Dropdown rows: the glyph and label were not vertically centred on the same line (icon `float:left` top-aligned against the 36px line box); and buttons that open dropdowns (`.z-tbeditor-open-dropdown`) had no affordance. | Pure implementation/theme choices. The `.z-tbeditor-open-dropdown` marker + caret is ALREADY in the skill (`tbeditor.md` line 37). **Implementation gap, not a rules gap.** | implementation (CSS) | `tbeditor.css`: dropdown buttons `display:flex; align-items:center` (svg as fixed flex child, no float); added `.z-tbeditor-open-dropdown::after` caret (mirrors default theme). No skill change. | 2026-05-27 fix verified live: svg-vs-button center delta 0px; caret present on formatting+link buttons. |
| 2026-05-27 | pdfviewer | In fullscreen the first page abutted the top edge of the browser with no breathing room — `.z-pdfviewer-page` used `margin: 0 auto` (horizontal centring only), so pages stack flush to the canvas top in every mode (just most visible fullscreen). | Theme spacing choice; harness had no fullscreen-canvas spacing assertion. | component CSS (theme value) | `pdfviewer.css`: `.z-pdfviewer-page { margin: var(--zk-spacing-4) auto }` — adds top/bottom breathing room + inter-page gap. | 2026-05-27 fix verified live: top gap from container now 16px in normal mode; margin applies in fullscreen too. Answer to user's question: the flush-to-top look was not intended design. |
| 2026-05-27 | organigram | **Regression from the 2026-05-27 leaf-stub fix (row above).** That fix scoped the parent drop-out (`.z-orgnode::after`) to the only-child case, on the false premise that the bus sits flush against the parent node. Result: multi-child parents drew NO short vertical line between node and bus — the user's reported "短的垂直連接線消失了". The skill invariant (line 68) had been rewritten last round to claim the bus is flush, contradicting the skill's own lines 82/84 which already say `.z-orgnode:not(:only-child)::after`. | The ZK default theme's connector model (`ZK10/zkcml/zkmax/.../organigram.css.dsp`) uses `.z-orgnode:not(:only-child)::after` + a node↔bus gap from `.z-orgnode { margin:10px }`. Our theme never opened that gap (bus flush) so the previous author "fixed" the resulting overshoot by deleting the line instead of opening the gap. | skill + component CSS | `organigram.css`: `.z-orgchildren { margin-top: var(--zk-spacing-3) }` opens the gap; drop-out rule changed to `.z-orgitem > .z-orgnode:not(:only-child)::after { top:100%; height: var(--zk-spacing-3) }`. `components/organigram.md` line 68 corrected to the every-parent model (consistent with lines 82/84). | 2026-05-27 fix verified live: 6/6 parents draw the drop-out (1px, 12px) with a 12px node↔bus gap; 0/8 leaves draw any. |
| 2026-05-27 | textbox (multiline) | Focusing a `textarea.z-textbox` made its height visibly shrink. The shared input `:focus` rule sets `padding: 0 calc(spacing-3 - 1px)` (vertical padding 0); since a textarea also carries `.z-textbox`, focus wiped its 8px vertical padding while the border grew 1px→2px, collapsing the box. | The shared `:focus` rule uses the `padding` shorthand, which silently overrides the multiline variant's vertical padding — a CSS-authoring trap, not a ZK fact. No MD3/Mira analog exercises the textarea focus state. | implementation (CSS) | `input.css`: added `textarea.z-textbox:focus, …:focus-visible { padding: calc(var(--zk-spacing-2) - 1px) calc(var(--zk-spacing-3) - 1px) }` — re-asserts vertical padding, the -1px absorbing the border growth so box height is constant. | 2026-05-27 override rule confirmed in served CSS; math nets 0px height delta (before 8px pad+1px border = after 7px pad+2px border). Live `:focus` measurement blocked — automation window reports `document.hasFocus()===false` so `:focus` never matched. |
| 2026-05-27 | listbox (mold=select) | The Select-Mold listbox rendered as an unstyled native `<select class="z-select">` while the visually-identical Selectbox (`.z-selectbox`) was fully themed. ZK renders `<listbox mold="select">` as the SAME native control as Selectbox (`<select class="z-select">` + `<option class="z-option">`), not the table-based listbox DOM. | Skill `selectbox.md` documented the native-select fact for Selectbox but never recorded that listbox's select mold is the same control with a different class; no contract checked `.z-select`. The two controls' CSS bundles are separate and each loads only with its own widget, so `.z-select` styling can't live in `selectbox.css`. | skill + component CSS | `listbox.css`: appended a `.z-select` block mirroring `selectbox.css` (base native-select + hover/focus/disabled + `@supports (appearance: base-select)` popup). `components/selectbox.md`: added "`<listbox mold=select>` is the same native control" section noting the bundle-duplication requirement. | 2026-05-27 fix verified live: `.z-select` computes min-height 40px, 1px outline border, 4px radius, `appearance: base-select` — identical to `.z-selectbox`. |
| 2026-05-27 | tbeditor | Dropdown popup corner radius (`var(--zk-shape-corner-small)` = 8px) was inconsistent with every other floating menu popup (combobox/bandbox/selectbox use `var(--zk-shape-menu)` = 4px). User asked whether it should match. | "Use the same value as sibling popups" + radius-only = weak signal → theme value. No cross-component popup-radius consistency assertion exists. | component CSS (theme value) | `tbeditor.css`: `.z-tbeditor-dropdown { border-radius: var(--zk-shape-menu) }`. | 2026-05-27 fix verified live: dropdown rule resolves `--zk-shape-menu` → 4px, matching the other popups. |
| 2026-05-27 | pdfviewer | User design question: should the PDF page be vertically centred in fullscreen (per Material Design)? | n/a — design question, not a defect. | (no code change) | Answer: **no, don't vertically centre.** Every established document viewer (Chrome PDF, pdf.js, Acrobat, Drive) and MD's guidance for scrollable content top-aligns pages in a scrollable canvas — vertical centring breaks the moment a page is taller than the viewport (the top scrolls out of reach). Current `.z-pdfviewer-page { margin: var(--zk-spacing-4) auto }` (horizontal centre + 16px top/bottom breathing room, added the row above) is the correct MD-consistent behaviour. | 2026-05-27 no change; documented the rationale. |
| 2026-05-27 | selectbox, listbox (mold=select) | Both `::picker(select)` popups used `var(--zk-shape-corner-medium)` (12px) corner radius, inconsistent with every other floating option-list popup (combobox, bandbox, menu, toolbar, bandpopup, tbeditor all use `var(--zk-shape-menu)` = 4px). User asked to confirm and unify. | "Use the same value as sibling popups" + radius-only = weak signal → theme value. No cross-component popup-radius consistency assertion exists (same class of gap as the tbeditor radius row above). | component CSS (theme value) | `selectbox.css` + `listbox.css`: `::picker(select) { border-radius: var(--zk-shape-menu) }` (was `--zk-shape-corner-medium`). Both files edited since each bundle loads only with its own widget. | 2026-05-27 fix: both picker popups now resolve `--zk-shape-menu` → 4px, matching all other popups. |
| 2026-05-27 | selectbox, listbox (mold=select) | User reported DevTools shows every `var(--zk-*)` in the `@supports (appearance: base-select) .z-selectbox option {…}` rule as "not defined". NOT a real bug: `getComputedStyle` on the `<option>` resolves all tokens correctly both closed AND with the popup open in the top layer (`--zk-spacing-2`→8px, padding→8px 12px, `--zk-color-primary`→#376fd0). It is a Chrome DevTools Styles-pane artifact — its var() resolver can't locate the `:root` definitions for `<option>`s hosted inside `::picker(select)` (top layer), so it strikes them through though the engine applies them. | No harness miss — this is a debugging false-positive that would waste a future session "fixing" correct CSS (e.g. hardcoding values or re-declaring tokens on the option). Knowledge gap, not an implementation gap. | skill (no code change) | `components/selectbox.md`: added "DevTools falsely reports `:root` vars as not-defined inside `::picker` options" trap; verify with `getComputedStyle`, never hardcode. | 2026-05-27 confirmed live (popup open): option computes correct values; documented as DevTools artifact, no CSS touched. |
| 2026-05-27 | drawer | Clicking the mask did not close the drawer. ZK binds mask `onClick`→`close` and `close()` ran, but `setVisible(false)` registers `jq(real).one('transitionend', …)` before hiding (display:none) — and the theme defined NO `transform`/`transition` on `.z-drawer-real`, so `transitionend` never fired and the panel stayed `display:block` forever. | ZK-JS animation contract: the close path is gated on a real CSS transition completing. No MD3/Mira reference exercises the drawer open/close lifecycle, and no contract asserted a slide transition existed, so the harness never noticed the panel had no animation. | skill + component CSS | `drawer.css`: added `transition: transform …` + per-position closed transforms (`translateX/Y(±100%)`) and compound open selectors `.z-drawer-open.z-drawer-<pos> .z-drawer-real { transform:none }`; mask opacity fade. New `components/drawer.md` records the transitionend-gated close + compound-selector specificity rule. | 2026-05-27 fix verified live: open slides in from right with scrim; mask click now sets `display:none`/`isVisible()==false`. First attempt used a descendant `.z-drawer-open .z-drawer-real` (ties on specificity with the closed rule, loses) — stock ZK iceblue uses the compound form; switched to that. |
| 2026-05-27 | errorbox | (1) Warning ⚠ icon sat only 8px from the box border (cramped). (2) The pointer arrow that points at the source field was invisible: theme set `.z-errorbox-pointer { display:none }` and coloured the arrow `error-container` (faint pink). ZK reveals the pointer with `jq(pointer).show()` (Errorbox._fixarrow) which only clears an inline display value — it cannot beat a stylesheet `display:none`, so the arrow never showed. | ZK-JS reveal contract: `jq().show()` can't override a stylesheet `display:none`. No MD3/Mira analog has a tooltip beak; no contract checked the pointer was visible or its colour. Also the served errorbox CSS lives in the inp bundle (`inp/css/input.css` → `input.css.dsp`), NOT the apparent `wgt/css/errorbox.css` — editing the wrong copy did nothing until the inp copy was fixed. | skill + component CSS | `js/zul/inp/css/input.css` (authoritative) + `js/zul/wgt/css/errorbox.css` (kept in sync): icon `left` 8px→12px, content `padding-left` 28px→34px, pointer `display:block`, arrow colours `error-container`→`error`. New `components/errorbox.md` records the `jq.show()` trap + the inp-bundle location. | 2026-05-27 fix verified live: icon 12px from border; pointer `display:block`, `z-errorbox-left` red (rgb 211,47,47), positioned by ZK pointing at the field. Live errorbox forced via widget API (`w.setValue('temp'); w.setValue(''); fireOnChange`) since synthetic typing was unreliable. |
| 2026-05-27 | calendar | Selected-date highlight filled the whole 38×36 table cell (border-collapse, zero gap), so circles touched neighbours and the grid edge — cramped vs Mira. The day number is a bare text node in the TD (no inner wrapper). | Theme spacing + a ZK structural fact (bare-text TD) with no MD3/Mira parity assertion on cell density / highlight inset. | skill + component CSS | `calendar.css`: cell `height` 36→40, `padding` 2px→`4px 3px`, `background-clip: content-box` so the highlight clips to a centred ~32px circle with breathing room; today indicator changed from rectangular `outline` to a circular `::before` ring; month/year/decade cells pinned to `background-clip: border-box` to keep their full-cell pill. `components/calendar.md` records the bare-text-TD → `background-clip` circle technique. | 2026-05-27 fix verified live across all 3 variants (default/week-of-year/today-link): selected 27 is a clean inset circle, ~11px off the grid edge for Sun/Sat (3px cell pad + 8px table pad). |
| 2026-05-27 | navbar | Navbar was transparent (showed the gray page) while every other surface component (panel, groupbox, grid) renders on a white card — user asked whether to align. | Pure theme choice, not a ZK fact; no harness assertion on navbar surface treatment. | component CSS (theme value) | `nav.css`: `.z-navbar` given `background: var(--zk-color-surface)` + `var(--zk-shape-card)` radius + `1px solid var(--zk-color-outline-variant)` border + `var(--zk-spacing-2)` padding; inner `ul` stays transparent. | 2026-05-27 fix verified live in SPA: vertical + horizontal navbars now sit on white surface cards consistent with panel/groupbox. Answer to user's question: align it — the transparent look was not intentional. **2026-05-28 reversed** — when navbar lives inside a borderlayout west (surface-container-low + own border), the card-on-card framing reads as too heavy; aligned the visual treatment with menubar (flat, transparent), see row below. |
| 2026-05-28 | usecase/index.zul (nav category "Form Controls") | User asked whether "Form Controls" is the right label for the side-nav category that lists Bandbox/Button/Checkbox/…/Textbox; "Inputs" is the more accurate term (button isn't strictly a form control either, but the category is dominated by input widgets and "Inputs" matches MUI's `inputs/` directory). | n/a — sidebar taxonomy decision; harness has no opinion. | preview ZUL (test resource) | `src/test/resources/web/usecase/index.zul`: nav `label="Form Controls"` → `"Inputs"`; the @load condition `vm.openNavLabel eq 'Form Controls'` updated to `eq 'Inputs'`. | 2026-05-28 fix complete; deep-links to `#bandbox`, `#button`, etc. continue to open this category. |
| 2026-05-28 | usecase/index.zul (Calendar placement) | User pointed out Calendar is fundamentally an input/picker, not feedback; previously listed under "Feedback" alongside Errorbox/Notification/Loading. | n/a — sidebar taxonomy decision. | preview ZUL (test resource) | `src/test/resources/web/usecase/index.zul`: moved `<navitem label="Calendar" .../>` from the "Feedback" nav into the "Inputs" nav (alphabetical position). | 2026-05-28 fix complete. |
| 2026-05-28 | navbar (regression of 2026-05-27 card framing) | User: "navbar 明顯有外框，但其他元件都沒有". In `usecase/index.zul` the navbar sits inside borderlayout `west` (which already has its own `surface-container-low` background + `outline-variant` border); the 2026-05-27 card framing layered a second surface card on top of that, producing a visible inner border / "card-in-card" look that the surrounding panel/grid don't have. | Theme choice was made on the standalone navbar preview page (where the card looked correct) without checking the SPA context where the navbar is wrapped in an already-styled container. No cross-context check exists for "this surface treatment should adapt when nested in a styled container." | component CSS (theme value) + skill | `nav.css`: removed `background-color`, `border`, `border-radius`, `padding` overrides from `.z-navbar` (transparent again). Visual cohesion now provided by the parent (west pane, navbar.zul page surface). Per the spiral CSS rule (page-level decoration belongs to the page, not the component), nav.css owns only the inner item layout/state. | 2026-05-28 fix complete (pending live verify). |
| 2026-05-28 | menubar | User: "menubar 底色跟其他元件不一致". `.z-menubar` used `surface-container` + `elevation-2` so it rendered as a darker bar with shadow on a `surface` page — every other component on the same page (panel, grid, navbar after the row above) sits on `surface` with no shadow. The MD3 top-app-bar tone is correct in isolation but creates an inconsistent surface stack inside the usecase SPA. | Same gap class as the navbar row above: top-app-bar tone was chosen against MD3 spec alone without checking the SPA's flat-card surface convention. | component CSS (theme value) | `menu.css`: `.z-menubar` `background-color: surface-container → transparent`, removed `box-shadow: elevation-2`. Inner `.z-menu-content` / `.z-menuitem-content` colours unchanged (still `on-surface`). | 2026-05-28 fix complete (pending live verify). |
| 2026-05-28 | calendar (categorisation, not styling) | User asked whether Calendar belongs under Inputs rather than Feedback. ZK `<calendar>` is a date-picker render component — clearly an input. Style/behavior unchanged. | n/a — sidebar taxonomy. | preview ZUL | See "Calendar placement" row above. | 2026-05-28 fix complete. |
| 2026-05-28 | datebox + timezone popup (`.z-datebox-timezone`) | When `<datebox timezones="...">` is used the popup renders a `<div class="z-datebox-timezone"><select>…</select></div>` block under the calendar/time grid. Two issues: (1) the wrapper had no margin so the `<select>` sat in the bottom-left corner of the popup, flush against the border; (2) the inner native `<select>` was unstyled (system default chrome) while everywhere else in the theme a comparable native `<select>` is themed as `.z-selectbox` (40px height, outline border, MD3 caret, focus ring). | Skill never recorded the `.z-datebox-timezone` DOM (sibling of `.z-calendar` and `.z-timebox` inside `.z-datebox-popup`); contract had no row for it. Native `<select>` styling lives only on `.z-selectbox` — there is no shared base rule for "native select inside ZK popup", so each new appearance reintroduces the OS default. | skill + component CSS | `js/zul/inp/css/datebox.css`: added `.z-datebox-timezone { margin: 0 var(--zk-spacing-3) var(--zk-spacing-3); }` and styled `.z-datebox-timezone > select` to match `.z-selectbox` (height 40, outline border, MD3 caret, focus ring, surface fill, on-surface text, font-family/size tokens). Skill candidate: `components/datebox.md` — record timezone block DOM + "native select inside a popup must be themed". | 2026-05-28 fix complete (pending live verify). |
| 2026-05-28 | borderlayout west/east collapse icons (`z-west-icon z-icon-ellipsis-v`, `z-east-icon z-icon-ellipsis-v`) | When a `<west>`/`<east>` region is collapsible (no `collapsible="false"`), ZK renders `.z-west-icon.z-icon-ellipsis-v` (or east counterpart) in the collapsed placeholder as the drag-handle/expand affordance. Theme has no rule for these classes; the icon font is reset, so the affordance is invisible — user can't tell the pane is collapsible. (User also asked for a fuller borderlayout redesign; that is captured separately, see `doc/borderlayout-redesign-todo.md`.) | Skill has no `components/borderlayout.md` entry listing the collapse-toggle icons; contract `borderlayout.md` doesn't enumerate them. Generic `[class^="z-icon-"]::before` machinery requires an explicit `--_icon` definition, which only the icon-class fallback in `_icons.css` provides — `z-west-icon`/`z-east-icon` are *positional* classes, not icon classes, so they were never wired to a mask. | skill + component CSS | `js/zul/layout/css/borderlayout.css`: added explicit visibility rules for `.z-west-icon.z-icon-ellipsis-v`, `.z-east-icon.z-icon-ellipsis-v`, `.z-north-icon.z-icon-ellipsis-h`, `.z-south-icon.z-icon-ellipsis-h` — show the matching Lucide grip-vertical/grip-horizontal mask at 16×16 with `on-surface-variant` colour, centred in the collapsed placeholder. Skill candidate: `components/borderlayout.md` — collapse-toggle classes + icon visibility requirement. | 2026-05-28 fix complete (pending live verify). Borderlayout fuller redesign deferred to follow-up — current pass only restores affordance visibility. |
| 2026-05-28 | vlayout (parent `gap` overrode ZK `spacing` attribute) | `.z-vlayout` had a static `gap: var(--zk-spacing-4)` (16px). ZK sets each `.z-vlayout-inner`'s `padding-bottom` inline based on the `spacing=` attribute (default `5px`, `0` when the user sets `spacing="0"`). Both apply, so `spacing="0"` still rendered with a 16px gap from the parent — user said "spacing=0 應該為 0". `.z-hlayout` already follows the ZK-inline-padding pattern (with `padding-right: 0 !important` on inner overriding ZK's inline). | Skill has no rule "Layout box widgets (`<vbox>`/`<hbox>`/`<vlayout>`/`<hlayout>`) use ZK inline `padding-*` on children for spacing; CSS must not impose a parent `gap`". The two layout files used different mechanisms (vlayout = parent gap, hlayout = !important reset + nothing) so the inconsistency went unnoticed. | skill + component CSS | `js/zul/box/css/layout.css`: removed `gap: var(--zk-spacing-4)` from `.z-vlayout` (default spacing now driven by ZK's inline 5px on `.z-vlayout-inner`; user `spacing=` continues to work). Skill candidate: `reference/box-spacing-mechanism.md` — record that vlayout/hlayout spacing is JS-driven inline padding, not CSS gap. | 2026-05-28 fix complete: `spacing="0"` vlayout now collapses to 0 inter-item gap; default vlayout uses ZK's 5px default. |
| 2026-05-28 | cardlayout (selected card not displayed; no theme CSS at all) | `<cardlayout>` rendered all child `.z-cardlayout-inner` siblings stacked top-to-bottom in normal flow because the theme never supplied the structural CSS `.z-cardlayout { position: relative; overflow: hidden }` + `.z-cardlayout-inner { position: absolute; top: 0; left: 0; overflow: hidden }`. ZK's `_setChdexPosition` JS writes `chdex.style.left` / `chdex.style.top` to slide cards but those properties are ignored when the inner isn't absolute, so `selectedIndex=1` still showed card 1 (or all three stacked). The marble theme had `cardlayout.css.dsp` only as an empty stub (declared in `scripts/build-css.js` `stubPaths`), no source CSS. | Skill has no `components/cardlayout.md`; the harness had no contract for cardlayout; the build pipeline's stub-without-source pattern silently shipped an empty CSS file. Same root failure mode as the rangeslider 2026-05-14 row (stub CSS replaced ZK's mold-default structural rules). | skill + component CSS + build pipeline | New file `src/main/resources/web/js/zkmax/layout/css/cardlayout.css` with the two structural rules; build-css.js auto-scan picks it up and overrides the stub. Skill candidate: `components/cardlayout.md` — DOM (`.z-cardlayout > .z-cardlayout-inner[#uuid-chdex]` ← one per child), JS positioning model (chdex `top`/`left` written by `_setChdexPosition`), absolute-positioning requirement. | 2026-05-28 fix complete: `selectedIndex="1"` now displays only the green Card 2 inside a 300×200 viewport; Previous/Next buttons animate; Toggle Orient switches between horizontal/vertical. |

| 2026-05-29 | listitem (token violation), searchbox (wrong family), tree/menu/combobox/chosenbox-dropdown (no contract assertion) | Selected-state colours were inconsistent across components and no rule documented WHY: `.z-listitem-selected` used hardcoded `rgba(55,111,208,0.12)` (a token-banned literal); `.z-searchbox-selected` used `secondary-container` (chip family) while every other list-row dropdown used `primary-container`. Cross-component audit showed the missing rule was the MD3 "three families" taxonomy: list-row (primary-container), chip (secondary-container), single-point picker (filled primary). | Skill had no `selected-state-families.md` so each new selectable component picked a colour by intuition; contracts never asserted family membership, so an evaluator pass couldn't catch a wrong-family regression. Listbox's hardcoded RGBA also slipped past the "all colours via tokens" rule because no evaluator step grepped CSS files for literal colour values. | skill + contract + component CSS | Skill: `.claude/skills/zk-component-rules/reference/selected-state-families.md` (new — three families, component→family mapping, required CSS pattern, forbidden patterns, contract assertion template). Component CSS: `js/zul/sel/css/listbox.css` (rgba → `var(--zk-color-primary-container)` + `on-primary-container` text), `js/zkmax/inp/css/searchbox.css` (secondary-container → primary-container, list-row family comment). Contract follow-up (deferred): add family assertion row to `doc/contracts/{listbox,combobox,tree,menu,searchbox,chosenbox}.md`. | 2026-05-29 fix in progress: listbox + searchbox CSS updated with comments documenting the family; skill file authored; contract assertions still to propagate. |
| 2026-05-29 | borderlayout splitter-button (geometry + a11y) | (a) Splitter strip is 6px wide; the 20px filled-tonal pill button anchors to the strip's left edge because ZK JS only sets inline `top:`, not `left:` — the button visually leans 14px off-axis instead of sitting centred on the strip. (b) The button had no `:focus-visible` ring, so keyboard users couldn't tell it had focus. | Skill had no rule "when a button sits on a layout primitive's axis, the actuator's centreline must equal the primitive's centreline"; contract never asserted button-vs-strip alignment. The missing `:focus-visible` is a generic a11y gap across many interactive components — no shared baseline rule. | component CSS + agent | `js/zul/layout/css/borderlayout.css`: added `margin-left: -7px` (W/E) and `margin-top: -7px` (N/S) — `(20 - 6) / 2` — to centre the pill on the 6px strip's axis, plus a `:focus-visible { outline: 2px solid primary; outline-offset: 2px }` ring on all four splitter buttons. Inline comment captures the geometry rule for future reference. | 2026-05-29 fix complete. Sibling sweep follow-up: check standalone `splitter.css` / `splitlayout.css` for the same centring + focus-visible pattern. |
| 2026-05-29 | borderlayout region headers (cramping) | Region header had `padding: 0 12px` with no `gap` and no `justify-content` — title text and the collapse-angle icon crowded each other when title text was non-trivial. MD3 Top App Bar small / Navigation Drawer header both call for 16dp horizontal inset + 8dp gap to the trailing icon. | Contract had no row asserting header padding/gap/justify-content; skill had no "app-bar-style header" baseline rule. | component CSS | `js/zul/layout/css/borderlayout.css`: `.z-*-header` padding 12 → 16px, added `gap: var(--zk-spacing-2)` (8px) and `justify-content: space-between` so the title hugs the left edge and the trailing icon hugs the right. Inline comment cites the MD3 spec. | 2026-05-29 fix complete. |
| 2026-05-29 | datebox popup (nested-card artefact) | Inside `.z-datebox-popup`, two CSS files wrote conflicting rules for the same `.z-datebox-popup .z-calendar` selector — calendar.css gave the inner calendar its own border + radius + elevation-2 shadow (its standalone card look), while datebox.css tried to undo it with a `border-bottom: 1px outline-variant`. Result: a visible hairline + soft shadow seam between the calendar and the timebox/timezone fields, making the popup look like nested cards instead of one MD3 picker surface. | The standalone-calendar styling rule in calendar.css was unscoped, so it leaked into every consumer (datebox popup, future date-range popup, etc.). Skill had no rule "a picker popup must be the only elevated surface; contents are flat". No contract assertion checked the inner inputs' resting border-color inside the popup. | skill + component CSS | `js/zul/db/css/calendar.css`: narrowed standalone card rule to `.z-calendar:not(.z-datebox-popup .z-calendar)` so it can't bleed into the popup. `js/zul/inp/css/datebox.css`: explicit overrides making the calendar transparent/border-none/shadow-none/radius-0 inside the popup, plus `border-color: transparent` on the in-popup timebox input and timezone `<select>` (focus restores `primary` border). Comment block documents the "popup is the only elevated surface" rule for future picker components. | 2026-05-29 fix complete. Skill follow-up: codify "one-elevation rule" inside a new `reference/picker-popup-elevation.md`. |
| 2026-05-29 | datebox ROC calendar locale (framework bug, NOT a theme issue) | With `locale="zh_TW_TW"` (Republic-of-China minguo calendar), opening the popup throws `Uncaught RangeError: Invalid language tag: zh-tw-x-lvariant-tw` at `zul.db.wpd:210` (`new DateTimeFormat`). ZK converts Java's `Locale("zh","TW","TW")` to a Unicode-extension-style string with a private-use `x-lvariant-` subtag that `Intl.DateTimeFormat` rejects per BCP 47. | This is an upstream framework bug in `zul.db.wpd` (the date-mold JS). The theme CSS layer cannot intercept the JS error. Skill / contract should not silently absorb framework bugs as theme gaps. | framework-bug (no theme action) | None in this repo. Logged here for traceability so a future audit doesn't keep re-discovering it as a theme gap. Upstream issue should be filed against the ZK 10 repo (`zul.db.wpd` Calendar mold `titleHTML` constructing the DateTimeFormat locale). Preview workaround if needed: use `locale="zh_TW"` for the ROC datebox example so the preview isn't stuck in an error state. | 2026-05-29 documented only. |
| 2026-05-29 | layout primitives (vlayout, hlayout, borderlayout, div, vbox, hbox, cell, *-body) — design decision | Authors keep expecting that wrapping content in `<div>` / `<vlayout>` will produce MD3-correct padding around the text, but ZK layout primitives are intentionally paddingless. First-impression preview screenshots show text hugging container edges. | This is a DESIGN.md-level fact, not a styling bug. Skill had no entry codifying "layout primitives carry no padding by design; use `z-p-*` utilities on a wrapper, or rely on semantic containers (panel, groupbox, window) that DO have intrinsic body padding". | DESIGN.md / docs | Added documentation in `doc/component-dom-structures.md` (the layout-primitives section) and updated `src/test/resources/web/borderlayout.zul` to wrap region content in `<div sclass="z-p-4">` so the preview demonstrates the canonical pattern instead of perpetuating the antipattern. | 2026-05-29 documented + preview updated. |
| 2026-05-29 | navbar / menubar (tonal-surface rationale) | Repeat question: "navbar and menubar look transparent / inconsistent with other components — is this a bug?" Answer: no, they use higher tonal surface tokens (`surface-container-low` and `surface-container`) per MD3 navigation-chrome guidance, which sits above content surfaces using TONAL elevation, not box-shadow. The differences look small on a white-page background but they are correct. | The pattern was never written down, so each new contributor sees the colour mismatch and proposes "unifying" them with `surface`, which would defeat the MD3 chrome/content hierarchy. | DESIGN.md | Added `doc/spec/navigation-surface-rules.md` documenting which ZK components map to which MD3 surface tier and why; cross-referenced from `doc/component-dom-structures.md`. | 2026-05-29 documented. |

| 2026-05-29 | --zk-color-background token (page body) | Token resolved to the SAME hex as --zk-color-surface-container-low, so the navbar's tonal step was invisible whenever it sat directly on the page body. User reported the navbar bg looked identical to the page bg in the usecase SPA. | Token-vs-token collision — the harness has no rule "sibling tokens that share a hex erase the tonal hierarchy they're meant to express". No contract assertion compared chrome-tier tokens to body-tier tokens. | DESIGN.md (theme value) + preview override | tokens/_colors.css: --zk-color-background #f7f9fc → #ffffff (= --zk-color-surface). test/.../usecase/usecase.css: body { background-color: var(--zk-color-surface-container) } so usecase pages tint one step DARKER than the navbar (surface-container-low). Standalone preview pages keep the new white default. | 2026-05-29 fix verified live: usecase shell shows 3 distinct tonal layers (navbar #f7f9fc < body #f0f4fa < content cards #ffffff). |
| 2026-05-29 | borderlayout splitter strip + region edge (double-paint) | The 6px transparent splitter strip occupied the inter-region gap, while the region itself drew a 1px outline-variant border on the SAME edge. Result: 1 visible line from the region's border + 5px of empty padding from the strip — user couldn't tell where the drag handle began. At hover the strip's primary-24% tint also stacked on top of the region border. | Skill had no rule "a divider on a splittable edge must be painted exactly once". Contract never asserted that the region's border-{side} on a splittable edge is omitted. | skill candidate + component CSS | borderlayout.css: removed border-{bottom,top,right,left} from .z-{north,south,west,east}; the splitter strip's ::before now paints a 1px outline-variant centred line at idle, growing to 3px primary on hover. Non-splittable regions lose the boundary by default — author can re-add via sclass if needed (99% of layouts have splitters). | 2026-05-29 fix verified live: thin centred hairline visible in the 6px strip on all 4 axes; hover thickens to 3px primary. |
| 2026-05-29 | borderlayout region header (justify) | The 2026-05-29 fix that adopted MD3 Top App Bar justify-content: space-between pushed the title to the leading edge and the collapse-angle icon to the trailing edge, which read as two disconnected controls ("title here, action far away"). User feedback: the icon and label belong to the SAME control and should sit together. | Theme judgment call — Top App Bar pattern vs. Navigation Drawer subsection pattern. Skill had no triage rule for choosing between them. | DESIGN.md (theme value) | borderlayout.css: justify-content: space-between → flex-start; gap: spacing-2 retained. Icon now leads title (ZK header DOM is <i> then title text), grouped at the 16dp leading inset. Comment updated to cite the Navigation Drawer subsection rationale. | 2026-05-29 fix verified live: West/East/North/South headers all show "<<icon> Label" grouped at the leading edge. |
| 2026-05-29 | selectbox (option:checked wrong selected-state family) | Native <select> popup option:checked used --zk-color-secondary-container (chip family) while every other dropdown row component used --zk-color-primary-container (list-row family). User reported the inconsistency: blue selection in listitem/comboitem/searchbox vs. teal in selectbox. | The 2026-05-29 selected-state-families.md skill table did not list selectbox at all, so each contributor decided the family by intuition. Contract had no row asserting which family applied to option:checked. | skill + contract + component CSS | skill: added selectbox row to the list-row family table in reference/selected-state-families.md. contract: contracts/selectbox.md added c11/c12 rows asserting option:checked uses primary-container + on-primary-container, plus a States-to-evaluate note. CSS: js/zul/wgt/css/selectbox.css option:checked bg secondary → primary; on-secondary → on-primary; hover mix updated to match. | 2026-05-29 fix verified live: selectbox 'Bob' checked option now shows primary-container blue, matching listitem/comboitem/searchbox. |
| 2026-05-29 | calendar (weekend+selected text colour, disabled vs outside, circular highlight) | Three calendar issues: (a) on a weekend-selected day the day-number stayed on-surface (dark) instead of becoming on-primary (white) because .z-calendar-body tbody td.z-calendar-weekend (specificity 0,2,2) outranked .z-calendar-cell.z-calendar-selected (0,2,0); (b) .z-calendar-disabled and .z-calendar-outside both rendered at 38% opacity grey with no visual distinction, so a constraint="no past" day looked identical to a previous-month grey-out; (c) the selected disc was painted via background-color + border-radius:full on the TD with background-clip:content-box, so table-layout shrank the box in the week-of-year column → the same selector painted an oval there but a circle in the default view. | (a) no skill rule "state classes must outrank position classes in specificity"; (b) calendar contract had no row distinguishing disabled from outside; (c) no skill rule "picker highlights must be fixed-size pseudo-elements, not background-clipped cell fills". | component CSS + skill candidate | calendar.css: (a) selected selector bumped to .z-calendar-body tbody td.z-calendar-cell.z-calendar-selected (0,3,2) so on-primary wins on weekends; (b) .z-calendar-disabled now adds text-decoration:line-through + cursor:not-allowed (MD3 disabled-date treatment); (c) selected/hover disc rewritten as a fixed 36×36 ::before circle with isolation:isolate + z-index:-1 so the disc paints behind the day-number text regardless of cell width — today indicator moved to ::after (and hidden when also selected, since the fill IS the indicator). Month/Year/Decade views override ::before to a cell-filling pill. | 2026-05-29 fix verified live: weekend Saturday 30 selected → white text on blue disc; constrained "no past" dates show strike-through; default + week-of-year + today-link variants all paint identical circles. |
| 2026-05-29 | columnlayout / columnchildren | `<columnchildren width="33%">` columns did not render as three side-by-side 33% columns. CSS used `display:flex; flex:1; min-width:200px` on `.z-columnchildren`, which (a) `flex:1` overrode ZK's inline `style="width:33%"`, and (b) `min-width:200px` forced wrap on viewports where 3×33%≈600px is below 3×200px. ZK ships this widget as an inline-block container — `Columnchildren.ts`/`mold/columnchildren.js` render `<div class="z-columnchildren" style="width:…"><div class="z-columnchildren-content">…</div></div>` and rely on inline-block flow + ZK-set inline width. SECOND ROOT CAUSE: even after rewriting to inline-block, the rules had been placed in `js/zul/layout/css/layout.css` → `…/zul/layout/css/layout.css.dsp`, but the WCS bundler reads columnlayout rules from `js/zkex/layout/css/columnlayout.css.dsp` (which existed in `target/` as a 0-byte stub). So the rules were never bundled — preview kept rendering with no column rules at all (default block stacking on weather-prone widths). | Skill had no `components/columnlayout.md`. No rule about "inline-block widgets must not be retrofitted to flex". Build-routing issue invisible because empty 0-byte `.css.dsp` files were treated as valid by the bundler. | skill + component CSS + build-routing | Skill: new `.claude/skills/zk-component-rules/components/columnlayout.md` documenting the DOM tree (cave + 1×1 spacer mold artefact), the inline-block rendering contract, and the css file path `js/zkex/layout/css/columnlayout.css`. CSS: new `src/main/resources/web/js/zkex/layout/css/columnlayout.css` with inline-block rules. Old `src/main/resources/web/js/zul/layout/css/layout.css` reduced to a header-only stub. | 2026-05-29 fix verified live: three columns render at offsetLeft 32 / 639 / 1246 on a 1840px viewport, each width 607 ≈ 33%. Build-routing surprise — worth a follow-up sweep for other 0-byte `.css.dsp` files that might hide the same pattern. |
| 2026-05-29 | hlayout | `<hlayout spacing="16px">` produced no visible gap. CSS put `display:flex; gap:…` on `.z-hlayout` and `padding-right:0 !important` on `.z-hlayout-inner`. ZK's `zul.box.Layout` (`Layout.ts` `encloseChildHTML_`) writes inline `style="padding-right:16px"` on every non-last `-inner` to realise the `spacing` attribute. The `!important` override wipes that inline value, so the attribute became a no-op and only the fixed parent `gap` (or default) was left. | Skill `components/hlayout-vlayout.md` was a stub — no rule "spacing comes from ZK-injected inline padding, themes must not use flex/gap on the root". Contract never asserted spacing="16px" → 16px gap between children. | skill + component CSS | Skill: expanded `components/hlayout-vlayout.md` with the "Spacing comes from ZK-injected inline padding — not from parent `gap`" section explicitly listing the broken patterns (flex+gap; padding-right !important) and the correct ones (inline-block inner + nowrap parent). CSS: `src/main/resources/web/js/zul/box/css/layout.css` rewritten to match the vlayout shape — no flex on root, `display:inline-block; vertical-align:top` on `-inner`, no `padding-right:0 !important`. Added `.z-valign-*` modifier rules per Hlayout.ts. | 2026-05-29 fix verified live: spacing="0" → 0px, default → 5px, spacing="16px" → 16px between children; mixed-children layout (label/textbox/button) also honours the 5px default. |
| 2026-05-29 | space / separator-vertical-bar | `<space bar="true">` rendered nothing visible. `Space.ts` extends Separator with `_orient="vertical"`, so `bar="true"` always lands on `.z-separator-vertical-bar`. separator.css styled `.z-separator-horizontal-bar` (1px outline-variant line) but had no rule for the vertical variant. Follow-up: `<space spacing="40px" bar="true">` initially rendered a 40px-wide solid block because ZK writes the widget's `spacing` as inline `width` on the vertical bar; the new rule had to pin `width:1px !important` to keep the divider a hairline (symmetric with `.z-separator-horizontal-bar` pinning `height:1px`). | Skill `components/separator.md` already documented both bar classes existed — the generator just never implemented the vertical rule. No contract row asserted "vertical-bar paints a visible 1px line". | component CSS only (skill already correct) | CSS: `src/main/resources/web/js/zul/wgt/css/separator.css` added `.z-separator-vertical-bar { display:inline-block; width:1px !important; min-height:1em; background-color: var(--zk-color-outline-variant); margin: 0 var(--zk-spacing-2); vertical-align:middle; align-self:stretch }`. The `!important` defeats ZK's inline `width=<spacing>` so the bar stays a hairline regardless of attribute; visual gap comes from margin. | 2026-05-29 fix verified live: both `<space bar="true">` instances render w=1px, h=20px, bg=rgba(0,0,0,0.12); the `spacing="40px" bar="true"` instance keeps w=1px even with inline `style="width:40px"`. |
| 2026-05-29 | loading / showBusy (z-loading, z-modal-mask, z-loading-icon) | `zAu.cmd0.showBusy('Loading…')` showed nothing: the scrim was visible but the busy box and its text were invisible because (a) `.z-loading` had no style at all (left as `position:static`, so the JS-set inline `left/top` could not centre it; no background, no padding, no shadow, no z-index above the mask), and (b) `.z-loading-icon` had no spinner styling. Root pipeline cause: `.z-loading` / `.z-modal-mask` / `.z-tooltip` are emitted directly by ZK core (`zUtl.progressbox` in `zk/utl.ts`), not by any registered widget — so the auto-scan 1:1 `js/zul/wgt/css/misc.css.dsp` was generated but never requested by the page (no widget triggers the load), and the WCS bundle (`zk.wcs`) contained none of those rules. | Skill had no rule "selectors emitted by ZK core (showBusy, progressbox, tooltip helper) live in a misc/global CSS file that must be bundled into norm.css.dsp — they are not widget CSS and the 1:1 per-widget auto-scan will silently drop them". Contract never asserted "after showBusy, the busy box has position:absolute + z-index above the mask + visible background". | skill + component CSS + build-routing | CSS: `js/zul/wgt/css/misc.css` added real `.z-loading` (position:absolute, z-index:1450 above mask 1400, surface-container-high bg, shape-corner-medium, elevation-3, padding-4×6, white-space:nowrap, cursor:wait), `.z-loading-indicator` (inline-flex + gap), `.z-loading-icon` (20px CSS spinner with @keyframes z-loading-spin). Build: added `'js/zul/wgt/css/misc.css'` to the `normFiles` array in `scripts/build-css.js` with a comment explaining why (zk core emits these, not a widget). | 2026-05-29 fix verified live: zk.wcs now contains the misc rules; `zAu.cmd0.showBusy('Loading…')` renders scrim covering full 1904×579 viewport, busy box centred at (882, 261) with 139×57 size, padding 16/24, elevation-3 shadow, z-index 1450; spinner animates. Sibling sweep recommended for `.z-tooltip` (already in misc.css, same hidden-from-bundle bug). |
| 2026-05-29 | toast (z-toast-content padding) | `Toast.show(msg, type, …)` with type=info/warning/error rendered the type icon overlapping the message text. Real DOM (per `Toast.ts redraw()`): `<div.z-toast><i.z-toast-icon.z-icon-info-circle/><div.z-toast-content>msg</div>[<div.z-toast-close>...]</div>`. CSS placed the icon `position:absolute; left:16px; width:20px` as a sibling of content, but `.z-toast-content` had `padding: 8px 16px` so text started exactly where the icon was painted (left 16). Same risk on the right edge with the absolute close button at `right:12px width:20px`. Static preview gallery in `toast.zul` used the wrong DOM (no `.z-toast-icon` sibling, `.z-toast-close` nested INSIDE content) so the bug was invisible there. | Contract had no row asserting "content left/right padding must clear the absolute-positioned icon + close-button siblings". Preview gallery was hand-built instead of mirroring the widget's `redraw()` output, so the visual review never showed the broken state. | contract + component CSS + preview-gallery | CSS: `js/zul/wgt/css/toast.css` `.z-toast-content` padding changed from `8px 16px` → `12px 48px` (reserves icon space on the left and close-button space on the right) with an inline comment naming the absolute siblings being cleared. Preview: `src/test/resources/web/toast.zul` State-Gallery rewritten to the real `redraw()` DOM — `.z-toast-icon` as sibling (z-icon-info-circle / -exclamation-circle / -times-circle) and `.z-toast-close > .z-toast-icon.z-icon-times` as a separate sibling. | 2026-05-29 fix verified live (click Info button): icon at x=48–68, content text starts at x=80 (= toast.left 32 + padding-left 48), text ends at x=304 (= toast.right 352 − padding-right 48), close button at x=320–340. Zero overlap on either side. |
| 2026-06-01 | showBusy (.z-modal-mask top gap) | `zAu.cmd0.showBusy('…')` left a Y-pixel gap at the top of the scrim whenever the page was scrolled. Root cause: `zk/utl.ts progressbox()` always emits inline `style="left:Xpx;top:Ypx"` on `.z-modal-mask` using current scroll offsets. Those inline values were meaningful when the legacy theme positioned the mask `absolute` from document origin, but our (and ZK's stock 10.x) theme uses `position:fixed; inset:0`, where the inline `top:Ypx` becomes a literal viewport offset and pushes the scrim down by the scroll amount. | Skill had no rule "the JS-set inline top/left on .z-modal-mask is a vestigial absolute-positioning hint and MUST be neutralised under position:fixed"; contract never asserted "the mask scrim covers the full viewport regardless of page scroll position". | skill + component CSS | CSS: `js/zul/wgt/css/misc.css` `.z-modal-mask`/`.z-mask` now pin `top:0 !important; left:0 !important;` after `inset:0`, with a comment documenting the inline-style quirk. Skill: extended `.claude/skills/zk-component-rules/reference/zk-core-emitted-selectors.md` with a "Mask quirk" section listing the vestigial inline left/top and the !important pin. | 2026-06-01 fix verified live: bundle now contains `top:0 !important; left:0 !important` on `.z-modal-mask`; scrim covers full viewport regardless of scroll position. |
| 2026-06-01 | renderdefer (.z-renderdefer) | `<element renderdefer="2000">` rendered an unstyled placeholder (default `<div>`, zero visible feedback) while waiting for the timer. Default ZK theme styles `.z-renderdefer` with an animated progress GIF — visually consistent with showBusy. Our theme had no `.z-renderdefer` rule, so the deferred-render UX was silent. | Skill listed `.z-renderdefer` as a ZK-core-emitted selector in `zk-core-emitted-selectors.md` but had no contract or implementation hint that the visual should match the showBusy spinner. | component CSS | CSS: `js/zul/wgt/css/misc.css` added `.z-renderdefer { display:inline-block; width:24px; height:24px; border:3px solid primary-container; border-top-color:primary; border-radius:50%; animation:z-loading-spin .8s linear infinite }` — reuses the same `@keyframes z-loading-spin` as `.z-loading-icon` for visual consistency. | 2026-06-01 fix verified live: bundle contains `.z-renderdefer` spinner rule sharing the showBusy keyframes; renderdefer placeholder now animates while waiting. |
| 2026-06-01 | component busy (.z-apply-mask / .z-apply-loading) | `Clients.showBusy(component, msg)` (or `wgt.showBusy(msg)`) on a target Window did nothing visible — the per-widget mask + indicator DOM was created but had no styles. ZK core (`zk/effect.ts` `Mask._draw`) appends `<div><div class="z-apply-mask" .../><div class="z-apply-loading"><div class="z-apply-loading-indicator"><span class="z-apply-loading-icon"/> msg</div></div></div>` to `document.body`, with JS-set inline `top/left/width/height` on the mask and JS-centred `top/left` on the indicator. Without `position:absolute` + scrim color + indicator card, the user sees nothing. | Skill listed `.z-apply-loading` as ZK-core-emitted but did not enumerate the sibling `.z-apply-mask` (scrim) or `.z-apply-loading-indicator`/`.z-apply-loading-icon` (indicator card + spinner). Contract had no rule "after wgt.showBusy, the widget receives a dimmed overlay + centred busy card". | skill + component CSS | CSS: `js/zul/wgt/css/misc.css` added `.z-apply-mask` (position:absolute, scrim background, z-index:89000), `.z-apply-loading` (position:absolute, surface-container-high card, z-index:89500, shape-corner-medium, elevation-3, padding 4×6), `.z-apply-loading-indicator` (inline-flex + gap), `.z-apply-loading-icon` (CSS-spinner sharing the showBusy keyframes). Skill: extended `zk-core-emitted-selectors.md` with the full DOM tree for `Clients.showBusy(component)`. Preview: `src/test/resources/web/loading.zul` got a new "Component Busy" section bound to `zk.example.LoadingComposer` that exercises `Clients.showBusy(demoWin, "Processing...")` so the path is visible. | 2026-06-01 fix verified live: zk.wcs contains all four apply-* rules; the per-widget overlay renders correctly. |
| 2026-06-01 | error panel (.z-error) | `zk.error('…')` / `Clients.evalJavaScript("zk.error('…')")` and "Trigger Multiple Errors" buttons in `error.zul` produced no visible message. ZK core (`zk/zk.ts` `_Erbx` class) appends `<div class="z-error" id="zk_err"><div id="zk_err-p"><div class="errornumbers">N Errors</div><div class="button"><i class="z-icon-times"/></div><div class="button"><i class="z-icon-refresh"/></div></div><div class="messagecontent"><div class="messages">msg</div></div></div>` to body with `display:none`, then calls `jq($id).slideDown(1000)`. With no `.z-error` styling, the panel had no visible card, the slideDown happened on a block whose computed appearance was browser-default — effectively invisible. Subsequent pushes append `<div class="newmessage">` (also `display:none`) which the script renames to `.message` and slideDown(600)s; that path also depended on `.newmessage { display:none }`. | Skill listed `.z-error` only inline (a row in the "ZK-core-emitted selectors" table) without the full DOM tree, the `.errornumbers`/`.button`/`.messagecontent`/`.messages`/`.message`/`.newmessage` child selectors, or the `display:none` precondition for `slideDown`. Contract never asserted "after zk.error, a visible error panel appears at top with close + refresh buttons". | skill + component CSS | CSS: `js/zul/wgt/css/misc.css` added `.z-error` (fixed top-centered card, hidden by default, error-container colours, max-width clamp), `.z-error #zk_err-p` (drag handle, flex with title + buttons), `.z-error .errornumbers` (title-medium), `.z-error .button` (24×24 round, hover state-layer), `.z-error .messagecontent / .messages` (scrollable list), `.z-error .messages .message` (separator border-top), `.z-error .newmessage { display:none }` (slideDown precondition for additional pushes). Skill: extended `zk-core-emitted-selectors.md` with the full `_Erbx` DOM tree and the slideDown precondition. | 2026-06-01 fix verified live: zk.wcs contains the .z-error rules; bundle size dropped from 1.68MB to 1.52MB after the FA removal (separate gap below). User can now see the error panel after clicking Trigger Error. |
| 2026-06-01 | font-awesome bundling (zk.wcs FA banner / ZK85Icons font face) | Final `zk.wcs` bundle shipped ~167KB of Font Awesome 6.4.2 + ZK85Icons font-face declarations even though Marble renders every icon via Lucide-derived `::before mask-image` (see `_icons.css`). Root cause **A**: ZK's stock `zk.wcs` (in `zul-*.jar`) pulls in `~./zul/font/font-awesome.css.dsp` as a `<stylesheet>` entry. Root cause **B**: `MarbleThemeProvider.beforeWidgetCSS` had a (now-removed) attempt to redirect that path via `ServletFns.resolveThemeURL` to our empty theme-local stub at `~./marble/zul/font/font-awesome.css.dsp` — but the provider was NEVER INVOKED. The desktop-level `<theme-provider-class>` in `metainfo/zk/zk.xml` from a jar can be silently overridden by `StandardThemeProvider` depending on jar load order (documented in `Configuration.java` as ZK-1671). With no provider invoked, `WcsExtendlet` included the stock FA path verbatim. | Skill had no rule "jar-shipped `<theme-provider-class>` is unreliable for webapp-wide WCS-extendlet hooks — themes must wire the provider explicitly in `WebAppInit` and call `setCustomThemeProvider(true)` to lock it". The static stub at `target/.../marble/zul/font/font-awesome.css.dsp` (0 bytes) confused everyone into thinking the override was working — it was just dead weight on disk. | skill + WebAppInit | Java: `MarbleThemeWebAppInit.init()` now explicitly calls `config.setThemeProvider(new MarbleThemeProvider()); config.setCustomThemeProvider(true);` — guarantees our provider survives jar-load-order races. `MarbleThemeProvider.beforeWidgetCSS` now returns `null` for `"~./zul/font/font-awesome.css.dsp"`, telling `WcsExtendlet` to skip the entry entirely (cleaner than relying on a 0-byte theme-path stub). Verified the provider is invoked by curl + temporary println before stripping the debug. Skill: documented the ZK-1671 race + `setCustomThemeProvider(true)` requirement in `zk-core-emitted-selectors.md`. | 2026-06-01 fix verified live: `zk.wcs` size 1,683,460 → 1,516,278 bytes (-167KB); `grep -cF "Font Awesome Free"` = 0; `grep -cF "ZK85Icons"` = 0; provider println confirmed `beforeWidgetCSS` runs for every stylesheet entry. Sibling sweep — `~./zul/font/font-awesome.css.dsp` is the only FA-emission source; no further entries to skip. |
| 2026-06-01 | component busy (.z-apply-mask scrim opacity + duplicate .z-apply-loading) | Component Busy (`Clients.showBusy(component, msg)`) looked **inconsistent with Global Busy** (`zAu.cmd0.showBusy(msg)`): (a) `.z-apply-mask` had `opacity: 0.4`, so its effective scrim was 50%×40% = 20% black — much dimmer than `.z-modal-mask`'s direct `--zk-color-scrim` (50% black); and (b) a stray duplicate `.z-apply-loading { opacity: 0.5; pointer-events: none }` block lower in `misc.css` cascade-overrode the properly-styled card (made the busy indicator semi-transparent and unclickable). Net effect: the user clicked Show Busy and saw a faint scrim + a ghosted card — fundamentally different from Global Busy. | After the previous gap row (174) added `.z-apply-*` styling, the harness had no contract assertion "Component Busy scrim color and indicator card MUST match Global Busy's `.z-modal-mask` + `.z-loading`" — only that each rule existed individually. Duplicate rules in the same file went undetected because no check enforces "single canonical definition per selector". | skill + contract + component CSS | CSS: `js/zul/wgt/css/misc.css` — removed `opacity: 0.4` from `.z-apply-mask` (now uses `--zk-color-scrim` directly, matching `.z-modal-mask`); removed the duplicate `.z-apply-loading { opacity: 0.5; pointer-events: none }` block so the proper surface-container-high card + elevation-3 styling at line 88 wins uncontested. Added inline comment naming the cross-selector parity rule. Skill: extend `zk-core-emitted-selectors.md` "Per-widget busy overlay" section with a "Visual parity rule: Component Busy MUST look identical to Global Busy — same scrim opacity, same card surface/elevation/padding/spinner" callout (TODO if not present). | 2026-06-01 fix verified live: served `zk.wcs` has `.z-apply-mask` with no `opacity` declaration, single canonical `.z-apply-loading` rule with surface-container-high card. Component Busy and Global Busy now share visual identity. |
| 2026-06-01 | component busy (.z-apply-mask / .z-apply-loading position) | Decision rule for Component Busy positioning was not documented anywhere — it was easy to mis-pick `position: fixed` to match the Global Busy `.z-modal-mask` (viewport-anchored scrim). User clarified the intent: per-widget overlay MUST be `position: absolute` so the scrim + indicator stay locked to the **component's document position**, not the viewport region. ZK core (`zk/effect.ts` `Mask._draw` / `sync`) emits inline `top/left/width/height` from `$anchor.revisedOffset()` — document coords (boundingClientRect + scroll). With `position: absolute` those coords resolve correctly: as the user scrolls the body, both mask and indicator move with the component because the document position is fixed. This is the desired behaviour — the mask's purpose is to cover the targeted component, NOT to lock the user out of a viewport region. Counter-pattern (`position: fixed`) would freeze the mask in the original viewport area, leaving the component uncovered after any scroll. | Skill / contract had no rule pinning the positional contract for the per-widget overlay: "`.z-apply-mask` + `.z-apply-loading` MUST be `position: absolute` so they stay anchored to the component's document position as the body scrolls — NEVER `position: fixed` (that breaks the cover-the-component invariant)". Row 177's "Component Busy must look identical to Global Busy" guidance was misleading because it implied positional parity too, when only the *visual* tokens (scrim opacity, card surface/elevation/padding) match — Global Busy covers the viewport (fixed), Component Busy covers the component (absolute). | skill + contract | CSS: `js/zul/wgt/css/misc.css` `.z-apply-mask` + `.z-apply-loading` confirmed `position: absolute` (no change from the canonical state once a stray `position: fixed` experiment was reverted). Inline comment expanded to document (a) the JS contract — `revisedOffset()` returns document coords; (b) the cover-the-component invariant; (c) the known limitation — if the component is nested inside a scrollable DIV (not body), the inner-container scroll moves the component but not the mask, and JS (not CSS) would need to listen for the container's scroll to re-sync. Skill: extend `zk-core-emitted-selectors.md` "Per-widget busy overlay" section with "Positional contract: `.z-apply-*` track the component's document position via `position: absolute`. Visual contract (`row 177`) and positional contract are independent — Global Busy uses `fixed`, Component Busy uses `absolute`." | 2026-06-01 fix verified live: served `zk.wcs` has `.z-apply-mask { position: absolute }` and `.z-apply-loading { position: absolute }`; scrolling the body in the loading demo keeps the busy overlay covering the demo window (mask travels with the component, as intended). Container-scroll edge case left as documented limitation. |
| 2026-06-01 | error panel (.z-error not aligned with messagebox dialog pattern) | `.z-error` (floating error console) had been made visible (row 175) but used **error-container background + 1px solid error border** — visually unique in the theme, not aligned with any other floating window. The project's analog pattern is `.z-messagebox-window` (a dialog): `surface` background, `--zk-shape-dialog` corner, `--zk-elevation-dialog` shadow, no border, header with title + outline-variant separator + content. The error panel ignored that pattern and used MD3 Banner colours instead, while structurally being a Dialog. Result: inconsistent typography (no error icon next to title), inconsistent shape/elevation tokens (used generic `corner-medium` + `elevation-3` instead of dialog-specific `shape-dialog` + `elevation-dialog`), inconsistent header treatment (no outline-variant divider between title bar and message list). | Contract for `.z-error` only asserted "after zk.error, an error panel appears" — never asserted "the panel must use the same shape/elevation tokens + structural pattern as `.z-messagebox-window`". The skill listed `.z-error` selectors but had no cross-component design rule "floating dialog-style panels in this theme share `shape-dialog` + `elevation-dialog` + `surface` + `outline-variant` divider — see messagebox for canonical example". | contract + component CSS | CSS: `js/zul/wgt/css/misc.css` `.z-error` rewritten to messagebox parity — `surface` background, `--zk-shape-dialog` corner, `--zk-elevation-dialog` shadow, `overflow:hidden` (so the rounded corners clip the header divider cleanly), no border. `.z-error #zk_err-p` now uses `min-height:56px` + padding `3 4 3 6` + `border-bottom:1px solid outline-variant` matching `.z-messagebox-window .z-window-header`. Added `.z-error #zk_err-p::before` rendering the same error-icon SVG mask as `.z-messagebox-error` (24×24, `--zk-color-error`) for cross-component icon consistency. `.errornumbers` colour switched to `--zk-color-error` (title-medium). `.button` enlarged to 32×32 round with `on-surface-variant` colour + on-surface 8% hover state-layer (standard icon-button treatment). `.messagecontent` padding `3 6 4` (matches messagebox button-row padding rhythm); `.messages .message` separator switched to `outline-variant` (matches messagebox internal divider). | 2026-06-01 fix verified live: served `zk.wcs` has `.z-error` with `background-color:--zk-color-surface`, `border-radius:--zk-shape-dialog`, `box-shadow:--zk-elevation-dialog`, no border declaration; header has 56px min-height + outline-variant border-bottom + error-icon mask + error-colour title; button rows separated by outline-variant. Panel now visually a peer of the messagebox dialog. |
| 2026-06-02 | portallayout | Adjacent `.z-portalchildren` columns rendered flush — **0px horizontal gutter** between them (col0 right edge x=948 = col1 left edge x=948; panels touch edge-to-edge). Stacked panels within a column already had the correct 12px vertical rhythm (`margin-bottom: --zk-spacing-3`), but the cross-axis gutter was missing entirely, so the two columns read as one merged block — violating MD3's layout-grid gutter requirement and breaking the theme's own 12px spacing rhythm. | The contract asserted the *vertical* inter-panel gap (panel `margin-bottom`) but had **no check for a horizontal inter-column gutter**, so the evaluator never measured `col[n+1].left - col[n].right`. ZK portallayout provides no built-in gutter (column widths are JS-set inline; columns are bare floats) — a feature-without-theme-analog the harness was blind to. | skill + contract + component CSS | skill: `.claude/skills/zk-component-rules/components/portallayout.md` composition-invariants — added "columns have NO built-in horizontal gutter; the theme supplies it via adjacent-sibling `padding-left` (box-sizing:border-box keeps the JS-set inline width intact and outer edges flush); margin/negative-margin are unusable because inline %-widths + root `overflow:hidden` would overflow or clip". contract: `doc/contracts/portallayout.md` added `col-7` (gutter between adjacent columns = `--zk-spacing-3`) to Expected-values + State matrix + prose. CSS: `js/zkmax/layout/css/portallayout.css` added `.z-portallayout-vertical > .z-portalchildren + .z-portalchildren { padding-left: var(--zk-spacing-3) }`. | 2026-06-02 fix verified live: candidate rule measured in-browser before commit — columns stay side-by-side (916px each, no overflow), outer left/right edges stay flush, gutter between adjacent panels = 12px. |
| 2026-06-02 | portallayout (horizontal orient) | Adding an `orient="horizontal"` preview case revealed the theme never implemented the row-based layout: panels inside a row rendered with `float: none`, stacking vertically and overflowing the row (Calendar content 389px clipped inside a 249px row) instead of sitting side-by-side. `portallayout.css` only had the `.z-portallayout-vertical` column-float rule — nothing for `.z-portallayout-horizontal`. | The contract listed `horizontal-orient` under "States to evaluate" but had **no expected-value rows** for it, so the evaluator never measured the horizontal case. The skill *already documented* the invariant ("In horizontal orient, `.z-portalchildren-content > .z-panel` elements float left") — so this was a CSS implementation gap, not a knowledge gap: the theme simply hadn't implemented an already-known rule, and no contract assertion forced it. | contract + component CSS (skill already correct) | CSS: `js/zkmax/layout/css/portallayout.css` added `.z-portallayout-horizontal > .z-portalchildren { height: 100% }` and `.z-portallayout-horizontal .z-portalchildren-content > .z-panel, …> .z-panel-move-block { float: left }` (mirrors ZK default mold + the skill invariant). contract: `doc/contracts/portallayout.md` added `col-h1` (row height 100%) + `col-h2` (panel float left) to Expected-values + State matrix, checked off the horizontal-orient state, and added a prose paragraph. preview: `src/test/resources/web/portallayout.zul` added a "Horizontal Orientation" section. | 2026-06-02 fix verified live: two rows stack (each 249px ≈ 50% of 500px shell), row 0 = Calendar(left=32) + Colorbox(left=949) side-by-side at equal 249px height, row 1 = full-width Editor; all panels `float:left`, no overflow. |
| 2026-06-02 | portallayout (horizontal orient gutters) | In horizontal orient the inter-panel spacing was inconsistent with vertical: side-by-side panels in a row sat **flush (0px)** and rows stacked **flush (0px)**, while the vertical orient has a consistent 12px (`--zk-spacing-3`) gutter on both axes (column `padding-left` + panel `margin-bottom`). The horizontal CSS (added in the prior row) only handled `float: left` + row `height`, never the gutters. | The contract's `col-h1`/`col-h2` asserted layout (float + height) but **no gutter assertion existed for horizontal orient** — neither inter-row nor side-by-side — so the evaluator never measured `panel[n+1].left - panel[n].right` or `row[n+1].top - row[n].bottom` in horizontal. The deeper blind spot: the spacing *mechanism* is not symmetric across orients (vertical's side-by-side units are transparent JS-px columns; horizontal's are opaque author-%-width panels), and nothing forced the harness to recognise that asymmetry. | skill + contract + component CSS | skill: `.claude/skills/zk-component-rules/components/portallayout.md` composition-invariants — added a "no built-in gutter in horizontal orient either" invariant documenting the asymmetry: inter-row gutter via transparent-row `padding-top` (mirrors vertical's `padding-left` on the vertical axis); side-by-side gutter MUST be `margin-left` on `.z-panel + .z-panel` (chrome is on the opaque `.z-panel`, so padding would open space *inside* the card); and ZK does NOT compute panel widths to px in horizontal (leaves `style.width` authored), so panels summing to 100% + margin wrap — the gutter width budget is the page author's responsibility (`width="calc(50% - 6px)"` for two-up). contract: `doc/contracts/portallayout.md` added `col-h3` (inter-row `padding-top` = `--zk-spacing-3`) + `col-h4` (side-by-side `margin-left` = `--zk-spacing-3`) to Expected-values + a "horizontal gutters" State-matrix row + prose. CSS: `js/zkmax/layout/css/portallayout.css` added the two rules. preview: `src/test/resources/web/portallayout.zul` two side-by-side panels changed `width="50%"` → `width="calc(50% - 6px)"` to budget for the gutter. | 2026-06-02 fix verified live (real CSS, post-build, after reload): all four gaps now exactly 12px and consistent=true — VERTICAL interPanel=12 interColumn=12; HORIZONTAL sideBySide=12 interRow=12, panels equal-width (911px), Editor full-width (1834px), no wrap. Asymmetry consistent with vertical: 2nd-row panel 237px vs 249px (12px from padding-top), exactly as vertical's 2nd-column panel is 904px vs 916px (12px from padding-left). |
| 2026-06-02 | badge overlay (.z-badge / .z-badge::after) clipped by container overflow | The MUI-style overlay badge (corner dot/count via `::after`, straddling the host corner with `transform: translate(50%, -50%)`) rendered with its **top arc cropped** — not a full circle. The badge overhangs ~10px above the host's top edge; the host icons sat in an `<hlayout>`, whose root carries `overflow: hidden` (layout.css — to contain the `white-space:nowrap` inline-block row). Absolute children cannot escape an ancestor's `overflow:hidden`, so the row clipped the overhang. Same trap applies to `.z-card` and any `overflow:hidden` ancestor; `.z-button`'s own `overflow:hidden` was already handled with a scoped `overflow:visible`. | No MD3/Mira reference has an "overlay clearance" concept, so no contract asserts that a corner-overflowing element is *unclipped by its ancestors* — the evaluator only measures the badge's own box, never the clipping chain above it. The skill's `hlayout-vlayout.md` documented the spacing mechanism but never recorded that `.z-hlayout` sets `overflow:hidden`, nor that it crops children which overflow their box (overlays, outside focus rings, tooltips). | skill + preview | skill: `.claude/skills/zk-component-rules/components/hlayout-vlayout.md` — added an "Overflow clipping" section (`.z-hlayout` root is `overflow:hidden`; clips any child that overflows its box; host overlay-decorated widgets in a non-clipping flex row such as `z-hstack` / `z-d-flex`, NOT `<hlayout>`). preview: `src/test/resources/web/usecase2/badges.zul` rows changed from `<hlayout>` to `<div sclass="z-hstack-lg">` (flex, visible overflow). `_badges.css` usage header notes the non-clipping-container requirement. CSS itself was correct — this is a structural/usage gap, not an implementation one. | 2026-06-02 fix verified live (post-reload): all four sections render full circles; DOM walk confirms the row container is `z-hstack-lg` (overflow visible) and `.z-hlayout` no longer appears in the badge's clipping-ancestor chain. Sibling sweep: any future overlay pattern (notification dot on avatar, ribbon, corner tag) must avoid `<hlayout>`/`overflow:hidden` ancestors. |
| 2026-06-04 | checkbox (mold="switch") | Switch sized from the **MD3 spec sheet** (52×32 track, 2px outline border, 16→24px thumb inside the track) instead of the project's visual benchmark **MUI v7 Switch** (34×14 track, radius 7, no border, 20px white elevation-1 thumb overhanging the track, translateX(20px) travel) — visually ~2.3× the height of every sibling control (18px checkbox, 13–14px labels) and out of family with Mira's `MuiSwitch-sizeMedium`. User flagged it as "proportionally too large". | `doc/contracts/checkbox.md` had **zero Expected-values rows for the switch variant** — it existed only as an unchecked `- [ ] switch variant, toggle variant` States-to-evaluate item, so the evaluator never measured it and the generator free-styled from the MD3 spec (CSS comment literally says "MD3 Switch"), skipping the mandatory MUI-reference read (`Inputs/Switch.css`). Same root cause as inplace + portallayout horizontal-orient: a states-checklist entry with no matching expected-value row is invisible to the evaluator. | contract + agent | Contract: `doc/contracts/checkbox.md` — added sw1–sw9 switch Expected-values rows (MUI-derived, token-adapted) + split the switch state into its own checklist item. Agent: evaluator §1.5 — added a gate: any States-to-evaluate entry with zero matching Expected-values rows must be reported as a contract defect (`NO-ASSERTION`), never silently skipped (3rd occurrence of this root cause ⇒ promoted per periodic-review rule). CSS: `js/zul/wgt/css/checkbox.css` §Switch variant resized to MUI values. | 2026-06-04 fix verified live: all sw1–sw9 measured in-browser — track 34×14 r7 no-border, off-track rgba(0,0,0,.383) (≈MUI .38), on-track primary@.5, thumb 20px white+elevation-1 → primary, travel −3px→17px (20px), hover halo 38px @ .08, disabled .38. Sibling sweep: usecase/account-settings (2 switches) renders new size with no ancestor clipping. Toggle mold confirmed as a further sibling gap (solid-primary fill + inset shadow vs MUI ToggleButton selected = primary-tinted 8% bg) — flagged NO-ASSERTION in the contract, deferred as its own design decision. |
| 2026-06-04 | splitlayout (splitter button off-center) | `.z-splitlayout-splitter-button` rendered at the bottom of the bar instead of centered (user report, `-horizontal` orient). Root cause: **double centering** — ZK `Splitlayout.ts setBtnPos_` (`zkmax/layout/Splitlayout.ts:425-432`) writes inline `margin-top = (bar.offsetHeight − btn.offsetHeight)/2` (horizontal orient) / `margin-left` (vertical orient) to center the button, while Marble's `splitlayout.css` *also* centered via `top:50%; left:50%; transform:translate(-50%,-50%)`. Both apply (inline margin offsets from the CSS-positioned 50% point) → button lands ~half-bar past center. Same JS pattern exists in `zul.box.Splitter` (`Splitter.ts:290-296`) and borderlayout `LayoutRegion` — borderlayout's CSS already worked around it (comment block) but the fact was only recorded in the borderlayout skill file, not as a family-wide rule. | `doc/contracts/splitlayout.md` had no centering assertion (no row measures button midpoint vs bar midpoint), so the evaluator never caught the offset; the skill file for splitlayout never mentioned `setBtnPos_` even though the borderlayout skill documented the identical mechanism — single-component skill entry for a family-wide ZK fact. | skill + contract + component CSS | Skill: `components/splitlayout.md` + `components/splitter.md` add "Splitter button positioning — JS centers via inline margin (`setBtnPos_`); never add CSS centering (top/left %, transform, margin) on the JS-managed axis"; `components/borderlayout.md` cross-refs the family rule. Contract: `doc/contracts/splitlayout.md` new outcome rows (button midpoint within ±2px of bar midpoint, both orients). CSS: `js/zkmax/layout/css/splitlayout.css` remove `top/left/transform` centering from `.z-splitlayout-splitter-button`. | 2026-06-04 verified: contracts re-approved; Gate-1 iter 3 caught the residual M9 failure (JS timing trap — see the G1-refinement row below); after CSS axis-ownership fix, iter 4 measured M8/M9 = 0px diff on all 5 splitter instances in both timing scenarios (load + simulated resize); Gate 2 PASS (critical=0) → VERIFIED. |
| 2026-06-04 | splitter, borderlayout, splitlayout, goldenlayout (splitter family fragmentation) | The four splitter implementations shipped four different visual schemes: thickness 8/12/8/8px; idle bg `surface-variant`/`surface-container-low`/`surface-container`/`transparent`; hover primary-12%-mix / `surface-container` / primary-hover-opacity-mix / `primary-container`; affordance 30px-button / 8×28-pill / unsized-button / `::before`-dots. No DESIGN.md section defined a canonical splitter spec, so each Generator pass free-styled per component. User ruled 2026-06-04: unify all four (8px bar, `surface-container` idle, primary-tinted hover, grip-pill on the 3 ZK-mold splitters; GoldenLayout adopts colors only — its `.lm_splitter` is library-injected with no button element). | MD3 has no "splitter" component, so no reference forced consistency; contracts were authored per-component with no cross-component citation, and nothing in DESIGN.md owned the family spec — same-concept-different-values drift was invisible to per-component evaluation. | DESIGN.md + contract (×4) | DESIGN.md: new "Splitter family" section (canonical thickness/colors/affordance, GL exception documented). Contracts: `splitter.md`, `borderlayout.md`, `splitlayout.md`, `goldenlayout.md` splitter rows revised to cite the DESIGN.md section. | 2026-06-04 verified: all four re-evaluated against §14 — Gate-1 all-pass (splitter 25/25, splitlayout 35/35, borderlayout 42/42, goldenlayout 63/63) + Gate 2 PASS ×4 (critical=0); splitter/splitlayout/borderlayout → VERIFIED, goldenlayout → VERIFIED_WITH_ESCALATION (M11/M16 carried). Family addendum from Gate 2: nosplitter hover-tint suppression added to §14 + splitter/splitlayout contracts + CSS (borderlayout exempt — ZK hides the strip). |
| 2026-06-04 | goldenlayout (tab styling diverges from tabbox) | GL tabs used `body-medium` 13px/400 vs Tabbox `label-large` 14px/500, a visible border-frame hover vs Tabbox's MD3 state-layer, and `primary` close-icon hover vs Tabbox's `on-surface` — three same-concept divergences with no documented rationale (hdr-7 cited DESIGN.md §4 which doesn't mandate it). User ruled 2026-06-04: GL tabs adopt Tabbox style for all three; the user-ruled intentional differences (surface-container strip, surface-filled active tab — Gate 2 #1) stand. | goldenlayout has no MD3 analog, so its contract was authored standalone instead of by decomposition to the nearest ZK sibling (tabbox); nothing forced the spec-author to diff same-concept values against the sibling contract. | contract | `doc/contracts/goldenlayout.md` — hdr-7 → label-large size+weight; tab hover rows → state-layer pattern; close-2 → on-surface hover; rationale cites tabbox.md decomposition. | 2026-06-04 verified: iter 15 — hdr-7 14px / hdr-7b 500 / tab-sl-1..3 + tab-h2 state layer / tab-4 retirement (no hover border) / close-2 on-surface all measured PASS; Gate 2 confirms the tab recipe is now identical to the tabbox sibling → VERIFIED_WITH_ESCALATION. |
| 2026-06-04 | goldenlayout (goldenpanel diverges from panel card standard) | `.z-goldenpanel` used 8px `corner-small` radius vs Panel's 6px `--zk-shape-card` (DESIGN.md §5 standard) and 20px/16px padding vs the 16/16/24 card standard (DESIGN.md §10) — the padding cited an iceblue legacy note as its source. User ruled 2026-06-04: full align to Panel (6px radius on panel AND header top corners — uniformity preserved at the new value; 16/16/24 padding). | Same standalone-contract root cause as the tab divergence: no decomposition diff against `panel.md`; the iceblue legacy citation survived spec revisions unchallenged. | contract | `doc/contracts/goldenlayout.md` — panel-3/panel-4 + header top-corner rows (hdr-3/hdr-4, panel-top-radius-*) → `--zk-shape-card`; panel-5 padding → 16/16/24; rationale cites panel.md decomposition. | 2026-06-04 verified: iter 15 — all radius rows 6px `--zk-shape-card`, panel-5 padding 16px 16px 24px measured PASS; Gate 2 confirms radius/padding identical to the panel sibling → VERIFIED_WITH_ESCALATION. |
| 2026-06-04 | borderlayout (contract stale vs deliberate CSS) | Contract rows c12/c13 (strip 6px), c19–c22 (button 20×64 / 64×20), grip font-size 14px assert values the shipped CSS deliberately does not implement (12px strips, 8×28/28×8 pills, 8px grips — with documented MD3 bottom-sheet-handle rationale in the CSS comments). The CSS evolved past the contract without a contract revision — silent drift on a T1 contract-approved component, exactly the DR-2 failure class. User ruled 2026-06-04: CSS wins; revise contract to the implemented pill design, adjusted to the 8px bar from the family unification. | Component is grandfathered VERIFIED (pre-dual-gate); no re-eval ran after the CSS redesign, and Gate 1 only re-runs on RE_EVAL_NEEDED — nothing diffs contract vs CSS at rest. | contract | `doc/contracts/borderlayout.md` — c12/c13 → 8px (family spec), c19–c22 → pill 8×28/28×8, grip rows → 8px; sources cite DESIGN.md splitter-family section. | 2026-06-04 verified: iter 4 — all reconciled rows (c12/c13 8px, c14/c17 family colors, c19–c22 pill, c23 outline-variant, c26 none, c30b 8px grips, c30c caret fade) measured PASS; the re-eval also exposed c4/c6/c8/c10 (region hairlines asserted but never implemented — second silent-drift instance on the same component) → implemented iter 5, all PASS; Gate 2 PASS → VERIFIED. |
| 2026-06-04 | checkbox (mold="toggle") | Toggle mold free-styled with no reference: an **empty 34×36 box** (padding 6px 16px around zero content — ZK renders the mold element empty for all molds), ON state = **solid primary fill + inset 0 2px 4px shadow**, border `outline` (0.23) — none of which exist in the MUI v7 benchmark (ToggleButton: selected = primary text on primary-8% tint, border outline-variant 0.12, no inset shadow) or MD3 (toggle icon button: selected = filled/tonal container with icon). The blank frame next to an 18px checkbox / 14px-track switch reads oversized because it is all chrome, no signal. | Same NO-ASSERTION root cause as the switch (flagged in the same contract update): `doc/contracts/checkbox.md` had zero Expected-values rows for the toggle mold, so the evaluator never measured it and the generator invented a skeuomorphic pressed-button look. Additionally a skill blind spot: nothing recorded that the mold element is rendered EMPTY (one shared mold renderer), which is what forces the icon-injection design and rules out a label-inside toggle button. | skill + contract | Skill: `.claude/skills/zk-component-rules/components/checkbox.md` — new "The mold element is always EMPTY" section (shared renderer, label always a sibling, glyphs must come from pseudo-elements, closest Material analog = toggle icon button). Contract: `doc/contracts/checkbox.md` — added tg1–tg7 Expected-values rows and replaced the toggle NO-ASSERTION checklist flag. Design iterated WITH the user in the same session: first pass = MUI ToggleButton tint + check icon; user rejected the check (selection-confirmation idiom diverges from toggle press semantics) ⇒ final = 32×32 r4 outline-variant box, ON = **solid primary fill, no glyph** (MD3 "selected = filled container" reading), no inset shadow, ON hover = on-primary 8% over primary (filled-button convention); tg5 (check icon) retired, id not reused. CSS: `js/zul/wgt/css/checkbox.css` §Toggle mold restyled accordingly. | 2026-06-04 final design verified live: tg1 32×32; tg2 1px rgba(0,0,0,.12) r4; tg3 off transparent; tg4 ON solid rgb(55,111,208), border primary, shadow none, ::after display none; tg6 ON hover rgb(71,123,212) (= on-primary 8% over primary); tg7 disabled .38. Measurement lesson promoted to evaluator agent: occluded-tab CSS transitions freeze at t≈0 and serialize as oklab() snapshots — kill transition inline before reading hover/focus computed styles (added to evaluator Dynamic-states as "Transition-freeze trap"). |
| 2026-06-04 | splitlayout (orient="vertical" — JS centering unreliable at load; G1 refinement) | After the G1 fix (CSS centering removed, axis left to JS), Gate-1 re-eval failed M9: the `-vertical` pill sat at the bar's **left edge** (902px off center). Root cause: `setBtnPos_` runs from `_fixsz()` at bind, but the horizontal bar's `offsetWidth` is still **0** (flex width not yet computed), so JS writes `margin-left: 0` and never re-runs until the next window resize. Verified live: re-invoking `setBtnPos_(true)` post-layout writes the correct margin (903px → 1px diff) — the mechanism works, the timing doesn't. The G1 rule "never add CSS centering on the JS-managed axis" was half right: half-mixing is the bug, but pure JS ownership is unreliable on a flex-resolved axis. | The G1 skill rule was derived from the double-centering failure mode only; nothing tested the JS-only path at load time on the orientation whose bar dimension comes from flex stretch (the `-horizontal` orient happened to work because the bar's height is CSS-fixed at 8px, available at `setBtnPos_` time). | skill + component CSS (+ contract mechanism-prose errata) | Skill: `components/splitlayout.md` / `splitter.md` / `borderlayout.md` rules rewritten — "never half-mix; axis owned entirely by JS OR by CSS; CSS ownership = `margin-left/top: 0 !important` (stylesheet `!important` beats inline non-important style) + `left/top: 50% + transform`; CSS must own the flex-resolved axis". CSS: `js/zkmax/layout/css/splitlayout.css` — both orientations take CSS ownership of the long axis. Contracts: `splitlayout.md`/`splitter.md` mechanism prose corrected (outcome rows M8/M9 ±2px unchanged — no assertion change). | Verified live pre-fix 2026-06-04: injected the CSS-ownership pattern, re-ran `setBtnPos_(true)` (simulating resize) → diff 0px on all 3 instances in both timing scenarios. Gate-1 iter 4 re-eval: M8/M9 = 0px diff on all 5 instances in both timing scenarios → component VERIFIED 2026-06-04. |

| 2026-06-05 | cropper (any pre-2026-06-03 contract re-approved without re-authoring) | Contract reached contract-audit GATE2: PASS + user approval + dual-gate VERIFIED with NO `## Outcome assertions` section — the crop-selection anatomy (highlighted region, dim mask, 8 resize handles, crop/cancel toolbar) was never asserted anywhere; only 5 wrapper-chrome D-tier rows existed. User caught it reading ZKCompRef_Cropper.png. | Contract was authored 2026-05-29 (d14c187), 5 days BEFORE the outcome-driven format landed in zk-spec-author Step 6 (2026-06-03). The 2026-06-04 orchestrator run saw "already in spec-author format" → skipped re-dispatching zk-spec-author, so Step 6 never ran on it. The contract-audit gate (md3-design-verifier) had zero checks for outcome-section presence, and the Wave-4 "pending" slot in outcome-migration-status.md made the omission look like planned deferral — but cropper is T3, where Step 6 says outcome rows are MANDATORY (≥5) regardless of wave. | contract + agent | Contract: `doc/contracts/cropper.md` gained `## Visual outcome` (theme-agnostic UI anatomy from ZKCompRef_Cropper.png) + `## Outcome assertions` M1–M6 (image visible, dim-vs-highlight contrast, 8 handles, 2-action toolbar visible+adjacent, rest-state clean) with an explicit measure-only carve-out for Jcrop forbidden-selectors. Agent: `md3-design-verifier.md` Step 0 gained an outcome-assertions presence check (Critical for T3/layout/data-rich, Suggested for inputs/stubs). | sibling sweep 2026-06-05: 12 of 16 approved contracts lack outcome sections (borderlayout, grid, listbox, organigram, pdfviewer, portallayout, signature, splitter, stepbar, tabbox, tbeditor, tree) — all legacy pre-format approvals; the new Step 0 check forces backfill when each next reaches the audit gate per its wave. Cropper M1–M6 ran same day: M3 FAILED on first run (8 handles invisible) — the backfilled assertions immediately caught a real shipped bug (see next row); fixed and dual-gate VERIFIED iter-6 2026-06-05. |
| 2026-06-05 | cropper | All 8 Jcrop resize handles render 0×0 (invisible); drag-out crop UI has no visible handles/guides. Root cause: Marble shipped ZERO structural CSS for the Jcrop-injected subtree because the skill/contract declared it "off-limits for theme CSS" — but Jcrop ships NO stylesheet in ZK's integration; upstream codegen `cropper.css.dsp` carries ~30 structural rules (handle 6×6 + 8 ordinal transforms, dragbars, area outline, guide lines, tracker, holder cursor, `display:inline-block` shrink-wrap) that every theme MUST reproduce. | The "library-owned DOM" T3 rule was applied as "don't style it at all", conflating DOM ownership with style ownership. No assertion covered handle visibility until M3 was added (same day, previous gap row) — M3 failed on its first run and exposed this. | skill + contract | Skill: `components/cropper.md` intro + Notes rewritten — structural rules REQUIRED (geometry verbatim from upstream codegen path), only decorative values are theme choices; Edition also corrected EE→PE. Contract: boundary yaml restructured into `restyle-freely` / `structural-required` / `forbidden-selectors` (only the nonexistent `.z-cropper-canvas` stays forbidden); added c6 `display:inline-block`. CSS fix via Generator (same day). | M3 (added hours earlier) caught this on its first evaluator run — the outcome-assertion layer worked as designed. |
| 2026-06-05 | goldenlayout | Header maximize/close controls were 20×20 text glyphs (`⤢`/`×`) while panel/window render 28×28/32×32 icon buttons with Lucide SVG masks — no theme-wide "header control icon button" family rule existed, so GL invented its own size and glyph technique. | M9/M13/M15 asserted presence, position, and glyph literal but never size or icon-technique parity with the panel/window family; no DESIGN.md rule declared the family standard to compare against. | contract (+ DESIGN.md family rule) | DESIGN.md §12 control-icon-button family table; contract ctrl-* rows revised (28×28, corner-full, Lucide masks), M15 revised (glyph literal → Lucide `expand` mask pin), new M18 (size parity). | Sweep: panel/window/tabbox already conform; GL was the only deviator. |
| 2026-06-05 | goldenlayout | Per-tab close icon sat visually flush against the tab title — bare `×` text glyph with tight side-bearings and no button box, vs tabbox's 16×16 corner-full button with centered 12px icon at the same 4px margin. | close-1/close-2/M14 asserted color and bbox ≥ 8×8 but not the button-box geometry (fixed 16×16, corner-full, centered mask icon) that gives tabbox its breathing room; "tabbox-alignment" ruling 2026-06-04 only propagated colors, not geometry. | contract | Contract close-* rows extended to full `.z-tab-button` parity (16×16, corner-full, 12px Lucide `x` mask, hover bg mix); M14 revised from content-glyph check to mask+bbox check. | — |
| 2026-06-05 | goldenlayout | `.lm_splitter` kept family bar colors but was effectively unusable as an affordance: dot marker at opacity .5 imperceptible, no actuator pill, and cursor stayed `auto` (GL's bundled CSS that normally provides resize cursors is never loaded in ZK). | DESIGN.md §14 GL exception ("no button element → no pill") was over-broad — a `::before` pseudo can draw the pill since GL has no `setBtnPos_` JS centering; M12 asserted only non-zero bbox, not perceivability; the family `cursor` row was never propagated into the GL contract. | contract (+ DESIGN.md §14 revision) + skill | DESIGN.md §14 GL paragraph narrowed (pill via pseudo; only collapse caret stays out); contract spl-cursor-* rows added, pill rows added, M12 strengthened to perceivability; skill note: GL ships no cursor — theme must set col/row-resize. | — |
| 2026-06-05 | goldenlayout | `.z-goldenpanel` bottom border clipped by 1px: rendered `.lm_header` was 45px (tab content 44px + 1px border-bottom, border-box min-height 44 not binding) while `bind_` snapshots `min-height` 44 into `config.dimensions.headerHeight` — GL's inline child heights ran 1px past the flex-shrunk `.lm_items` (overflow:hidden). | M16 asserted panel containment vs the WRAPPER (which is 144px taller), not vs the nearest overflow-hidden ancestor (`.lm_items`); no assertion tied rendered header height to the min-height that bind_ reads. | skill + contract | Skill `components/goldenlayout.md`: header-height-sync rule (rendered height MUST equal min-height; never let tab content or content-box borders exceed it). Contract: new M17 (panel inside `.lm_items` + header height == min-height), tab-2 revised to `0 spacing-4` padding, lm_tabs stretch row. | — |
| 2026-06-05 | splitlayout (+ theme-wide css-flex) | Splitter drag never persists (both orientations) — panes snap back to 50/50 on mouseup. ZK's `_doDragEndResize` persists drag by writing inline px `width`/`height` on the cave divs after disabling child flex (`clearCSSFlex` removes `z-flex*` classes), but Marble caves carry `flex: 1 1 0` whose `flex-basis: 0` makes the browser ignore inline main-axis sizes, and the root hard-codes `display: flex` so the container stays flex even after JS removes `z-flex`. Deeper cause: the framework classes `.z-flex`/`.z-flex-row`/`.z-flex-column`/`.z-flex-item` (toggled at runtime by `zk/flex.ts` for every hflex/vflex container, defined in stock `zul/less/footer.less`) were never ported — the utility layer renamed them (`.z-d-flex`, `.z-flex-col`), so ZK's css-flex engine is silently inert theme-wide and splitlayout was wrong-layer "fixed" by hard-coding flex on its root. | Skill `components/splitlayout.md` already documented the correct stock block/inline-block layout model, but no contract assertion covered drag persistence (a behavior outcome, not a static style), so the implementation drifted to a flex model that passed all visual rows. No skill reference documented the JS-toggled framework class contract, so renaming `.z-flex` in the utility pass looked safe. | skill + contract + component CSS + global CSS | Skill: new `reference/css-flex-classes.md` (framework class contract — define verbatim, never rename, never hard-code `display:flex` on JS-managed containers) + `components/splitlayout.md` drag-persistence section. Contract: `doc/contracts/splitlayout.md` M10/M11 drag-persistence outcome rows (encoded failing pre-fix). CSS: new `zul/css/base/_cssflex.css` (stock footer.less block verbatim, appended to footerFiles in `scripts/build-css.js`) + `js/zkmax/layout/css/splitlayout.css` layout-model rewrite to stock block/inline-block (visuals unchanged). | 2026-06-05 fix verified: pre-fix probe FAILED all 6 draggable instances (M10: JS inline `width:1013px` ignored, bbox stayed 913; M11: drag-end `z-flex-column` removal + Marble's `:not(.z-flex-column)` fallback flipped the vertical layout to a ROW). Post-fix: M10/M11 PASS on all instances via both the `_doDragEndResize` API path and a real mousedown/mousemove/mouseup drag (+120px H persisted; +80px V persisted + docked). M1–M9 regression sweep all pass (M8/M9 pill centering 0px diff). Sibling sweep (borderlayout, hlayout, splitter, tabbox, usecase2 #default/#tables-advanced): zero `z-flex` consumers on those pages, no layout change, no h-scroll — splitlayout is currently the only css-flex consumer, so blast radius was minimal. |
| 2026-06-05 | goldenlayout | Marble omitted the entire goldenlayout-base.css structural block that ZK's upstream codegen `goldenlayout.css.dsp` reproduces verbatim — maximize rendered in-flow at half width (no `position:absolute` on `.lm_maximised`, plus a specificity trap vs `.lm_item.lm_stack`), the drop-target indicator was a permanently visible dashed line at rest, and `.lm_transition_indicator` sat in-flow at `<body>`. | Contract row max-1 was mislabeled "JS-driven; do not change" so nobody owned the rule; no M-row exercised the maximise interaction or asserted rest-state invisibility of GL utility nodes; the harness never diffed Marble's css.dsp against the upstream codegen css.dsp baseline (exact repeat of the cropper/Jcrop gap logged earlier the same day). | skill + contract | Skill `components/goldenlayout.md`: base-CSS block enumerated as must-have rules (incl. specificity trap, body-level unscoped selectors). Contract: max-1 corrected + max-2/max-3 + base-1…base-7 rows; new M19 (rest-state utility nodes invisible) + M20 (maximise interaction: full coverage + restore-icon swap). CSS fixed same day. | Found by clicking the maximize icon during verification of gap rows 1–4 — interaction-level M-rows (M20) are what catch this class; declared-property rows cannot. |
| 2026-06-05 | cropper | The crop/cancel toolbar shipped with no design at all — bare unstyled `<a>` links on a `surface` strip with a `border-top`: no button affordance, no hover/pressed state layers, flush against the selection edge. Both symptoms were already flagged by Gate 2 (Suggested ×2) and deferred; user escalated asking how it should be designed under MD3. | The contract only ever had 2 toolbar D-rows (c4 bg, c5 border-top) inherited from the original strip idea — no rows for the action items themselves, so the Evaluator had nothing to fail; Gate 2 caught it but Suggested findings are non-blocking and were parked in todo.md instead of being promoted to failing contract rows. | contract | Contract `doc/contracts/cropper.md`: toolbar redesigned as MD3 floating toolbar + text buttons — c4 revised (surface-container), c5 replaced (border-top → elevation-2 shadow), new c7 (corner-full), c8 (margin 8px/4px clearance — JS uses `outerWidth(true)`, margin is the sanctioned gap mechanism), c9/c10 (crop=primary, cancel=on-surface-variant), c11 (hover/pressed ::before state layer), c12 (item corner-full); M5 revised to assert 4–24px clearance (was ≤24px, passed even when flush). CSS same day. | — |
| 2026-06-05 | cropper | 300px of visible blank space right of the image inside the outlined wrapper. Previously dismissed as "page-level sizing, not theme-actionable (LOW)" — wrong call: the preview ZUL's `width="600px"` can NEVER have an effect because ZK's load handler caps the `<img>` at `naturalWidth` (300px) and never upscales; the inline 600px just leaves dead space inside the root in EVERY theme. Iceblue shows no blank only because its wrapper is borderless (dead space invisible); Marble's approved border made it visible. | The iter-6 evaluator labeled the finding LOW/non-actionable without tracing the JS (`Cropper.ts` load handler `cropperWidth > imgNaturalWidth ? imgNaturalWidth : cropperWidth`) or comparing against the iceblue baseline render; no outcome row asserted the wrapper shrink-wraps its image. | skill + contract | Skill `components/cropper.md`: width-cap fact added under Attribute support (width > naturalWidth ⇒ dead space; never upscales) + new Composition invariant: cropper REQUIRES an externally determined size (`onSize → resizeImage(root size)` is a feedback loop — removing `width` entirely collapsed the holder to 0×400 live). Contract: new M7 (root bbox ≤ holder bbox + 4px), M1 selector precision (Jcrop hides the source img — measure `.z-cropper-holder img`). Page fix: `cropper.zul` width rightsized 600px → 300px (= natural width) and the single-child column-flex wrapper removed (flex stretch was a second dead-space contributor). | — |
| 2026-06-05 | splitlayout (+ every rhythm-listed widget as css-flex child) | Horizontal splitlayout panes left a 12px hole between the pane child and the splitter/trailing edge: `_rhythm.css` gave container widgets (`.z-window`, `.z-panel`, `.z-grid`, …, `.z-splitlayout`) a theme-invented `margin-block-end: 12px`, and ZK's css-flex engine (`zk/flex.ts` `applyCSSFlex` ~600–624) sizes a cave-wrapped child with `calc(100% - zk(c).marginHeight())` in **row** mode — on **both axes** — so the vertical margin became a 12px deficit on width AND height (measured: 4/10 caves on splitlayout.zul, all horizontal-orient, incl. a nested splitlayout child; inline `width/height: calc(100% - 12px)`). Column mode subtracts `marginWidth()` (=0) → vertical split was unaffected. Root decision: stock ZK widgets carry ZERO default margin and ZK's JS sizing paths assume that — a theme-default margin on flex-capable widgets is an open-ended bug class, not a one-off. User ruling 2026-06-05: remove the default rhythm entirely (Plan B), spacing is opt-in via `.z-vstack`/`.z-mb-*`/`<vlayout>`; reverses the 2026-05-28 row's auto-rhythm approach with the new information that ZK JS measures margins. | Contract asserted splitter adjacency between caves (M4) but never "pane child fills its cave"; the hole is inside the cave. No MD3/Mira splitlayout analog. The rhythm rule itself shipped without sweeping JS-sized containers (flex caves) because no skill rule recorded the margin-measurement fact. | skill + contract + global CSS | Skill `reference/css-flex-classes.md`: new "Margin subtraction" section (ZK fact: row mode subtracts marginHeight from both axes; never give default margins to flex-capable widgets). Skill `components/splitlayout.md`: caves are wrapper flex children (`fcc != c`) — child margins become trailing holes. Contract `splitlayout.md`: new M12 pane-fill (child bbox == cave bbox ±1px, both orients, incl. nested; pre-fix 4/10 fail) + §14 note: the 8px bar IS the pane separation, no gutter. CSS: `_rhythm.css` deleted (build-css.js unwired); portallayout keeps its verified stacked-panel gap via a scoped rule in `portallayout.css`; `doc/spec/spacing-policy.md` rewritten to opt-in policy. | 2026-06-05 verified: M12 PASS on all 10 caves (0px deficit both axes); M10/M11 drag persistence re-run green via real mouse-event drag (H +120px, V +80px, docked) AND pane fill stays 0px in the post-drag classless state. Portallayout stacking gap intact (12px vertical, 12px side-by-side via the new scoped rule). Rhythm-loss sweep over window/panel/grid/listbox/tabbox/groupbox/tree/cardlayout previews + usecase2 #default/#tables-advanced + usecase dashboard: only cardlayout.zul regressed (cardlayout touched the button row — the original 2026-05-28 complaint); fixed opt-in with `z-vstack` on the wrapper (gap 12px verified). `utility/stack.zul` demo section rewritten for the opt-in policy. |
| 2026-06-06 | goldenlayout | `.lm_splitter` idle bar (`surface-container`) is the SAME color as the `.lm_header` strip it abuts — the 8px band and the header below read as one undifferentiated region (worst on `lm_vertical`); only the pill hinted at a boundary. | spl-1 asserted `surface-container` as the EXPECTED value (§14 family fill, 2026-06-04 ruling) — declared-property rows can't see that two abutting regions share a computed background; no outcome row asserted adjacent-surface contrast. The §14 fill was designed for bars between surface panes; GL bars abut surface-container headers. | contract + DESIGN.md §14 | User re-ruling 2026-06-06: idle bar = `transparent` (GL panels are self-bordered cards; the splitter region is a canvas gutter, MUI dashboard convention); hover/drag tints become translucent mixes over transparent. Contract spl-1/2/3 corrected; new outcome M21 (splitter idle bg must differ from adjacent `.lm_header` computed bg); DESIGN.md §14 GL paragraph re-ruled with rationale. | — |
| 2026-06-06 | goldenlayout | `.lm_tab.lm_active` fill (`surface` #fff) equals `--zk-color-background` (#fff) — the active tab reads as a hole punched in the strip connecting to the white page, not as a selected tab. | tab-a2 asserted `surface` as the EXPECTED value ("selected page" Gate-2 ruling 2026-06-04); the ruling never checked the fill against the page background token (surface == background in this palette). MD3 primary tabs never fill the selected tab — selection = indicator + label color. | contract | User re-ruling 2026-06-06: tabbox parity — active tab transparent on the strip, `primary` label + 2px `primary` underline (exactly `.z-tab-selected`); tab-a2 corrected to `transparent`, tab-a7/a8 (top radii on the fill) retired; contract prose "selected page" paragraph superseded. | — |
| 2026-06-06 | hbox, vbox, splitter (default mold) | Splitter drag is dead on `/splitter.zul` Default Mold: (1) dragging the `z-splitter-vertical` bar never moves it; (2) after dragging `z-splitter-horizontal` right, the inner vertical bar never lengthens. Root cause: Marble's `box.css` forces the hbox/vbox **outer `<table>`** to `display: inline-flex` (plus styles `.z-hbox-cell`/`.z-vbox-cell` — classes that exist nowhere in ZK; the table mold emits only `z-splitter-outer`/valign). With the table's display destroyed, the tbody gets an anonymous-table wrapper with auto size, so the inner `-real` table's inline `width/height:100%` never resolves (measured: real table 68px tall inside a 300px vbox) — rows squash to content, the drag clamp (`run.next.offsetHeight`) is ≈0, and the px sizes `_doDragEndResize` writes onto the adjacent `<td>`s have no room to act. Stock ZK never touches `display` on `.z-hbox`/`.z-vbox` (box.less only resets border-spacing/padding). Verified live: injecting `display: table` restores the 100%-chain (real table 300px) and the bar responds to td heights. | Same class as the 2026-06-05 splitlayout css-flex gap: the harness verifies declared styles, and no skill file documented the hbox/vbox TABLE mold (no `components/box.md` existed), so the first-iteration Generator hallucinated a div-flex DOM and shipped flex rules that passed every visual row; `doc/contracts/splitter.md` (pre-outcome-format, on the 12-contract backfill list) had zero M-rows, so drag behavior was never exercised. | skill + contract + component CSS | Skill: new `components/box.md` (table mold anatomy, %-chain dependency, never override display, no `-cell` classes) + `components/splitter.md` drag-persistence section (px-on-td mechanism, snap clamp). Contract: `doc/contracts/splitter.md` gains `## Outcome assertions` M1–M5 (encoded failing pre-fix). CSS: `js/zul/box/css/box.css` — flex invention replaced with stock-equivalent table reset. | 2026-06-06 pre-fix probe: M-drag FAILED (bar bbox unchanged after +80px pointer drag; real-table h=68 vs 300). Post-fix verification recorded in the contract rows. |
| 2026-06-06 | borderlayout | Sibling sweep of the GL splitter↔header merge (same day): 5 live adjacencies on borderlayout.zul where a `z-{west,east,north,south}-splitter` bar (`surface-container`) abuts a `z-*-header` region header (also `surface-container`) — same boundary-erasing pattern, measured `rgb(240,244,250)` on both sides of every adjacency. | Same as the GL rows: spl bars and region headers each assert `surface-container` as EXPECTED values; no adjacent-surface-contrast outcome row exists in `doc/contracts/borderlayout.md`. The §14 family fill was never checked against what it abuts. | contract + DESIGN.md §14 (deferred) | **Deferred to a dedicated borderlayout pass (user ruling 2026-06-06)** — borderlayout regions are flush tiles, not self-bordered cards, so the GL transparent-gutter rationale does not transfer 1:1; the pass must make its own ruling (transparent gutter vs darker bar vs header tone) via failing-first contract rows (an M21-style adjacency comparison) + Gate-2 review. DESIGN.md §14 already records the general principle ("bar idle fill must contrast with every surface it abuts"). | — |
| 2026-06-05 | slider (knob mold) | `.z-slider-input` shipped with native number-input spin buttons visible and the value left-aligned — Chrome reserves spinner space inside the digit-count-sized JS box so the number reads off-center. Iceblue suppresses spinners (`appearance: textfield`) and centers the text; MD3/MUI never show native spinner chrome, and the user-approved mockup (`doc/contracts/slider.html` k3–k5 block) already encoded suppression + `text-align: center` + extra-small radius — but only as mockup CSS, never as contract rows. | Contract Expected-values table had only k3/k4/k5 (background/border/color); rules that exist solely in the mockup's CSS are invisible to the evaluator — nothing enforces mockup-CSS ↔ Expected-values-table parity, so the generator implemented exactly k3–k5 and stopped. | skill + contract | Skill `components/slider.md`: knob-input ZK fact (overlay is `<input type="number">`; UA spin buttons render unless suppressed; `_getInputProperty()` sizes the box from digit count so spinner space de-centers the value). Contract `doc/contracts/slider.md`: new k6 (text-align center), k7 (appearance textfield + `::-webkit-*-spin-button` none), k8 (border-radius extra-small) — encoded failing pre-fix (probe: appearance auto / text-align start / radius 0). CSS: `js/zul/inp/css/slider.css` same day. | Mockup-only CSS rules are a recurring blind spot candidate: when approving a mockup, every visual rule in its component CSS block must have a matching Expected-values row, or it will silently not ship. |
| 2026-06-05 | goldenlayout | Hovering `.lm_close_tab` shows the 16×16 circular state layer but the 12px `x` icon is NOT concentric with it (sits up-left): ZK's bundled GL fork (`zkmax .../goldenlayout/ext/goldenlayout.js` Tab ctor, line ~2746) unconditionally chains `.css('display','inline-block')` after show/hide — an inline style upstream GL 1.x does not write — so Marble's `display: inline-flex` + `align-items/justify-content: center` on `.lm_close_tab` is dead CSS and the icon pseudo falls back to inline-flow placement (text-align start + baseline) inside the circle. | M14 asserted existence + size of the `::before` (bbox ≥ 8px, mask non-none) and close-3/4 asserted the 16×16 box, but no row asserted icon↔button concentricity; the declared `inline-flex` row passed in the stylesheet while the inline style silently won the cascade. Evaluator never measured with the live inline style in mind. | skill + contract | Skill `components/goldenlayout.md`: new Notes bullet — ZK GL fork writes inline `display:inline-block` on `.lm_close_tab`; theme `display` on this element is dead, icon centering must be display-independent (absolute `inset:0; margin:auto` pseudo) or `!important`. Contract `doc/contracts/goldenlayout.md`: new M22 (icon box concentric with `.lm_close_tab` bbox ±1px both axes, measured with the inline style intact) — encoded failing pre-fix. CSS `goldenlayout.css` same day: `.lm_close_tab` → `position:relative`, `::before` → absolute-centered. | — |
| 2026-06-06 | splitter family (splitter, borderlayout, splitlayout, goldenlayout) | The DESIGN.md §14 canonical values are hand-copied as literals into 4 CSS files (~10 declaration groups each: bar idle/hover/active fills, transition, 8px thickness, pill 8×28 geometry/fill/hover, grip-icon color/size). Audit 2026-06-06 found a live drift instance this duplication already produced: `borderlayout.css` ships `ew-resize`/`ns-resize` cursors while §14 and the other three implementations use `col-resize`/`row-resize` — and no borderlayout contract row asserts cursor, so nothing could catch it. | The 2026-06-04 family unification fixed the *values* but left the *mechanism* that produced the drift: four files each restating §14 by hand, with contracts as the only sync (and the borderlayout contract simply lacked the cursor row). No single CSS source existed for the family values. | contract + global CSS (tokens) + agent | Contract: `doc/contracts/borderlayout.md` new c43/c44 cursor rows (encoded failing pre-fix). CSS: new `zul/css/tokens/_splitter.css` — `--zk-splitter-*` family token group (single source for §14 values, global via norm.css.dsp); all 4 component files migrated to consume the tokens (visual no-op; GL keeps its documented transparent-gutter exception locally); borderlayout cursors corrected. DESIGN.md §14 now names the token file as the implementation source. Agent: generator §1.3 + evaluator §1.5 gain a splitter-family trigger (read §14 + family tokens; flag sibling re-eval after family-value changes). | 2026-06-06: post-migration computed-style spot check on all 4 preview pages (bar bg, hover mix, pill fill/size, cursor) — values unchanged except borderlayout cursors now col/row-resize per §14. |
| 2026-06-05 | goldenlayout (+tabbox finding, deferred) | Closable GL tab's close button sat 16px from the trailing edge — same inset as the text — instead of the reduced ~half inset every dismiss-capable inline reference uses (MD3 input chip 8dp vs 16dp; MUI Chip deleteIcon 5px vs 12px label; Chrome tabs ~6–8px). User design question 2026-06-05 ("close icon 是不是應該要再靠邊緣一點?") → ruling: closable tabs reduce trailing padding to `spacing-2` (8px); geometry becomes 16/4/8 (leading/gap/trailing). Sibling sweep against the parity anchor found WORSE: Marble tabbox renders `.z-tab-button` on the LEADING side of the label (`× Tab1`) — ZK emits the button before `.z-tab-text` in DOM, iceblue repositions it right via absolute positioning, Marble left it in flow — contradicting the GL contract's "tabbox parity = immediately after the label" prose and all references. | No row in either contract asserted the close button's *side* or *trailing inset*: GL had close-1..6 (colors/size/radius) + M14/M22 (existence/centering); tabbox only c11a (visibility). Placement-within-component was a check-category blind spot — `doc/spec/md3-close-affordance-placement.md` (written same day) now records the convention the contracts never encoded. | contract (GL now; tabbox deferred) | GL contract: new close-7 (`.lm_tab:has(> .lm_close_tab)` padding-right `spacing-2`, failing-first) + prose updated with the 16/4/8 ruling and the parity-anchor caveat; close-glyph-2 revised (stale after the M22 abs-centering fix: declared `inline-block` blockifies to `block` under `position:absolute`). CSS `goldenlayout.css` same day. **Tabbox side-flip deferred to a dedicated pass (user ruling 2026-06-05)** — needs its own failing-first rows (`order:1` flip + trailing inset) + Gate-2 review since the leading-× visual was previously shipped. | — |
| 2026-06-05 | cascader | Expand caret `.z-cascader-item .z-cascader-icon` floats immediately after the item text — its horizontal position varies with label length — instead of anchoring at the row's trailing edge. MD3 menus place the submenu indicator in a fixed trailing-icon slot; the user-approved mockup already encoded the anchor (`margin-left: auto` in cascader.html item-icon CSS) but shipped CSS used `margin-left: spacing-1`. User caught it visually. | Two compounding causes: (1) mockup-only CSS rule — contract Expected-values had only c25 (icon color), no positional row, so the trailing anchor approved in the mockup was invisible to the evaluator (exact repeat of the slider knob-input blind spot, 2026-06-05 row); (2) shipped CSS targeted a nonexistent wrapper — `.z-cascader-item > label { flex:1 }` is dead code (Cascader.ts `_renderItems0` emits a bare text node via `zUtl.encodeXML(item.content)`, no label/span), so the flex push the author intended never happened. | skill + contract | Skill `components/cascader.md`: bare-text-node fact (items have NO element wrapper around the text; `> label` selectors match nothing; icon positioning must live on the icon itself, e.g. flex `margin-left: auto`). Contract `doc/contracts/cascader.md`: new c26 (expand icon `margin-left: auto`) + outcome M7 (all expand icons within one cave share equal bbox.right ±1px regardless of label length) — encoded failing pre-fix. CSS `js/zkmax/inp/css/cascader.css` same day (dead `> label` rule removed; icon anchored). | Pre-fix probe + post-fix verification recorded below in this row's fix; recurring lesson now twice-logged: every visual rule in an approved mockup's component-CSS block needs a matching Expected-values row or it silently never ships. |
| 2026-06-05 | tabbox | Deferred side-flip pass executed (user order, same day as the deferral): close × moved from the leading side to the trailing edge in ALL molds. Sweep also exposed a wrong skill DOM tree: strip-mode tree drew `.z-tab-button` as a *sibling after* `.z-tab-content` — live DOM has it as the **first child inside** `.z-tab-content` (before the label), and `img` tabs nest `img.z-tab-image` *inside* `.z-tab-text` (not `img.z-tab-icon` as a content child). The wrong tree is how the leading-× shipped unseen. | c11a checked visibility only; no row asserted the button's side (logged in the prior goldenlayout+tabbox row). Additionally the skill tree itself was wrong (rules-wrong per the triage table), so even a careful generator reading the skill would mis-place the button. | skill + contract | Skill `components/tabbox.md`: DOM tree corrected (button first-child of content; img nesting), attribute line + new Notes bullet (leading emission → themes must reposition via `order`/absolute). Contract `doc/contracts/tabbox.md`: new §Close button c31–c35 + first Outcome assertion M1 (trailing side + mold-specific inset), encoded failing-first (pre-fix probe: gap label→icon −50px). CSS `tabbox.css` same day: `order:1` + gap-only spacing + `:has` closable padding (16/4/8) + vertical `flex:1`/`margin-left:auto` + accordion chevron `order:2`. | 2026-06-05 verified live on /tabbox.zul: strip 4/8 (gap/inset), strip+images icon-label-× order, vertical-left inset 10 (8+2px indicator), vertical-right inset 8, accordion `label … × chevron` at 32px trailing cluster — M1 PASS all molds. GL contract parity caveat updated to "corrected". |
| 2026-06-07 | tabbox | Accordion mold rendered the close × on closable headers. User ruling 2026-06-07: accordion must NEVER show the close icon regardless of `closable` — accordion = MD3 expansion panel, and MD3 expansion panels carry no inline dismiss affordance (`doc/spec/md3-close-affordance-placement.md`). Supersedes the iceblue-baseline choice. | c11a asserted visibility by copying the iceblue baseline ("inherits strip-mode; no override") instead of consulting the MD3 analog for the mold — for the accordion mold the right reference is the expansion panel, which has no dismiss icon. The 2026-06-05 close-button pass (c31–c35, M1) then specced the accordion ×'s *position*, cementing the wrong premise. | contract + skill + doc | Contract `doc/contracts/tabbox.md`: c11a corrected to `display: none` (failing-first), M1 accordion clause replaced with hidden-assertion, prose + c35 rationale updated. Mockup `doc/contracts/tabbox.html`: accordion ×s removed (visual contract was wrong; user prompt = ruling). Skill `components/tabbox.md`: Notes bullet rewritten — ZK fact (button IS emitted in accordion DOM) kept, theme-prescriptive sentence removed. Doc `md3-close-affordance-placement.md`: expansion-panel rule added. CSS `tabbox.css` same day. | 2026-06-07 verified live on /tabbox.zul: all 6 accordion `.z-tab-button`s compute `display: none`; all 36 strip-mode buttons remain visible — no strip regression. |
| 2026-06-07 | cascader | Pre-selected path ("Japan / Kyoto", width 220px) renders with ellipsis at initial load, but the SAME selection made interactively shows full text. Root cause is a ZK initial-render display gap: `mold/cascader.js:17-18` emits BOTH `-label` and `-placeholder` divs with empty `style` when `placeholderVisible` is falsy, and `Cascader.java` never renders `placeholderVisible` (unlike `Searchbox.java:612`), so the flag is always undefined at redraw; `Cascader.ts bind_` only patches the empty-selection branch. The stale empty placeholder div stays visible; Marble's `flex: 1` on both divs lets it eat half the trigger width → label ellipsizes. Interactive `setSelectedUuids` writes inline `display:none` on the placeholder, which is why the symptom vanishes after one interaction. | Contract state-matrix row "has-selection" only checked c9/c10 (label color/font) and the mockup hard-coded `style="display:none"` on the placeholder — modeling the post-interaction inline style, not what ZK actually emits at initial render. No outcome row asserted "label shows full text with no interaction". Searchbox doesn't have the bug (its Java renders the flag), so the sibling never hinted at it. | skill + contract | Skill `components/cascader.md`: initial-render display-gap fact (mold/bind_ asymmetry; any theme granting the placeholder horizontal space squeezes a pre-selected label). Contract `doc/contracts/cascader.md`: new c27 (`.z-cascader:has(.z-cascader-label:not(:empty)) .z-cascader-placeholder` → `display: none`) + outcome M8 (pre-selected initial render without interaction: label scrollWidth ≤ clientWidth and placeholder occupies 0 width) — encoded failing-first. CSS `js/zkmax/inp/css/cascader.css` same day. | 2026-06-07 verified live on /cascader.zul fresh load: pre-selected trigger probe shows label+placeholder both emitted with EMPTY inline style (root cause confirmed in vivo); post-fix placeholder computes `display: none` / 0-width, label scrollWidth = clientWidth = 218 (full "Japan/Kyoto", no ellipsis). Interactive select→clear cycle re-tested: inline styles drive all transitions, settled states correct — c27 inert outside initial render. Sibling sweep: searchbox unaffected (`Searchbox.java:612` renders `placeholderVisible`); gap is cascader-specific. |
| 2026-06-08 | inputgroup | In `orient="vertical"`, the centre textbox rendered with no left border (only top/right/bottom). | The horizontal border-collapse rule `.z-inputgroup-text + .z-textbox { border-left: none }` was never scoped to `:not(.z-inputgroup-vertical)`, so it fired in vertical mode and stripped the wrong edge; MD3/Mira has no inputgroup analog and the contract had no vertical-border check. | skill + contract + impl | skill: inputgroup.md "Border-collapse is axis-aware"; contract: vertical-border row; CSS: scope horizontal rule + add vertical `border-top` collapse |
| 2026-06-08 | colorbox (menu-content mold) | ZK's `<menu content="#color=#RRGGBB">` auto-creates a colorbox **inside a Menu** (handled by `zkex.inp.ContentHandler` for `zul.menu.Menu`). It renders mirror classes `.z-menu-popup` / `.z-menu-paletteicon` / `.z-menu-pickericon` / `.z-menu-image.z-colorbox-color` — distinct from the standalone `.z-colorbox-*` classes. Marble styled only the `.z-colorbox-*` set, so the menu-mold popup shipped with NO chrome (no surface/border/shadow/padding), invisible palette/picker tab toggles, and an unstyled label swatch. The shared `.z-colorpicker`/`.z-colorpalette` rendered fine (no zclass prefix), masking how broken the frame was. | The harness only ever previewed standalone `<colorbox>`; the menu-content feature was never on a preview page, so the evaluator never saw `.z-menu-popup`. MD3/Mira has no "color menu" analog. Contract enumerated only `.z-colorbox-*` selectors. | skill + contract + impl | Skill `components/colorbox.md`: new "Menu-content mold" section (ContentHandler, `content="#color="` syntax, `.z-menu-*` mirror class map, `.z-menu-popup` ≠ `.z-menupopup`). Contract `doc/contracts/colorbox.md`: new menu-mold rows m1–m5 (popup chrome mirrors `.z-colorbox-popup`; tab icons mirror; swatch chip) encoded failing-first. CSS `js/zkex/inp/css/colorbox.css`: add `.z-menu-*` selectors to existing grouped popup/icon rules + `.z-menu-image.z-colorbox-color` swatch. Preview `pv/colorbox-content.zul`: "Colorbox in Menu" section now uses the real `<menu content="#color">` feature. |
| 2026-06-08 | drawer | (1) Mask: `mask="false"` drawer ("No Mask") still showed the dark backdrop scrim. (2) Close button: `closable="true"` drawer rendered the × completely unstyled (default-flow `<div>` after the header, no positioning/size/color). | (1) ZK ALWAYS renders the `.z-drawer-mask` element; `setMask(false)` only strips the `.z-drawer-mask-enabled` modifier (mold: `_mask ? $s('mask-enabled') : ''`). Marble's open-state scrim rule keyed on `.z-drawer-open .z-drawer-mask` (no modifier), so the scrim showed regardless of mask. (2) `drawer.css` had NO `.z-drawer-close` rule — the harness only previewed default (closable=false) drawers when first verified, so `.z-drawer-close` was never measured; MD3/MUI Drawer has no built-in close button so no reference forced it. | skill + contract + impl | Skill `components/drawer.md`: strengthen mask rule (visible scrim MUST gate on `.z-drawer-mask-enabled`, element is always present) + close-button anatomy (sibling of header inside `.z-drawer-real`, `<i class="z-icon-times">`, side-sheet → top-trailing corner per `md3-close-affordance-placement.md`). Contract `doc/contracts/drawer.md`: fix stale DOM selectors (`.z-drawer-body` does not exist), new c6 (open+mask-disabled scrim opacity 0), c7–c9 (close button positioned/sized/colored), outcome M1 (close glyph at top-trailing corner) — encoded failing-first. CSS `js/zkmax/wgt/css/drawer.css`: gate scrim on `.z-drawer-mask-enabled`; add `.z-drawer-close` icon-button (window-close pattern). Siblings: none. |
| 2026-06-08 | listbox | (1) Single-selection `checkmark="true"` renders icon `z-icon-radio` (a radio), but listbox.css styled only `z-icon-check`, so the single-selection control fell through to the generic flat mask glyph instead of a Material radio. (2) Header "select all" checkbox sat 4px right of the row checkboxes (broken vertical column line). | (1) Preview page only had a `multiple="true"` checkmark listbox; the single-selection case was never on a page, so the evaluator never saw `z-icon-radio`. MD3 has no listbox-checkmark analog. (2) The always-present `.z-listheader-sorticon` (margin-left:4px) precedes the checkable span in the header content wrapper, offsetting it; no contract row asserted header↔row checkbox X-alignment. | skill + contract + impl | Skill `components/data-components.md`: new "Checkmark column renders checkbox (multiple) OR radio (single)" section (icon-class fork + class map + header-sorticon alignment trap). Contract `doc/contracts/listbox.md`: rows cm1–cm5 (multi checkbox box; single radio circle; header↔row X-align) encoded failing-first. CSS `js/zul/sel/css/listbox.css`: add `z-icon-radio` Material radio rendering; zero sorticon margin on checkable header. Preview `listbox.zul`: add single-selection checkmark case. | |
| 2026-06-08 | tree | `checkmark="true"` was entirely unstyled: multi-select rendered `z-treerow-icon z-icon-check` as a generic flat mask glyph (no box/border), single-select would render `z-icon-radio` (also unstyled), and the two existing `.z-treerow.z-treerow-checkable` rules in tree.css were DEAD (the `z-treerow-checkable` class lives on the inner span, never on the TR — the compound selector matches nothing). | The only tree-checkmark preview was `multiple="true"`, and tree was verified before checkmark styling was considered; MD3/MUI TreeView has no checkbox-row analog so no reference forced it. Contract had no checkmark rows. The dead selector passed silently because it matched zero elements (no failure, no effect). | skill + contract + impl | Skill `components/data-components.md`: same new checkmark section documents the tree class map (`z-treerow-checkable/-checkbox/-radio`, icon `z-treerow-icon`, header `z-treecol-checkable/-checked`) + the dead-`.z-treerow.z-treerow-checkable` trap. Contract `doc/contracts/tree.md`: rows cm1–cm5 encoded failing-first. CSS `js/zul/sel/css/tree.css`: port listbox checkbox+radio rendering, fix dead selectors (span-scoped). Preview `tree.zul`: add single-selection checkmark case. | |
| 2026-06-08 | timepicker | Detached popup rendered at the viewport bottom-left instead of anchored under the trigger — `.z-timepicker-popup` never declared `position: absolute`, so ZK's inline `left`/`top` on the body-detached popup were inert under `position: static` (the box fell into normal body flow). | `reference/floating-popup-in-body.md` documented the body-detach + inline `left`/`top` but never stated the corollary that the popup CSS MUST supply `position: absolute`/`z-index`; no contract row asserted popup `position`, so the Evaluator had nothing to fail. The earlier lr-3 `width: max-content !important` masked it by capping width, hiding that the box was full-body `static`. | skill + contract | Skill: `reference/floating-popup-in-body.md` (new "Popup CSS must declare position:absolute" section). Contract: `doc/contracts/timepicker.md` (new c16 `position:absolute` + c17 `z-index`; c7/c8 retoken to `--zk-elevation-dropdown`/`--zk-shape-menu`; popup-default state-matrix). CSS: `zkmax/inp/css/timepicker.css` (popup rule aligned to combobox dropdown — position/z-index/margin/shape-menu/elevation-dropdown/border:none; removed `width:max-content !important` workaround). | Swept every `*-popup` base rule — timepicker was the only detached popup missing `position`; `.z-nav-popup` flagged as a maybe (out of scope, not user-reported). |
| 2026-06-08 | tree | Tree-with-checkmark header select-all box sat 4px right of the row checkboxes (centerX 61 vs 57), breaking the column's vertical line. The listbox fix (`sorticon { margin-left: 0 }`) had been ported to tree but did NOT work: `.z-treecol-content` is `display:flex; gap:4px`, so the always-present zero-width sorticon is still a flex item and the 4px flex GAP — not a margin — offsets the checkable span. | The prior listbox gap-fix encoded the trap as "sorticon margin-left:4px" and assumed it transferred; the skill note prescribed `margin-left:0` for both. tree's offset mechanism (flex gap, not margin) was never measured, and contract cm5 only asserted the header box SHAPE, not header↔row centerX alignment, so the evaluator could not catch the residual 4px. | skill + contract + impl | Skill `components/data-components.md`: header-misalignment trap now splits the two mechanisms (listbox = sorticon margin-left:4px → zero it; tree = `.z-treecol-content` flex gap:4px → `display:none` the sorticon flex item) with "measure, don't assume the fix transfers". Contract `doc/contracts/tree.md`: new cm6 asserting header.icon.centerX === row.icon.centerX. CSS `js/zul/sel/css/tree.css`: change `.z-treecol:has(.z-treecol-checkable) .z-treecol-sorticon` from `margin-left:0` to `display:none`. | listbox uses the inline margin mechanism (already correct); grid emits no checkable header. |
| 2026-06-08 | timepicker | Left top/bottom corner border read as "missing": `.z-timepicker-input` has an OPAQUE bg (`--zk-color-surface-container`, rgb 240/244/250) because Timepicker's constructor forces `setReadonly(true)`, so `.z-timepicker-readonly .z-timepicker-input` (c15) ALWAYS applies. The root has `border-radius:4px` but `overflow:visible`, and the input has square corners → on the left its opaque bg paints over the rounded corner (right side is fine — the button carries the matching `0 4px 4px 0` radius). | No contract row asserted that the root clips its children to the radius; MD3/Mira has no "always-readonly composite input" analog. The corner bleed only manifests because of the always-readonly-opaque-input ZK fact, which the skill noted (readonly "always present") but never connected to the clipping requirement. | skill + contract + impl | Skill `components/timepicker.md`: note the always-readonly opaque input requires `overflow:hidden` on the root to clip the rounded corners. Contract `doc/contracts/timepicker.md`: new c18 (`.z-timepicker` `overflow: hidden`) encoded failing-first. CSS `zkmax/inp/css/timepicker.css`: add `overflow:hidden` to root. Siblings unaffected — their inputs are transparent (not always-readonly). | Verified live on /timepicker.zul: `.z-timepicker` computes `overflow: hidden` (c18 pass), so the always-opaque readonly input is clipped to the 4px radius. Confirmed in vivo the input bg IS opaque `rgb(240,244,250)` = surface-container (root cause confirmed). Sibling sweep: combo-family inputs are transparent → bleed is timepicker-specific, not ported. |
| 2026-06-08 | timepicker, datebox, timebox, bandbox, spinner/doublespinner | Focus border grew the box by 2px (measured: timepicker 36→38px, datebox 40→42px). `box-sizing:border-box` + root `min-height` + focus `border-width:1px→2px` shrinks the content box 2px, but the input AND button children are pinned at `min-height` equal to the REST content box → they cannot shrink → they force the root to grow. The textbox fix (border 2px + `padding:…-1px`) only absorbs HORIZONTAL growth on a single `<input>`; composite inputs grow VERTICALLY from pinned child heights, so padding compensation does not transfer. | No contract/outcome row asserted "focus-within does not change the root bbox dimensions"; the evaluator measured focus border color/width but never compared rest-vs-focus geometry. MD3/Mira reference shows a 2px focus border but says nothing about composite layout stability, so the reference never forced the check. The bug was family-wide but only timepicker was user-reported. | skill (reference) + contract + impl | Skill `reference/focus-affordance-no-layout-shift.md` (NEW): composite outlined inputs MUST NOT grow on focus — render the ring as `box-shadow: inset 0 0 0 1px <color>` (keep border 1px), the MUI notched-outline trick; documents the single-input padding variant (textbox) as the other valid form. Contract `doc/contracts/timepicker.md`: retoken c5 (focus = 1px border-color primary + inset box-shadow ring, not border 2px) + new outcome M5 (focus-within root bbox height == rest height). CSS: `:focus-within` (and invalid+focus) → inset box-shadow ring in timepicker.css + datebox.css + timebox.css + bandbox.css + spinner.css. Combobox excluded (borders the input/button children, not the root; not reported). | Verified live (transition disabled to read focus target): rest-H == focus-H on all measured — timepicker 36→36 (was 36→38), datebox 40→40 (was 40→42), spinner 43→43; focus computes border-width 1px + `box-shadow: inset 0 0 0 1px rgb(55,111,208)` (= primary) + border-color primary. Combobox confirmed safe & unchanged (40→40, 160→160) — borderless root + children min-height==root. bandbox was already non-growing via an OUTSET ring + `transition:none`; standardized to the inset ring for family consistency + clip-safety. timebox/bandbox share datebox's exact edit (pattern proven on datebox). |
| 2026-06-08 | tree, tree-header | The "Tristate Selection (PARTIAL)" preview rendered no partial state at all: it used a plain `DefaultTreeModel`, but ZK gates the entire partial/indeterminate codepath on `(TristateModel<?>) _model` (`Tree.java:1733`), so `z-treerow-partial` / `z-icon-minus` were never emitted. The theme's `.z-treerow.z-treerow-partial` rule was a DEAD compound selector anyway (class is on the TR; ZK's real path scopes through the icon, and the indeterminate glyph is `z-icon-minus`). | No reference theme has a tree-tristate analog, so nothing forced a real check; ZK ships no concrete `TristateModel`, so the obvious `DefaultTreeModel` setup silently no-ops. The contract asserted only the checkbox/radio shapes, never the indeterminate box, and never that the demo model must implement `TristateModel`. | skill + contract + impl + preview | Skill `components/data-components.md`: new "Tristate / indeterminate (partial)" subsection (requires `TristateModel`; ZK ships none; `z-treerow-partial` on TR + icon→`z-icon-minus`; header keyed by icon class not `z-treecol-partial`; `setMultiple(true)` + render-partials gotchas; scope through icon, not the dead compound selector). Contract `doc/contracts/tree.md`: cm7 (row indeterminate minus box) + cm8 (header indeterminate). CSS `js/zul/sel/css/tree.css`: replace dead `.z-treerow.z-treerow-partial` rule with `z-icon-minus` MD3 indeterminate box (row + header). Preview: moved the case from `tree-header.zul` to `tree.zul` next to the other checkmark cases, backed by a real inline `MarbleTristateModel implements TristateModel`. | Header select-all also goes indeterminate (`z-icon-minus`) — covered by the same rule. listbox has no tristate (no parent/child hierarchy). |
| 2026-06-09 | tree | Tristate demo was non-interactive: deselecting Child A-1 left Parent A still indeterminate (should go unchecked), and deselecting Child B-1 left Parent B fully checked (should go indeterminate). The prior fix wired a STATIC `partials` set with no listeners — `getPartials()` returned a frozen `{parentA}` regardless of user clicks. | ZK's `TristateModel` is a PASSIVE data holder: `Tree.java` only READS `getPartials()` to render and re-reads it on `SELECTION_CHANGED`/`TRISTATE_CHANGED` — it NEVER computes parent state from children. ZK ships zero concrete impls and zero cascade logic; the child→parent (and parent→child) cascade is by design 100% the application's job. No theme reference exercises interactive tree selection, so the harness only ever verified static render. | skill + preview | Skill `components/data-components.md`: extend the tristate subsection — ZK does NOT cascade; to make tristate interactive the app must listen for selection changes, recompute each parent (all children selected→SELECTED, some/any-partial→PARTIAL, none→UNSELECTED), update the model's selection + partials sets, and call the public `AbstractTreeModel.fireEvent(TreeDataEvent.TRISTATE_CHANGED,…)` to force a re-render; partial parents MUST be removed from selection (else they render checked, overriding the minus). Preview `tree.zul`: replace the static partials with an `onSelect` cascade (down-propagate on parent click, bottom-up recompute, fire TRISTATE_CHANGED); derive initial parent states via `recompute()` instead of hand-setting partials. | Not a ZK bug and not a CSS gap — CSS already renders all three states; the gap was unimplemented app-level cascade. listbox/grid have no parent/child hierarchy so no cascade applies. |
| 2026-06-09 | coachmark | On page load the pointer triangle did not point at the target and the close (×) rendered as a full-width row at the card BOTTOM. Both `.z-coachmark-pointer` and `.z-coachmark-close` computed `position:static`, so the mold JS's inline `top`/`left` on the pointer (`_fixarrow`, measured `left:77px`) were ignored (triangle collapsed to the card's left edge x:0) and the close fell into static flow below the content (bbox x:0 w:291 y:328). | The default theme pins both with `position:absolute` (pointer also `z-index:100`; close `top:8px right:4px` + `~ .z-coachmark-up`/`-right` bumps); Marble's `coachmark.css` kept only color/border rules and dropped all positioning. The contract never asserted pointer/close `position`, and outcome M3 was too weak — a full-width close has `bbox.right === content.right` by accident, so it passed while the × sat at the bottom. | contract + impl (+ skill clarification) | Contract `doc/contracts/coachmark.md`: new c15 (`.z-coachmark-pointer` position:absolute) + c16 (`.z-coachmark-close` position:absolute), strengthened M3 (close must be top-right: near content.top AND not full-width) + new M7 (pointer position:absolute so JS coords apply), c5 retoken to asymmetric right padding reserving close space. CSS `zkmax/nav/css/coachmark.css`: add `position:absolute;z-index:100` to pointer, `position:absolute;top/right` pin + `~` direction bumps to close, `position:relative` + asymmetric right padding to content, `top/left:0` to root. Skill `components/coachmark.md`: fix the lines-56-58 contradiction (close is positioned relative to the `.z-coachmark` ROOT, not the content box). | pending re-verify |
| 2026-06-09 | timepicker | On focus the ring looked thicker around `.z-timepicker-button` than around `.z-timepicker-input`. The 2026-06-08 fix rendered the focus ring as `box-shadow: inset 0 0 0 1px primary` on the ROOT. An inset box-shadow paints BELOW the element's children, so the input — which carries an opaque (always-readonly) `surface-container` background — painted OVER the ring on the input side, while the transparent button let it show through → asymmetric ring. (The 1px border itself is symmetric; only the inset 2nd pixel was covered.) | The inset-ring reference never noted that an OPAQUE child background occludes a root inset shadow on that child's side; the asymmetry is a paint-order interaction, not measurable by the existing per-selector computed-style checks (the box-shadow value WAS correct — it was just visually covered). Timepicker is the only always-readonly composite (opaque input by default), so the 2026-06-08 family sweep didn't surface it. | skill (reference) + contract + impl | Skill `reference/focus-affordance-no-layout-shift.md`: new "Opaque child backgrounds occlude an inset ring" caveat — when any child has an opaque bg, draw the ring on an always-present `::after` overlay (paints ABOVE children, `pointer-events:none`, clipped to the rounded shape by `overflow:hidden`), NOT as a root inset shadow; transparent-input siblings are unaffected. Contract `doc/contracts/timepicker.md`: c5 re-pointed to `.z-timepicker:focus-within::after` box-shadow + new c5d (`.z-timepicker:focus-within` box-shadow == `none` — the ring must NOT live on the root where the opaque input covers it; failing-first). CSS `zkmax/inp/css/timepicker.css`: focus ring moved to an always-present `::after` overlay (transparent at rest, primary/error on focus). | Verified live (pseudo-element transition disabled to read target): root focus box-shadow = `none`, `::after` focus box-shadow = `inset 0 0 0 1px rgb(55,111,208)`=primary (invalid → `rgb(211,47,47)`=error); border stays 1px primary; focus-H == rest-H (overlay is `position:absolute`, no layout shift). Single overlay above both children ⇒ ring uniform by construction. Sibling sweep: datebox/timebox/bandbox/spinner have TRANSPARENT inputs, so their root inset ring shows symmetrically — not affected; left on the inset-ring mechanism. |
| 2026-06-09 | grid, listbox, tree | `sclass="z-sticky-header"` did nothing — the column header did not pin to the top of the scroll container on scroll. Marble had ZERO `.z-sticky-header` CSS; all three preview pages (`grid-header.zul`, `listbox-header.zul`, `tree-header.zul`) carried a sticky demo wrapped in a `height:200px; overflow-y:auto` scroller, but the header scrolled away with the body. | `z-sticky-header` (ZK-4795, since 9.6.0; Grid/Listbox/Tree) is a **bare opt-in class** — ZK's own `zul/less` ships NO rule for it; the implementation lives in the `zkmax` addon LESS (`zkmax/grid/less/grid.less`, `zkmax/sel/less/{listbox,tree}.less`), which Marble does not inherit. So the theme must supply it. MD3/MUI/Mira has no "sticky table header opt-in class" analog, so no reference forced it, and no contract row asserted header `position` under `.z-sticky-header`. ZK's own LESS also omits an opaque header bg, which would let rows bleed THROUGH the pinned header. | skill + contract + impl | Skill `components/data-components.md`: new "Sticky header (`z-sticky-header`) is a bare opt-in class" section — ZK ships no default CSS; theme must set `overflow:visible` on the `.z-{grid,listbox,tree}` root (un-clip the header) + `position:sticky;top:0;z-index` on `.z-{comp}-header`, AND give the header an OPAQUE bg (ZK omits this → bleed-through); the scroll ancestor (page or an `overflow:auto` container) drives where it pins. Contracts `doc/contracts/{tree,grid,listbox}.md`: new `sh1`/`sh2` rows (header `position:sticky` + opaque bg under `.z-sticky-header`) encoded failing-first. CSS `js/zul/grid/css/grid.css` + `js/zul/sel/css/{listbox,tree}.css`: mirror the zkmax mechanism + opaque surface header bg. | Verified live 2026-06-09: scrolled each page's 200px sticky-demo container 150px, header stays pinned at the scroller top — tree (afterTop 3072 ≈ scrollerTop 3071), grid (3400 ≈ 3399), listbox (5233 ≈ 5232); all compute `position:sticky; top:0px; z-index:2; background:rgb(255,255,255)`(=surface) and root `overflow:visible`. Preview cases already present (no ZUL change needed). |
| 2026-06-10 | coachmark | A `<button>` placed inside the coachmark card was invisible: the default `.z-button` is `background:--zk-color-primary; color:--zk-color-on-primary; border:none`, and the coachmark card (`.z-coachmark-content`) bg is ALSO `--zk-color-primary` → blue button on blue card, no border, so only the white label showed (the button lost its shape/affordance entirely). All three demo buttons (`Got it!`/`Next`/`Finish`) were affected. | The contract treated coachmark children as "arbitrary widgets the theme does not control" and never asserted that an action control on the primary-filled surface must contrast. No reference (MD3/MUI) ships a brand-filled tour card hosting a filled button, so nothing forced the same-fill-on-same-fill collision check; the evaluator only measured the card/pointer/close, never a nested button vs. the card bg. | contract + impl (+ skill clarification) | Contract `doc/contracts/coachmark.md`: new M8 (a `.z-button` inside `.z-coachmark-content` must have `background-color` distinct from the card AND WCAG ≥ 3:1 against it) + c17 (`.z-coachmark-content .z-button` background == `--zk-color-on-primary`/white, color == `--zk-color-primary`), encoded failing-first. CSS `zkmax/nav/css/coachmark.css`: inverse button on the colored surface — white bg + primary text + state-layer `::before` flipped to primary + focus-visible outline flipped to on-primary (visible ring on the blue card). Skill `components/coachmark.md`: composition invariant — because the surface is brand-filled, child filled controls inherit the global primary fill and vanish; the theme MUST invert them via `.z-coachmark-content .z-button` (bg↔text swap + state-layer + focus-ring flip). | Any future colored coachmark surface needs the same inverse treatment (recorded as the general "consistency-by-role" rule in doc/spec/DESIGN.md §3). notification/toast host close icons (already inverted per type) not free-form filled buttons; popup/bandpopup are neutral surfaces so child buttons already contrast — coachmark is the only brand-filled popup hosting arbitrary action buttons. |
| 2026-06-10 | coachmark (combobutton) | Sibling sweep of the 2026-06-10 nested-button gap: a `<combobutton>` placed inside the coachmark card was ALSO invisible — both halves (`.z-combobutton-content` + `.z-combobutton-button`) are `background:--zk-color-primary; color:on-primary; border:none`, identical to the card bg (measured contrast 1.00 on both), so the whole split button melted into the blue surface; the label↔arrow divider (`rgba(255,255,255,.3)`) would also be invisible on the fix's white fill. The prior fix only covered `.z-coachmark-content .z-button` (different class), so combobutton was left unprotected. | Same root cause as the button gap (colored surface + global filled control). The contract's M8 named only `.z-button`; the evaluator never measured a nested combobutton, and no preview case exercised a combobutton inside a coachmark. | contract + impl + preview (+ skill generalization) | Preview `src/test/resources/web/coachmark.zul`: new "Coachmark with Combobutton" case (trigger button opens a coachmark containing a label + combobutton). Contract `doc/contracts/coachmark.md`: M8 generalized to "every filled action control (`.z-button` AND both `.z-combobutton` halves)", new c19/c20 (combobutton halves → white fill + primary label), new `nested-combobutton` state-matrix row — encoded failing-first (measured 1.00 before CSS). CSS `zkmax/nav/css/coachmark.css`: inverse combobutton — both halves white/primary, divider retinted to `--zk-color-outline`, state-layer `::before` flipped to primary; extra `.z-combobutton` in selector raises specificity above the toolbar-mold rules. Skill `components/coachmark.md`: generalized the invariant from `.z-button` to any filled child control + added an audit-and-extend note. | Verified live on /coachmark.zul: content + arrow both rgb(255,255,255), label rgb(55,111,208), both-vs-card contrast 4.83 (was 1.00), divider rgba(0,0,0,0.23)=outline. Remaining lower-risk candidates documented but NOT fixed (no preview case yet): checked checkbox/radio box fill (primary, small sub-element — "don't show again" pattern) and selected calendar day; structural/selection primary fills (slider, progressmeter, tabbox indicator, listbox/tree selected rows, layout chrome) are not coachmark content. |
| 2026-06-10 | tree | `mold="paging"` tree had NO divider between the paging bar and the rows (in any position). Tree wraps the bar in `.z-tree-paging-top` / `.z-tree-paging-bottom` (mold `$s('paging-top/bottom')`, same as grid's `.z-grid-paging-*`), but `tree.css` shipped zero rules for those wrappers — grid had them (`grid.css:469-476`), tree was simply never given the equivalent. Measured live: `.z-tree-paging-bottom` borderTop = `none`, and the last `.z-treerow` borderBottom = `none` (the `:last-child` rule strips it) → the paging bar floated against the rows with no separation; `-top` had the mirror problem (no borderBottom against the header below). | MD3/MUI has no ZK-specific "tree paging wrapper" analog, and no contract row asserted the tree paging divider, so the evaluator never measured it. The omission was invisible because the grid sibling looked correct — the gap was tree-only (a missed port of the grid rule). | contract + impl (+ skill note) | Contract `doc/contracts/tree.md`: new pg1 (`.z-tree-paging-top .z-paging` border-bottom 1px outline-variant) + pg2 (`.z-tree-paging-bottom .z-paging` border-top 1px outline-variant), encoded failing-first. CSS `js/zul/sel/css/tree.css`: add the two paging-wrapper rules mirroring grid. Skill `components/paging.md`: note that the host data-component (grid/listbox/tree) owns the divider between the bar and the rows via its `.z-<comp>-paging-top/bottom` wrapper (top → border-bottom, bottom → border-top). | Sibling sweep: grid already correct; listbox defines `.z-listbox-paging-bottom` border-top only (no `-top` rule) — flagged, lower priority, not user-reported. |
| 2026-06-10 | tree, listbox (shared mesh/css/auxhead.css) | Two adjacent `<auxhead>` rows merged into one indistinct tonal band — no visible divider between them (tree-header 2nd/3rd trees). `auxhead.css` put the row divider on `.z-auxhead` (the **TR**) as `border-bottom: 1px`, but tree & listbox header tables use `border-collapse: separate` (required so frozen-col TH `box-shadow`/`border-inline-end` paint) — and **browsers do NOT paint borders on TR/TBODY in `separate` mode**, only on table/TH/TD. So the auxhead divider silently never rendered; stacked aux rows (same `surface-container-low` fill, no painted line between) read as a single block. (Grid's auxhead `*-headtbl` table is ALSO `separate` — verified live — so grid's TR border never painted either; grid merely *looked* right because its preview shows a single `<auxhead>` reading against the column-header row, not two stacked aux rows needing a line between them. Hence the report "reference grid's design".) | The skill documented "header-row dividers go on the TR, not the cell" (data-components.md §"borders on the ROW") — but that rule is only valid for the **body** table (collapse mode); it was wrongly assumed to transfer to the **header** table, which is `separate`. No contract row asserted the auxhead bottom divider paints, and no MD3/Mira reference exercises two stacked aux rows, so the evaluator never caught the non-painting TR border. | skill + contract + impl | Skill `components/data-components.md`: new rule — mesh **header** tables are `border-collapse: separate` (frozen-col box-shadow needs it), so TR borders do NOT paint there; header-row dividers (auxhead, multi-level headers) MUST go on the `.z-auxheader`/`.z-*col` **TH cell** `border-bottom`. Explicitly contrast with the body-table collapse rule (TR border is correct there). Contract `doc/contracts/tree.md` + `doc/contracts/listbox.md`: new ax1 (`.z-auxheader` border-bottom 1px outline-variant, paints under separate border-collapse), encoded failing-first. CSS `js/zul/mesh/css/auxhead.css`: move `border-bottom` from `.z-auxhead` (TR) to `.z-auxheader` (TH) — paints in both collapse & separate, uniform across grid/listbox/tree. | Sibling sweep: shared CSS fixes listbox-header at the same time; grid auxhead now paints a proper TH divider too (verified live: `.z-auxheader` border-bottom 1px outline-variant; grid `*-headtbl` is `separate` so its single auxhead previously relied on the adjacent column-header weight — strictly an improvement, no regression). |
| 2026-06-10 | grid | A 2px white line showed between auxhead cells/rows in grid's header ("Auxhead + Column Combinations") but not in tree/listbox. Grid's header table is emitted UNCLASSED as `<table id="…-headtbl">` (grid.js:35) — there is NO `.z-grid-header-inner` class in ZK 10. Marble's only header-table rule was keyed on `.z-grid-header-inner` (`border-collapse:collapse; table-layout:fixed`), a **dead selector** (matched nothing, verified `hasHeaderInner:false` live), so the real `-headtbl` fell back to the UA default `border-collapse:separate; border-spacing:2px`. In separate mode that 2px spacing exposes the table background between every cell/row → the white gaps. tree/listbox were fine because their resets key on `.z-tree-header table` / `.z-listbox-header table` (descendant `table`), which DO match the unclassed `-headtbl` and set `border-spacing:0`. | The grid.css DOM comment claimed `table.z-grid-header-inner`, so the spacing reset was written against a class ZK 10 no longer emits; the dead selector no-ops silently. No contract row asserted header-table `border-spacing`, and MD3/Mira has no table-spacing analog, so the evaluator never measured it. The collapsed body table hid the issue (collapse ignores border-spacing) — only the separate header table leaked it. ZK's OWN default theme avoids the trap by resetting `table { border-spacing:0 }` (grid.less/tree.less/listbox.less ~line 6), never keying on a class. | skill + contract + impl | Skill `components/data-components.md`: new "The header table is emitted UNCLASSED — reset border-spacing on the `table`, not a class" subsection (header table = `…-headtbl`, no `.z-grid-header-inner`; UA default 2px leaks in separate mode; reset via `.z-<comp>-header table { border-spacing:0 }` mirroring ZK default less). Contract `doc/contracts/grid.md`: new hs1 (`.z-grid-header table` border-spacing `0px`) encoded failing-first. CSS `js/zul/grid/css/grid.css`: replace dead `.z-grid-header-inner` rule with `.z-grid-header table { border-collapse:separate; border-spacing:0; table-layout:fixed }`; fix the stale DOM comment. | Verified live: grid header table border-spacing 0px; horizontal gap between aux cells 0 (was 2), vertical gap aux→columns 0 (was 2); white line gone. tree/listbox already correct (their `.z-<comp>-header table` resets match the unclassed table). ZK default theme confirmed to set border-spacing:0 (grid.less:6) — Marble was the only one that lost it, via the dead class selector. |
| 2026-06-11 | listbox, grid, tree | `<listhead sizable="true">` columns are resizable but Marble gave zero affordance — no resize cursor, no edge cue. ZK detects the drag in the rightmost 8px of each header cell (`HeaderWidget._insizer`) and toggles `.z-{c}-sizing` on the **TH** during mouse-move (removed on mouse-out); it emits NO persistent "sizable" class, so a resting per-column divider can't be scoped. Marble had no `.z-listheader-sizing`/`.z-treecol-sizing` rule at all, and grid's `.z-column-sizing` rule was actively WRONG (`position:absolute; width:4px` on the TH would collapse the column on hover) plus a dead `.z-column-sizer` selector (ZK renders no sizer element). User: 「resizable 時可以拖 header 邊界，但預設沒有 border，怎麼處理？」 | MD3/Mira has no "resizable column" analog, so no contract asserted the resize cursor; the evaluator never added the JS-only `.z-{c}-sizing` class to measure it. The skill never recorded that the sizing class lands on the TH (not a handle child) and that no persistent sizable class exists — so grid's CSS was authored against an imagined 4px handle. | skill + contract + impl | Skill `components/data-components.md`: new "Sizable header → resize affordance" section (8px zone, `.z-{c}-sizing` on the TH, no persistent class, trap: don't position/size it, `.z-{c}-sizer` is dead). Contract `doc/contracts/listbox.md`: sz1 (`.z-listheader-sizing` cursor==col-resize, add class manually) + sz2 (NOT position:absolute), failing-first. CSS: `listbox.css` + `tree.css` add `.z-{c}-sizing { cursor:col-resize; box-shadow: inset -2px 0 0 primary }`; `grid.css` replaces the broken positioned rule with the same and deletes dead `.z-column-sizer`. | Sibling sweep folded grid + tree in at once; grid's latent collapse-on-hover bug fixed in passing. Affordance is hover-time (matches MUI DataGrid) since ZK gives no resting hook. |
| 2026-06-11 | listbox, grid, tree | A header `image="…16x16.png"` in a narrow `width="40px"` column made the header ROW taller than default (measured 53px → 73px). Root cause: 40px − 16px×2 default padding = 8px content width; the bare `<img>` (ZK emits no class) hits the global `img{max-width:100%}` and shrinks to 8px, AND the squeezed content wraps to a 2nd line → row inflates. Not the image's height — the narrow column forcing a wrap. Same family as the existing "narrow checkable column" squeeze. User: 「16x16 的小圖為何讓 header 變高？合理嗎？」(answer: not reasonable — it's the padding squeeze; collapse it). | The existing skill rule covered only the *checkable* narrow column (icon clipping), not image/icon columns or the row-height inflation symptom. No contract asserted header-image intrinsic size or that an image must not inflate the header row. | skill + contract + impl | Skill `components/data-components.md`: generalized "Narrow checkable column" → "Narrow icon / checkbox / image column" with all three symptoms (clip / squeeze+inflate / stray ellipsis) and the `:has(img)` padding fix. Contract `doc/contracts/listbox.md`: ni1 (`.z-listheader:has(img)` padding 4px) + ni2 (image renders 16×16) + ni3 (image header row height == no-image row), failing-first. CSS: `.z-listheader:has(img)` / `.z-column:has(img)` / `.z-treecol:has(img)` collapse side padding to spacing-1. | Validated live: with padding 4px the image renders 16×16 and the row returns to 53px. Swept grid + tree image columns at the same time. |
| 2026-06-11 | listbox (grid/tree swept) | Follow-up to the image-inflation gap: the **select-all checkbox** header (`multiple checkmark`, `width="40px"` column) ALSO inflated the header row to 74px (vs 53px default). The first sweep only measured the IMAGE listboxes, not the checkmark listbox, so it missed that the padding-collapse fix is INSUFFICIENT here: `.z-listheader` is `white-space: normal` (for text-label wrapping), and the `.z-listheader-checkable` span is ~40px wide inside the ~32px content box, so it wraps below the zero-width sorticon → 2-line content → 74px row. User: 「第 3 個 listbox 的 listheader 比預設高，合理嗎？」(no — same wrap inflation). | The narrow-column fix addressed padding + image squeeze but not the wrap mechanism for an over-wide checkable span; no contract asserted the select-all header row height; the sibling sweep checked image columns only (the checkmark listbox was a different preview block). | skill + contract + impl | Skill `components/data-components.md`: symptom 2 rewritten as "header row inflated by a wrapping icon column" (two triggers: image + select-all checkbox), fix block now mandates `white-space: nowrap` on `.z-{c}:has(.z-{c}-checkable)`/`:has(img)` (padding-collapse alone is insufficient for the checkbox header). Contract `doc/contracts/listbox.md`: ni4 (icon header white-space==nowrap) + ni5 (select-all header row ≈53px not ~74px), failing-first. CSS: add `white-space: nowrap` to the `:has(.z-listheader-checkable)` + `:has(img)` rules (listbox), `:has(img)` (grid/tree). | Validated live: select-all header row 74→54px, single line. Lesson: when sweeping a "narrow column" family, measure EVERY preview block that has such a column (image AND checkbox), not just the first one found. |
| 2026-06-11 | listbox (tree differs) | "Two small dots" appeared to the right of the checkbox in a `multiple checkmark` listbox (narrow 40px column, empty `<listcell/>`), absent from listbox.zul's Checkmarks. Root cause: the checkbox cell `.z-listcell-content` is `white-space:nowrap; overflow:hidden; text-overflow:ellipsis`; the checkable span (16px icon + `padding:0 12px`) is ~8px wider than the 32px content box (scrollWidth 48 vs clientWidth 40), so the browser paints a leftover **ellipsis "…"** to the right of the box. Header has none (`.z-listheader` is `white-space:normal`); listbox.zul Checkmarks has none (labelled cell + auto width → no overflow). | The ellipsis-on-cell-content is a theme choice (no MD3/Mira analog forces it), and no contract asserted that a control-only cell must not render an ellipsis. The evaluator only checked the checkbox `::before` box, never the trailing ellipsis. Diagnosis needed live DOM (scrollWidth/clientWidth) — the DOM has no element for the dots, they're the `text-overflow` glyph. | contract + impl (+ skill note) | Skill `components/data-components.md`: added the stray-ellipsis symptom + `text-overflow: clip` to the narrow-column section (with note that tree's flex `.z-treecell-content` makes this listbox-specific). Contract `doc/contracts/listbox.md`: cm6 (`.z-listcell:has(.z-listitem-checkable) > .z-listcell-content` text-overflow == clip), failing-first. CSS `listbox.css`: add `text-overflow: clip` to the checkable cell-content rule. | Validated live: `text-overflow:clip` removes the dots, checkbox intact. Tree uses flex cell content (ellipsis on `.z-treecell-text`) so symptom is listbox-only; grid has no checkmark column. |
| 2026-06-11 | biglistbox | First data row looked taller than the rest: an 8px gap sat between the header's bottom border and row 1 (and 2px between every row). Cause — biglistbox's head & body `<table>`s are emitted unclassed and Marble never reset their `border-spacing`, so the UA default `border-spacing:2px` (separate mode) leaked between cells/rows. This is the **exact same class of bug as the grid header-gap (2026-06-10)** — the border-spacing reset was documented for grid/listbox/tree but **never swept to biglistbox** (biglistbox wasn't in the skill at all). | The grid fix lived in `data-components.md` scoped to `.z-<comp>-header table` for the row-trio; biglistbox is a separate zkmax virtual-mesh component with no skill entry and no contract row asserting table `border-spacing`, and MD3/Mira has no table-spacing analog, so the evaluator never measured it. | skill + contract + impl | Skill (new) `components/biglistbox.md`: "reset border-spacing:0 on the head & body tables" rule + cross-ref to the data-trio rule. Contract `doc/contracts/biglistbox.md`: new bs1 (`.z-biglistbox table` border-spacing `0px`) encoded failing-first. CSS `js/zkmax/big/css/biglistbox.css`: `.z-biglistbox table { border-collapse:separate; border-spacing:0 }` (mirrors ZK default `.resetTable()`). | Fixed: header→row1 gap 8px→2px, inter-row gap 2px→0, rows now uniform. Confirms the "sweep siblings" step was skipped on the original grid gap — biglistbox is a 4th data table that needed the same reset. |
| 2026-06-11 | biglistbox | Clicking a sortable header made the header **~2× tall**: the `.z-biglistbox-sorticon` is a full-width `display:block` div sitting *before* the label inside `.z-biglistbox-header-content`, so when the caret glyph appeared it stacked the label onto a 2nd line. Marble styled only `.z-biglistbox-header`, leaving `-header-content`/`-sorticon` at their `display:block` defaults. | MD3/Mira has no sort-caret-in-header analog, and no contract row asserted single-line header height; the sorticon only renders a glyph *when sorted*, so a static (unsorted) screenshot never exposed it. ZK's default theme hides the problem with `position:absolute` on the sorticon — Marble didn't carry that over. | skill + contract + impl | Skill `components/biglistbox.md`: header DOM (`th > .z-biglistbox-header-content > .z-biglistbox-sorticon > i` + label) and "lay the caret inline, never let it add a row". Contract: new h1 (header height unchanged after sort). CSS: `.z-biglistbox-header-content{display:flex;align-items:center}` + `.z-biglistbox-sorticon{order:1;flex:0 0 auto}` (caret trails, MUI DataGrid style). | Fixed: header stays 37px after sorting; caret renders inline at 18×19px. |
| 2026-06-11 | biglistbox | No scrollbar appeared when content overflowed (MultipleRow=100 rows, MultipleColumn=100 cols): biglistbox draws its **own** scrollbar (`zul.WScroll` → `.z-biglistbox-wscroll-*`), not a native one, and Marble had **zero CSS** for the entire wscroll widget — so it collapsed to 0px and overflowed rows/cols were unreachable. | The scrollbar is a ZK-only custom widget (no native overflow element to measure); MD3/Mira has no analog; the evaluator only checks computed styles on documented selectors and the wscroll family was never in the contract or skill. ZK's default theme sizes it via `@biglistboxSize` and paints `ZK85Icons` glyph buttons Marble doesn't bundle. | skill + contract + impl | Skill `components/biglistbox.md`: full WScroll mechanics — element roles (node/edrag/edragBody/epos/eend), the `_gap = edrag.offsetHeight − epos.offsetHeight` construction-time coupling, scale-cap behavior, "epos must keep a box (no display:none)". Contract: new sc1 (`.z-biglistbox-wscroll-vertical` width>0 when overflowing). CSS: thin MD3 overlay — 12px transparent track, 6×40px rounded translucent `-drag` thumb, arrow buttons `display:none`, `-pos` kept boxed-but-invisible for the gap math. | Fixed: vertical+horizontal bars now visible & draggable; wheel works; thumb traverses the full track proportionally for large data (HugeRow scale 0.04, travel 39→460). For modest datasets ZK caps scale=1 so the thumb sits high — inherent WScroll behavior, identical in every ZK theme, not CSS-tunable. |
| 2026-06-11 | listbox (select mold) | `<listbox mold="select" rows="4">` rendered its options in **2 side-by-side columns** instead of one vertical list. `rows="4"` → native `<select size="4">`, an **in-page list box** (not a dropdown), but the `@supports (appearance: base-select)` block in `listbox.css` forced `appearance: base-select; display: inline-flex` on **all** `.z-select`, so the sized list's two `<optgroup>`s (from `<listgroup>`) flowed horizontally as flex-row children (measured: Frontend group x=45, Backend group x=192). The `rows="1"` dropdown was fine (it IS a popup). | `base-select` is a **popup-only** feature — the spec only restyles the dropdown trigger + `::picker`. A `<select size>1>` shows its options inline and base-select's flex trigger layout was never meant for it; nothing scoped the block to single-row selects. MD3/MUI has no "sized native list box" analog, and no contract row asserted the select-mold list lays out vertically, so the evaluator never measured a multi-row select. | skill + contract + impl | Skill `components/selectbox.md`: new "Sized / multiple `<select>` is an in-page list box — exclude it from base-select" section (rows>1 → size>1 → vertical native list; base-select inline-flex flows optgroups into columns; opt out via `[multiple]`/`[size]:not([size="1"])`). Contract `doc/contracts/listbox.md`: new "Select mold" section sm1 (`.z-select[size]:not([size="1"])` display == `block`, NOT flex) + sm2 (single-row `.z-select[size="1"]` keeps base-select inline-flex) — encoded failing-first. CSS `js/zul/sel/css/listbox.css`: inside the `@supports (appearance: base-select)` block, reset `.z-select[multiple], .z-select[size]:not([size="1"])` to `appearance:none; display:block`. | Verified live: the `rows="4"` select now computes `display:block; appearance:none`; all 5 options + both optgroups stack at x=45 (single column); no chevron bg. The `rows="1"` dropdowns keep `display:inline-flex; appearance:base-select`. Sibling: `zul.wgt.Selectbox` has no `rows`/`multiple` API (always `size="1"`), so the trap cannot occur there — skill note covers the hypothetical, no speculative CSS. |
| 2026-06-11 | listbox, tree (shared mesh/css/auxhead.css) | The **right** edge of the last visible `<auxheader>` showed a **double border** (two parallel 1px lines), while the left edge was a clean single line; grid did not visibly show it. ZK appends a zero-width `.z-auxhead-bar` filler TH to every auxhead row, so the visible-last auxheader is `:nth-last-child(2)` / `:has(+ .z-auxhead-bar)` — **never `:last-child`**. The existing `.z-auxheader:last-child { border-inline-end: none }` rule therefore never fired on it, leaving its 1px `border-inline-end` (measured x=431) sitting 1px inside the listbox's own frame border (x=432) → two lines. Left edge is clean because auxheaders carry no border-left. Grid's narrower auto-width happened to land its last-auxheader border (x=333) coincident with the grid frame (x=332), masking the same latent trap. | The `:last-child` edge-stripping rule silently mismatches because of the trailing `.z-auxhead-bar` filler — a ZK DOM fact the skill noted as a structural element but never tied to the "strip the edge border" pattern. No contract row asserted the visible-last auxheader has no inline-end border, and MD3/Mira has no multi-level-header analog, so the evaluator never measured the right-edge doubling. | skill + contract + impl | Skill `components/data-components.md` (Auxhead section): note that the auxhead row's `:last-child` is the `.z-auxhead-bar` filler, so an edge-stripping rule MUST target `.z-auxheader:has(+ .z-auxhead-bar)` (not `:last-child`); a right border left on it doubles with the container frame in `separate` header tables. Contract `doc/contracts/listbox.md` (Auxhead section): new ax2 (`.z-auxheader:has(+ .z-auxhead-bar)` border-inline-end == `0px`/none — must not double the frame) encoded failing-first. CSS `js/zul/mesh/css/auxhead.css`: add `.z-auxheader:has(+ .z-auxhead-bar)` to the border-inline-end:none rule alongside `:last-child`. | Verified live (shared CSS → swept all 3): listbox + tree visible-last auxheader border-inline-end now `0px` (single frame line at x=432; aux right x=431), while the inner group divider (A+B→C) stays `1px`. Grid unaffected (already coincident). |
| 2026-06-11 | listbox (select mold) | The selected option in a `mold="select"` listbox showed a **green** highlight (`--zk-color-secondary-container`), while the identical native-select control as **Selectbox** (and a sized `<select>` rendered natively) showed **blue** (`--zk-color-primary-container`). `listbox.css` `.z-select option:checked` was set to `secondary-container`/`on-secondary-container` — the **chip** family — but a `<select>` popup/list option is the **list-row** family (`primary-container`), per `reference/selected-state-families.md` (row "Selectbox option (native `<select>` popup)"). `selectbox.css` had it right (primary-container, with an explicit "NOT secondary-container" comment); `listbox.css` diverged. | The two bundles (`selectbox.css.dsp` / `listbox.css.dsp`) duplicate the shared `<select>` styling (each loads only when its widget is on the page), and the duplication drifted: the list-row→primary-container rule was applied to `.z-selectbox` but the `.z-select` copy kept the old secondary-container. No contract row asserted `.z-select option:checked`'s family, and `getComputedStyle` on a native `<option>` returns transparent (the OS/engine paints the highlight), so the evaluator never measured it. | skill + contract + impl | Skill `components/selectbox.md`: the "same native control" section now states both `.z-selectbox` AND `.z-select` selected options MUST use the SAME list-row token (`primary-container`), and points at `selected-state-families.md`; flagged the duplication-drift risk. Contract `doc/contracts/listbox.md` (Select mold): new sm3 (`.z-select option:checked` background == `primary-container`, NOT secondary-container — read the CSS rule, not the live option, since computed-style on `<option>` is transparent). CSS `js/zul/sel/css/listbox.css`: `.z-select option:checked` + `:hover` retoned to primary-container / on-primary-container, matching `.z-selectbox`. | Verified live: both the sized-list and the base-select dropdown checked option now compute `rgb(214,228,255)` (=primary-container `#d6e4ff`) bg + `rgb(0,28,61)` (=on-primary-container) text; screenshot shows the sized list's selected "React" in light blue (was teal/green `#b2dfdb`). Tokens: primary-container `#d6e4ff` (blue) vs secondary-container `#b2dfdb` (green) — confirms the old chip-family mistake. |
| 2026-06-11 | listbox (select mold), selectbox | Focusing a `mold="select"` listbox (and a Selectbox) **grew the box ~2px** — the focus rule switched `border-width: 1px → 2px`. Even with `box-sizing: border-box`, a native `<select>` is **auto-sized** (width/height content-driven: a sized `rows>1` list sizes to its rows, a `rows=1` trigger to its min-width), so there is no fixed outer dimension to hold the 2nd border pixel inward → the box grows outward. Same class as the textbox/datebox focus-shift bugs (2026-06-08). | The focus-affordance reference classified `<select>` implicitly with single `<input>`s ("fixable by compensating padding"), but unlike a fixed-width textbox the select is intrinsically sized, so padding compensation wouldn't fully absorb the vertical growth of a sized list. No contract asserted select rest-dims == focus-dims, and `:focus` does not match under MCP programmatic focus (the page isn't the focused document), so the evaluator couldn't reproduce the grow via JS. | skill + contract + impl | Skill `reference/focus-affordance-no-layout-shift.md`: add `<select>` (selectbox + listbox mold=select) to mechanism A — intrinsically-sized, so use the inset box-shadow ring (border stays 1px), NOT border-width:2px (mechanism B's padding comp can't absorb a sized list's vertical growth). Contract `doc/contracts/listbox.md` (Select mold): new sm4 (`.z-select:focus` border-width == 1px, ring via box-shadow) — encoded failing-first. CSS `js/zul/sel/css/listbox.css` + `js/zul/wgt/css/selectbox.css`: focus → `box-shadow: inset 0 0 0 1px primary` + border stays 1px; transition border-width→box-shadow. | Verified in built listbox.css.dsp + selectbox.css.dsp: `:focus` sets `border-color:primary` + `box-shadow: inset 0 0 0 1px primary` with NO `border-width:2px`; transition now animates box-shadow. A box-shadow is not a layout box, so the auto-sized select cannot grow. (`:focus` does not match under MCP programmatic focus — the page is not the focused document — so the grow could not be reproduced via JS; the structural fix removes the border-width change that caused it.) |
| 2026-06-11 | biglistbox | Follow-up to the scrollbar gap (same day): after styling `.z-biglistbox-wscroll-*` the bar was **still invisible on the component** and the thumb **jumped off-track** on the first scroll. Two deeper causes the first pass missed. (a) Neither `.z-biglistbox` nor `.z-biglistbox-outer` was `position:relative`, so the `position:absolute` wscroll bars anchored to the **viewport** — rendered at the page's top-right corner, 500+px off the grid (measured track top:0/right:1898 vs component top:594/right:1866). (b) The end-stop `-endbar` had no `position` (Marble doesn't inherit ZK's less, which sets it absolute), so ZK's inline `top:175px` was ignored, `offsetTop` read 0, and WScroll's clamp `end = eend.offsetTop − thumbHeight` drove the thumb to `top:-48` (off the top) on wheel/drag. `display:none` on the endbar made it worse — `zk(eend).isVisible()` ignores stylesheet display and still returned true, while display:none also zeroes offsetTop. | The first fix verified scrollbar *width>0* and large-data thumb travel via programmatic `syncSize()` (which recomputes positions), but never exercised the **wheel/drag clamp path** live on the component, and the contract had no anchor (position:relative) or in-bounds-after-wheel assertion. The viewport-anchoring was invisible to programmatic relative-offset measurement (edrag.top − track.top is correct regardless of anchor). | skill + contract + impl | Skill `components/biglistbox.md`: new "Two positioning requirements" block (root+outer must be position:relative; every wscroll part needs its own position:absolute; endbar must be `position:absolute; visibility:hidden`, never display:none). Contract: sc3 (position:relative), sc4 (endbar position:absolute), sc5 (thumb in-bounds + monotonic after wheel-to-bottom). CSS: `position:relative` on root+outer; `.z-biglistbox-wscroll-endbar{position:absolute;right:0;visibility:hidden}`; thumb bumped to 8×48px @38% for visibility. | Fixed & verified live: bar anchored to component (track top/right ≈ component), thumb 8×48px clearly visible, moves monotonically 39→127 on wheel-to-bottom, stays in bounds. Lesson: verifying a JS-driven widget needs the **real interaction path** (wheel/drag), not just programmatic state-setters — and "relative offset correct" can hide an absolute-anchor bug. Residual: scale=1 cap still compresses thumb travel to the upper track for modest datasets (ZK-inherent, see prior row). |
| 2026-06-11 | biglistbox | User re-reported the **horizontal** axis ("MultipleColumn → I see no horizontal scrollbar / scroll indicator"). Investigated live: **no theme defect** — the horizontal bar renders and works (shift-wheel scrolls data to `_currentX`=86/100, thumb `offsetLeft` 0→86 = `endbarLeft 134 − thumbWidth 48`, in-bounds + monotonic; my `right:0` on the shared `-endbar` rule does NOT corrupt the horizontal `offsetLeft` clamp). Two reasons it *looks* missing, neither a CSS bug: (a) **below the page fold** — the main biglistbox is `height:500px` starting at page-y 594, so its bottom edge (the only place the horizontal bar lives) sits at ≈919px = the viewport height; the vertical bar was visible only because it runs the right edge *from the top*. (b) **scale=1 compression is far more visible horizontally** — same axis-agnostic `WScroll.js` cap the user already accepted for vertical, but the horizontal track is ~1832px wide vs the vertical track ~500px tall, so an identical 86-step travel parks the thumb in the far-left ~5% corner and looks static. | The contract's sc1/sc2/sc5 were written/verified on the **vertical** axis only; nothing asserted the horizontal bar, and the evaluator screenshots the component at the top of the page where the bottom edge (and thus the horizontal bar) is off-screen. The "I see nothing" was a viewport-fold + perception issue that no computed-style or top-anchored screenshot would surface. | (no code change) + skill + contract | Skill `components/biglistbox.md`: add a verification gotcha — the horizontal bar lives only on the bottom edge, so a 500px component on a ~900px page puts it below the fold; scroll the component into view (`scrollIntoView({block:'end'})`) before judging "missing", and note scale=1 compression reads worse horizontally (wider track). Contract `doc/contracts/biglistbox.md`: generalize sc5 to **both axes** (horizontal thumb `offsetLeft` 0→max in-bounds + monotonic) and add a States note that the horizontal bar requires scrolling the component into view. | Verified not-a-defect: horizontal scroll functions correctly and matches ZK's default theme (scale cap is axis-agnostic JS). No CSS change warranted; documented so the next session doesn't re-chase a working scrollbar. |
| 2026-06-11 | searchbox | Two user-reported bugs with one root cause. (1) Clicking the searchbox revealed its popup **bottom-up** (and positioned it far below the trigger) instead of dropping top-down like every other ZK popup. (2) After opening, **clicking outside did not close** the popup. Cause: the theme owns the searchbox popup's hidden state (the mold renders `.z-searchbox-popup` with NO inline display, unlike combobox/bandbox/datebox where ZK toggles inline `display:none`), but it hid via the anti-pattern `.z-searchbox-popup { display:none }` + `.z-searchbox-open .z-searchbox-popup { display:flex }`. `open()` detaches the popup to `<body>` via `makeVParent()`, so the `.z-searchbox-open` (trigger) ancestor selector stops matching → the detached popup is left `display:none` at the moment `_repositionPopup()` measures its height (→ 0) → `slideDown` anchors the reveal bottom-up + the popup mis-positions; the broken floating lifecycle also let outside-clicks miss the close. | Both are runtime/behavioural (animation direction + outside-click close) on a detached, JS-positioned popup — invisible to static computed-style checks and absent from MD3/Mira (no searchbox analog). The evaluator measured `.z-searchbox-popup` appearance (colors/shape/selected family) but never opened the popup via a real click to observe drop direction or close behaviour, and no contract row asserted the open-popup display/position/close. Programmatic `wgt.open()` did NOT reproduce it (a prior open leaves inline `display:block` lingering, masking the cold-first-click `display:none` measurement) — only a real first click reproduced it. | skill + contract + impl | Skill `reference/floating-popup-in-body.md`: new "Hiding a detached popup: scope `display:none` to the ATTACHED state" section (lifecycle table + anti-pattern). Skill `components/searchbox.md`: new sub-section under Popup detachment (mold sets no inline display → theme must hide via `.z-searchbox .z-searchbox-popup`, never the open-scoped show rule). Contract `doc/contracts/searchbox.md`: new p1–p4 (open popup display != none, drops downward, base rule not display:none, closes on outside-click after typing) + open state in checklist. CSS `js/zkmax/inp/css/searchbox.css`: popup base → `display:flex`; hide moved to `.z-searchbox .z-searchbox-popup { display:none }`; removed `.z-searchbox-open .z-searchbox-popup { display:flex }`. | Fixed & verified live (real-click): initial paint shows no stray popup; first click reveals the popup top-down directly below the trigger (top=214 = trigger bottom, h=312); the exact earlier-failing sequence (click→type "a"→click top-band 900,174) now closes; 5 outside-click scenarios all close. Sibling sweep: searchbox was the ONLY component with the bare-hide + open-scoped-show pattern (bandbox et al. correctly let ZK toggle inline display). |
| 2026-06-11 | searchbox | Multiple-select (and single-select) searchbox with a pre-existing selection truncates the selected-label text far too early ("Apple, Banana" → "Apple, B…") even though the trigger is wide enough to show it. Root cause: the theme's `.z-searchbox-placeholder` base rule has **no `display`**, so it defaults to `block`. ZK's JS sets inline `display` only in the *no-selection* state (label→`none`, placeholder→`inline-block`) and **clears both inline styles when a selection exists**, falling back to the CSS base; with no base `display:none` the placeholder stays `block`, and since both label and placeholder carry `flex:1`, the two split the row 50/50 — the label gets only half the trigger width. | The selected↔placeholder swap is driven by ZK JS clearing inline styles and relying on the theme's *base* `display` values; MD3/MUI has no placeholder-vs-value swap analog, and no contract asserted the placeholder is `display:none` (or that the label fills the trigger) in the has-selection state. Empty/default previews masked it — the placeholder only steals space once a non-empty label coexists with it. | skill + contract + impl | Skill `components/searchbox.md`: new "Label/placeholder visibility is JS-toggled only in the no-selection state" note — theme base MUST be label `display:inline-block` (visible) + placeholder `display:none` (hidden), mirroring stock ZK less. Contract `doc/contracts/searchbox.md`: new `lp1`–`lp3` rows (placeholder base rule `display:none`; with selection placeholder computed `display:none`; label fills trigger / no premature ellipsis). CSS `js/zkmax/inp/css/searchbox.css`: add `display:none` to the `.z-searchbox-placeholder` base rule. | Verified live 2026-06-11: with selection present placeholder computes `display:none`, label width grows to full trigger inner width, "Apple, Banana" renders untruncated (scrollWidth == clientWidth). Sibling sweep: `cascader` shares the identical label/placeholder-`<div>` swap and was already guarded for the same gap (via `:has(.z-cascader-label:not(:empty))`, cascader.css lines 55-64) — verified live untruncated ("Japan/Kyoto" 218==218); searchbox was the only swap left unfixed. combobox/datebox/timebox/spinner/bandbox use a real `<input>` with a native `placeholder` attr (no swapped `<div>`), so they are unaffected. Bidirectional skill cross-reference added between `components/searchbox.md` and `components/cascader.md`. |
| 2026-06-12 | colorbox (manifested); all pages (root cause) | colorbox preview page showed a vertical scrollbar and looked "unexpectedly taller" on a mobile/narrow viewport, despite content being only 522px tall in an 835px viewport. No hidden colorbox element was involved — all 18 colorbox/menu-content popup sub-elements are `display:none` with zero footprint. Root cause is a two-step interaction: (a) the preview matrix (`pv/matrix.zul` + `pv.css`) lays columns out at FIXED px widths (`.pv-cols-2 > .pv-row { grid-template-columns: 120px repeat(2,160px) }` = 440px, +`.z-p-8` 64px ≈ 504px min) so a 414px viewport overflows horizontally → horizontal scrollbar; (b) `body { min-height: 100vh }` in the base reset ignores the horizontal scrollbar's thickness, so body (835px=100vh) exceeds the now-829px visible area → a spurious vertical scrollbar on a page that fits. | Pure layout/reset robustness bug, not a per-component ZK characteristic. The screenshot harness renders preview pages at a wide desktop viewport where neither overflow occurs, so the narrow-viewport `100vh`+horizontal-scrollbar interaction was never exercised; MD3/MUI has no "page reset" analog and no contract asserts page-level overflow. The user only noticed on colorbox because its content is short, making the empty-but-scrolling page obviously wrong (tall pages mask the spurious bar). | skill + impl | Skill `reference/viewport-height-fill.md` (new — theme-agnostic: use `min-height:100%` not `100vh` on body; explains the scrollbar-blind footgun + background propagation + html-handles-dismiss-clicks). CSS `zul/css/base/_reset.css`: `body { min-height: 100vh }` → `min-height: 100%` with explanatory comment. NOT changed: the fixed-width preview matrix (intentional desktop-oriented harness; making it responsive would shift every baseline screenshot — left for discussion). | Verified live at 414px viewport: vertical scrollbar gone (scrollHeight 829 == clientHeight 829), body min-height now `100%`, background still fills via canvas propagation. The fix is global so every page benefits (any real app with a wide horizontally-scrolling table no longer gets a spurious vertical scrollbar). Residual: the horizontal scrollbar on narrow viewports remains (preview matrix fixed px) — separate, preview-only, not addressed. |
| 2026-06-12 | colorbox (reported); all floating popups + all pages (root cause) | **Regression from the 2026-06-12 `100vh→100%` reset fix above.** After opening any colorbox popup, clicking *below* it did NOT dismiss it, while clicking *above* it did — a position-dependent outside-click-close bug, in both desktop and mobile. Root cause: the prior fix changed `body { min-height: 100vh }` → `min-height: 100%` but left `html { min-height: 100% }`. A percentage `min-height` resolves to **0** when its containing block's height is indefinite, and `html { min-height: 100% }` leaves html's height indefinite — so body's `min-height: 100%` computed to 0 and **body collapsed to content height (522px in a 918px viewport)**. ZK core `mount.ts` `_docMouseDown` only fires the popup auto-close (`mimicMouseDown_` → `onFloatUp`) when the outside mousedown is `clientY <= document.body.clientHeight` (a "not a scrollbar click" guard). With body at 522px, every click below the content (`clientY > 522`) was swallowed → no `onFloatUp` → popup stayed open. Clicking above (over real content, `clientY <= 522`) still closed it. Live-proven: below-click (355,548) → target `HTML`, 0 float-ups, stays open; above-click (355,120) → target `DIV`, 12 float-ups, closes. **Theme-wide** (combobox/datebox/bandbox/menupopup all gated by the same ZK guard), not colorbox-specific. | The original fix's reasoning was incomplete: it assumed `html { min-height: 100% }` provided the click-to-dismiss empty area, but ZK's dismiss is gated on `body.clientHeight`, NOT html's height — and `min-height` on html doesn't even make body's `%` resolve. The screenshot harness rendered at a wide/tall desktop viewport and never (a) opened a popup and (b) clicked in the empty area below content, so the dismiss regression shipped silently. MD3/MUI has no "page reset → ZK float-up guard" analog and no contract asserts popup-dismiss-below-content. The prior Playwright `viewport-fill` test asserted only the *visual* scrollbar, not the *behavioural* dismiss. | skill + impl | Skill `reference/viewport-height-fill.md` REWRITTEN: corrected the broken claim (dismiss is NOT provided by `html { min-height: 100% }`), now states the rule is `html { height: 100% }` (DEFINITE, so body's `%` resolves) + `body { min-height: 100% }`, and documents the ZK `_docMouseDown` `clientY <= body.clientHeight` guard as the functional reason body must fill the viewport. CSS `zul/css/base/_reset.css`: `html { min-height: 100% }` → `height: 100%` with explanatory comment (body rule unchanged). Playwright (failing-first → passing): `screenshot.spec.ts` › `viewport-fill` › **"colorbox popup dismisses on a click in the empty area below content"** (opens popup via widget API, real `page.mouse.click` in the empty area below content, asserts `pp` display==none). | Verified: new behavioural test FAILED on the buggy build (`closed:false`), PASSES after the fix; full chromium suite 45/45 green (no baseline shift — `html { height:100% }` is visually identical since body already painted the canvas via background propagation). Live re-verified: `html` height 918, `body.clientHeight` 918; colorbox below-click closes (target `BODY`, float-up fires); sibling sweep — combobox dropdown below-click also now closes (target `BODY`, `closed:true`). `html { height: 100% }` confirmed scrollbar-aware: forced horizontal overflow → `body.clientHeight` shrinks 918→912, NO spurious vertical scrollbar — so the original 2026-06-12 scrollbar bug stays fixed too. |
| 2026-06-12 | colorbox (mobile only) | Two user-reported MOBILE-only bugs, one root cause. (1) After opening the popup, tapping ANY outside area did not close it (worse than the desktop below-content case — even tapping over content failed). (2) After selecting a colour the popup "closed" but a small `class="z-colorbox-popup z-palette-button"` element (18×18) stayed visible. Root cause: ZK 10.2.1-jakarta's `Colorbox.closePopup()`/`onHide()` (the RUNNING build, NOT the ZK10 source — which still has `pp.style.display='none'`) only call `undoVParent()`; they never reset the inline `display:block`/`position:absolute`/`zIndex:88000` that `openPopup()` set, nor remove the `z-palette-button` class `openPalette()` added to the popup. On a desktop UA `undoVParent()`'s style restore happens to leave the re-attached popup hidden (desktop close verified `display:none`), but on the iPad/Safari mobile UA it does NOT — the re-attached popup stays `display:block` (looks un-closed) with the palette-button artifact. onFloatUp DID fire on touch (12×) and onHide WAS called (`_open` went false) — proven via instrumented `touchscreen.tap` — so it is purely the missing display reset, not an event-delivery problem. Also observed (separate, ZK JS, not fixed): the popup mis-positions to `top:1112px` (page bottom, off-screen) on mobile regardless of html/body height. | UA-dependent: desktop passes, mobile fails, so the desktop screenshot harness and the desktop `viewport-fill` colorbox test never caught it. The bug lives in ZK's compiled mold JS (build differs from the ZK10 source tree), so reading source misled — had to dump `('' + wgt.closePopup)` from the running page. MD3/MUI has no colorbox analog and no contract asserted the closed-popup display on a touch UA. The earlier same-day desktop dismiss fix (body fills viewport so ZK's `clientY<=body.clientHeight` guard passes) was necessary but insufficient on mobile — a SECOND, independent ZK defect. | skill + impl | Skill `reference/floating-popup-in-body.md`: new section "the close method leaves an inline `display:block` (needs `!important`)" — documents the Colorbox running-build methods, the desktop/mobile divergence, the `!important` requirement, and the caution to verify the open popup is `<body>`-detached + not apply to `.z-menu-popup`. Skill `components/colorbox.md`: new "Closed popup must be force-hidden (mobile-only bug)" sub-section. CSS `js/zkex/inp/css/colorbox.css`: add `.z-colorbox > .z-colorbox-popup { display: none !important }` (closed/attached state only; open popup is body-detached). Playwright (failing-first → passing, TABLET project, mobile UA + touch): `tablet.spec.ts` › `tablet-colorbox-dismiss` › "outside tap closes the popup…" + "selecting a color leaves no visible popup/palette-button artifact". NOT applied to `.z-menu-popup` (zul.menu.Menu, own working close path — verified its open popup can stay a child of .z-menu, so `!important` would break it). | Verified: both tablet tests FAILED on the buggy build (popup stays `display:block`; `z-colorbox-popup z-palette-button` artifact visible), PASS after the fix. Full suites green: chromium 45/45 (no desktop regression — the open popup detaches to `<body>` so the `.z-colorbox > …` rule never hides it), tablet 11/11. Live-confirmed via instrumented taps: open popup stays visible (body-detached), outside-tap → `display:none` + `_open:false`, colour pick → no leftover. Residual (not fixed, ZK JS, not reported): mobile popup opens at `top:1112px` off-screen — a separate ZK `_syncPosition` mobile defect a CSS theme can't address. |
| 2026-06-15 | datebox, timebox (sweep: calendar wheel mold) | On a mobile UA, tapping a datebox produced **no usable display**. ZK swaps the desktop grid calendar for an iOS-style **wheel picker** (`.z-calendar-wheel-*` / `.z-timebox-wheel-*`, rendered by `zkmax/touch/{datebox,timebox}-touch.ts`) and makes every input `readonly`. The theme never styled the wheel mold: `.z-*-wheel-list` had no bounded height, so its `<ul>` of 200+ `<li>`×40px collapsed to ~5079px. ZK's touch popup is a bottom sheet (`height=cave.offsetHeight; top=innerHeight; translateY(-height)`) → it slid 5079px up to `y≈-3949`, fully off-screen. Also the desktop `readonly` rule dimmed the trigger (`pointer-events:none`) on every mobile datebox. | MD3/Mira have no wheel-picker analog, so the harness never looked for `.z-calendar-wheel-*`. The tablet datebox test only measured input height + page overflow — it never OPENED the popup, so the unstyled 5079px wheel was never exercised. | skill + contract + agent + impl | Skill `reference/mobile-wheel-picker.md` (ZK swaps to wheel mold + readonly on mobile; DOM + the `offsetHeight/3` slot invariant) cross-linked from `components/calendar.md`, `components/timepicker.md`. Contract: `doc/contracts/datebox.md` + `timebox.md` tablet wheel rows (failing-first). CSS: new `zkmax/css/tablet/_wheel.css` (registered in `scripts/build-css.js`) + `zkmax/css/tablet/_inputs.css` mobile-readonly neutralization. Agent: evaluator §1.5 — open datebox/timebox popups on tablet. Tests: `tablet.spec.ts` describe `tablet-datebox-wheel` / `tablet-timebox-wheel`. |
| 2026-06-15 | datebox, timebox (follow-up to wheel mold; same day) | Follow-up to the wheel-mold fix above. User re-reported a subtler mobile bug: tapping the datebox **input** opens the wheel sheet ~18px **too low** (it overshoots below the viewport bottom, clipping the OK/Cancel row), while tapping the **icon** lands the sheet flush. Root cause is a ZK framework quirk, not the wheel CSS: `CalendarPop._syncPosition` (datebox-touch.ts) anchors the bottom sheet with `top = (zk.ios?window.innerHeight:jq.innerHeight()) + window.scrollY` then calls `$pp.zk.makeVParent()` to detach the popup to `<body>` — and makeVParent (via the iOS `_fixedVParent` override) **inflates the inline `top` by ~18px** (an offset-parent delta). That inflation is only undone if a *second* `_syncPosition` fires (from `onSize`). The **icon** tap happens to trigger that second sync (`top 1130→1112`); the **input** tap does not (focusing the readonly `<input>` suppresses the extra resize/onSize), so it stays at the inflated `top:1130` → 18px overshoot. Measured live (iPad UA, viewport 1112): input `top:1130 overshoot:+18`, icon `top:1112 overshoot:0`. The sheet was also left-aligned at `x:0` (datebox-touch never sets `left`, unlike combo-touch which centres). | The 2026-06-15 wheel test taps the `.z-datebox-button` (icon) — the one target that *happens* to self-correct via the second sync — so it never exercised the input tap that stays mis-anchored. The overshoot is invisible to static computed-style checks (it's an inline `top` set by JS after a re-parent) and has no MD3/Mira analog. The earlier note "datetime datebox setOpen gave styleTop:1605, real tap fine" conflated two issues and missed that *which* element you tap changes the anchor. | skill + contract + impl | Skill `reference/mobile-wheel-picker.md`: new "Bottom-sheet positioning is unreliable — pin it, don't trust ZK's inline `top`" section (documents the makeVParent +18px inflation + the input-vs-icon second-sync divergence; theme-agnostic). CSS `zkmax/css/tablet/_wheel.css`: pin the wheel sheet to the viewport bottom — `.z-datebox-popup:has(.z-calendar-wheel-cave), .z-timebox-popup:has(.z-timebox-wheel-cave) { position:fixed; left/right:10px; top:auto; bottom:0; width:auto; transform:none } ` + a `@keyframes` slide-up to replace ZK's now-overridden translateY transition (immune to both the makeVParent inflation AND scrolled-page `scrollY`; also centres the sheet). Contract `doc/contracts/datebox.md` + `timebox.md`: new tablet row — input-tap and icon-tap both land flush (`bottom ≈ viewport`, overshoot ≤ 2px). Tests: `tablet.spec.ts` `tablet-datebox-wheel`/`tablet-timebox-wheel` add an INPUT-tap-lands-flush assertion (failing-first on input tap). | Fixed & verified live: with the pin, BOTH input and icon taps land at `top:908 bottom:1112 overshoot:0`, centred (left:10/right:10), for datebox AND timebox, at scroll 0 and scrolled. |
| 2026-06-15 | combobox, selectbox (RELATED — logged, NOT yet fixed) | Sibling sweep of the datebox bottom-sheet fix surfaced a related defect: the **combobox** mobile bottom-sheet (`combo-touch.ts`, same `top=innerHeight+scrollY`+`makeVParent` pattern) also overshoots — icon-tap measured `top:959 bottom:1239 overshoot:+127px` (worse than datebox; its content height is dynamic so the delta differs). The input-tap popup was not reliably captured (closed-popup height 0 in the probe). Selectbox uses a different (select-touch) mold and rendered no measurable `-pp` in the probe. | Same class of bug (JS-anchored bottom sheet, no MD3/Mira analog, tablet tests never opened the combobox popup geometry) but a DIFFERENT magnitude/mold than datebox; reliable reproduction + the same pin-to-bottom treatment need their own verified pass rather than a blind bundled fix. | (to-investigate) | Deliberately NOT fixed in the datebox pass (scope discipline). Tracked here for a dedicated follow-up: re-reproduce combobox/selectbox input-vs-icon overshoot with a reliable open-popup probe, then apply the same `position:fixed; bottom:0` pin to `.z-combobox-popup`/`.z-selectbox-popup` if confirmed. |
| 2026-06-15 | combobox (sweep: any comboitem icon/image variant) | User-reported: in the "With icon" comboitem variant the leading icon sat too close to the label. Measured live: glyph-to-text gap was **0px** — the icon glyph touched the label. ZK's `comboitem.js` mold renders `.z-comboitem-icon` + `.z-comboitem-text` as adjacent flex children of the `<li>` with no built-in separation, and the theme had no rule for the icon/image sub-spans at all. The naive fix (a flex `gap` on `.z-comboitem`) is a trap: the mold emits an **empty** `.z-comboitem-image` span for *plain* items too ("we still output the image for backward compatibility"), so `gap` would phantom-indent every plain item. | Pure spacing on a dropdown sub-element with no MD3/Mira analog measured by the harness, and the contract asserted only comboitem padding + selected colours, never the icon→label gap. The screenshot `gallery` baseline only captures *closed* comboboxes (the popup renders detached in `<body>`), so an open-popup icon item was never exercised. The skill's documented popup DOM was also wrong — it claimed `li.z-comboitem > .z-comboitem-content`, an element the mold never renders, hiding the real icon/image/text/inner span structure. | skill + contract + impl | Skill `components/combobox.md`: corrected the popup DOM tree (icon/image/text/inner spans, not the nonexistent `.z-comboitem-content`) + new "Leading icon/image spacing — never use container `gap`" section documenting the empty-`.z-comboitem-image` backward-compat span trap and the `margin-right` + `:not(:empty)` pattern. Contract `doc/contracts/combobox.md`: new c16 (`.z-comboitem-icon` margin-right 12px, glyph-to-text gap ≥ 8px) + c17 (empty `.z-comboitem-image` margin-right 0 — no phantom indent). CSS `js/zul/inp/css/combobox.css`: `.z-comboitem-icon, .z-comboitem-image:not(:empty) { margin-right: var(--zk-spacing-3); flex-shrink: 0 }`. Test: `screenshot.spec.ts` › `combobox` › "comboitem icon is separated from its label" (opens icon combobox via widget API, asserts glyph→text gap ≥ 8px). | Fixed & verified live: icon variant glyph-to-text gap 0→**12px**; plain item text indent from content edge stays **0px** (empty image span keeps `margin-right:0` via `:not(:empty)`). Playwright test FAILED on buggy build (Received: 0), PASSES after fix. Sibling sweep: selectbox/listbox/menuitem use a *different* mold/label structure (not the `$s('icon')+$s('text')` comboitem mold) — comboitem is the only one with this empty-backward-compat-image-span quirk; combobox is the sole consumer of `comboitem.js`. |
| 2026-06-16 | window, panel, groupbox (+ theme-wide, root cause) | User-reported: `.z-window-header` (and panel/groupbox headers) rendered **much taller** than the design — window 88px, panel 80px, groupbox 73px. Root cause is **NOT a token/value error** (the 16px header padding already matches MUI DialogTitle/CardHeader): the packaged (minified) build was **missing the universal `*{box-sizing:border-box}` reset**. CleanCSS 5.3.3 (`scripts/build-css.js`, level 1) has a bug — a bare layer-order statement `@layer a, b;` is dropped **together with the single CSS rule that immediately follows it**, and in `_reset.css` that following rule is the box-sizing reset. With the reset gone, every element relying on it computed as `content-box`, so header `min-height` became **additive** with padding (window 56+32=88, panel 48+32=80, groupbox 48+24=73) instead of inclusive. A second, related cause for narrow no-border panels: the title-mold renders the title as a **bare text node** (not a `.z-caption` child), so the existing `.z-caption-content/.z-label` nowrap rules never reached it → long titles wrapped to 2 lines and inflated the header. | Two blind spots. (1) The **dev** build (unminified) keeps the reset, so the bug only existed in *packaged* CSS — the live preview the screenshot baselines were captured against had border-box, so the visual baselines were correct while production silently drifted. No test or contract asserted computed header *height* (window.md c4 asserted header *padding* = 16px, which was correct), so a content-box blow-up to 88px passed every check. (2) The harness never minified-and-measured: it measured the dev CSS. (3) Bare-title-node wrap: the harness `window`/`panel` gallery baselines capture wide demos whose short titles don't wrap; the narrow (280px) no-border panel that wraps was never measured for height. | impl (build) + contract | Build: `scripts/build-css.js` `minifyCss` — extract bare `@layer …;` statements (strict `/@layer\s+[\w-]+(?:\s*,\s*[\w-]+)*\s*;/g`, so it never matches `@layer` text inside comments like `} /* end @layer zk-utilities */`) before minifying, then re-prepend at the top (allowed to precede `@import` per CSS spec) so neither the statement nor the rule after it is dropped. CSS: `js/zul/wnd/css/window.css` + `js/zul/wnd/css/panel.css` — add `white-space: nowrap` to the header so the bare title text node (inherited) can't wrap. Contract: `doc/contracts/{window,panel,groupbox}.md` — new header-*height* rows (border-box; window ≤72, panel ≤68, groupbox ≤56). Test (failing-first → passing): `screenshot.spec.ts` › `container-header-height` › window/panel/groupbox each assert `box-sizing === 'border-box'` AND a height upper bound. | Verified: 3 tests FAILED on the buggy build (`content-box`; window 88 / panel 80 / groupbox 73), PASS after the fix (window ~64, panel ~60, groupbox ~49). Full chromium suite **50/50 green — including the window & panel visual gallery baselines**, confirming the baselines already encoded the compact border-box headers and the fix restores production to match (no baseline regeneration needed). The build fix is theme-wide: every element relying on the universal reset is border-box again in production. Sibling note: groupbox 3d-mold header already used `padding:0` so it was unaffected by the box-sizing stacking; its title uses `.z-groupbox-title-content` (already nowrap) so it needed no wrap fix. |
| 2026-06-16 | errorbox | User-reported: the same errorbox looks **different depending on where it lands** relative to the field — when it pops to the right of the field its icon/close padding looks one way, when it flips to the left/below another. Expected: identical except the pointer direction. Root cause is a ZK framework behaviour, not a token error: `Errorbox._fixarrow` (Errorbox.ts) rewrites the **outer `.z-errorbox`** padding on every reposition, per pointer direction — `padding-left:8px` when the beak points left (box on the right of the field), `padding-top:8px` when up, etc. `.z-errorbox-content` is the `display:table-cell`, so that 8px shifts the content; but `.z-errorbox-icon` and `.z-errorbox-close` are absolutely positioned against `.z-errorbox` and stay put. Measured live (left-pointer box vs. up-pointer box): icon→text gap **4px vs 12px**; content offset 8px vs 0. The beak is positioned 4px OUTSIDE the box, so the padding buys no beak room — it is pure layout noise. | MD3/Mira have no inline-validation-tooltip-with-caret analog, so the harness never measured errorbox geometry at all (the contract only asserted bg/colour/padding/radius/shadow, never icon/close placement). Floating popups also never appear in the gallery screenshot baselines (they render detached in `<body>` and only on a triggered constraint), so visual diffing never saw them. And nothing exercised **more than one** pointer direction, which is the only way the drift is visible. | skill + contract + impl | Skill `components/errorbox.md`: new section "`_fixarrow` rewrites the `.z-errorbox` OUTER padding per direction" (theme-agnostic ZK fact + the `padding:0 !important` neutraliser + why `!important` is required, same inline-style reason as the pointer `display`). Contract `doc/contracts/errorbox.md`: new "position-invariant layout" state (icon→text ~12px, close→edge ~4px constant across all four positions). CSS: `js/zul/inp/css/input.css` (authoritative `input.css.dsp`) **and** mirror `js/zul/wgt/css/errorbox.css` — `.z-errorbox { padding: 0 !important }`. Test: `screenshot.spec.ts › errorbox-position-invariance` (triggers all inputs via `setErrorMessage`, asserts every real errorbox has icon gap ∈[10,14] & close gap ∈[2,6] and the spread across directions ≤2px). | Fixed & verified live: after the fix every errorbox (left & up pointers exercised) reads icon→text **12px**, close→edge **4px**, computed padding **0** — identical layout, only the pointer differs; the right-positioned beak still touches the field exactly (box.left == field.right, tip 4px into field). Playwright test FAILED failing-first on the stripped build (`iconLeftFromContent: 4`, expected ≥10), PASSES after the fix. Full chromium suite **51/51 green** (no baseline regression — floating errorboxes aren't in any gallery snapshot). |
| 2026-06-16 | label (form rows; textbox/combobox/radiogroup adjacency) | User noticed a `<label>` beside an input aligns **differently** depending on its container — and "worse" beside a radiogroup. Measured live (`label-center − field-center`): bare `<div>` **−30px** (label clings to field top), default `<hlayout>` (`valign="top"`) **−10px**, `<hlayout valign="middle">` **0px (centred)**. The offset is **identical** across textbox/combobox/radiogroup — the variance is purely the container's vertical-align reference (baseline-vs-`middle` inline mix in a `div`; top-vs-top cells in default hlayout; `.z-valign-middle` cells when valign=middle). The radiogroup only *looks* worse because its glyph is centred in a tall empty 40px box, so a top-clinging label floats in blank space above the circle (same geometry, different ink). **Not a CSS bug** — it is the correct output of three layout modes. | The harness drives off MD3/Mira, and **MD3 has no standalone-label-beside-field construct** (MD3 labels live inside/above the field), so alignment of a bare label next to a field was never something a contract or evaluator looked at. No component owns the rule because the correct answer is "the container decides", which no per-component contract expresses. | skill + design (not-a-bug; no code change, no test per loop rule) | Skill `reference/inline-label-alignment.md` (the container-valign matrix + baseline-vs-middle mechanism + why `.z-label` must stay untouched + radio perceptual note; theme-independent ZK fact), cross-linked from `components/hlayout-vlayout.md`. DESIGN.md §13 "Label ↔ field horizontal alignment (form rows)": theme stance — never auto-centre a bare label; canonical row is `<hlayout valign="middle">`/`<hbox valign="middle">` or flex centre. | Resolved as **not-a-bug**: `<hlayout valign="middle">` already centres at 0px in the current theme (ZK-core `.z-valign-middle .z-hlayout-inner{vertical-align:middle}`, theme doesn't break it). No CSS change, no Playwright test (loop rule: not-a-bug needs no test). Fix is an authoring rule, encoded in skill + DESIGN.md. |
| 2026-06-16 | errorbox (follow-up to the position-invariance row above; same day) | Two follow-ups to the errorbox fix above. (1) **Regression I introduced:** my first fix neutralised ZK's one-sided padding with `.z-errorbox { padding:0 !important }`. That fixed the icon/close drift but **pulled the beak inside the content**: the pointer is at `left/top:-4px` and the triangle is 12px wide, so its base sits at `-4+12 = 8px` from the box edge — ZK insets the content by `pw=8px` precisely so the base lands flush at the content edge. With padding:0 the content fills to the box edge while the base stays at 8px → the arrow ends up 8px INSIDE the red box. User-reported: "the red triangle should be just outside the box edge, now it's inside — wrong." The padding is **load-bearing for the beak**, not "layout noise" as the first row claimed. (2) **Gallery cleanup:** the errorbox State Gallery had 3 static mockups (no-arrow / arrow-only / closable-only); a real errorbox always has all three parts (pointer+icon+close), so they were merged into one canonical mockup mirroring `Errorbox#redraw`. | The first-round Playwright test only asserted the icon/close **gaps** (which padding:0 satisfied) — it never measured **where the beak sits relative to the content**, so the arrow-inside regression passed every check. The beak-vs-content geometry had no contract row and no test assertion; the first fix was reasoned about geometrically but not guarded, so an over-correction shipped. | impl + skill (correction) + contract (correction) | CSS `js/zul/inp/css/input.css` (+ mirror `wgt/css/errorbox.css`): replace `padding:0` with **symmetric** beak room — `.z-errorbox { --zk-errorbox-beak: 8px; padding: var(--zk-errorbox-beak) !important }` and re-add the beak to the sub-element offsets: `.z-errorbox-icon { left: calc(var(--zk-spacing-3) + var(--zk-errorbox-beak)) }`, `.z-errorbox-close { right: calc(var(--zk-spacing-1) + var(--zk-errorbox-beak)) }`. Content is now inset equally on all sides → beak flush at whichever edge it points to AND gaps constant. Skill `components/errorbox.md`: corrected the "neutralise it" section to "make it symmetric, don't zero it" (documents the `-4px + 12px triangle = 8px base` math + the arrow-inside trap + the border-coupling of the 8px). Contract `doc/contracts/errorbox.md`: corrected the position-invariance row + new "beak flush OUTSIDE the content (intrusion ≤1px)" + "complete structure (always pointer+icon+close)" rows. ZUL `src/test/resources/web/errorbox.zul`: 3 gallery cols → 1. Test: extended `errorbox-position-invariance` to also measure `beakIntrusion` per direction and assert ≤1px. | Fixed & verified live: every errorbox (gallery down-pointer + live left/up pointers) reads icon→text **12px**, close→edge **4px**, computed padding **8px**, beak intrusion **0** (base flush at content edge, triangle outside). Test FAILED failing-first on the re-stripped `padding:0` build (`beakIntrusion:8`, `iconLeftFromContent:0`), PASSES after the symmetric fix. Full chromium suite **51/51 green**. |
| 2026-06-17 | button (all color variants: secondary, success, warning, error, info) | Color variant disabled state overridden by same-specificity rule — disabled button retains full saturated color, indistinguishable from an enabled button. Root cause: color variant rules (`.z-button-{color}.z-button`, specificity 0,2,0) appear **after** the base disabled block (`.z-button[disabled]`, same specificity 0,2,0) in button.css — last rule wins. The `z-button-outlined` / `z-button-text` / base `z-button` variants are unaffected because they already have explicit compound disabled rules placed after their variant rules. | Evaluator never applied color-variant sclasses AND disabled simultaneously; the button contract only checked `.z-button[disabled]` (the base variant) and did not enumerate per-color-variant disabled states. Gallery screenshot showed disabled base buttons looking grey — the per-variant disabled appearance was never measured. | skill + contract | Skill: `.claude/skills/zk-component-rules/reference/color-variant-disabled-pattern.md` (new — any theme adding sclass-based color variants must pair them with higher-specificity disabled overrides); Contract: `doc/contracts/button.md` — new rows c15–c19 asserting each color-variant disabled button uses `--zk-color-disabled-container` bg + `--zk-color-disabled` text; CSS: `js/zul/wgt/css/button.css` — compound `.z-button-{color}.z-button[disabled]` block; Test: `screenshot.spec.ts` › `button` › `color-variant-disabled-state` |
| 2026-06-17 | button (outlined color variants: outlined-secondary/success/warning/error/info; text color variants: text-secondary/error/info) | **Sibling sweep of the 2026-06-17 contained-variant gap above.** Same specificity-tie root cause, different variant families. `z-button-outlined-secondary.z-button` (0,2,0) appears after `.z-button[disabled]` (0,2,0) → variant color wins for `color` and `border-color` on disabled outlined buttons. `z-button-outlined.z-button[disabled]` (0,3,0) does NOT match these because the button only carries `z-button-outlined-secondary` class, not `z-button-outlined`. Same issue for text variants. | The 2026-06-17 fix only covered contained variants. The pattern is identical but the selectors are different (`z-button-outlined-{color}` / `z-button-text-{color}` never carry the base `z-button-outlined` / `z-button-text` class, so the existing base disabled overrides don't apply to them). | skill + contract | Same skill file: `.claude/skills/zk-component-rules/reference/color-variant-disabled-pattern.md` — updated to document outlined and text sub-families; Contract: `doc/contracts/button.md` rows c20–c28; CSS: compound `.z-button-outlined-{color}.z-button[disabled]` + `.z-button-text-{color}.z-button[disabled]` blocks; Test: extended `color-variant-disabled-state` |
| 2026-06-17 | button (text color variants: missing success and warning) | `.z-button-text-success` and `.z-button-text-warning` were never authored — text variant only had primary, secondary, error, info while both filled and outlined had all 6 colors. Root cause: simple omission when the text variant family was first written; the two neutral-semantic colors (success, warning) were skipped without comment. | Harness evaluator never applied text-variant sclasses in isolation; the button gallery only tested the base text variant and the error/info semantic variants (test failures drove those), so the missing pair was never exercised. | impl | `js/zul/wgt/css/button.css`: add `.z-button-text-success` / `.z-button-text-warning` color rules, their `::before` hover overlay rules, and extend the disabled compound selector block to include both. No skill or contract change needed — implementation omission with no ZK-structural subtlety. |
| 2026-06-16 | label (CORRECTION to the row above) | Re-measured the label↔field alignment at a **real 1280px viewport**; the earlier row's "uniform −30px, label clings to top in a bare `<div>`" was a **measurement artifact of a collapsed 0×0 MCP viewport** (inline content stacked; radios wrapped to a fake 80px). Real numbers: bare `<div>` → textbox **−1.5**, combobox **0**, radiogroup **+5**; `<hlayout valign="middle">` → **0** for all. So a bare `<div>` already *centres* single-line inputs (textbox via theme `vertical-align:middle`; combobox via its `inline-flex` baseline coinciding with its box centre) and only drops the **radiogroup ~5px**, because a radiogroup's baseline is its **caption text** baseline (~6px below the box centre). Proof: toggling the `<div>` radiogroup to `vertical-align:middle` moves it +5→−1.5 (matches textbox); the combobox is already 0. `valign="middle"` replaces baseline with box-middle, so only the caption-bearing control (radio/checkbox) visibly shifts when you add it. | The 0×0 viewport trap (memory `reference_screenshot_capture_hang`) silently distorts every width-dependent measurement; the first pass trusted it. The real mechanism (caption controls baseline-align below their box centre, single-line inputs don't) is finer-grained than "div misaligns everything". | skill + design (correction; still not-a-bug) | Corrected `reference/inline-label-alignment.md` (real-width matrix + caption-baseline mechanism + 0-width caveat), `doc/spec/DESIGN.md` §13 (replaced the wrong "~30px high" claim). Still resolves not-a-bug: `<hlayout valign="middle">` centres all field types; no CSS change, no test. | Verified at innerW=1280: div {textbox −1.5, combobox 0, radiogroup +5}, hlayout-middle {all 0}. Matches user's report (textbox/combobox same between div & hlayout; radiogroup differs). |

| 2026-06-18 | notification | (1) Dark background: `.z-notification` outer wrapper had `padding: 12px 16px` + `box-shadow` + `border-radius` added by the Generator. The transparent 12–16px padding zone showed the page background while the inner content's own shadow rendered into that zone, creating a dark halo. (2) Icon touching left stripe: the `.z-notification-icon` is positioned `left: 16px` from `.z-notification` (the containing block). With `padding-left: 16px` on the outer table, `.z-notification-content` starts at 16px from notification left — same position as the icon — so the icon landed directly on the 4px `::before` accent stripe instead of 16px to the right of it. Both bugs share one root: padding on the outer container. | Generator treated `.z-notification` as a visual card and added padding/shadow/radius to it, not knowing that ZK's `.z-notification` is a bare `display:table` layout shell. The static State Gallery in `notification.zul` omitted the `z-notification-icon` element, so the icon-stripe overlap was invisible. No contract row checked icon position relative to the stripe. | skill + impl | CSS `js/zul/wgt/css/notification.css`: remove `padding`, `border-radius`, `box-shadow` from `.z-notification`; ZUL `src/test/resources/web/notification.zul`: add icon element to each State Gallery mockup; Skill (pending): `.claude/skills/zk-component-rules/components/notification.md` — `.z-notification` is a bare layout container, no visual styling. |
| 2026-06-18 | notification | **Translucent overlay bleed-through.** The info/warning/error variant `.z-notification-content` backgrounds used `rgba(<status>,0.12)` — only 12% opaque. A notification floats over arbitrary page content (e.g. `top_left` lands at viewport 0,0 over the page header). The notification *was* on top (inline `z-index:1800`), but the 12%-alpha fill let the page header text bleed through, so it visually read as "the header is on top of the notification." User asked whether this follows MD3 — it does not: MD3/MUI Alert backgrounds are **opaque** tints, not translucent fills, precisely so a floating alert reads as a distinct surface above content. | The earlier tint refactor replaced the opaque `surface-container-highest` base with a translucent `rgba(...,0.12)` fill; over the white preview page background it looked fine in the static gallery (no content behind it), so no gallery check caught it. The contract rows c6/c8/c9 *asserted* the translucent rgba values as expected — codifying the bug. No test asserted background opacity. The defect only manifests when the notification overlays real content, which the static gallery never does. | skill + contract + impl | CSS `js/zul/wgt/css/notification.css`: replace each variant's `rgba(<hex>,0.12)` with `color-mix(in srgb, <hex> 12%, var(--zk-color-surface))` (same hue over white, but opaque); Skill `components/toast-notification.md`: new "transient overlay must be opaque" rule; Contract `doc/contracts/notification.md`: correct rows c6/c8/c9 to assert opaque tint (alpha=1); Test `screenshot.spec.ts` › `notification` › `variant-backgrounds-are-opaque`. | Fixed & verified: failing-first test reported `alpha=0.12`, passes after fix; re-screenshot of `top_left` shows an opaque card occluding the page title (no bleed-through); gallery hue unchanged. Sibling sweep: `toast.css` variants already use opaque `*-container` tokens / `rgba(50,50,50,0.95)` base — toast unaffected. |
| 2026-06-18 | notification | A dead duplicate generic snackbar rule `.z-notification` in `misc.css` (inverse-surface dark bg + padding + shadow + radius) overrode the dedicated `notification.css`, producing a dark navy frame around the light content card (two backgrounds) and pushing the icon onto the accent stripe (padding shifted the absolute icon's containing block). | The contract `notification.md` (c2/c3/c4) *asserted* `.z-notification` should have padding/radius/shadow — codifying the buggy architecture and contradicting the skill — so an evaluator marked it PASS; and there was no Playwright test, so the `.z-notification` "optimization" that stripped those props from `notification.css` looked complete while the `misc.css` copy silently kept applying them. | contract + implementation | delete `misc.css` `.z-notification` block; correct `doc/contracts/notification.md` rows c2/c3/c4 to assert bare shell (card props on `.z-notification-content`); add Playwright `notification` describe in `screenshot.spec.ts` (bg-transparent + zero-padding + icon-clears-stripe) | Fixed: test failed-then-passed; gallery now shows one light card per type, icon clears stripe. Skill already documented the bare-shell rule (no skill change). Sibling sweep: `.z-toast` is styled by its own `toast.css` and was never matched by the misc.css duplicate, so toast was unaffected. |
| 2026-06-18 | notification, toast | Single-line message text is not vertically centred in the card — it sits ~3px above centre (measured), reading as top-heavy and misaligned with the centred severity icon. `.z-notification-content`/`.z-toast-content` carry `min-height:48px` but a single 13px/1.5 line + 12px top/bottom padding is only ~43.5px tall; block layout flows the text from the top and dumps the ~4.5px slack at the bottom. The icon is centred (`top:50%`) but the text is not. | No reference (MD3/MUI Alert renders single-line content centred) flagged it, and the contract had no row asserting vertical alignment of the content text — the evaluator measured the icon (centred) and the card box, never the text's optical centre relative to the card. The static gallery looked "fine enough" because the misalignment is only ~3px. | skill + contract + impl | CSS `js/zul/wgt/css/notification.css` + `js/zul/wgt/css/toast.css`: add `display:flex; align-items:center` to `.z-notification-content`/`.z-toast-content` (pointer variant keeps its higher-specificity `table-cell`/`vertical-align:middle` centring). Contract `doc/contracts/notification.md`: new row c11 (single-line text centred within ±1px of card centre). Skill `components/toast-notification.md`: new "Content min-height taller than one line → centre the text explicitly" rule. Test `screenshot.spec.ts` › `notification` › `single-line-content-is-vertically-centered`. | Fixed & verified: failing-first test reported off-centre by -3.00px, passes after fix (≤1px); all 3 notification tests green. Sibling sweep: `toast.css` had the identical `min-height:48px` + `line-height:1.5` + centred-icon pattern and got the same fix. |
| 2026-06-22 | linelayout | Lineitem content (buttons) do not line up with their cave points and are not adjacent to the connector line. The Marble rewrite of `linelayout.css` set `flex-direction:column` only on `.z-linelayout-vertical .z-linelayout-cave`, but NOT on the `.z-linelayout-vertical .z-linelayout-first/-last` content columns (and not the `row` analog for horizontal). With the columns defaulting to `flex-direction:row`, the per-lineitem content wrappers (`.z-linelayout-last > .z-lineitem`) flow along the wrong axis — all buttons land at the same main-axis position (measured cy=282 for all 4) instead of spreading to match the points (cy=207/257/307/357). The rewrite also dropped the stock `align-items` rules (`flex-end` on first/cave, `flex-start` on last) that pull each content slot toward the cave, so even once axis-aligned the content would not be adjacent to the points. | The contract had `justify-content:space-around` rows (ll8a/ll8b/ll11a/ll11b) and a cave `flex-direction` row (ll16/ll16a) and a lineitem `flex-direction` row (li4), but NO row asserting `flex-direction` on the first/last CONTENT columns, and no `align-items` rows at all. The skill (`components/linelayout.md` line 72) claimed `space-around` alone aligns content with points — omitting that the columns must also carry the per-orientation `flex-direction` and `align-items`. The evaluator measured all 56 rows PASS and logged the bunched-buttons screenshot finding as a false positive ("preview ZUL too narrow"), because no row exercised the missing properties. | skill + contract + impl | Skill `.claude/skills/zk-component-rules/components/linelayout.md` (line 72 corrected to require per-orientation `flex-direction` + `align-items` on first/last columns); Contract `doc/contracts/linelayout.md` rows ll8c/ll8d/ll8e (first), ll11c/ll11d/ll11e (last), ll14c (cave align-items), outcome M7 (content-aligns-with-point); CSS `js/zkmax/layout/css/linelayout.css` (add `flex-direction` to first/last per orientation + `align-items` on all three columns); Test `screenshot.spec.ts` › `linelayout` › `content-aligns-with-points`; eval-report false-positive corrected. | Fixed & verified: failing-first test reported all buttons at cy=282 (points at 207/257/307/357); after fix each button center-y matches its point within ±2px and hugs the cave. |
| 2026-06-22 | linelayout | Each timeline button was clipped (~"cut in half") by the `.z-linelayout-last` column's `overflow:hidden`. The column was only 46px wide while each ~90px button overflowed to ~218px. Root cause is preview-sizing, NOT a theme defect: the preview `<linelayout>` was given `height="200px"` but **no width**, and its container is `z-d-flex z-flex-col z-align-start` (shrink-to-fit). Once the content columns correctly stacked vertically (after the 2026-06-22 flex-direction fix above), their min-content width became one button wide; `flex:1 0 0` (flex-basis 0) + `overflow:hidden` then let the columns collapse below button width, clipping the content. Stock ZK collapses identically without a width — `flex:1 0 0` + `overflow:hidden` are correct, contracted structural properties (ll8/ll11/ll10/ll13). | The prior `flex-direction:row` bug masked this: with buttons laid horizontally the column's min-content width was the SUM of all button widths (~360px), so the linelayout never collapsed. Fixing the axis exposed the latent width-collapse. No harness layer missed a theme rule — the preview page simply never gave the layout primitive a width (analogous to the height it already declared). | not-a-bug (theme) — preview/usage fix | Preview `src/test/resources/web/linelayout.zul`: add `width="440px"` to the `<linelayout>` (mirrors its existing `height="200px"`); Test: extended `screenshot.spec.ts` › `linelayout` › `content-aligns-with-points` with assertion (d) — no button's right edge may overflow the last column. No CSS, skill, or contract change. | Fixed & verified: all 4 buttons render in full (none clipped), screenshot doc/screenshots/linelayout/fixed-width.png; no-clip assertion failed-first at 46px column / 218px button overflow, passes at width=440px (last column 196px). |
| 2026-06-22 | linelayout | Icon and image point content is not centred in the point circle (both axes). `.z-lineitem-point-inner` used `width/height:inherit`, inheriting the point's 24px computed size into the 20px bordered interior (24px − 2×2px border), so the inner box overflowed `overflow:hidden` and its centre sat +2px/+2px down-right of the point centre (measured innerC=(642,209) vs pointC=(640,207), consistent across all points). Image content (`pointImageSrc` → inline `background-image`) had `background-position:0% 0%` (top-left; ZK sets no position) → the image anchored to the top-left corner, not centred. Icon content (`pointIconSclass` → `.z-icon-*` on the inner, rendered as a 12×12 `::before` mask) was centred only via `text-align:center` + `line-height:20px` baseline alignment — off by the inherit-overflow offset and not pixel-centred. | No outcome assertion measured the inner/glyph centre against the point centre; the Expected-values rows asserted the OLD mechanism (pt10/pt11 `inherit`, pt13 `text-align:center`, pt13a `line-height:20px`) and there was NO `background-position` row, so the evaluator measured those PASS while content was visibly off-centre. MD3/MUI has no timeline-point analog to flag it, and the "point with image"/"point with icon" rows were unchecked in States-to-evaluate. | skill + contract + impl | Skill `.claude/skills/zk-component-rules/components/linelayout.md` (point-inner note: `width/height:inherit` overflows the bordered interior; `pointImageSrc` `background-image` carries no position so a theme must centre it; flex-centring is the robust axis-independent approach, not the stock `::before`/line-height offsets); Contract `doc/contracts/linelayout.md` rows pt10/pt11 (`inherit`→`100%`), pt13/pt13a (text-align/line-height → `display:flex`/`align-items:center`), new pt13c (`justify-content:center`) + pt12a (`background-position:center`), outcome M8 (icon/image inner centre == point centre ±1px), States-to-evaluate icon/image checked; CSS `js/zkmax/layout/css/linelayout.css` `.z-lineitem-point-inner`; HTML mockup `doc/contracts/linelayout.html` inner rule synced; Test `screenshot.spec.ts` › `linelayout` › `point-content-centered`. | Fixed & verified: failing-first test reported image/icon inner offset +2/+2px and bgPos `0% 0%`; after fix inner centre matches point centre within ±0.5px, bgPos `50%`, inner flex-centres the glyph. |
| 2026-06-23 | splitlayout (vs splitter, borderlayout, goldenlayout) | Cross-component splitter audit: `.z-splitlayout-splitter` carried a `1px solid var(--zk-color-outline-variant)` bar border that NO other splitter-family member has — `splitter` (zul.box) and `borderlayout` region strips are borderless 8px `surface-container` tonal bars; GL is a transparent gutter. Between splitlayout's two already-bordered panes the splitter's own frame stacked into a double-line and broke the family's "single resize affordance" goal (DESIGN.md §14). The bar's `surface-container` fill already contrasts with the white panes, so the border was redundant decoration, not a needed divider. | The family spec (DESIGN.md §14) listed the splitlayout 1px bar border as a *documented per-component exception*, and contract row c2 *asserted* `border-color: outline-variant` as expected — codifying the outlier — so the evaluator measured it PASS. No test compared bar treatment across family members, and the static splitlayout gallery looked plausible in isolation. The exception was never re-justified against the family's own "bar fill must contrast with every surface it abuts" principle (which the white panes satisfy). | contract + theme-spec + impl | Remove border from CSS `js/zkmax/layout/css/splitlayout.css` `.z-splitlayout-splitter`; correct contract `doc/contracts/splitlayout.md` c2 (border-color → border-width 0) + sync `doc/contracts/splitlayout.html`; drop the "splitlayout 1px bar border" exception from `DESIGN.md §14` + `tokens/_splitter.css` header; Test `screenshot.spec.ts` › `splitter-family` › `bars-are-borderless-across-the-family`. NOT a skill change — bar border is a theme choice, not a ZK structural fact. | Fixed & verified: failing-first test reported splitlayout border 1px (box/borderlayout 0px), passes after fix at 0px family-wide; re-rendered splitlayout shows the tonal strip with no frame, matching splitter/borderlayout. |
| 2026-06-23 | borderlayout (vs splitter, splitlayout) | Splitter family hover audit: borderlayout's actuator pill grew `28px → 44px` on hover (`height:44px`+`translateY(-8px)` W/E, `width:44px`+`translateX(-8px)` N/S) while the other family members keep 28px and signal hover only via colour. The CSS comment justified it as "grow to accommodate the now-visible caret" — but the caret is hidden with `opacity:0` (which still reserves its layout space), so the 28px pill already fits grip/caret/grip at idle (the box splitter proves it). The growth was therefore decorative emphasis that broke family consistency and contradicted MD3's state model (hover = state-layer colour, not geometry/resize). | The growth was undocumented in the contract's Expected-values rows (no `:hover` size row existed) and the prose only described colour + caret-fade, so the evaluator never measured it; it lived only in CSS + a (since-removed) DESIGN.md §14 "borderlayout 44px pill growth" exception line. No test drove a real hover to measure pill geometry. | contract + theme-spec + impl | Remove growth from CSS `js/zul/layout/css/borderlayout.css` (W/E + N/S hover rules; drop orphaned `transform` idle + `width`/`height`/`transform` transitions); add contract rows c29c/c29d (`doc/contracts/borderlayout.md`) pinning hover long-axis to 28px + prose; drop the "borderlayout 44px pill growth" exception from `DESIGN.md §14` + `tokens/_splitter.css`; Test `screenshot.spec.ts` › `splitter-family` › `pill-does-not-resize-on-hover`. NOT a skill change — hover geometry is a theme choice. | Fixed & verified: failing-first test reported hover pill 44px (idle 28px), passes at 28px after fix; re-rendered borderlayout hover shows the pill tinting primary at constant size with the caret fading in. |
| 2026-06-23 | stepbar | Error step icon is invisible — no glyph rendered. ZK `Step._adjustIconContent()` (zkmax `Step.ts:179`) emits the bare class `z-icon-exclamation` for error steps (and `z-icon-check` for complete). The theme's icon build (`scripts/build-css.js` `FA_TO_LUCIDE`) only generates `z-icon-exclamation-circle` and `z-icon-exclamation-triangle`, never bare `z-icon-exclamation`, so `--_icon` is unset → `mask-image:var(--_icon)` is invalid → the `::before` mask draws nothing. `z-icon-check` happens to be a real Lucide name so complete steps already render. Probe: error icon classes `z-step-icon z-icon-exclamation`, computed `mask-image:none`, `--_icon:""`. | The error state has no MD3/Mira analog that names a glyph; the contract checked the error icon's `background-color`/`border`/`color` (chrome) but had no row asserting the glyph actually resolves a mask-image, and the preview ZUL's error step was SKIPPED in the eval report ("no error step in preview" for title-8). Lucide has no bare exclamation, so the alias map silently lacked it. | impl + contract | CSS-build `scripts/build-css.js`: add a `CUSTOM_ICONS` map (name→raw SVG) emitted as `.z-icon-{name}{--_icon:…}` alongside the Lucide loop, with `exclamation` = bare-"!" SVG matching `doc/contracts/stepbar.html`; Contract `doc/contracts/stepbar.md` new row error-4 (`.z-step-error .z-step-icon` resolves a non-empty `--_icon` / mask-image ≠ none); Test `screenshot.spec.ts` › `stepbar` › `error-icon-glyph-renders`. ZK fact (Step emits bare `z-icon-exclamation`/`z-icon-check`) already in skill — no skill change. | Fixed & verified: failing-first test reported `mask-image:none`; passes after build adds `.z-icon-exclamation`; error step shows white "!" on red circle. |
| 2026-06-23 | stepbar | Vertical orientation renders totally incorrectly — connectors appear as short stripes to the LEFT of each row instead of vertical lines joining the circles. `.z-stepbar-vertical .z-step{flex-direction:row}` (contract row vert-2) keeps the connector `::before` as a horizontal-flow flex item beside `.z-step-content`; in vertical mode the step must be `flex-direction:column` so the `::before` stacks ABOVE the content and is horizontally centred under the icon. Probe: step flex-direction `row`, connector center-x far from icon center-x (70). | The contract codified the wrong layout: vert-2 asserted `flex-direction:row`, vert-6 `flex:1`, with no row pinning the connector's horizontal position. The evaluator measured vert-1…vert-9 against an INJECTED element (no native vertical stepbar in the preview at eval time) and they all PASSED because the assertions matched the buggy CSS. No relational predicate required the connector center-x to align with the icon center-x. | contract + skill + impl | CSS `js/zkmax/wgt/css/stepbar.css`: rewrite `.z-stepbar-vertical` (`.z-step{flex-direction:column;align-items:flex-start}`, `::before{flex:0 0 auto;min-height:var(--zk-spacing-6);margin-left` centres the 2px stripe under the 24px icon}); Contract `doc/contracts/stepbar.md` correct vert-2 (`column`), vert-6 (`0 0 auto`), add vert-1b/vert-2b (`align-items:flex-start`) + vert-10 (connector margin-left centres under icon); Skill `components/stepbar.md` relational invariants: add "vertical connector center-x == icon center-x ±2px"; Test `screenshot.spec.ts` › `stepbar` › `vertical-connector-centered-under-icon`. | Fixed & verified: failing-first test reported step flex-direction `row` / connector off-centre; passes after fix with connector center-x within ±2px of icon center-x and `::before` taller than wide. |
| 2026-06-23 | stepbar | Wrapped-label mode is completely unstyled — the theme ships ZERO `.z-stepbar-wrapped-label` CSS, so `wrappedLabels="true"` renders identically to default (title beside the icon, no stacking, no connector). In wrapped mode `.z-step-content` must switch to a column (icon above title) and the connector must move from `.z-step::before` to `.z-step-content::before`/`::after` (absolute horizontal segments centred at circle mid-height). Probe: content flex-direction `row` (should be `column`), titleTop < iconBottom (title not below icon), `.z-step-content::before` content `none`. | The contract listed wrapped-label under "States to evaluate" but had ZERO expected-value rows for it, and there was no Playwright test, so the missing CSS was invisible to the harness — nothing ever asserted the wrapped layout. The skill DID document the wrapped mechanism (connector doubling, `.z-step-content` always present) but no contract row or test enforced it. | contract + impl | CSS `js/zkmax/wgt/css/stepbar.css`: add `.z-stepbar-wrapped-label` block (`.z-step{flex:1 1 0}`, kill inline `.z-step::before`, `.z-step-content{flex-direction:column;align-items:center;width:100%;position:relative}`, `.z-step-content::before/::after` absolute segments at `top:calc((24px-2px)/2)` from edge to circle, first/last hidden, lit tokens for active/complete, title `white-space:normal;margin-top`); Contract `doc/contracts/stepbar.md` add wrap-1…wrap-N expected-value rows + State-matrix entries; HTML `doc/contracts/stepbar.html` add a wrapped-label mockup section; Test `screenshot.spec.ts` › `stepbar` › `wrapped-label-stacks-title-below-icon`. Skill already documents the mechanism — no skill change. | Fixed & verified: failing-first test reported title beside icon + no content connector; passes after fix with title below icon and `.z-step-content::before` segment at circle mid-height; matches iceblue baseline. |
| 2026-06-24 | stepbar | Wrapped-label connector line jogs at a multi-line step. With `.z-stepbar` root `align-items: center` (the default), a step whose title wraps to two lines is taller than its single-line siblings; centring pushes the shorter steps' icons DOWN relative to the tall one, so the icons — and the connector segments pinned at the circle's mid-height via `.z-step-content::before/::after { top: 11px }` — sit at different heights and the line steps up/down at the wrapped node. Probe: step-1 (2-line title) icon center-Y 1195 vs 1205 for the single-line steps → 10px jog. | The 2026-06-23 wrapped-label rows (wrap-1…wrap-18) verified per-step geometry (content column, connector top offset, lit colour) but NEVER asserted CROSS-step alignment — every step measured fine in isolation. The `wrapped-label-stacks-title-below-icon` test only inspected one step, so the inter-step jog (which only appears when one label actually wraps) was invisible. No row pinned the root's `align-items` for wrapped mode. | contract + impl | CSS `js/zkmax/wgt/css/stepbar.css`: `.z-stepbar-wrapped-label { align-items: flex-start }` (override root `center`); Contract `doc/contracts/stepbar.md` new row wrap-1b (root `align-items: flex-start`) + State-matrix "wrapped root" entry; HTML `doc/contracts/stepbar.html` synced; Test `screenshot.spec.ts` › `stepbar` › `wrapped-label-icons-share-one-baseline` (all step icons share one center-Y ±2px). Not a skill change — top-align is a layout consequence of the column-stacked title, already implied by the wrapped mechanism. | Fixed & verified: failing-first test reported icon center-Ys [1205,1195,1205,1205] (10px jog); passes after fix with all icons at one Y; connector line now unbroken across the 2-line node. |
| 2026-06-25 | inputgroup | Focusing any input inside `.z-inputgroup` shifted the whole group (it "looks bigger"). The single-input Mechanism-B `:focus` rule in `input.css` (`border-width:2px` + `padding:0 calc(spacing-3 - 1px)`, correct only for a *lone* input) matches grouped children. Subtlety found by probe: `.z-inputgroup .z-textbox{border:1px}` and `.z-textbox:focus{border-width:2px}` have EQUAL specificity (0,2,0) and inputgroup.css loads later, so the *border* stays 1px — but the `:focus` *padding* (a property the base rule never sets) still wins and shrinks the child ~2px with NO border growth to offset it, so the shrink-to-fit group jumps. Measured: group[0] width −2px on focus, border unchanged `[1,1,1,0]`. Repro: Suffix text, Both sides, Input + Button, orient="vertical", multiline. | The contract had a generic "focus" state checkbox but NO outcome assertion that `.z-inputgroup` bbox at `:focus-within` equals its rest bbox. The no-layout-shift principle lived only in `reference/focus-affordance-no-layout-shift.md`, which enumerated composite inputs (datebox/timebox/`<select>`) and the combobox exception but never inputgroup — a *group of independently-styled* inputs whose children each carry their own focus rule. No Playwright test drove a real focus and measured group geometry, so the leak was invisible. | skill + contract + impl | CSS `js/zul/wgt/css/inputgroup.css`: add child-focus override (`.z-inputgroup .z-textbox:focus/:focus-visible` + combobox-input → pin `border-width:1px; padding:0 var(--zk-spacing-3)`; separate `textarea` rule re-asserts `spacing-2 spacing-3` so multiline keeps its vertical padding), group keeps its existing `:focus-within` outline ring; Skill `reference/focus-affordance-no-layout-shift.md`: add inputgroup note (a group of independently-styled inputs must neutralize each child's Mechanism-B padding/border change inside the group; the container owns the ring); Contract `doc/contracts/inputgroup.md`: add focus-bbox==rest row; Test `screenshot.spec.ts` › `inputgroup` › `no-layout-shift-on-focus`. | Fixed & verified: failing-first test reported group[0] width −2px on focus (border unchanged); passes after fix with rest bbox == focus bbox (±0) and zero border growth on every `.z-inputgroup`. |
| 2026-06-30 | scrollbar (custom/simulated — overlay + embedded, via `nativebar=false`) | ROOT CAUSE — the entire `js/zul/wgt/css/scrollbar.css` was **never loaded**: it was emitted as a standalone 1:1 `scrollbar.css.dsp` that ZK never requests (the simulated bar is drawn by the `zul.Scrollbar` helper — NO widget, NO lang.xml css-uri — so its CSS must be bundled into the global `norm.css.dsp` like notification/toast/captcha/misc; it was missing from `scripts/build-css.js` `normFiles`). The Overlay/Embedded bars rendered fully UNSTYLED — live probe of all 6 `.z-scrollbar-indicator`: radius `0px`, bg transparent, transition `ease` (CSS initial), `ruleFound: null`. Secondary (only matters once loaded): the source values were off-MD3 too — hardcoded `border-radius: 4px` + `ease` keyword vs the native bar's `--zk-shape-corner-full` (pill) + tokenised values. (Thumb COLOURS are a deliberate "bolder draggable thumb" user ruling — outline idle → on-surface-variant hover — NOT part of the gap.) | No scrollbar contract, skill entry, or Playwright test existed, and nothing ever asserted the simulated bar's CSS was even SERVED; MUI and Mira both ship native bars (zero custom-scrollbar CSS) so the evaluator had no reference, and the bar is `display:none` until hover so gallery screenshots never captured the unstyled state. The orphaned-`.dsp` failure mode is silent — the build emits the file and raises no error. | build + skill + contract + impl + test | Build `scripts/build-css.js` (add `js/zul/wgt/css/scrollbar.css` to `normFiles` — **PRIMARY fix, makes the CSS load**); CSS `js/zul/wgt/css/scrollbar.css` (4× `border-radius:4px`→`var(--zk-shape-corner-full)` on indicator/rail/both embeds; `ease`→`var(--zk-motion-easing-standard)` in the indicator transition); Skill `.claude/skills/zk-component-rules/components/scrollbar.md` (new — simulated-bar DOM tree from `Scrollbar.ts redraw()`: `.z-scrollbar.z-scrollbar-{vertical,horizontal}` › `-wrapper` › `-indicator`(+`.z-scrollbar-icon`) + `-rail`, hidden `-up/-down/-left/-right` arrows, `*-embed` placeholder shown at rest when `embed=true` & hidden on hover, `data-embedscrollbar`→`embed` default-true-since-7.0.2 gated on `nativebar=false`, **the must-bundle-globally loading trap**); Spec `doc/spec/DESIGN.md` §15 Scrollbar (pill radius, 6px thumb/8px lane, transparent track, short2 + standard easing, bolder simulated thumb tokens, must-bundle note); Contract `doc/contracts/scrollbar.md` (new — s0 loaded-rule guard + indicator/rail/embed radius+colour+size rows, overlay-vs-embedded + vertical/horizontal states); sibling sweep `_reset.css`+`frozen.css` already bundled+pill+tokenised → no edit; Test `screenshot.spec.ts` › `scrollbar` › `simulated-thumb-md3-radius-and-easing`. | Fixed & verified: failing-first probe found all `.z-scrollbar-indicator` at radius `0px` / transparent / `ease` and `ruleFound: null` (CSS absent); after adding scrollbar.css to `normFiles` + the token edits + `npm run build:css`, the same probe reads radius `9999px`, bg `rgba(0,0,0,0.23)`, easing `cubic-bezier(0.4, 0, 0.2, 1)` and the test passes. Key lesson promoted to skill: a non-widget helper's CSS (no lang css-uri) is silently dropped by a 1:1 auto-scan build — it MUST be in the global bundle list. Worth a proactive sweep for other helper/mold-only CSS files not in `normFiles`. |
| 2026-06-30 | scrollbar — embedded mode (`data-embedscrollbar=true`) | The rest `*-embed` rail and the hover `.z-scrollbar` bar disagreed on their **cross-axis** offset, so on mouse-over the bar **jumped ~5px laterally** and the thumb detached from the edge — it read as "two different scrollbars". Measured on the embedded grid (dist. of right/bottom edge from `.z-grid-body`): rest embed rail **0px** (flush, center 3px) vs hover thumb **5px** (center 8px), both axes. Cause: `*-embed` resolved flush to the edge while `.z-scrollbar-vertical` carried `margin-right:2px` AND the rail/thumb were **left-anchored** inside the lane (`rail{left:2px} indicator{left:3px}`), pushing them away from the right edge; horizontal mirror. Mechanism (confirmed `Scrollbar.ts` ~L841): ZK's scroll-sync pins BOTH the bar and the `*-embed` rail to the same cross-axis anchor inline (`right`/`bottom = -scrollPos`, flush when unscrolled), so the embed's CSS `right`/`bottom` is inert and the anchor is always shared. The jump was therefore NOT an anchor problem but thickness/inset: the 12px lane left the track inset (centred) from the shared anchor while the thin 6px embed sat flush AT it. CSS-only fix. | The 2026-06-30 scrollbar contract/skill/test (added hours earlier) asserted the thumb's radius/easing/colour and the *rest-state* embed look, but **never asserted rest-state position == hover-state position**. MD3 has no simulated-scrollbar analog, and the bar is `display:none` until hover, so no static screenshot or single-state probe ever compared the two states' geometry. The "distinct from native" redesign focused on each state in isolation. | contract + impl + test | CSS `js/zul/wgt/css/scrollbar.css`: **edge-anchor** the rail/thumb/arrows inside the lane (vertical `right:0`/thumb `right:1px`; horizontal `bottom:0`/thumb `bottom:1px`, drop `left`/`top`), drop the lane `margin-right`/`margin-bottom`, AND **size the `*-embed` rail to the 8px hover-*track* width** (not the 6px thumb) — JS force-flushes the embed anchor, so an 8px rail = the hover track's flush footprint → rest and hover share one 8px flush footprint ~4px from the edge (Option A "edge-hugging, grow-in-place", user ruling 2026-06-30); hover only refines into thumb-in-track + arrows. Contract `doc/contracts/scrollbar.md` (new s14 rest-embed cross-axis center == hover-thumb center ±1px, both axes; s11 embed=8px; State-matrix "no lateral jump rest→hover" entry). Skill `.claude/skills/zk-component-rules/components/scrollbar.md` (new "Cross-axis positioning" section: scroll-sync pins bar+embed to one anchor → fix is thickness/inset, size embed to track width). Spec `doc/spec/DESIGN.md` §15 (edge-hugging continuity ruling). Test `screenshot.spec.ts` › `scrollbar` › `embed-rail-and-hover-thumb-share-cross-axis-no-jump`. | Fixed & verified: failing-first probe measured rest embed center 3px vs hover thumb 8px (5px jump) on both axes; after the fix (8px embed + edge-anchored track/thumb/arrows + dropped lane margin, `npm run build:css`), the re-probe reads rest embed 8px flush center 4px == hover thumb center 4px == hover track 8px flush center 4px on BOTH axes (diff 0). Test RED→GREEN; screenshots confirm the rest rail and hover bar share one footprint (bar refines into thumb-in-track, no lateral movement); overlay mode + State Gallery unaffected. |
| 2026-06-30 | div, label, span, html, image, imagemap (real widgets, NO lang.xml css-uri) | The proactive sweep the scrollbar row above recommended found **6 more orphaned component CSS files**, same root cause: each is emitted as a standalone 1:1 `*.css.dsp` that ZK never requests (these widgets have no lang.xml css-uri), so their own `.z-<comp>` rules are served by NO stylesheet and the widgets render with browser/inherited defaults. Runtime probe (`document.styleSheets`, own-root-rule vs other-file compound refs) on a page that renders each: `.z-label` **0 own rules across 611 elements**, `.z-div` 0/26, `.z-span` 0/1 (progressmeter.zul), `.z-html` 0/1 (html.zul), `.z-image` 0/1 (camera.zul), `.z-imagemap` 0/1 (imagemap.zul). Biggest losses: `label.css` (font-family/size/weight/colour/word-break), `html.css` (full heading/p/a/code/pre/table normalization); `image`/`imagemap` (radius, `position:relative` for area maps); `div`/`span` minimal (`box-sizing`). | Identical silent failure mode to scrollbar (the 1:1 auto-scan emits the `.dsp`, raises no error; no contract/skill/test asserted these were SERVED). Pure static analysis (no css-uri ⇒ orphan) **over-predicted 11 suspects** — only the runtime probe separated real orphans from two false classes: (a) `select`/`cell`/`bandpopup` ARE served (own rules present) despite no css-uri — Marble's `zk.wcs` serves them via package aggregation; (b) `space.css`(`.z-space`)/`toolbarpanel.css`(`.z-toolbarpanel`) are **dead CSS** — `<space>` renders `.z-separator`, Toolbar `panel` mold renders `.z-toolbar-panel`, so those selectors match nothing. | build + test + skill + contract + guard | Build `scripts/build-css.js` (add the 6 files to `normFiles` — PRIMARY fix; add `assertNoOrphanComponentCss()` build-time guard + `WCS_SERVED_ALLOWLIST` for select/cell/bandpopup); Skill `.claude/skills/zk-component-rules/components/{div,label,span,html,image,imagemap}.md` (CSS LOADING TRAP — helper widget with no css-uri, must bundle globally); Contracts `doc/contracts/{label,html,image,imagemap}.md` (s0 loaded-rule guard + key values; div/span loaded-rule guard only); Reference note `.claude/skills/zk-component-rules/reference/component-css-must-be-bundled-or-css-uri.md` (recurring rule); Tests `screenshot.spec.ts` › `orphaned-helper-css` › `{div,label,span,html,image,imagemap}-css-is-served` (6, failing-first). CSS values themselves were correct — no CSS edits, fix is bundling only. | Fixed & verified: 6 failing-first tests (`orphaned-helper-css › <comp>-css-is-served`) reported each widget's own `.z-<comp>` root rule served by NO stylesheet while the widget rendered (`.z-label` across 611 elements, `.z-div` 26, others 1 each); after adding the 6 files to `normFiles` + `npm run build:css` all 6 selectors appear in `norm.css.dsp` and all 6 tests pass (red→green). Build-time `assertNoOrphanComponentCss()` guard added + self-tested (removing `label.css` from `normFiles` fails the build naming the orphan, exit 1); `WCS_SERVED_ALLOWLIST` registers the probe-verified served-anyway files (select/cell/bandpopup); the two dead-selector files `space.css`/`toolbarpanel.css` (selectors match no rendered element) were **DELETED** (user-approved) rather than bundled, and no dead-CSS allowlist is kept so a future matches-nothing file will trip the guard for triage. Recurring rule promoted to `reference/component-css-must-be-bundled-or-css-uri.md`. Key lesson: the static "no css-uri ⇒ orphan" heuristic over-predicted 11; only the runtime `document.styleSheets` probe separated the 6 real orphans from 3 served-anyway + 2 dead-CSS files. |
| 2026-07-01 | progressmeter, messagebox, menu (separator) | 4 redundant `!important` shipped — `progressmeter.css` `.z-progressmeter-image{display:block !important}`, `messagebox.css` `.z-messagebox-buttons{display:flex !important}` + `> *{margin-left:0 !important}`, `menu.css` `.z-menupopup-content>li.z-menuseparator{margin:… !important}`. Empirically (remove → `npm run build:css` → re-measure on the 10.3.0.1 preview app) each computed value is byte-identical without the keyword: the layered/high-specificity rule already wins (ZK sets no competing inline style; `.z-hlayout` sets no `display`; the LI-scoped selector out-specifies framework `ul>li{margin:0}`). | Nothing in the harness ever asserted whether an `!important` was *load-bearing* vs redundant, so cargo-culted overrides accumulated invisibly — screenshots (stale baselines) can't tell "same look with `!important`" from "same look without it", and no computed-style probe compared the two cascade states. | impl + test + doc | CSS: dropped `!important` (kept declarations) in the 3 files; Tests `screenshot.spec.ts › important-removal-guards › {progressmeter fill bar…, messagebox button row…, menupopup separator…}` (assert the ex-`!important` computed value still holds — RED if a future @layer/ZK change lets a lower rule win); Record `doc/spec/important-inventory.md`. Method note: the authoritative count is comment-stripped (`scratchpad/count-important.js`) — raw grep over-counts commented-out `!important`. |
| 2026-07-01 | colorbox | The `.z-colorbox > .z-colorbox-popup{display:none !important}` justification comment was pinned to "ZK 10.2.1-jakarta" and implied a desktop/mobile split that no longer describes the shipped runtime. On the current dependency (10.3.0.1) the rule is STILL load-bearing (proven: outside-tap dismiss leaves the reattached popup with inline `display:block`; removing `!important` computes `display:block`/visible on the mobile UA), but ZK 10.4-SNAPSHOT *source* already resets display on close — so a source-only reading would wrongly conclude "removable". | Version-pinned code comments rot silently across ZK upgrades; a future editor trusting the comment (or the newer source) could delete a load-bearing override. The behavior was already guarded by `tablet.spec.ts › tablet-colorbox-dismiss`, but the comment did not point to that guard or state the version it was verified against. | doc | Comment rewritten in `colorbox.css` to describe the version-independent mechanism, cite the guarding tablet test, and record "verified on 10.3.0.1 / re-check on 10.4"; kept `!important`; recorded in `doc/spec/important-inventory.md`. Structural rule (already in the approved plan): distrust any version-pinned/JS-behavior comment — re-verify against the current runtime, not the comment or newer source. |
| 2026-07-02 | button, combobutton | Icon/image-to-label spacing was zero: `<button image=..>` / `iconSclass` render `<img class="z-button-image">` / `<i class="z-icon-*">` + a plain SPACE text-node + label inside the `align-items:center` flex `.z-button`, but the base had NO `gap`, so the space collapses and the graphic sits flush against the label ("圖片與文字太近"). MD3 icon-label gap is 8dp. Same on `.z-combobutton-content` (its `image` glued to the label). | No contract row asserted a graphic-to-label gap (c1–c29 cover height/padding/colour/state, none the icon→label distance); MUI encodes it as per-element `startIcon`/`endIcon` margins, not a container gap, so the evaluator never mapped it to `.z-button`; and the collapsing plain-space separator is a ZK-render fact absent from the skill, so nothing flagged the missing space. FAB-extended (`gap:8px`) and vertical (`:has(br){gap:2px}`) each set their OWN gap, which masked that the base had none. | skill + contract + impl + test | CSS `js/zul/wgt/css/button.css` (base `.z-button{gap:var(--zk-spacing-2)}`, dedupe FAB-extended `gap:8px`), `js/zul/wgt/css/combobutton.css` (`.z-combobutton-content{gap:var(--zk-spacing-2)}`); Skill `.claude/skills/zk-component-rules/components/button.md` (graphic-to-label separator is a collapsible plain space → space with a container gap, not a margin; order-independent so it also covers `dir="reverse"`); Contract `doc/contracts/button.md` c30; Test `screenshot.spec.ts › button › graphic-and-label-have-md3-gap`. | Fixed & verified on ZK 10.3.0.1. Failing-first test measured the graphic→label edge gap at **0px** for the `iconSclass` button (`z-icon-volume-2`) on the no-gap build (RED). After adding `gap:var(--zk-spacing-2)` to base `.z-button` (deduping the redundant FAB-extended `gap:8px`) + `.z-combobutton-content`, and `npm run build:css`, the test flips GREEN (all graphic+label buttons ≥6px, target 8dp) for image / iconSclass / `dir="reverse"`; served bundle confirmed to carry the combobutton gap. Regressions checked: the 7 button variant/state screenshots + `combobox › comboitem icon…` geometry test all still pass (single-item buttons unaffected). Sibling sweep held: combobutton fixed; toolbarbutton already carried `.z-toolbarbutton-content{gap:var(--zk-spacing-1)}` so no edit; vertical (`:has(br)`) stayed 2px. Baselines regenerated (diff confirmed spacing-only, ~0.01px-ratio, localised to the With-icon + Image/Reverse rows): `button/gallery.png`, `button/tablet.png`, `combobutton/tablet.png`. |
| 2026-07-03 | combobox, bandbox, datebox | Question: clicking the INPUT text area — not just the dropdown icon — opens the popup; is that JavaScript design or a theme CSS defect? **Resolved NOT a bug, and NOT CSS.** Clicking the input opens the popup **only** for `readonly` + `buttonVisible` (intentional ZK convenience — a readonly select can't be typed into, so the whole field is the trigger). For editable it does not open on input-click (only the icon, or autodrop-on-type). For `buttonVisible="false"` **no click opens the popup at all** — ZK gates the whole-control click-to-open on `_buttonVisible` in JS, so the "No button" case already behaves exactly as expected. | Interaction (click→popup) behavior is JS-driven and has no MD3/Mira analog; no contract row asserts click-to-open gating, and the harness measures computed styles, not JS event handling. The theme's readonly `cursor:pointer` made the behavior *look* theme-owned, inviting the "is it CSS?" question. | skill (not-a-bug, no code fix) | Skill `.claude/skills/zk-component-rules/reference/buttonVisible-attribute.md` — new "Click-to-open behavior is gated by `buttonVisible`" section (ZK JS state matrix + source refs); Decision note `doc/combo-click-to-open.md`. **No CSS/JS change, no test** (per skill-feedback-loop.md, not-a-bug needs no regression test). | Confirmed by ZK 10.3 source (`inp/ComboWidget.ts:779-793` `doClick_` gate `this._readonly && !this.isOpen() && this._buttonVisible`; `:742-756` `_doBtnClick` early-return on `!this._buttonVisible`; `db/Datebox.ts:714-719` identical gate) **and** a live Playwright icon-vs-input click probe on `/combobox.zul` `/datebox.zul` `/bandbox.zul` — all three produced the same matrix (editable+btn: input=no/icon=yes; readonly+btn: input=YES/icon=yes; no-button: input=no; disabled: no click). Noted one minor deferred cursor-honesty edge (readonly + `buttonVisible=false` still shows `cursor:pointer` though nothing opens) — user chose not to fix. |
| 2026-07-07 | navbar (navitem) | The selected navitem's blue left-edge indicator renders as a curved *arc* rather than a straight bar: it is a 3px `border-left` on `.z-navitem-content`, which also carries `border-radius: var(--zk-shape-corner-small)` (8px), so the border curls at the top/bottom corners (user: "有弧形"). Fix: keep the left-accent-bar convention but draw it as a straight inset `::before` decoupled from the corner radius. | MD3 navigation uses a rounded active-indicator *container*, not a left-accent bar, so no reference prescribes the indicator's *shape*; no contract row asserted the indicator geometry and the evaluator measures colour tokens, not whether a border follows a corner radius. The apparent inconsistency vs listbox is a red herring — listbox's blue left line is its *focus* indicator (`box-shadow: inset 3px 0 0`), not its selected state (a `primary-container` fill). | skill + contract + agent-test | CSS `js/zkmax/nav/css/nav.css` (indicator switched from radius-clipped `border-left` to an inset `::before` bar); contract `doc/contracts/navbar.md` (indicator-shape row + c3/c4 tokenized); skill `.claude/skills/zk-component-rules/reference/selected-state-families.md` (new Navigation family); `doc/spec/DESIGN.md` (theme choice + focus-vs-selected note); Playwright `screenshot.spec.ts` (`navbar › selected item indicator is a straight bar` — computed-style guard, not a screenshot baseline: the arc↔straight distinction is captured numerically as "no coloured `border-left` on the radiused box + a ~3px `::after` strip"). | Fixed and verified red→green: the guard failed on the pre-fix build (`border-left width=3px alpha=1`) and passes on the fixed build; the static gallery baseline is unchanged (default navbar has no selected item). No siblings — `anchornav` has no selected-item accent bar. |
| 2026-07-07 | navbar (navitem) | Follow-up / design reversal of the row above. Viewing the straight-bar result live, the user judged that a **rounded MD3 tint container + a straight left bar** stacks two different "active" idioms (MD3 pill vs classic-sidebar rail) — redundant, and the square bar corners clash with the container's rounded corners. Decision: go **pure MD3** — the rounded tonal container is the sole active marker; **the left bar is dropped entirely**. | The earlier fix treated "keep the bar, make it straight" as settled; only a live visual review surfaced that the *bar itself* was the design problem, not just its curvature. No automated check catches "two competing selection idioms" — it is a taste/consistency judgment against MD3 Navigation Drawer / MUI ListItemButton. | contract + skill + agent-test | CSS `js/zkmax/nav/css/nav.css` (removed the `::after` bar rule; rounded tint container from the shared selected rule is now the only marker); contract `doc/contracts/navbar.md` c7 rewritten (no left accent); skill `selected-state-families.md` (Navigation family → no accent bar); `doc/spec/DESIGN.md §16` rewritten; Playwright `screenshot.spec.ts` (`navbar › selected item is a rounded tonal container with no left accent` — asserts rounded + tint fill present AND no `border-left`/`::after` accent). | Fixed and verified red→green: the guard fails on the bar build (`::after` width=3) and passes bar-removed; static gallery baseline unchanged. usecase2 Mira sidebar left as a deliberate dark-sidebar override (out of scope). |
| 2026-07-14 | checkbox (tristate mold) | `mold="tristate"` checked and indeterminate render identically as a bare unchecked box. The theme's `checkbox.css` styles only the default-mold state classes (`.z-checkbox-on` / `.z-checkbox-indeterminate`: primary fill + reveal the `::after` glyph), but ZK builds a non-default mold's state class with a **mold prefix** (`Checkbox.ts` `getMoldPrefix_()` / `getClassNameByState_()`), so a tristate wrapper carries `.z-checkbox-tristate-on` / `-off` / `-indeterminate` — which match nothing in the theme → no fill, `::after` stays `display:none`, no checkmark and no dash. | The skill's `checkbox.md` documented only **three** molds (default/switch/toggle) and never listed `tristate`, so no one knew a 4th mold with its own prefixed state classes existed; no contract row asserted the tristate on/indeterminate glyphs; and MD3/MUI have no "tristate mold" analog, so no reference ever prescribed it — the exact blind-spot `skill-feedback-loop.md` predicts ("`checkbox` toggle mold"). | skill + contract + impl + test | CSS `js/zul/wgt/css/checkbox.css` (extend the default-mold fill/`::before`/`::after`/hover/disabled selector lists to also match `.z-checkbox-tristate-on` + `.z-checkbox-tristate-indeterminate`); Skill `.claude/skills/zk-component-rules/components/checkbox.md` (tristate = 4th mold; mold-prefixed `-on`/`-off`/`-indeterminate` state classes); Contract `doc/contracts/checkbox.md` (tristate-on = checkmark+fill, tristate-indeterminate = dash+fill); Test `screenshot.spec.ts › checkbox › tristate checked shows checkmark, indeterminate shows dash`. | Fixed & verified red→green: the test failed on the unstyled build (transparent box, `::after` display none) and passes after pairing every default-mold state selector with its `-tristate-` twin. Live `/checkbox.zul` confirms the tristate row now renders unchecked=empty / checked=✓ on primary fill / indeterminate=− on primary fill (checkbox gallery baseline regenerated, 0.01 px-ratio, localized to the tristate row). No sibling molds affected (switch/toggle already had their own prefixed rules). |
| 2026-07-14 | textbox, intbox, longbox, doublebox, decimalbox, passwordbox, combobox, datebox (timezone select) | Focusing an input jitters/shifts its text ~1px. **(a)** The textbox-family base rule (`input.css`) **transitions `border-width`** (1px→2px on focus) but not the compensating `padding` (`0 spacing-3` → `0 calc(spacing-3 - 1px)`); steady-state edge is equal, but mid-animation the padding snaps while the border eases → the content edge drifts and slides back (transient jitter on focus AND blur). **(b)** combobox default+error and the datebox timezone `<select>` set `border-width:2px` on focus with **no padding compensation at all** → a permanent ~1px right-shift of the input text (the `<select>` also grows its box, violating the doc rule that `<select>` = Mechanism A). | The focus-affordance reference doc and the "focus bbox == rest bbox (±0px)" contract assertion only compare **settled** geometry, which is equal for the textbox family (padding comp is correct) — a shift that exists only *during* the transition is invisible to a rest-vs-settled probe. Combobox was explicitly flagged "do not fix" in `focus-affordance-no-layout-shift.md`, but that note only guaranteed the **root height** doesn't grow; it never addressed the in-child horizontal text shift. | skill + contract + impl + test | CSS: `js/zul/inp/css/input.css` (drop `border-width` from the base `transition`), `js/zul/inp/css/combobox.css` (drop `border-width` from `.z-combobox-input` transition + add `padding-left: calc(spacing-3 - 1px)` on `:focus-within`), `js/zul/inp/css/datebox.css` (timezone `<select>` → Mechanism A: keep border 1px, `box-shadow: inset 0 0 0 1px primary`, transition `box-shadow` not `border-width`); Skill `focus-affordance-no-layout-shift.md` (new "do not transition `border-width`" section + combobox note clarified to height-only); Contract `doc/contracts/{textbox,combobox,datebox}.md` (focus content-edge == rest ±0px, and focus `transition-property` excludes `border-width`); Test `screenshot.spec.ts › inputs › focus does not shift the content edge`. | Fixed & verified red→green across all three: the guard (`input focus (no layout shift)` — asserts `transition-property` excludes `border-width` + content edge == rest ±0px) failed on the buggy build and passes after the fix. Live probe on `/textbox.zul`: `.z-textbox` is 174px at BOTH rest and focus (border 1→2px + padding 12→11px cancel exactly — no growth, no jitter). combobox input text no longer shifts right; datebox timezone `<select>` now uses an inset box-shadow ring. Sibling sweep held: the whole textbox family (intbox/longbox/doublebox/decimalbox/passwordbox) shares the one `input.css` transition rule, so the single edit covered all; the 5 already-Mechanism-A composites (bandbox/datebox-root/timebox/spinner/doublespinner) were untouched and still pass. Regenerated 5 stale textbox-family `focus.png` baselines (pre-existing 172→174px element-width drift, unrelated to this change — proven width-neutral) + combobox `focus.png`. Full chromium suite 113/113 green. This is now guarded on every CI run, answering the "make sure it never happens a 3rd time" requirement. |
| 2026-07-14 | datebox, bandbox (open state) | Clicking the calendar-popup **icon** produces a different focus affordance than clicking the **input**. ZK adds `z-*-open` to the wrapper on icon-open; datebox's `.z-datebox-open` still used `border-width: 2px` (the layout-shift trap — with `box-sizing:border-box` + min-height-pinned children the whole field grows ~1px/side and the ring reads thicker), while its `:focus-within` had been migrated to Mechanism A (1px border + inset box-shadow). bandbox had the same split in milder form: `:focus-within` uses an **inset** ring but `.z-bandbox-open` used an **outset** `0 0 0 1px` ring (no layout shift, but a visibly different halo on icon-open vs input-focus). | Contracts asserted the `:focus-within` ring but had **no** assertion for the JS-toggled `z-*-open` emphasis state; the evaluator never opened the popup, so the icon-open path was never measured. Distinct from the 2026-07-14 row above, which covered the timezone `<select>` + textbox-family transition jitter — NOT the main wrapper's open state. | skill + contract + impl + test | CSS: `js/zul/inp/css/datebox.css` (`.z-datebox-open` → drop `border-width:2px`, use `box-shadow: inset 0 0 0 1px primary` = same as `:focus-within`), `js/zul/inp/css/bandbox.css` (`.z-bandbox-open` outset → inset ring); Skill `focus-affordance-no-layout-shift.md` (note: JS-toggled `z-*-open`/emphasis states must reuse the `:focus-within` mechanism, never a raw `border-width` bump or outset ring); Contract `doc/contracts/{datebox,bandbox}.md` (open-state op1/op2: border-width 1px + inset primary ring, identical to `:focus-within`); Test `screenshot.spec.ts › input focus (no layout shift) › datebox open state (icon click): no border-width growth, no layout shift`. | Fixed & verified red→green. Two guards added: a **behavioural** test (click `.z-datebox-button`, wait for `.z-datebox-open`, measure the wrapper) failed on the buggy build (`borderTopWidth` "2px", field grew) and passes after the fix (stays "1px", no size change); and a **compiled-CSS** guard (`datebox & bandbox open-state: inset ring, no border-width bump`) failed (datebox open rule contained `border-width:2px`, bandbox open ring was outset) and passes after. Fix: both open rules now `box-shadow: inset 0 0 0 1px var(--zk-color-primary)` — byte-identical affordance to `:focus-within`, so icon-open and input-focus are indistinguishable and the field never grows. Sibling sweep across all `js/**/*.css`: only datebox (2px) and bandbox (outset) were affected; combobox is exempt (border on children + padding comp, open & focus already matched); timepicker/timebox/spinner have no defect. Note: the pre-existing `datebox timezone <select>` test in the same block fails **on the dev/preview server only** because that server serves unminified CSS and the test's `[^}]*` regex swallows an inline comment containing the words "border-width"; it passes in CI (minified) and is unrelated to this change (that rule was untouched). |
| 2026-07-15 | coachmark | The pop-up card (`.z-coachmark-content`) was filled with `--zk-color-primary` (blue) + `--zk-color-on-primary` text. Any nested filled widget inherits the *global* filled style (itself primary-on-primary) and vanishes into the card, forcing the theme to hand-invert each control type (`.z-button`, `.z-combobutton` today) — a combinatorial burden that grows with every new slotted control. **Resolved by switching the card to a neutral MD3 rich-tooltip surface** (`--zk-color-surface-container-low`, `on-surface` text) so nested widgets render with their STANDARD styling and no inversion is needed. | The card's primary fill was an approved DESIGN choice, so no assertion questioned it; the "child controls on a colored card must be inverted" skill rule *documented* the workaround as the norm rather than flagging the root cause; MD3 has no "coachmark" so no reference mapped the composable-container pattern to its rich-tooltip guidance (neutral surface + standard text buttons). | contract + skill + impl + test | CSS `js/zkmax/nav/css/coachmark.css` (card bg/text + pointer border-color → neutral surface/on-surface; **removed** the `.z-coachmark-content .z-button` and `.z-coachmark-content .z-combobutton …` inversion blocks; close icon → `on-surface-variant`); Contract `doc/contracts/coachmark.md` (c1/c2/c5/c10 retargeted, inverse rows c17–c20 + outcome M8 removed, M1/M6 recomputed; mockup `coachmark.html` regenerated + re-approval gate); Skill `.claude/skills/zk-component-rules/components/coachmark.md` (the inversion invariant becomes *conditional* — default surface is neutral, inversion only IF a future variant re-colors the card); Test `screenshot.spec.ts › coachmark › card is a neutral surface and nested button keeps the standard fill`. | |
| 2026-07-15 | coachmark (close button) | Designer review of the top-right close `×`: it renders as a bare 16×16 glyph (no hit target, no hover state layer, no focus ring) sitting at an **asymmetric** corner inset — 4px inside the content top but 8px inside the right — so it reads as unfinished and "positioned oddly". Root cause of the asymmetry: the close is `position:absolute` against the `.z-coachmark` **root**, and ZK injects **20px** padding on the pointer side of the root (`Coachmark.ts` `_fixPadding`: `ph = 10 + borderHeight/2 = 10 + 10`, pointer border hardcoded 10px). The theme's pointer-side compensation bump was **`+16px` — 4px short of 20px**, so on a pointer-up/right card the close lands 4px (not 8px) inside the content edge. Separately the close was never given the theme's icon-button affordance. | MD3 rich-tooltip guidance prescribes *action buttons*, not a corner `×`, so no reference ever specified the close affordance or its inset; the contract asserted the close's existence/corner-pinning (M3) and colour (c10) but never its **size**, **shape (circular)**, **hover state layer**, **focus ring**, or **symmetric inset**; and the `+16px` bump was copied from ZK's default without checking it against the 20px the JS actually injects. A rest-vs-settled geometry probe can't flag "feels cramped" — only a designer's eye + an explicit symmetric-inset assertion catches it. | skill + contract + impl + test | CSS `js/zkmax/nav/css/coachmark.css` (`.z-coachmark-close` → `--zk-control-height-xs` circular target, flex-centered, `surface-container` hover state layer, `--zk-focus-ring` on `:focus-visible`; pointer bumps `+16px`→`calc(spacing-2 + spacing-5)` = 20px; content `padding-right` reserves 44px for the 28px circle); Skill `.claude/skills/zk-component-rules/components/coachmark.md` (record: root gets 20px JS-injected padding on the pointer side → close pointer-side offset compensation MUST be 20px, and the close should reuse the theme icon-button affordance); Contract `doc/contracts/coachmark.{md,html}` (new close expected values + outcome assertion for circular affordance + symmetric 8px content-corner inset); Test `screenshot.spec.ts › coachmark › close button is a circular icon-button target with symmetric corner inset`. | Fixed & verified red→green. Two guards added: `close button is a circular icon-button target with symmetric corner inset` (failed on the old build — width 16<24, and 4px/8px asymmetric inset — passes now: 28px circle, radius 9999px, 8px/8px inset) and `close button shows a hover state layer` (old build had no `::before` → rest opacity 1≠0; now 0→0.08 on hover). Live re-measure: close 28×28, `border-radius:9999px`, `top/right` insets 8px/8px, card `padding` `16px 44px 16px 16px`; hover paints the on-surface `::before` state layer, `:focus-visible` shows the primary ring. Sibling sweep: combobutton + tour-step coachmarks on the page all render pointer-up with the same symmetric 8px/8px inset; pointer-down/left inject padding away from the top-right corner (no bump needed) and pointer-right reuses the same +20px, so all four directions land symmetric by construction (`_fixPadding` injects a uniform `pw=ph=20px`). Full chromium suite 118/118 green; coachmark group stable 9/9 over `--repeat-each=3`. Mockup `coachmark.html` regenerated to match; `contract-approved` flipped to false pending re-approval of the close-affordance change. |
| 2026-07-20 | daterangebox | The input shows large symmetric left/right whitespace around the date and the whole field is ~2× datebox width (measured: root 398.6px, each input 172px, value "Jan 10, 2025" ≈ 78px centered → ~35px gutter/side). Root cause is **NOT padding** (both daterangebox & datebox use `padding: 0 12px` = `--zk-spacing-3`): `.z-daterangebox-input` has `text-align: center` + `flex: 1` and the two `<input>`s carry no `size`/`width` (ZK `Daterangebox.ts` `redraw()` writes only class/aria/placeholder/autocomplete), so each falls back to the UA-default ~20ch (~172px) intrinsic width and the short date sits centered with wide gutters. datebox is single + left-aligned, so its gutter is one-sided and far less visible. | The contract asserted the input's centered text (Design prose) and font-size (c5) but **never constrained input width or the gutter**; MD3/MUI has no two-input date-range analog (MUI `SingleInputDateRangeField` is one left-aligned `OutlinedInput` at 14px padding), so no width target was ever measured; and daterangebox had **no Playwright `describe` block at all** — only a render-smoke 200-check — so nothing could observe the width. | skill + contract + impl + test | CSS `js/zkmax/db/css/daterangebox.css` (`.z-daterangebox-input`: `flex:1`→`flex:0 1 auto`, add `field-sizing:content`, `min-width:0`→date-fit floor so inputs hug their content instead of the UA ~20ch default); Skill `.claude/skills/zk-component-rules/components/daterangebox.md` (record: the two inputs ship with no `size`/`width`, so a theme MUST bound their width or each renders ~20ch and, centered, shows large symmetric gutters); Contract `doc/contracts/daterangebox.md` (new outcome M-row: a seeded `.z-daterangebox-input` bbox width is content-fitted, <130px; new Expected-value row for `field-sizing`); Test `screenshot.spec.ts › daterangebox › input hugs its date content (not the UA ~20ch default)`. | Fixed & verified red→green on /daterangebox.zul. The guard failed on the old build (seeded begin input **172px** > 130px) and passes now (**102.4px**; `field-sizing: content`, `min-width` floor `84.5px` = 6.5em). Root width **398.6px→258.9px** (−35%); empty field 398px→223.6px. Live screenshot confirms seeded ranges read compactly with no clipping and the `showTime` variant auto-widens for the time text. Full date family green (7/7: datebox timezone/open-state/gallery/hover/focus + daterangebox). **Sibling sweep — no code change:** datebox/combobox/bandbox/timebox/spinner are single-input, and combobox/bandbox are free-text where content-hug would jitter per keystroke; datebox is the user's stated reference baseline, so the fix is intentionally scoped to daterangebox only. `field-sizing` degrades to the prior ~20ch look where unsupported (no regression). |
| 2026-07-20 | datebox, timebox | DateBox does **NOT** hug its content either. `.z-datebox-input` uses `flex: 1; min-width: 0` (grow-to-fill) with **no** `field-sizing`/width/`min-width` floor, and ZK emits the input size-less (`InputWidget._cols=0` → `ComboWidget.redraw_` writes only class/aria/autocomplete). In practice `flex:1;min-width:0` collapsed the input to a **fixed ~108px** that ignored content — empty, short date and long-format date all rendered the same width — and **clipped** long formats (`yyyy/MM/dd HH:mm` "2025/01/15 00:00" → scrollWidth 132 > clientWidth 108, measured). TimeBox shares the identical collapse. **This corrects the 2026-07-20 daterangebox row's premise** that "datebox is the content-hugging reference baseline" — datebox never hugged (it uses a fixed collapse, not content-fit). The over-wide *symmetric gutters* were daterangebox-specific (two centered inputs at ~172px); datebox's own defect is the content-insensitive fixed width plus the long-format clipping (NOT a 172px over-width — a bare datebox shrink-wraps to ~146px root / ~108px input). | No datebox/timebox contract or DESIGN.md rule ever constrained or measured input width — both contracts just delegate to combobox c1–c8 metrics, none of which cover width. MUI's single date/text field has no content-hug analog (it fills its container / UA width, left-aligned), so no width target was ever derivable from the reference. And the defect was masked by left-alignment: one-sided trailing whitespace reads as "a normal-width field", so no screenshot review flagged it — the daterangebox fix even codified the wrong premise into its sibling-sweep note. | skill + spec (DESIGN.md) + contract + impl + test | CSS `js/zul/inp/css/datebox.css` + `js/zul/inp/css/timebox.css` (`.z-{datebox,timebox}-input`: `flex:1;min-width:0` → `flex:1 1 auto; field-sizing:content; min-width` date/time-fit floor — basis=content hugs in an auto context yet still fills an `hflex`/width-forced root, unlike daterangebox's `flex:0 1 auto`); Skill `.claude/skills/zk-component-rules/components/combo-trio.md` (family-level: all combo-trio inputs ship size-less → a theme MUST set an input-width policy); Spec `doc/spec/DESIGN.md §10` (theme policy — content-hug fixed-format date/time fields; keep free-text combobox/bandbox + numeric spinner at UA-default/fill to avoid per-keystroke jitter); Contract `doc/contracts/{datebox,timebox}.md` (new outcome M-row: seeded `.z-{comp}-input` bbox width content-fitted <130px/<120px; new Expected-value rows for `field-sizing` + `flex`/`min-width`); Preview `pv/{datebox,timebox}-content.zul` (add a seeded plain instance to measure); Test `screenshot.spec.ts › datebox › input hugs its date content` + `› timebox › input hugs its time content`. **Sibling sweep — no code change:** free-text combobox/bandbox (content-hug jitters per keystroke) + numeric spinner (variable length) intentionally excluded; daterangebox already done. | Fixed & verified red→green on /datebox.zul + /timebox.zul. Guards failed pre-fix (computed `field-sizing: fixed`; datebox long-format clipped, long == short width) and pass after (`field-sizing: content`; long not clipped, long > short). **Empirical re-measure corrected the premise:** a bare datebox was never 172px — the old `flex:1;min-width:0` collapsed it to a fixed ~146px root / ~108px input regardless of content and clipped `yyyy/MM/dd HH:mm` (scrollW 132 > clientW 108). After the fix: short 108→102px, empty 108→85px (6.5em floor), long 108→132px (no clip); an `hflex=1` datebox still fills its container (362px in a 400px parent), so form layouts are unaffected. Recipe uses `flex:1 1 auto` (not daterangebox's `0 1 auto`) precisely to preserve hflex fill. Full date family green (datebox/daterangebox/timebox). |
| 2026-07-20 | timepicker | Similar-case sweep of the datebox/timebox content-fit fix: **timepicker also clips**. It's a fixed-format, readonly time field, but ZK **hardcodes the input at `size="5"`** (fits `HH:mm` only). Under the old `.z-timepicker-input { flex:1; min-width:0 }` with no `field-sizing`, an `HH:mm:ss` value ("10:30:00") is **clipped** (`scrollWidth 79 > clientWidth 66`, measured) whenever the root has no explicit width — every preview cell masked it with `width="160px"`. | timepicker had **no Playwright `describe` block at all** (zero geometry coverage); the datebox/timebox sibling-sweep note enumerated datebox/combobox/bandbox/timebox/spinner but **never listed timepicker** (a separate `zkmax/inp` component/file); and every preview instance forced `width="160px"`, hiding the clip from screenshot review. | impl + preview + test | CSS `js/zkmax/inp/css/timepicker.css` (`.z-timepicker-input`: `flex:1;min-width:0` → `flex:1 1 auto; field-sizing:content; min-width:5em` — mirrors timebox; readonly input → no keystroke jitter); Preview `pv/timepicker-content.zul` (add one seeded **auto-width** `HH:mm:ss` cell that exposes the clip); Test `screenshot.spec.ts › timepicker › input hugs its time content (field-sizing, no clip)` (new describe block — order-independent: no seeded input may clip, all must report `field-sizing:content`). | Fixed & verified red→green on /timepicker.zul. RED: input `field-sizing: fixed`; the auto-width `HH:mm:ss` cell clipped (`size="5"` → clientW 66 < scrollW 79). GREEN: `field-sizing: content`; that cell now hugs its value (rootW 116px, no clip); all 7 seeded inputs content-sized, none clip; the `width="160px"` instances still fill (`flex:1 1 auto`). Date/time family 14/14 green; `timepicker-forced-colors` baseline regenerated for the new cell (`timepicker-gallery` unchanged within tolerance). **Found via** the similar-cases audit (`doc/content-fit-similar-cases-audit.md`): combobox/bandbox/chosenbox (free-text) + spinner/doublespinner (numeric) correctly excluded (content-hug would jitter); colorbox/paging/listbox use by-design fixed widths. |
| 2026-07-22 | datebox, daterangebox, confirmpopup | Under `forced-colors: active` (Windows High-Contrast) the datebox calendar popup has **no border**: `.z-datebox-popup` is a floating surface whose only boundary is a `box-shadow: var(--zk-elevation-dialog)`, which WHCM strips — so the popup (and, in the time/timezone variants, the calendar + time section it wraps) merges into the page. Sibling sweep found the identical shadow-only `border:none` popup on **daterangebox** (`.z-daterangebox-popup`) and **confirmpopup** (`.z-confirmpopup`), the latter's CSS-triangle arrow (`.z-confirmpopup-arrow`) also collapsing into an opaque square without `forced-color-adjust:none`. | The `check:forced-colors` linter scores coverage **per file** (a file is "covered" if ANY of its `.z-*` classes appears in the guard), so `datebox.css` — already listed for its `:focus-within` outline (block 1b) — masked its *second* fragile pattern, the `.z-datebox-popup` elevation, and the linter reported 0 gaps. A per-rule-block check can't be adopted cleanly: static CSS can't distinguish a same-element modifier (`.z-window-overlapped`, covered via the `.z-window` on the same root) from a child surface needing its own entry (`.z-datebox-popup`), so per-block scoring adds false positives (window modes, decorative `-shadow` casters, native `::picker(select)`). | impl + test (+ linter limitation logged) | CSS `zul/css/tokens/_forced-colors.css` — add `.z-datebox-popup`/`.z-daterangebox-popup` to the (1a) input-popup border list, `.z-confirmpopup` to the (1a-cont) floating-popup list, and `.z-confirmpopup-arrow` to the (1d) `forced-color-adjust:none` beak list (mirrors coachmark/errorbox); Test `src/test/playwright/forced-colors.spec.ts › datebox popup gains a border (box-shadow elevation is stripped)` + `› confirmpopup gains a border and keeps its arrow (elevation + CSS-triangle beak)`; Coverage note `doc/spec/forced-colors.md`. | Fixed & verified red→green: both tests failed pre-fix (`.z-datebox-popup` / `.z-confirmpopup` `border-top-style: none`) and pass after; full forced-colors project 13→15 green; linter 163→167 guard classes, still 0 gaps. Sibling sweep enumerated every elevation-`box-shadow` rule across `js/**/*.css` — the only real child-popup gaps were datebox/daterangebox/confirmpopup; `.z-window-*` modes (element also carries covered `.z-window`), `.z-menu*-shadow`/`.z-panel-shadow` (decorative casters; content class already covered), and `::picker(select)` (OS-rendered / already bordered) are false positives, so the linter was **not** rewritten — its per-file scoring is the residual harness gap and the Playwright assertion is the reliable gate for child-popup elevation surfaces. The pre-existing in-popup `.z-calendar` border (unchanged) merges with the new frame, so no card-within-a-card. |
| 2026-09-04 | navbar, nav, navitem | Keyboard focus on a navbar item was never styled, so Chrome's own ring stood in for it — and that ring is OUTWARD (`outline-style: auto`, 1px, rgb(0,95,204), `outline-offset: +1px`), so it was visibly **cut off**. Designer repro: expand "Get Started" → click "Step One" → press Space. Measured: the item's box (x 32 → 208.17) is exactly the box of the submenu cave `.z-nav > ul` that holds it, and that cave carries `overflow: hidden`, so the ring overshot its clip rect by 2px on every side. A second, quieter half: with no `:focus-visible` rule at all, the ring was also off-palette (UA blue, 1px) rather than `--zk-focus-ring`. | The navbar contract had no focus row of any kind — "States to evaluate" listed only default / selected / hover, so no evaluator run ever looked at focus, and a missing rule is indistinguishable from a correct one in a static screenshot (the UA draws a plausible-looking blue ring either way). Nothing in the skill said that an outward ring is unsafe on a full-bleed item; `reference/focus-affordance-no-layout-shift.md` covers focus that changes SIZE, not focus that gets CLIPPED, and its one sentence about clipping was buried in the box-shadow section. navbar also had no `components/` file at all, so the DOM facts that make the clip inevitable (unclassed cave sitting on the item's own edges, two content classes) were nowhere. | skill + contract + impl | CSS `js/zkmax/nav/css/nav.css`: `.z-nav-content:focus-visible, .z-navitem-content:focus-visible { outline: var(--zk-focus-ring); outline-offset: -2px }` — inset, same mechanism as `.z-treerow`; covers BOTH content classes because za11y's roving tabindex means arrow keys focus group headers too. CSS `zul/css/tokens/_forced-colors.css` block (2a focus): `outline-color: HighlightText` on a selected+focused navitem, because selection fill and `--zk-focus-ring` both remap to `Highlight` (measured RGB delta 78 → invisible; 687 after). Skill NEW `reference/focus-ring-clipping.md` (at-risk DOM shapes, the two working mechanisms, how to assert ring GEOMETRY not colour) + NEW `components/navbar.md` (cave/content/state-class/za11y-keyboard facts), both registered in SKILL.md. Contract `doc/contracts/navbar.md` rows c8-c11 + focus states. Tests `screenshot.spec.ts` › `navbar` › `submenu item keyboard focus ring is the theme inset ring and is not clipped` (asserts outline-style solid / 2px / primary / offset ≤ 0, and walks clipping ancestors for geometric containment) and `forced-colors.spec.ts` › `focus ring on a SELECTED navbar item stays visible (Highlight-on-Highlight)`. | Fixed & verified. Failing-first: the geometry assertion reported "ring is clipped by UL (unclassed, inside .z-nav) (overflow hidden/hidden) — ring overshoots its clip rect by 2px" plus outline-style `auto`, width 1, offset +1; all green after the fix. Forced-colors test proven RED→GREEN by temporarily removing the (2a focus) block (delta 78, threshold 200). NOT SWEPT: block (2a) fills five other selected-tint families with `Highlight` — `.z-treerow`, `.z-listitem`, `.z-paging-selected`, accordion `.z-tab-selected`, `.z-searchbox-selected`, `.z-orgitem-selected` — and any of them that draws focus with `outline` has the same Highlight-on-Highlight collision. Only navbar was measured and fixed here (blast radius of this change). `.z-treerow` is the near-certain next one (same `outline` + negative-offset mechanism); `.z-listitem` is a different pre-existing gap, since its focus marker is a `box-shadow` and forced-colors strips those outright. None of the five were measured — see the report's decision item. Also unswept: the 66 component CSS files with no `:focus-visible` rule at all (`for f in $(find src/main/resources/web/js -name '*.css'); do grep -q focus-visible $f \|\| echo $f; done`), most of them non-focusable widgets but not all. |
| 2026-09-06 | button, toolbarbutton, a, hlayout, linelayout, caption, carousel, toolbar | Follow-up sweep for the 2026-09-04 navbar row (decision D13, option A). Built a scan that finds clipped focus rings mechanically instead of waiting for a design review to report them one at a time, and ran it across every preview page. **13 distinct (element ⊂ clipper) pairs, 77 occurrences, 11 pages** — all the same shape as navbar: an OUTWARD ring (`outline-offset: 2px`) against a container that clips overflow. Worst group is `.z-button` inside `.z-hlayout` / `.z-lineitem` / `.z-caption-content` / `.z-carousel` (measured on hlayout.zul: button box 187→223, hlayout box also 187→223, so the whole 4px ring above and below is outside the clip — completely invisible, not merely cropped). Second group `.z-toolbarbutton` inside `.z-toolbar` / `.z-toolbar-popup`. Third is the preview pages' own `overflow-x: auto` state-matrix row (`overflow-x` makes the y axis compute to `auto` too, so it clips top/bottom as well). | The harness only ever measured what a contract row named, and no contract named focus geometry; the 2026-09-04 navbar fix added the rule to ONE component's contract, which by construction could not surface siblings. Nothing existed that could enumerate the population at risk. The generalisable obstacle was reachability, not measurement: driving each widget's focus by scripted Tab/click does not work uniformly (navbar's roving tabindex being the proof), so every previous attempt at breadth would have been per-component anyway. | agent/harness + doc | NEW `src/test/playwright/focus-ring-scan.spec.ts` + `focus-scan` project + `npm run test:focus-scan`. Reads the LOADED stylesheets for `:focus-visible` rules that draw an outline, forces the pseudo-class through CDP `CSS.forcePseudoState` (no interaction needed — this is what makes it general), then inflates the rect by offset+width and walks the clipping ancestors. Second pass repeats it under forced-colors for selected+focused items, hit-testing the ring band to find what the ring is really painted on. Baseline `doc/focus-ring-known-clips.json` (fails on a NEW finding and on a baseline entry that no longer reproduces, so it stays a shrinking backlog rather than a waiver list; regenerate with `FOCUS_SCAN_UPDATE=1`). Backlog + fix options written up in `doc/focus-ring-clip-backlog.md`. | Harness verified in both directions (drop a baseline entry → "NEW clipped focus ring"; add a bogus one → "these clips are FIXED"), and the forced-colors pass verified to still go RED when the navbar (2a focus) guard is removed. 3 consecutive full runs green (55 passed / 47 skipped). NO CSS was changed by this row: the 13 pairs are recorded, not fixed, because the navbar remedy does not transfer — `--zk-focus-ring` and a filled button's fill are BOTH `--zk-color-primary`, so an inset ring on `.z-button` is primary-on-primary and would trade a clipped ring for an absent one. Needs a decision between dropping `overflow:hidden` where it is not load-bearing (only hlayout/lineitem qualify), a two-tone WCAG 2.2 focus indicator, or per-context overrides. TWO CORRECTIONS made during the run, both caught before landing: (1) the stylesheet walk found 0 rules because current Chrome exposes `cssRules` on a plain CSSStyleRule (CSS Nesting), so "has cssRules" is not "is a grouping at-rule"; (2) the forced-colors pass first reported paging as a defect by comparing the ring against the selected button's own fill — its ring is OUTSET and is painted on the bar behind the button, so it is fine. Also checked and clean: tree row, organigram node, anchornav. Noted but out of scope: accordion `.z-tab` and `.z-listitem` have NO outline focus at all (state-layer `::before` / inset `box-shadow`), both of which forced-colors strips — their focus is invisible in high contrast regardless of selection. |
| 2026-09-08 | codeeditor | js-source drift detected — contract hash `7041c98df9ada3a34d15341994baf153faccd316620782a12b2d51ff5545fb80` does not match recomputed hash `d4b94046a72e6433250872ff9f517f8c032d33329d2b1df9711e5d8df0f3837d` for `Codeeditor.ts` + `mold/codeeditor.js`. Root cause: commit `f6429e7f8bb943b45151f26aadfd94fb86d3f106` ("fix ZK-6086: Codeeditor theme collides with ZK's page theme", 2026-09-02) renamed the widget's light/dark surface API on both the TS client widget and the Java server component — `theme`/`getTheme`/`setTheme` → `colorScheme`/`getColorScheme`/`setColorScheme`, no back-compat alias kept. The preview ZUL (`src/test/resources/web/codeeditor.zul`) still authors all five dark-mode instances with the old `theme="dark"` attribute, which no longer binds to any Java setter — the contract's "dark surface" state-matrix rows may be unreachable in the live preview as currently authored, and the skill file's "Attribute support" section documents the now-removed `theme=` attribute name. | §0b js-source-hash drift detection gate fired; measurement refused before any CSS row, macro assertion, or screenshot was attempted. | js-drift | doc/contracts/codeeditor.md — re-run `zk-spec-author codeeditor` to update the hash, rename `theme=`→`colorScheme=` throughout the skill file and contract prose, and fix the preview ZUL's five `theme="dark"` attributes before the next Gate-1 pass. |
| 2026-09-08 | codeeditor | **Correction to the `js-drift` row directly above — that block was raised against the wrong artefact.** Gate 0b recomputes `js-source-hash` over the ZK **source working tree** (`/Users/hawk/Documents/workspace/ZK10/zk/zul`), not the jar on the classpath. That tree has advanced past the pinned release, so the `theme`→`colorScheme` rename (`f6429e7f`, 2026-09-02) shows as drift even though the shipped `zul-11.0.0-jakarta.FL.20260904.jar` still exports `getTheme`/`setTheme`/`THEMES` (verified: `unzip -p … Codeeditor.class \| strings`). `theme="dark"` binds today, the five dark preview instances render, and contract rows c22–c31 are reachable — measurement should not have been refused. Two things are nonetheless real: (a) the rename lands in the next ZK drop, so the preview ZUL / skill attribute section / contract prose will need `colorScheme=` then; (b) gate 0b is a false-positive generator for **every** component whenever the local ZK checkout is ahead of the pinned jar. | The gate compares against a git working tree that nothing pins to the dependency coordinate, so "source differs from contract" is conflated with "the running widget differs from contract". Only the second is a reason to refuse to measure. | agent/harness | Evaluator §0b must hash the widget JS **as served** (or from the resolved jar) rather than the ZK source checkout; until then a 0b failure needs an artefact check before it blocks. Eval report `doc/harness/eval-reports/codeeditor.md` corrected in place. | Verified against the jar, not the checkout: `setTheme`/`getTheme`/`THEMES` all still present in `zul-11.0.0-jakarta.FL.20260904.jar`, and the live probe measured both the light and dark editors on the running preview. Gate 0b left unchanged in this pass (harness edit, not a theme fix) — recorded here so the next 0b block is checked against the jar before it routes anyone to `zk-spec-author`. |
| 2026-09-08 | codeeditor | Designer report, two cases on `/codeeditor.zul`. (1) Focused **default** editor: the ring is visibly thinner on the line-number edge. (2) Focused **dark** editor: the ring is unreadable against the black fill. Measured with an `elementFromPoint` probe 1.5px inside the border edge on each side, walking to the first ancestor painting a non-transparent background: the light editor is occluded on `left` by `.cm-gutters` (opaque `rgb(247,249,252)`, flush to the border because the root has zero padding) → 1px there vs 2px on the other three sides. The dark editor is occluded on **all four** sides, because `.z-codeeditor-dark .cm-editor` sets an opaque background — so the inset pixel is completely covered and the ring collapses to the bare 1px border, which is `--zk-color-primary` `#376fd0` at only **3.45:1** against the `#1e1e1e` fill. Case 2 was therefore worse than reported: not just a weak colour but a half-width ring. | **The rule already existed and was not applied.** `reference/focus-affordance-no-layout-shift.md` has carried the "an opaque child background occludes a root inset ring" caveat and the `::after`-overlay remedy since the timepicker case (2026-06-09) — codeeditor was built on the plain root inset ring without ever checking whether its children paint opaque, and the CodeMirror payload does (gutter always; whole editor in dark mode). No contract row asserted ring uniformity, and the evaluator reads the root's **computed** `box-shadow`, which says `inset 0 0 0 1px primary` whether or not anything paints over it — a computed value is not a visible value. The dark ring colour was reasoned about as "3.45:1 clears the 3:1 UI bar" without noticing MD3 prescribes a lighter primary tone on dark surfaces. | contract + impl + test | CSS `js/zul/code/css/codeeditor.css` (root `position:relative`, ring moves off the root onto an always-present `.z-codeeditor::after` overlay painted above the children — same shape as timepicker; dark focus uses its own knob); Token `zul/css/tokens/_component-theme.css` (new `--zk-codeeditor-dark-border-color-focus: oklch(from var(--zk-color-primary) 0.8 c h)` — MD3 dark-scheme tone 80, hue-consistent under a brand override); Skill `reference/focus-affordance-no-layout-shift.md` (add codeeditor to the overlay applies-to list + record the third-party-payload trigger) and `components/codeeditor.md`; Contract `doc/contracts/codeeditor.{md,html}` (ring-uniformity + dark-ring rows; mockup redrawn → `contract-approved` back to false); Test `screenshot.spec.ts › codeeditor › focus ring is uniform on all four sides` + `› dark surface focus ring is legible against the fill`. | Fixed & verified RED→GREEN. RED on the current build: light `{top:2,right:2,bottom:2,left:1}`, dark `{top:1,...}`. GREEN after: both editors 2/2/2/2, light ring `#376fd0` on white (4.83:1), dark ring `#80bdff` on `#1e1e1e` (**8.45:1**, was 3.45:1). Guards assert **painted pixels** — the shot is decoded on a canvas inside the page and each edge's ring run-length counted from the midpoint — precisely because `getComputedStyle(root).boxShadow` reported `inset 0 0 0 1px primary` in both broken states; a computed-style guard would have been green throughout. **Sibling sweep — no code change:** the same occlusion probe was run against every component drawing an inset ring (datebox, timebox, spinner, bandbox, daterangebox, selectbox, timepicker, listbox) and found **no occlusion outside codeeditor** — the others keep padding between border and children, and timepicker was already on the overlay. Regression: chromium 125 passed / forced-colors 17 / focus-scan 55 (codeeditor adds no clipped-ring entry) / codeeditor gallery baseline unchanged (the ring only paints on focus). Two PRE-EXISTING failures confirmed unrelated: `multislider › hover` fails identically with these CSS changes stashed (`.z-sliderbuttons` intercepts the pointer), and focus-scan `tree-header` is a CDP `forcePseudoState` timing flake that passes on re-run. Also corrected while in the mockup: `codeeditor.html` still put `pointer-events:none` on `.cm-editor`, contradicting the already-agreed c21 (root). `contract-approved` returned to **false** — the ring mechanism and the dark focus colour both changed, so the visual contract needs re-approval (coachmark precedent, 2026-07-15). |
| 2026-09-08 | codeeditor | First real Gate-1 measurement (46/49 PASS) found two more gaps behind the focus-ring fix. (1) **c19/c27 asserted a caret that never renders.** The CSS coloured `.cm-cursor`/`.cm-dropCursor` via `border-left-color`, but those nodes are created by CodeMirror's `drawSelection` extension and the CE build's `_baseExtensions()` does not load it — confirmed in the shipped jar and by counting nodes in a live focused editor (**0**). The rules were dead code. Measured consequence the eval report did not state: because `caret-color` does **not** inherit from `color`, the browser's native caret falls back to the UA default — pure `rgb(0,0,0)` on the light surface and `rgb(255,255,255)` on dark, against theme foregrounds `rgba(0,0,0,.87)` / `#d4d4d4` — so the caret has been off-palette the whole time and the rules intended to fix that never once applied. (2) **No CTV regression net**: `component-theming.spec.ts` had zero codeeditor tests and `component-theming.zul` had no codeeditor instance, so all 15 knobs were unguarded. | (1) The skill already said the CE build omits `drawSelection` — in the bullet about `.cm-activeLine`/`.cm-selectionLayer` — but a **separate** bullet three paragraphs down described `.cm-cursor` as a legitimate styling surface, and the contract was written from that second bullet. Two true-in-isolation statements that contradict each other, with nothing forcing them to be read together. No screenshot could catch it either: a caret is 1px wide, blinks, and a plausible-looking one renders whether or not the theme rule applies. (2) The CTV cross-cutting checklist was satisfied by *declaring* the knobs in `component-theme-variables.md`; nothing checked that a test existed, so `ctv: shipped` was recorded while the test count was zero. | skill + contract + impl + test | Skill `components/codeeditor.md` — the `.cm-cursor` bullet now states the nodes are NOT rendered, records the `caret-color`-does-not-inherit measurement, and the "legitimate styling surface" list is corrected to exclude them (the contradiction is removed, not just annotated); CSS `js/zul/code/css/codeeditor.css` — both dead rules deleted, the reason recorded under the file's existing "NOT STYLED ON PURPOSE" header so a future pass cannot re-add them; Contract `doc/contracts/codeeditor.{md,html}` — c19/c27 **retired** (ids not reused, with a note explaining the gap), mockup caret hard-coded to the UA colours it actually renders as; Test `component-theming.spec.ts` › `codeeditor — regional bg/border/gutter override…` + `› codeeditor — whole-app :root override wins…`, plus two codeeditor instances in `component-theming.zul`. | D7 = option B (clean up the dead code, accept the UA caret) and D8 = option A, both user decisions 2026-09-08. The caret is a **knowingly accepted deviation**, recorded in three places so it does not read as an oversight next time; if it is ever revisited the fix is `caret-color` on `.cm-content`, never a `.cm-cursor` rule. CTV guards verified by **mutation**, not just by passing: dropping the `!important` from the light `.cm-gutters` background (the one thing that lets the theme beat CodeMirror's unlayered StyleModule) turns both tests RED, and restoring it turns them GREEN — so they genuinely guard the knob rather than the default. `component-theming-gallery.png` baseline regenerated for the two added instances. Regression after the removal: codeeditor chromium 2/2, CTV 2/2, gallery 1/1, forced-colors-gallery 1/1. |
| 2026-09-09 | multislider | Every knob of a multi-range multislider is unreachable **at its own centre** — the point a user aims at. `<sliderbuttons>` is a child widget, so N ranges render N sibling `.z-sliderbuttons` divs and the theme stretches each across the full rail (`top:0;left:0;width:100%;height:100%`); with no `z-index` anywhere they resolve by DOM order, so the LAST range's container paints over every earlier range's knobs and swallows the pointer. Measured on `/multislider.zul`: 4 of 6 knobs dead on the 3-range horizontal slider, 2 of 4 on the 2-range vertical — `2(N-1)` of `2N`, i.e. only the topmost range is grabbable. The knob is 20px and the intercepting overlay only 4px, so the edges still respond and the widget looks and behaves fine unless you aim at the middle. | Three compounding reasons. (1) **Every preview instance that mattered had one range.** The four single-range multisliders further down the same page are genuinely healthy — the defect cannot exist below two ranges — so the only broken shape on the page was the one shape nothing asserted. (2) **No check can see it.** Contract rows, computed styles and screenshots all pass: the knob renders at the right place in the right colour, and `getComputedStyle` says nothing about who owns the pixel. (3) **The one test that did catch it named the wrong culprit.** `screenshot.spec.ts › multislider › hover` failed as `locator.hover: Timeout … <div class="z-sliderbuttons"> intercepts pointer events`, which reads as a flaky-hover/test problem and points at a rail, not at a sibling range; it had been triaged twice as a pre-existing unrelated failure (see the 2026-09-08 codeeditor row) without the mechanism being read out. | test + impl | Test NEW `src/test/playwright/hit-target.spec.ts` + `hit-target` project + `npm run test:hit-target` — asserts `elementFromPoint(knob centre) === knob` for **every** knob (not `.first()`: the topmost range always passes, so a spot check is a coin flip) across all three slider variants (multislider, rangeslider, plain slider). CSS `js/zkmax/slider/css/multislider.css` — `.z-multislider .z-sliderbuttons-button { z-index: 2 }`, scoped for the `.z-sliderbuttons-*` name collision already documented in rangeslider.css. Analysis + by-hand repro `doc/multislider-knob-hit-target-analysis.md`. | Fixed & verified RED→GREEN (guard written before the CSS): RED listed exactly 6 knobs, all naming the same blocker; GREEN after one declaration. `multislider › hover` now passes; multislider/rangeslider hover+focus and all three gallery baselines **unchanged** (the restacked elements share the accent colour); 6 CTV knob-contract tests pass; a real drag from a previously dead centre moves 70%→90% with no other knob touched. One declaration suffices because neither `.z-multislider` nor `.z-sliderbuttons` establishes a stacking context (positioned, `z-index:auto`), so containers and knobs compete in one context — `-area` deliberately gets **no** `z-index`, since knobs at 2 already outrank every area. **Two guard bugs caught before landing, both of general use for the sweep below:** (a) `elementFromPoint` takes viewport coordinates and returns `null` outside them, so every slider below the fold reported a phantom failure until each knob was `scrollIntoView`'d and its rect re-read — an unscrolled hit-test measures the fold, not the CSS; (b) disabled widgets inherit `pointer-events:none` and must be skipped via `getComputedStyle(el).pointerEvents`, not asserted. **NOT SWEPT (deferred 2026-09-09, D2 = option A, paused for the zk-repo migration):** the guard covers sliders only. The signature to hunt is narrower and more useful than "absolutely-positioned child with no z-index" — it is *a widget that renders one full-size positioned overlay per child widget*, which is what turns a missing `z-index` into a dead hit target. Such a component is invisible to every style check AND only breaks with two or more children, so the sweep needs multi-instance preview cases, not just more assertions. |
