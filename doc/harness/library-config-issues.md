# Library-config issues log

Append-only. T3 components only. The Orchestrator appends a component, the failing check ids and
the reason here when a fix requires widget `.ts`/`.java` or third-party library configuration
rather than CSS — terminal for this harness. See `doc/orchestrator-playbook.md` and
`doc/verification-harness-decisions.md`.

Empty is a valid state: it means nothing has been escalated as non-CSS-fixable since this log was
created.

| date | component | check ids | reason | upstream ref |
|------|-----------|-----------|--------|--------------|
