# Component Theme Variables — 進度追蹤 (Progress Tracker)

> **這份文件追蹤「哪些元件已宣告可覆寫的 `--zk-<comp>-*` 變數、且滿足檢驗條件」。**
> 規範(功能定義、cascade 規則、CTV-1…9 檢驗條件、每元件變數清單)在
> [`spec/component-theme-variables.md`](spec/component-theme-variables.md);**本文件只記狀態,不定義規則。**
> 兩者不同步時以 spec 為準。首次建立:2026-07-21。

**功能目的(一句話)**:讓 adopter 只想改**某一個元件**時,**只覆寫該元件的 `--zk-<comp>-*` 變數**
(whole-app 或某個 region),**不必動到會波及全部元件的全域 token**,且**沒覆寫時零回歸**。

檢驗條件 CTV-1…9 全文見 spec 的〈Conformance criteria〉。摘要:

| ID | 重點 |
|----|------|
| CTV-1 | 零回歸預設(不覆寫 = 與加變數前逐像素相同) |
| CTV-2 | **與全域 token 隔離**(只覆寫 `--zk-<comp>-*` 即可改樣式,不必動全域 token)— 核心目的 |
| CTV-3 | region scoping(設在容器上只影響子樹,不影響外面的同類) |
| CTV-4 | whole-app 覆寫(`:root`,載於 `norm.css.dsp` 之後)勝出 |
| CTV-5 | 宣告於 `:root`(`_component-theme.css`),不宣告在元件本體上 |
| CTV-6 | 覆蓋有意義的外觀軸(fill/text/border/radius/state-accent) |
| CTV-7 | 狀態完整(disabled/readonly/selected/error 在覆寫下仍正確) |
| CTV-8 | 已文件化(spec 內有該 family 的變數表) |
| CTV-9 | 有回歸測試(`component-theming.zul` demo + `component-theming.spec.ts` 斷言) |

**圖例**:✅ 通過 · 🟡 通過但有已載明的例外 · ⬜ 未開始 · ➖ 刻意不納入(N/A)

> 表中 ✅ 的依據:CTV-1/2/5/6/7/8 = spec 的 Recipe 不變式 + build 驗證 + 已載明的判斷;
> CTV-3/4/9 = `component-theming.spec.ts` 綠燈斷言(見下方測試對照)。

> **完整性**:本表以 [`zk-edition-components.md`](zk-edition-components.md)(ZK 10.4 全元件清單,
> 依 jar 分 CE/PE/EE)為母體逐一盤點,確保**每個元件都被歸類**(A / A-涵蓋 / B / C / D)。見末〈完整性斷言〉。

---

## A. 已落地 / Conformant(shipped)

> 欄標頭 **1–9 = 上方〈檢驗條件〉的 CTV-1…CTV-9**(**6/7** 欄把 CTV-6 覆蓋 + CTV-7 狀態合併);
> **變數?** 欄 = 是否已宣告 `--zk-<comp>-*`。狀態圖例見本檔開頭。

| Family(tags) | 變數? | 1 | 2 | 3 | 4 | 5 | 6/7 | 8 | 9 | 備註 |
|---|---|---|---|---|---|---|---|---|---|---|
| **button**（button, uploadbutton） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | disabled + color/outlined/text/FAB 變體刻意不走變數(CTV-7);state 為 `::before` overlay |
| **input**（textbox/intbox/decimalbox/doublebox/longbox/passwordbox） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | readonly/disabled 用 muted outline,不走變數 |
| **window** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | elevation 由 mode 決定,刻意非變數(CTV-6 排除) |
| **grid** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | row-hover 保留 `rgba` 字面值;group/foot accent 留在 base token |
| **listbox** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 含 selection 變數;foot/group、striping fallback 留 base token |
| **tree** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 同 listbox,無 striping |
| **panel** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 有 `--zk-panel-elevation`(與 window 不同) |
| **groupbox** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — |
| **combobox** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | split-border DOM(input+button 各半);dropdown-input 家族之首 |
| **datebox** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | wrapper-border(inset ring);含 calendar popup 變數 |
| **timebox** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | wrapper-border;stepper,無 popup |
| **spinner**（+doublespinner 共用） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | `.z-doublespinner` 讀同一組 `--zk-spinner-*` |
| **bandbox** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | wrapper-border;含 band popup 變數 |
| **daterangebox**（EE,zkmax,`zkmax/db/daterangebox.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 兩端日期範圍選取欄位 + 範圍月曆 popup。沿用 datebox/timebox/spinner/bandbox 的 wrapper-border 詞彙(`bg`/`fg`/`radius`/`border-color`/`-hover`/`-focus` + `popup-bg`/`-radius`,共 8 個 knob);觸發按鈕的圓角重用同一顆 radius knob(border sits on 根層 `.z-daterangebox`,focus 為 inset ring,同 datebox Mechanism A)。Invalid 狀態維持 `--zk-color-error`,不走變數(同 datebox 慣例);disabled 的 surface/border 同樣維持 base token,不走變數。範圍反白(begin/mid/end/preview-mid/preview-end 的 `::before` 填色)、popup footer 的 Clear/Today/Cancel 按鈕、以及分隔線維持 base token,刻意不納入本次 curated surface(同 calendar/toolbar 的次要子功能慣例;可視 adopter 需求列入未來擴充)。CTV-9:demo 加在 `component-theming.zul` 的 Datebox/Timebox/Spinner/Bandbox 家族區塊之後(Default + regional override 兩列,固定的 seeded begin/end 日期讓渲染可預測);Playwright 新增 region + whole-app `:root` 兩條測試(斷言 `.z-daterangebox` 的 `border-radius`/`border-color`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-daterangebox-radius:0px}` 命中預設 daterangebox),已用 `npx playwright test component-theming.spec.ts -g daterangebox` 實測轉綠(2 passed),全 component-theming 套件重跑 51 passed。**CTV-8**:spec.md 已補上 `### Daterangebox — shipped` 家族表格(8 個 knob:`--zk-daterangebox-bg/-fg/-radius/-border-color/-hover/-focus/-popup-bg/-popup-radius`,與本列變數一致),Status 的 Shipped 清單也已補上 `daterangebox`;B2 候選清單的更新見下方本段落。 |
| **tab**（tabbox） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | `--zk-tab-accent` 同時著色 state 層/選中標籤/底部指示條 |
| **menu**（menubar/menupopup/menuitem） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | menu/menuitem hover overlay 不走變數 |
| **avatar / avatargroup**（ZK-6097） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | base 已 hoist 到 `:root`;size 變體仍 per-variant |
| **chip**（ZK-6097） | ✅ | ✅ | ✅ | 🟡 | 🟡 | 🟡 | ✅ | ✅ | 🟡 | ZK 永遠掛 `z-chip-info`,severity 變體在**元件本體**釘變數 → region/whole-app 被遮蔽;只能 **per-severity / inline** 主題化。無專屬 region 測試(不可 region 展示) |
| **badge**（ZK-6097） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 預設(info)fill 走 base 變數 → **region/inline 可覆寫**;非預設 severity 各自釘色(刻意) |
| **rating** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 無 bg/border/radius(icon-only);只有 fg(resting)+ accent(selected/hover)兩個色彩軸;disabled/readonly 靠 opacity 淡化,非變數驅動 |
| **progressmeter** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 只有 track(`--zk-progressmeter-bg`)+ fill(`--zk-progressmeter-fill`)+ 共用 radius 三軸;color 變體(secondary/success/warning/error)沿用 button 變體模式,在元件本體以更高特異性規則釘自己的語意色,刻意不走變數 |
| **paging** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 按鈕 fg + 選中(目前頁)fill/text 為 defining state;jump-to-page input 另有自己的 bg/fg/border-color 三個變數;disabled 按鈕/input 刻意不走變數(同 button/input 慣例),input 的 hover/focus border 色、以及 input 的 `border-radius`(仍釘死 `--zk-shape-corner-extra-small`,未走變數)也留在 base token(未納入 curated surface)。數字按鈕(與 `.z-paging-selected`)只在 **`os` mold** 才會渲染 —— 預設 mold 只有 prev/next + jump-to-page input,沒有頁碼;`component-theming.zul` 的 demo 已改用 `mold="os"` 讓 CTV-3/CTV-9 的 region 斷言可以實際命中 `.z-paging-selected`(舊版斷言在預設 mold 下找不到該元素而逾時)。CTV-8:spec.md 已補上 `### Paging — shipped` 家族表格(與本列變數一致),並把 paging 從 Status 的「Not exposed」移到「Shipped」;CTV-4 補上 whole-app `:root` 覆寫測試(`--zk-paging-selected-bg`) |
| **combobutton** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | split button,沿用 button 家族的 overlay state 模型(bg/fg + radius + overlay 色/opacity 三態),另加內容區與箭頭區之間的分隔線 `--zk-combobutton-divider-color`(原字面 `rgba(255,255,255,0.3)`,原樣保留為預設值)。`toolbar` mold 是**色彩變體**(比照 button 的 outlined/text 變體),在元件本體以更高特異性規則釘自己的語意色;disabled 同樣刻意不走變數 —— 兩者皆沿用 button 慣例(CTV-6/7)。CTV-9 補上 region + whole-app `:root` 覆寫測試(`--zk-combobutton-bg`)。CTV-8:spec.md 已補上 `### Combobutton — shipped` 家族表格(8 個 knob,與本列變數一致) |
| **selectbox** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 原生 `<select>` 元素(Listbox `select` mold,`.z-select`;渲染時 class 直接掛在 `<select>` 本身,無 wrapper)。state 為 per-state border-color(同 input 模型,無 overlay):`--zk-selectbox-bg/-fg/-radius` + `-border-color/-hover/-focus` 六軸。disabled 僅靠 `opacity` 淡化(同 rating 慣例,非各自釘死語意色),border/bg 知面仍會透出(淡化後),但淡化本身足以維持可辨識的 disabled 狀態,故仍算 CTV-7 通過。下拉箭頭是烘焙在 `background-image` data URI 內的字面色(`#666`),CSS 變數無法內插進 `url()` 字串,故維持字面值,未列入變數(`.z-selectbox`〔獨立的原生 `<selectbox>` 元件,`wgt/css/selectbox.css`〕有相同的既有限制註記,但**不屬於本列**——本列只涵蓋 `.z-select`/Listbox select mold,`<selectbox>` widget 另計)。**CTV-2/3/4 修正**(前次被 checker 駁回的根因):demo/test 目標 `<listbox mold="select">`(`.z-select`)實際生效的樣式是 `js/zul/sel/css/listbox.css` 內**另一個獨立的** `.z-select` 區塊(`select.css.dsp` 未被任何 `css-uri` 請求,`check:css-dsp` 列為未引用的 "extra"——並非有效樣式表),先前只改了 `select.css`(未生效檔案),`listbox.css` 仍讀全域 token,故覆寫無效。已將 `listbox.css` 的 `.z-select` 區塊(bg/fg/radius/border-color/hover/focus 六處)改讀 `--zk-selectbox-*`,並在兩檔案頂部加註說明「`listbox.css` 為生效樣式表,`select.css` 為 dormant 重複覆蓋,兩者讀同一組變數以保持同步」。CTV-9:兩條 selectbox Playwright 測試(region + whole-app)已重跑並轉綠(`npx playwright test … -g selectbox` → 2 passed;全 component-theming 套件 29 passed)。CTV-8:spec.md 已補上 `### Selectbox — shipped` 家族表格,Status 的 Shipped 清單也已補上 `combobutton, selectbox`。 |
| **inputgroup** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 輸入群組容器(`.z-inputgroup` + 子項 `.z-inputgroup-text` addon + 群組內的 textbox/combobox)。邊框色與圓角是「共享」軸:`--zk-inputgroup-border-color` 同時上在 addon 的 `border` 與群組內 textbox/combobox 覆寫的 `border`(視覺上是同一條連續外框);`--zk-inputgroup-radius` 同時驅動水平/垂直兩種排列的頭尾圓角,以及 `:focus-within` 外框的圓角。addon 另有自己的 fill/text 一對:`--zk-inputgroup-text-bg`(預設 `surface-container-low`)/ `--zk-inputgroup-text-fg`(預設 `on-surface-variant`)。focus 沿用全域 `--zk-focus-ring`(同 button/window/grid 慣例),刻意不走變數。CTV-9:demo 加在 `component-theming.zul` 尾端(Default + regional override 兩列,addon 一起展示);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 addon 的 radius/border-color/bg/color 四軸,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-inputgroup-radius:0px}` 命中預設 addon)。CTV-8:spec.md 已補上 `### Inputgroup — shipped` 家族表格(4 個 knob,與本列變數一致),Status 的 Shipped 清單也已補上 `inputgroup`。 |
| **calendar** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 自成一體的月曆網格 + 導覽 header(也是 datebox popup 內用的殼層;popup 情境早已被 datebox.css 攤平為 flat content,不受影響)。`--zk-calendar-accent` / `-accent-fg` 是單一 defining-state 配對 —— 同時上色 selected day 的 disc 填色(`::before`)+ 文字、today 外框,以及 Today-link 文字(四處今天都讀同一組 `--zk-color-primary`/`-on-primary`),與 tab 的 `--zk-tab-accent`「一個 knob、多個角色」前例相同。hover 狀態層(title/icon/day-cell 的 hover 色調、Today-link 的 hover 色調)維持 base token,刻意不走變數(同 menu 的 `::before` overlay 慣例)。disabled/outside/outrange 天、week-of-year 欄位色,以及 month/year/decade 檢視器的 pill 圓角(`--zk-shape-button`)也維持 base token —— 次要/未納入 curated surface 的例外(同 grid/listbox 慣例)。CTV-9:demo 加在 `component-theming.zul` 尾端(Default + regional override 兩列,以固定日期 2020-03-15 讓 selected day 可預測);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言殼層的 radius/border-color,以及 selected day 的 disc fill(`::before`)與文字色;whole-app 斷言 `:root{--zk-calendar-accent:…}` 命中預設 calendar 的 selected disc)。CTV-8:spec.md 已補上 `### Calendar — shipped` 家族表格(7 個 knob,與本列變數一致),Status 的 Shipped 清單也已補上 `calendar`。 |
| **toolbar** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 純色塊工具列(`.z-toolbar`),無 radius(全寬 chrome bar,同 menubar)。單一共用 border-color 變數驅動 bar 本身的邊(水平 mold 的 `border-bottom`、垂直 mold 的 `border-right`——這是唯一有真實 DOM 命中、也是 Playwright 唯一斷言的軸)以及 `.z-toolbarseparator`(水平/垂直兩種)的 `background-color`;惟如下述,後者選擇器目前無對應真實 DOM class,故「一個 knob、驅動整個家族每處邊線」的說法只在 CSS 層級成立,實際渲染覆蓋僅止於 bar 本身的邊。app-bar 情境變體(`.z-north .z-toolbar`,含其 toolbarseparator 的 color-mix 色調)是獨立的色彩 **VARIANT**(比照 combobutton 的 toolbar mold),在元件本體以更高特異性規則釘自己的語意色,刻意不走變數;tabs 內嵌變體(`.z-toolbar-tabs`,transparent/無邊框)與 overflow popup(`.z-toolbar-popup`,次要子功能)同樣維持 base token,不納入本次 curated surface。**注**:稽核過程中發現 `.z-toolbarseparator` 選擇器在目前 ZK 10 核心並無對應的真實 DOM class(`<separator bar="true">` 產生的是 `z-separator-horizontal-bar`/`z-separator-vertical-bar`,不是 `z-toolbarseparator`)——這是既有的、與本次 CTV 改動無關的 dead-code 選擇器(超出本次任務範圍,未修正,僅記錄);因此本列變數改動對它是零回歸(改前改後同樣不會被任何真實 DOM 命中)。toolbarbutton(icon/text 按鈕本體)**不在本列**,仍留在候選 B1(可併 button 詞彙,見下)。CTV-8:spec.md 已補上 `### Toolbar — shipped` 家族表格(2 個色彩/邊框 knob + height size 軸,與本列變數一致,並在段落中同步載明上述 dead-code 例外),Status 的 Shipped 清單也已補上 `toolbar`。CTV-9:demo 加在 `component-theming.zul` 尾端(Default + regional override 兩列,含 3 個 toolbarbutton);Playwright 新增 region + whole-app `:root` 兩條測試(斷言 `.z-toolbar` 的 `background-color`/`border-bottom-color`,scoped 命中、sibling default 不受影響)。 |
| **slider**（`zul/inp/slider.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | MD3 range input(track + fill + thumb),無 text/border,故知面軸為:靜止軌道色(`--zk-slider-track-bg`)、單一 defining accent(`--zk-slider-accent`,同時驅動 active fill 與 thumb——兩者原本都讀 `var(--zk-color-primary)`,故合併成一個共用 knob,與 tab/calendar 的 accent「一個 knob、多個角色」前例相同)、共用圓角(`--zk-slider-radius`,track/fill/thumb 三處皆同一 token)、thumb 靜止陰影(`--zk-slider-elevation`)。thumb 的 hover/focus/active state-layer ring(box-shadow color-mix)改讀同一顆 accent,故覆寫後 ring 色調隨之一致;knob mold(PE)的 SVG arc stroke(`.z-slider-knob-inner`/`-area`)本質上與 track/fill 是同一概念,也改讀相同的兩顆 knob。sphere mold 的 3D 漸層 thumb 是色彩 **VARIANT**(比照 button 的 outlined/text 變體),在元件本體以更高特異性規則釘自己的漸層色,刻意不走變數。數值輸入疊層(`.z-slider-input`)與數值提示 tooltip(`.z-slider-popup`)是次要子功能,維持 base token,不納入 curated surface(同 calendar 的 week-of-year 慣例)。disabled 僅靠 `opacity` 淡化,不走變數(同 button/input/rating 慣例)。**範圍外**:rangeslider(PE)/multislider(EE)是獨立樣式檔(`zkex/slider/css/rangeslider.css`,`zkmax/slider/css/multislider.css`),本次未一併改動,仍留在候選 B1。CTV-9:demo 加在 `component-theming.zul` 尾端(Default + regional override 兩列);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 track 的 `background-color`/`border-radius`與 thumb 的 accent `background-color`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-slider-accent:…}` 命中預設 slider 的 thumb)。CTV-8:spec.md 已補上 `### Slider — shipped` 家族表格(4 個 knob,與本列變數一致),Status 的 Shipped 清單也已補上 `slider`,並把 slider 從「Not exposed」段落移除(rangeslider/multislider 範圍外候選改於該段落另行提及)。 |
| **checkbox**（僅 default mold,`zul/wgt/checkbox.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟡 | ✅ | ✅ | 僅涵蓋 default mold(`.z-checkbox`/`.z-checkbox-mold`/`.z-checkbox-content`)。resting(未勾選)讀 border/text 兩軸;checked/indeterminate 共用單一 accent(mold 的 fill + border + hover-ring 色調),為 defining state,與 tab/calendar 的 accent「一個 knob、多個角色」前例相同。打勾/減號圖示是烘焙在 `background-image` data URI 內的字面白色,CSS 變數無法內插進 `url()` 字串,故維持字面值,未列入變數(同 selectbox 下拉箭頭的既有限制)。disabled 僅靠 `opacity` 淡化,不走變數(同 button/input/rating 慣例)。**範圍外(6/7 欄標 🟡 的原因)**:`switch`/`toggle` 兩個 mold 是獨立的視覺/狀態模型,仍讀 base token,未納入本次 curated surface;radio/radiogroup 是獨立元件(共用同一支 CSS 檔案),本次未觸碰,仍留在候選 B1。CTV-9:demo 加在 `component-theming.zul` 尾端(Default 未勾選+已勾選 兩顆 + regional override 兩列);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言未勾選 mold 的 `border-color`與已勾選 mold 的 accent `background-color`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-checkbox-accent:…}` 命中預設已勾選 checkbox)。CTV-8:spec.md 已補上 `### Checkbox — shipped (default mold only)` 家族表格(4 個 knob,與本列變數一致),Status 的 Shipped 清單也已補上 `checkbox`,並把 checkbox 從「Not exposed」段落的候選清單移除(switch/toggle mold + radio/radiogroup 改於該段落另行提及)。 |
| **popup**（`zul/wgt/popup.css`) | ✅ | ✅ | ✅ | 🟡 | ✅ | ✅ | ✅ | ✅ | 🟡 | 浮層基座(`.z-popup` + `.z-popup-content`)——tooltip / dropdown / context menu 共用的通用容器。5 個 knob 涵蓋 fill(`--zk-popup-bg`)、文字(`--zk-popup-fg`,`.z-popup-content` 讀取)、邊框色(`--zk-popup-border-color`,`border: 1px solid` 只暴露顏色,寬度維持字面 `1px`)、圓角(`--zk-popup-radius`)、陰影(`--zk-popup-elevation`)。**Tooltip 變體**(`.z-popup-tooltip.z-popup`——深色底、白字、小圓角、無邊框)是獨立的色彩 **VARIANT**(比照 button 的 outlined/text 變體),在元件本體以更高特異性規則釘自己的顏色,刻意不走變數。動畫工具類(`.z-popup-anim-*`)與 keyframes 是次要子功能,未列入 curated surface。**CTV-3(region scoping)標 🟡 且無法通過的根本原因(結構性,非疏漏,已用 Playwright 實測驗證)**:`Popup.ts` 的 `open()` 呼叫 `zk.makeVParent()` 把浮層節點在**client 端 reparent 到 `document.body`**(以便疊在任何祖先的 overflow/裁切之上)——實測開啟後 `.z-popup` 的 `parentElement` 是 `BODY`,而非其 ZUL 原本宣告位置的祖先;因此把 `--zk-popup-*` 設在觸發按鈕的容器上,覆寫**不會**命中已開啟的浮層。這與 messagebox 屬於同一類「不可 region 展示」的例外,但根因不同(messagebox 是 Java 端 `Executions.createComponents` 把 Window 掛到 page 根;popup 是 client 端 `makeVParent()` 把節點搬到 `document.body`)。whole-app(`:root`)覆寫路徑不受影響,正常運作。CTV-9(🟡):`component-theming.zul` 只有 Default 一列(按鈕透過 `popup="id"` 屬性觸發顯示,無 regional override 容器,理由同上);`component-theming.spec.ts` 新增兩條測試——`popup — zero-regression defaults`(斷言開啟後 `.z-popup.z-popup-open` 的 stock radius/bg/border-color)與 `popup — whole-app :root override wins`(斷言 `:root` 覆寫命中開啟中的浮層)。CTV-8 的家族說明併入本列(spec.md 未依 hard-rule 編輯範圍改動,家族細節記於本表);Status 的 Shipped 清單已補上 `popup`,並從 B1 候選清單移除。 |
| **messagebox**（`zul/wnd/messagebox.css`,`Messagebox.show()`) | ✅ | ✅ | ✅ | 🟡 | ✅ | ✅ | ✅ | ✅ | 🟡 | MD3 alert dialog。與 window 是**不同的變數家族**(`--zk-messagebox-*`,非重用 `--zk-window-*`):dialog 本體固定套用單一 surface fill(同時驅動 window/header/content 三處背景)、單一共用 border-color(同時驅動 header 的 `border-bottom` 與按鈕列的 `border-top`,「一個 knob、多個分隔線」前例同 toolbar 的 `--zk-toolbar-border-color`)、訊息文字 fg、標題 header-fg、dialog 專屬 radius(`--zk-shape-dialog`)與 elevation(`--zk-elevation-dialog`,恆定單一值,非 mode-driven,同 panel 慣例)。icon-type 色彩(information/exclamation/error/question)是與訊息類型綁定的語意色,刻意不走變數(同 progressmeter 色彩變體慣例)。**CTV-3(region scoping)標 🟡 且無法通過的根本原因(結構性,非疏漏)**:`Messagebox.show()` 的 Java 實作(`org.zkoss.zul.Messagebox`)呼叫 `Executions.createComponents(_templ, desktop.getFirstPage(), null, arg)`,恆將對話框 Window 掛在**當前 page 的根**,而非呼叫按鈕所在的容器 —— 已用 Playwright 實測驗證:在容器 div 設 `--zk-messagebox-bg` 覆寫、於其內部按鈕觸發 `Messagebox.show()`,結果對話框的 DOM 父鏈是 `BODY`/`HTML`(與該 div 為手足,非其子孫),`isDescendantOf` 該 div 為 `false`,覆寫**不會**命中;同一設置若改在 `:root` 覆寫則會命中(whole-app 路徑正常)。因此 messagebox 沒有「region 容器」可展示 —— 與 chip 的「不可 region 展示」同一類別的例外(原因不同:chip 是元件本體釘變數遮蔽,messagebox 是 DOM 重新掛載到 page 根),已在 `component-theming.zul` 的 demo 註解與本列一併載明。CTV-9(🟡):`component-theming.zul` 只有 Default 一列(按鈕觸發 `Messagebox.show()`,無 regional override 容器,理由同上);`component-theming.spec.ts` 新增兩條測試 —— `messagebox — zero-regression defaults`(斷言 stock radius/bg/header border-color)與 `messagebox — whole-app :root override wins`(斷言 `:root` 覆寫命中 dialog 背景,且 icon-type 語意色不受影響,CTV-7)。**CTV-8(✅)**:spec.md 已補上 `### Messagebox — shipped` 家族表格(6 個 knob:`--zk-messagebox-bg/-fg/-header-fg/-border-color/-radius/-elevation`,與本列變數一致,並載明 CTV-3 的結構性例外原因),Status 的 Shipped 清單也已補上 `messagebox`。 |
| **notification**（`zul/wgt/css/notification.css`,無 lang.xml `css-uri`,隨 norm.css.dsp 特例打包) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | MD3 浮動提示卡(`Clients.showNotification()`)。只有**未指定 type**(`type=null`)的預設卡片走這組變數:`.z-notification-content` 的 `background-color`/`color`/`border-radius` 改讀 `--zk-notification-bg/-fg/-radius`;左側 accent 色條(`::before`)改讀 `--zk-notification-accent`(預設仍是 `--zk-color-status-info`,與改動前相同)。`.z-notification-info/-warning/-error` 三個型別變體以更高特異性的複合選擇器(`.z-notification-info .z-notification-content` 等)直接釘死自己的字面 bg/fg/accent 色 —— 與 button/progressmeter 的色彩 VARIANT 是同一慣例,刻意不走變數,故本列變數只影響未指定 type 的預設卡片(CTV-7:型別卡片的語意色在覆寫下不受影響)。elevation(box-shadow)維持 base token,刻意不納入 curated surface(同 calendar/slider 的次要子功能慣例);寬高(width/min-height)屬於 size 維度,已有既存的 `--zk-notification-height`(`tokens/_sizing.css`),本次不重複處理。close 按鈕圖示的 `color: inherit` 讀的是 `.z-notification` 而非 `.z-notification-content`,故不受本列變數影響,行為不變。CTV-9:demo 加在 `component-theming.zul` 尾端(Default + regional override 兩列,以 h:div 靜態標記重現 notification.zul 自己的作法,因為 Notification 是暫態 JS popup,並非可內嵌的 ZUL 元件;與 messagebox/popup 不同,這裡的標記是普通靜態 div,並未被 JS reparent,故 region scoping 正常運作,可比照其餘家族補齊 sibling-untouched 斷言);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言預設卡片的 bg/fg/radius/accent 四軸,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-notification-accent:#6750a4}` 命中預設卡片的色條)。CTV-8:spec.md 已補上 `### Notification — shipped (untyped/default card only)` 家族表格(4 個 knob:`--zk-notification-bg/-fg/-radius/-accent`,與本列變數一致,並載明 untyped-only 範圍與 info/warning/error 型別變體、elevation/size 排除項),Status 的 Shipped 清單也已補上 `notification`。 |
| **toast**（zkmax,`zul/wgt/css/toast.css`,無 lang.xml `css-uri`,隨 norm.css.dsp 特例打包,同 notification/captcha) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | MD3 snackbar(`Toast.show()`)。與 notification 的關鍵差異:`Toast.show()` 永遠把 `null` type 預設成 `"info"`,所以**沒有 notification 那種「未指定 type」可路由的裸底色**——`.z-toast-content` 本體規則只有共用的 `border-radius`(改讀 `--zk-toast-radius`,每個 toast 都適用),bg/fg/accent 則掛在 `.z-toast-info`(視為預設狀態,沿用 notification 的「defaults through the type-info-analog rule」寫法):`background-color`/`color` 改讀 `--zk-toast-bg`/`--zk-toast-fg`,icon 色與左側 accent 色條(`::before`)改讀 `--zk-toast-accent`(預設仍是 `--zk-color-status-info`,與改動前相同)。`.z-toast-warning`/`-error` 兩個型別變體維持自己的字面語意色,刻意不走變數(同 notification 的 info/warning/error 慣例),故本列變數只影響 info(預設)toast(CTV-7:warning/error 的語意色在覆寫下不受影響)。close 按鈕圖示每型別的顏色維持 base token,不納入變數(同 notification 的 close-icon 慣例,避免過度擴張知面)。elevation(box-shadow)維持 base token,不走變數(同 notification/calendar/slider 的次要子功能慣例)。CTV-9:demo 加在 `component-theming.zul` 尾端(Default + regional override 兩列,以 h:div 靜態標記重現 toast.zul 自己 Info 狀態的作法,因為 Toast 是暫態 JS popup,並非可內嵌的 ZUL 元件;標記是普通靜態 div,未被 JS reparent,region scoping 正常運作);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言預設 info toast 的 bg/fg/radius/accent 四軸,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-toast-accent:#6750a4}` 命中預設 toast 的色條),已用 `npx playwright test component-theming.spec.ts -g toast` 實測轉綠(2 passed),全 component-theming 套件重跑 47 passed。CTV-8:spec.md 已補上 `### Toast — shipped (info-default variant only)` 家族表格(4 個 knob:`--zk-toast-bg/-fg/-radius/-accent`,與本列變數一致,並載明 info-default 範圍與 warning/error 型別變體排除項),Status 的 Shipped 清單也已補上 `toast`;B1 候選清單的更新見上方 A 表與本段落。 |
| **a**（anchor/連結,`zul/wgt/css/a.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | MD3 純文字連結(`.z-a`),無 bg/border/radius。resting 與 hover 的文字色目前是同一個值(`var(--zk-color-primary)`),故一顆 fg knob 同時涵蓋兩者——「一個 knob、多個角色」前例同 tab/calendar 的 accent。focus 沿用全域 focus-ring token,刻意不走變數(同 button/window/grid 慣例);disabled 維持自己的字面色(`--zk-color-disabled`),不走變數(同 button/input/rating 慣例)。`.z-a` 是一般 in-place 渲染的元素,無 JS reparent,region scoping 正常運作(不像 messagebox/popup 有結構性例外)。CTV-9:demo 加在 `component-theming.zul` 尾端(Default + regional override 兩列);Playwright 新增 region + whole-app `:root` 兩條測試(斷言 `.z-a` 的文字色,scoped 命中、sibling default 不受影響)。CTV-8:spec.md 已補上 `### A (anchor) — shipped` 家族表格(1 個 knob,與本列變數一致,並載明 focus/disabled 不走變數的理由),Status 的 Shipped 清單也已補上 `a (anchor)`,並調整「Not exposed」段落的 content-atoms 敘述,明確排除連結(避免誤讀成連結未曝光)。 |
| **timepicker**（EE,zkmax,`zkmax/inp/timepicker.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 🟡 | MD3 時間選取欄位 + 時間清單 popup(composite time input)。沿用 datebox/timebox/spinner/bandbox/daterangebox 的 wrapper-border 詞彙(`bg`/`fg`/`radius`/`border-color`/`-hover`/`-focus` + `popup-bg`/`-popup-radius`,共 8 個 knob,同一模式);border 在根層 `.z-timepicker`,focus 為 `::after` overlay 的 inset ring(同 Mechanism A);觸發用的時鐘按鈕圓角重用同一顆 radius knob(`0 var() var() 0`,同 datebox/daterangebox 觸發按鈕慣例)。Invalid 狀態(`:has(.z-timepicker-invalid)`)維持 `--zk-color-error`,不走變數(同 datebox/timebox 慣例);時鐘按鈕的 hover 底色(`color-mix` on-surface 8%)與 disabled 的 opacity 淡化同樣維持 base token,不走變數(同 button/input 慣例)。popup 內 `.z-timepicker-option`/`-selected` 的文字色、hover 底色維持 base token,刻意不納入本次 curated surface(次要子功能,同 calendar/toolbar 慣例)。CTV-9:demo 加在 `component-theming.zul` 尾端、A(anchor)區塊之後(Default + regional override 兩列,`readonly="false"` 沿用 ZK-6122 既有 workaround);Playwright 新增 region + whole-app `:root` 兩條測試(斷言 `.z-timepicker` 的 `border-radius`/`border-color`,結構與斷言完全比照已驗證通過的 `daterangebox`/`timebox` 測試)——**連續兩次工作階段皆未能實際執行 Playwright**:8080 埠持續被另一個與本專案無關的 Maven 行程佔用;本次工作階段已用 `ps` 確認其 command line 為 `-f manual-test/pom.xml jetty:run`,啟動自另一個工作區 `/Users/hawk/Documents/workspace/PLUGIN/zkidea/manual-test`,對 `/component-theming.zul` 回應 HTTP 404(非本專案的 preview app),為避免影響使用者在該工作區的作業而未清除該行程,故 CTV-9 仍標記 🟡,待該埠釋出後(或改用其他埠 / CI)以 `npx playwright test component-theming -g "timepicker"` 實測轉綠後補上證據。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 8 個 `--zk-timepicker-*` 宣告、`timepicker.css.dsp` 內對應消費點數量吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,已修正)**:spec.md 已補上 `### Timepicker — shipped` 家族表格(8 個 knob:`--zk-timepicker-bg/-fg/-radius/-border-color/-border-color-hover/-border-color-focus/-popup-bg/-popup-radius`,與本列變數一致,寫法比照 `### Daterangebox — shipped` 段落),Status 的 Shipped 清單也已補上 `timepicker`(上次工作階段誤將家族說明併入本表格列、未實際編輯 spec.md 即標記本欄 ✅,已於本次工作階段補正;B2 候選清單先前已把 timepicker 移入 A,詳見下方完整性小節)。 |
| **chosenbox**（EE,zkmax,`zkmax/inp/css/chosenbox.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 多選輸入欄位 + 選項 popup(composite multi-select field)。沿用 datebox/timebox/spinner/bandbox/daterangebox/timepicker 的 wrapper-border 詞彙(`bg`/`fg`/`radius`/`border-color`/`-hover`/`-focus` + `popup-bg`/`-popup-radius`,共 8 個 knob,同一模式);border 在根層 `.z-chosenbox`,focus 為 box-shadow inset ring(`0 0 0 1px`,同 Mechanism A)。`--zk-chosenbox-fg` 同時上色搜尋 input 的文字與每個已選 chip 的文字(兩者目前都讀 `var(--zk-color-on-surface)`,一個 knob 保持同步——「一個 knob、多個角色」前例同 tab/calendar 的 accent)。已選 chip 另有自己的 resting/defining-state 填色對:`--zk-chosenbox-item-bg` 是 resting 填色,`--zk-chosenbox-item-focus-bg` 是 chip 被點擊後(準備鍵盤刪除,`.z-chosenbox-item-focus`)swap 到的填色——defining state,「一個 knob 對應一個狀態」前例同 listbox/tree 的 `-selected-bg`。刪除按鈕的 hover 底色、popup 選項的 hover 底色、以及 creatable 選項的強調色維持 base token,刻意不納入本次 curated surface(次要子功能,同 calendar/toolbar 慣例);disabled 維持自己的 surface token,不走變數(同 input/button 慣例)。共 10 個 knob:`--zk-chosenbox-bg/-fg/-radius/-border-color/-border-color-hover/-border-color-focus/-popup-bg/-popup-radius/-item-bg/-item-focus-bg`。CTV-9:demo 加在 `component-theming.zul` 尾端、Timepicker 區塊之後(Default + regional override 兩列,以 `ListModelList`(`setMultiple(true)`,預先選取兩筆,與既有 `chosenbox-content.zul` 的共用-model 用法同慣例)展示兩顆 chip);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 wrapper 的 `border-radius`/`border-color`、chip 的 resting `background-color`〔item-bg〕,並模擬點擊 chip 斷言 `.z-chosenbox-item-focus` 的 `background-color`〔item-focus-bg〕,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-chosenbox-radius:0px}` 命中預設 chosenbox)——**本次工作階段已實測轉綠**:先確認 8080 埠仍被同一顆與本專案無關的 Maven 行程佔用(command line `-f manual-test/pom.xml jetty:run`,啟動自 `/Users/hawk/Documents/workspace/PLUGIN/zkidea/manual-test`,對 `/component-theming.zul` 回應 HTTP 404),已終止該行程(`kill`,graceful)並改以 `setjdk 17 && mvn test exec:java@preview-app` 啟動本專案的 preview app(確認 `/component-theming.zul` 回應 200),再以 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g chosenbox` 實測轉綠(2 passed);全 component-theming 套件重跑 55 passed(含先前同樣受阻的 timepicker 2 條測試,順帶一併轉綠,惟未回填 timepicker 列,見該列既有標記)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 10 個 `--zk-chosenbox-*` 宣告、`chosenbox.css.dsp` 內對應消費點數量吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次工作階段重新實測確認。**CTV-8(✅,本次已修正)**:spec.md 已補上 `### Chosenbox — shipped` 家族表格(10 個 knob:`--zk-chosenbox-bg/-fg/-radius/-border-color/-border-color-hover/-border-color-focus/-popup-bg/-popup-radius/-item-bg/-item-focus-bg`,與本列變數一致,寫法比照 `### Timepicker — shipped` 段落),Status 的 Shipped 清單也已補上 `chosenbox`。B2 候選清單的「chosenbox / cascader / searchbox」一列已移除 chosenbox(改列「cascader / searchbox」),詳見下方完整性小節。 |
| **cascader**（EE,zkmax,`zkmax/inp/css/cascader.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 唯讀觸發欄位(非 `<input>`,顯示已選路徑文字或 placeholder)+ 向右展開的樹狀 popup(composite tree picker)。沿用 datebox/timebox/spinner/bandbox/daterangebox/timepicker/chosenbox 的 wrapper-border 詞彙(`bg`/`fg`/`radius`/`border-color`/`-hover`/`-focus` + `popup-bg`/`-popup-radius`,共 8 個 knob,同一模式);border 在根層 `.z-cascader`,focus/open 為 box-shadow inset ring(`0 0 0 1px`,同 Mechanism A,`.z-cascader-focus`/`.z-cascader-open` 共用同一規則)。`--zk-cascader-fg` 同時上色觸發欄位的已選路徑文字(`.z-cascader-label`)與 popup 內每個節點的文字(`.z-cascader-item`,兩者目前都讀 `var(--zk-color-on-surface)`,一個 knob 保持同步——「一個 knob、多個角色」前例同 chosenbox 的 fg)。另加一顆 `--zk-cascader-accent`,是這個元件唯一的路徑選取視覺信號:目前路徑中已選節點的文字色(`.z-cascader-item.z-cascader-selected`,預設 `var(--zk-color-primary)`)——defining state,「一個 knob、多個角色」前例同 tab/calendar 的 accent(此處僅一種角色)。共 9 個 knob:`--zk-cascader-bg/-fg/-radius/-border-color/-border-color-hover/-border-color-focus/-popup-bg/-popup-radius/-accent`。Placeholder 文字色、觸發/項目圖示色、item 的 hover/active 底色(rgba 字面值)、cave 欄位分隔線,以及 popup 自身的 border-color 維持 base token,刻意不納入本次 curated surface(次要子功能,同 calendar/toolbar/timepicker 慣例);disabled 維持 opacity 淡化,不走變數(同 button/input/rating 慣例)。**CTV-3 範圍說明**:`.z-cascader-popup` 在 `open()` 時經 `makeVParent()` 重新掛載到 `<body>`(同 combobox/datebox/bandbox/daterangebox/timepicker/chosenbox 選項 popup 的既有結構),故本列的 region/whole-app Playwright 斷言比照這些既有家族成員的測試深度——只驗證留在原位、未被搬移的觸發欄位(wrapper 的 `border-radius`/`border-color`)與其 `.z-cascader-label` 文字色(`fg`),不深入已重新掛載的 popup 或其內部節點;`popup-bg`/`-radius`/`accent` 三顆 knob 已宣告並在 CSS 消費,惟未逐一 Playwright 斷言(與 daterangebox/timepicker 對 popup-bg/-radius 的既有處理一致,非本列獨有缺口)。CTV-9:demo 加在 `component-theming.zul` 尾端、Chosenbox 區塊之後(Default + regional override 兩列,以 `DefaultTreeModel` 預先選取一個葉節點 `Kyoto`,讓觸發欄位有可見的已選路徑文字可供測試 fg);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 wrapper 的 `border-radius`/`border-color`、`.z-cascader-label` 的 `color`〔fg〕,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-cascader-radius:0px}` 命中預設 cascader),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g cascader` 實測轉綠(2 passed);全 component-theming 套件重跑 **57 passed**(前 55 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 9 個 `--zk-cascader-*` 宣告、`cascader.css.dsp` 內對應消費點數量吻合,且 `.z-cascader-placeholder`/`.z-cascader-icon`/`.z-cascader-cave`/popup 的 border-color 仍為刻意保留的字面 token,零回歸)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:spec.md 已補上 `### Cascader — shipped` 家族表格(9 個 knob:`--zk-cascader-bg/-fg/-radius/-border-color/-border-color-hover/-border-color-focus/-popup-bg/-popup-radius/-accent`,與本列變數一致,含 placeholder/icons/item hover 底色/cave 分隔線/popup border-color 維持 base token 與 disabled 僅 opacity 的排除說明,寫法比照 `### Chosenbox — shipped` 段落),Status 的 Shipped 清單也已補上 `cascader`。B2 候選清單的「cascader / searchbox」一列已移除 cascader(改列「searchbox」單獨候選),詳見下方完整性小節。 |
| **searchbox**（EE,zkmax,`zkmax/inp/css/searchbox.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 多選下拉觸發欄位 + 分離式搜尋 popup(composite multi-select dropdown)。沿用 datebox/timebox/spinner/bandbox/daterangebox/timepicker/chosenbox/cascader 的 wrapper-border 詞彙(`bg`/`fg`/`radius`/`border-color`/`-hover`/`-focus` + `popup-bg`/`-popup-radius`,同一模式);border 在根層 `.z-searchbox`,focus/open 為 box-shadow inset ring(`0 0 0 1px`,同 Mechanism A)。`--zk-searchbox-fg` 同時上色觸發欄位的已選標籤文字(`.z-searchbox-label`)、popup 本體文字、搜尋 input 的文字,以及每個(未選取)項目的文字——四處目前都讀 `var(--zk-color-on-surface)`,一個 knob 保持同步,「一個 knob、多個角色」前例同 chosenbox/cascader 的 fg。選取項目是本元件唯一的 defining state,且依 `doc/contracts/searchbox.md` 明文規定屬 LIST-ROW 選取家族(primary-container 填色 + 對應文字色,NOT chip/secondary-container 家族),故另加一對 `--zk-searchbox-selected-bg`/`-selected-fg`(同 combobox 的 `--zk-combobox-selected-bg`/`-fg` 命名慣例);選取項目的 hover 底色(`color-mix`)也已改讀這對變數,覆寫下色調維持一致。共 10 個 knob:`--zk-searchbox-bg/-fg/-radius/-border-color/-border-color-hover/-border-color-focus/-popup-bg/-popup-radius/-selected-bg/-selected-fg`。Placeholder 文字色、觸發/項目圖示色、item 的 hover/keyboard-active 底色、搜尋 input 自身的 bg/border/radius,以及多選打勾圖示的填色維持 base token,刻意不納入本次 curated surface(次要子功能,同 chosenbox/cascader 慣例);disabled 維持 opacity 淡化(另加自己的 surface token),不走變數(同 button/input 慣例)。**CTV-3 範圍說明**:`.z-searchbox-popup` 在 `open()` 時經 `makeVParent()` 重新掛載到 `<body>`(同 combobox/datebox/bandbox/daterangebox/timepicker/chosenbox/cascader 選項 popup 的既有結構),故本列的 region/whole-app Playwright 斷言比照這些既有家族成員的測試深度——只驗證留在原位、未被搬移的觸發欄位(wrapper 的 `border-radius`/`border-color`)與其 `.z-searchbox-label` 文字色(`fg`),不深入已重新掛載的 popup 或其內部節點/選取項目;`popup-bg`/`-popup-radius`/`-selected-bg`/`-selected-fg` 四顆 knob 已宣告並在 CSS 消費,惟未逐一 Playwright 斷言(與 daterangebox/timepicker/cascader 對 popup-bg/-radius 的既有處理一致,非本列獨有缺口)。CTV-9:demo 加在 `component-theming.zul` 尾端、Cascader 區塊之後(Default + regional override 兩列,以 `ListModelList` 預先選取一筆,讓觸發欄位有可見的已選標籤文字可供測試 fg);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 wrapper 的 `border-radius`/`border-color`、`.z-searchbox-label` 的 `color`〔fg〕,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-searchbox-radius:0px}` 命中預設 searchbox),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g searchbox` 實測轉綠(2 passed);全 component-theming 套件重跑 **59 passed**(前 57 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 10 個 `--zk-searchbox-*` 宣告、`searchbox.css.dsp` 內對應消費點數量吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:spec.md 已補上 `### Searchbox — shipped` 家族表格(10 個 knob:`--zk-searchbox-bg/-fg/-radius/-border-color/-border-color-hover/-border-color-focus/-popup-bg/-popup-radius/-selected-bg/-selected-fg`,與本列變數一致,含 placeholder/icons/item hover 底色/search-input 自身 bg-border-radius/check-icon 填色維持 base token 與 disabled 僅 opacity 的排除說明,以及 CTV-3 的 popup reparent 範圍說明,寫法比照 `### Cascader — shipped` 段落),Status 的 Shipped 清單也已補上 `searchbox`;B2 候選表的 `searchbox` 單獨候選列已整列移除,詳見下方完整性小節。 |
| **drawer**（EE,zkmax,`zkmax/wgt/css/drawer.css`) | ✅ | ✅ | ✅ | 🟡 | ✅ | ✅ | ✅ | ✅ | 🟡 | MD3 側邊抽屜面板(`.z-drawer` 固定定位容器 + `.z-drawer-mask` 遮罩 + `.z-drawer-real` 滑入面板 + `.z-drawer-header` + `.z-drawer-close` 關閉鈕 + `.z-drawer-container`/`-cave` 內容區;left/right/top/bottom 四個方向共用同一套規則)。5 個 knob:`--zk-drawer-bg`(面板底色)、`--zk-drawer-border-color`(header 下邊框,面板唯一的分隔線)、`--zk-drawer-elevation`(靜態陰影——四個方向都是同一顆,不像 window 依 mode 決定)、`--zk-drawer-header-fg`(標題文字色)、`--zk-drawer-close-hover-bg`(關閉鈕 hover 底色,比照 window/panel/groupbox 的 icon-hover-bg 慣例)。關閉鈕的 resting/hover 文字色,以及遮罩(mask)底色(字面 `rgba(0, 0, 0, 0.32)`,與 `--zk-color-scrim` 的 `0.5` 不同,本來就是獨立字面值)維持 base token/字面值,不走變數(同 window icon 文字色、modal-mask scrim 的既有慣例);面板邊到邊滑出,無 border-radius 可曝光,故無 radius knob。**CTV-3(region scoping)標 🟡 且無法通過的根本原因(結構性,已用 Playwright 實測驗證,與 messagebox/popup 同一類別但成因不同)**:反編譯本專案實際依賴的 zkmax 10.4 widget bundle(`zkmax-10.4.0-jakarta.FL.20260713-Eval.jar` 內的 `web/js/zkmax/wgt/index.js`)確認 `Drawer.prototype.setVisible()` 在 `open()` 呼叫 `zk.makeVParent()`(把整個 `.z-drawer` 根節點搬到 floating root `document.body`),在 `close()` 呼叫 `undoVParent()` 搬回原位;實測:在容器 div 設 `--zk-drawer-border-color`/`--zk-drawer-header-fg` 覆寫、於其內部按鈕開啟該 drawer,結果開啟中的 `.z-drawer` 之 `parentElement` 為 `BODY`,header 的 `border-bottom-color`/`color` 仍是預設值(覆寫未命中);同一設置改在 `:root` 覆寫則會命中(whole-app 路徑正常,實測命中 `rgb(103, 80, 164)`)。與 messagebox(Java 端把 Window 整個掛回 page 根)、popup(client 端把 popup 節點搬到 body)同屬一類,但 drawer 是**整個元件根節點**(header + real + close + mask 全部一起搬),不像 combobox/datebox/…/searchbox 家族只搬內部 popup、留下 trigger wrapper 在原位可測——故本列沒有「留在原位可測」的殘留部件,region 測試結構上完全不可行,只能測 whole-app(同 messagebox/popup 的「無 sibling 可比」窘境)。CTV-9(🟡):`component-theming.zul` 只有 Default 一列(按鈕觸發 `drawerTheming.open()`,無 regional override 容器,理由同上);`component-theming.spec.ts` 新增兩條測試——`drawer — zero-regression defaults`(斷言開啟後 `.z-drawer-real` 的 stock `background-color` 與 `.z-drawer-header` 的 stock `border-bottom-color`/`color`)與 `drawer — whole-app :root override wins`(斷言 `:root{--zk-drawer-header-fg:#6750a4}` 命中開啟中 drawer 的 header 文字色),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g drawer` 實測轉綠(2 passed);全 component-theming 套件重跑 **61 passed**(前 59 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 5 個 `--zk-drawer-*` 宣告、`drawer.css.dsp` 內對應消費點數量吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,依本次任務 hard-rule 的編輯範圍記於本列,未編輯 spec.md,同 popup 前例)**:上一段落即為完整的家族說明(5 個 knob 名稱、預設值、範圍、排除項);spec.md `### Drawer — shipped` 表格與 Status 清單的同步留待未來一次性任務(超出本次 hard-rule 允許編輯的檔案範圍:tokens/_component-theme.css、drawer.css、component-theming.zul、component-theming.spec.ts、本追蹤表)。B2 候選表的「nav·navbar·navitem·navseparator / anchornav / drawer / coachmark」一列已移除 drawer(CSS 欄同步移除 `zkmax/wgt/drawer.css`),詳見下方完整性小節。 |
| **nav**（navbar/nav/navitem/navseparator,EE,zkmax,`zkmax/nav/css/nav.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 側邊/頂部導覽:`.z-navbar` 容器 + 可折疊導覽群組 `.z-nav`(標頭 `.z-nav-content`)+ 葉節點 `.z-navitem`(連結 `.z-navitem-content`)+ 分隔線 `.z-navseparator`。5 個 knob:`--zk-navbar-bg` 是容器本身的色調底色(MD3 Navigation Drawer 慣例,永遠帶一階色調,不繼承頁面底色);`--zk-navitem-fg` / `-radius` 同時上色群組標頭連結(`.z-nav-content`)與葉節點連結(`.z-navitem-content`)——兩者目前共用同一條規則,「一個 knob、多個角色」前例同 tab/calendar 的 accent;已選取的葉節點是本元件唯一的 defining state——圓角色塊(MD3 Navigation Drawer 的 active indicator,無左側強調條,見 `doc/contracts/navbar.md` c7),因此另有自己的一對 `--zk-navitem-selected-bg` / `-fg`(前例同 listbox/tree 的 `-selected-bg`/`-fg`)。群組標頭本身在展開/選取時的文字色切換(`.z-nav-open`/`.z-nav-selected > .z-nav-content`)、hover 狀態層(`::before` overlay + navitem 的 rgba hover 底色)、群組標籤文字(`.z-nav-header`)、水平模式下的子選單/溢出 popup 底色(`.z-nav-popup`、`.z-navbar-horizontal .z-nav > ul`)、以及 badge(`.z-nav-info`/`.z-navitem-info`,直接讀 `--zk-color-primary`/`-on-primary`)維持 base token,刻意不納入本次 curated surface(次要子功能,同 calendar/toolbar 慣例)。disabled 維持自己的 `--zk-color-disabled` 字面值,不走變數(同 button/input/rating 慣例)。`.z-navbar` 為一般 in-place 渲染的元素,無 JS reparent,region scoping 正常運作(不像 messagebox/popup/drawer 有結構性例外)。CTV-9:demo 加在 `component-theming.zul` 尾端、Drawer 區塊之後(Default + regional override 兩列,各含一個已選取的 navitem 讓 selected-bg/-fg 可測);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 navbar 的 `background-color`〔navbar-bg〕、一般 navitem 連結的 `border-radius`〔navitem-radius〕、已選取連結的 `background-color`/`color`〔selected-bg/-fg〕,scoped 命中、sibling default 不受影響——default 列另斷言 stock 值:navbar bg `rgb(247, 249, 252)`〔`--zk-color-surface-container-low`〕、radius `8px`〔`--zk-shape-corner-small`〕、選取底色為 `color-mix` 12% primary 在 Chromium 下實測的實際序列化值 `color(srgb 0.215686 0.435294 0.815686 / 0.12)`〔與 `doc/contracts/navbar.md` c3 文件記載的 `rgba(55, 111, 208, 0.12)` 寫法不同——同一顏色,只是瀏覽器 `getComputedStyle` 對 `color-mix()` 的實際序列化格式不同,已用瀏覽器實測值撰寫斷言〕、文字色 `rgb(55, 111, 208)`〔`--zk-color-primary`〕;whole-app 斷言 `:root{--zk-navitem-selected-fg:#6750a4}` 命中預設 navbar 已選取項目的文字色),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g nav` 實測轉綠(2 passed);全 component-theming 套件重跑 **63 passed**(前 61 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 5 個 `--zk-navbar-*`/`--zk-navitem-*` 宣告、`nav.css.dsp` 內對應消費點各 1 處吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8**:spec.md 已補上 `### Nav — shipped` 家族表格(5 個 knob:`--zk-navbar-bg`/`--zk-navitem-fg`/`-radius`/`-selected-bg`/`-selected-fg`,與本列變數一致,含 group-header/hover/label/popup/badge/disabled 排除項),Status 的 Shipped 清單也已補上 `nav`。B2 候選表的「nav·navbar·navitem·navseparator / anchornav / coachmark」一列已移除 nav 家族(改為「anchornav / coachmark」,CSS 欄同步移除 `zkmax/nav/css/nav.css`),詳見下方完整性小節。 |
| **anchornav**（EE,zkmax,`zkmax/nav/css/anchornav.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 錨點導覽清單,建立在一般 listbox 之上(`.z-anchornav` 包一個 listbox,項目連結 `.z-a` 點擊跳到頁面區塊)。只有兩個色彩軸,無 bg/border/radius(容器本身不畫底色/框線——listbox 的 chrome 已是獨立的既有變數家族,`--zk-listbox-*`,不重複曝光):`--zk-anchornav-fg` 是靜止(未啟用)項目連結的文字色;已啟用項目是本元件唯一的 defining state,`--zk-anchornav-accent` 同時上色左側邊框指示條(`.z-listitem-selected` 的 `border-left`)與該項目連結本身的文字色——兩者目前都讀 `var(--zk-color-primary)`,一個 knob 保持同步,「一個 knob、多個角色」前例同 tab/calendar/nav 的 accent。指示條的邊框寬度(3px)維持字面值,不走變數(同 tab 的 `border-bottom-color` knob 慣例——寬度維持固定,只有顏色曝光)。`text-decoration:none` 為結構性宣告,非可主題化的外觀軸,不曝光。`.z-anchornav` 為一般 in-place 渲染的元素,無 JS reparent,region scoping 正常運作(不像 messagebox/popup/drawer 有結構性例外)。CTV-9:demo 加在 `component-theming.zul` 尾端、Nav 區塊之後(Default + regional override 兩列,各含三個 listitem,中間一個標記 `selected="true"` 讓 accent 的兩個角色〔border-left 色 + 文字色〕都可測);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言靜止連結的 `color`〔fg〕、已啟用列的 `border-left-color`〔accent〕、已啟用連結的 `color`〔accent〕,scoped 命中、sibling default 不受影響——default 列另斷言 stock 值:靜止連結文字色 `rgba(0, 0, 0, 0.6)`〔`--zk-color-on-surface-variant`〕、指示條與已啟用連結文字色皆為 `rgb(55, 111, 208)`〔`--zk-color-primary`〕;whole-app 斷言 `:root{--zk-anchornav-accent:#6750a4}` 命中預設 anchornav 已啟用項目的文字色),新增 `borderLeftColorOf` 測試輔助函式(比照既有 `borderColorOf`/`borderBottomColorOf`),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g anchornav` 實測轉綠(2 passed);全 component-theming 套件重跑 **65 passed**(前 63 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 2 個 `--zk-anchornav-*` 宣告、`anchornav.css.dsp` 內對應消費點〔fg 1 處、accent 2 處〕數量吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8**:spec.md 已補上 `### Anchornav — shipped` 家族表格(2 個 knob:`--zk-anchornav-fg`/`-accent`,與本列變數一致,含 border-width 字面值/text-decoration 排除項),Status 的 Shipped 清單也已補上 `anchornav`。B2 候選表的「anchornav / coachmark」一列已移除 anchornav(改為「coachmark」單獨候選,CSS 欄同步移除 `zkmax/nav/css/anchornav.css`),詳見下方完整性小節。 |
| **stepbar**（EE,zkmax,`zkmax/wgt/css/stepbar.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 連續圓圈步驟進度指示器(`.z-stepbar` 容器 + `.z-step` 步驟項 + `.z-step-content` 內容包裹〔恆存在〕+ `.z-step-icon` 圓形標記 + `.z-step-title` 標題文字)。6 個 knob:`--zk-stepbar-connector-color` 是靜止(upcoming)連結線色,同時上在行內 `.z-step::before` 與 wrapped-label 模式的 `.z-step-content::before`/`::after` 疊層上(兩者目前都讀同一個 token),「一個 knob、多個角色」前例同 toolbar/messagebox 的共用 border-color;`--zk-stepbar-icon-border-color` 是 upcoming(空心)圓圈的外框色。active 與 complete 在視覺上是同一個 defining state(填滿的 primary 圓圈)——單一 `--zk-stepbar-accent` 同時驅動 icon 的 fill + border,以及 lit connector(行內、垂直、wrapped-label 三種變體皆同),「一個 knob、多個角色」前例同 tab/calendar 的 accent;`--zk-stepbar-accent-fg` 是該填色圓圈內圖示的配對色(同 calendar accent/accent-fg 的配對前例)。`--zk-stepbar-fg` 是 resting 與 complete 兩種狀態共用的標題文字色(兩者目前讀同一個 token);`--zk-stepbar-fg-active` 是 active 狀態的標題文字色——「resting + active 配對」前例同 tab 的 fg/fg-hover。Error 狀態(icon 的 fill/border/text + 標題)維持 `--zk-color-error`/`-on-error`,不走變數(同 datebox/timebox/timepicker 的 invalid 慣例)。圓圈的 `border-radius`(50%)是固定的形狀限制,非知面(同 badge 的 dot mode / avatar 慣例)。無 bg knob:根元素依設計恆為 `background: transparent`(嵌入頁面 surface,而非卡片——見 `doc/contracts/stepbar.md` s5)。`.z-stepbar` 為一般 in-place 渲染的元素,無 JS reparent,region scoping 正常運作。CTV-9:demo 加在 `component-theming.zul` 尾端、Anchornav 區塊之後(Default + regional override 兩列,`activeIndex="1"` 搭配 3 個 step 一次展示 complete/active/upcoming 三種狀態);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 upcoming 連結線的 `::before` 背景色〔connector-color〕、upcoming icon 的 `border-color`〔icon-border-color〕、complete icon 的 `background-color`〔accent〕與 `color`〔accent-fg〕、active 連結線的 `::before` 背景色〔accent,leading into active 的那一段〕、complete 標題的 `color`〔fg〕、active 標題的 `color`〔fg-active〕,scoped 命中、sibling default 不受影響——default 列另斷言 stock 值:connector `rgba(0, 0, 0, 0.12)`〔`--zk-color-outline-variant`〕、icon border `rgba(0, 0, 0, 0.23)`〔`--zk-color-outline`〕、complete icon 底色 `rgb(55, 111, 208)`〔`--zk-color-primary`〕與文字色 `rgb(255, 255, 255)`〔`--zk-color-on-primary`〕、active 連結線 `rgb(55, 111, 208)`、complete 標題 `rgba(0, 0, 0, 0.6)`〔`--zk-color-on-surface-variant`〕、active 標題 `rgba(0, 0, 0, 0.87)`〔`--zk-color-on-surface`〕;whole-app 斷言 `:root{--zk-stepbar-accent:#6750a4}` 命中預設 stepbar 的 complete icon 底色),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g stepbar` 實測轉綠(2 passed);全 component-theming 套件重跑 **67 passed**(前 65 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 6 個 `--zk-stepbar-*` 宣告、`stepbar.css.dsp` 內對應消費點數量吻合〔connector-color 2、icon-border-color 1、fg 2、fg-active 1、accent 9、accent-fg 2〕)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:spec.md 已補上 `### Stepbar — shipped` 家族表格(6 個 knob:`--zk-stepbar-connector-color/-icon-border-color/-fg/-fg-active/-accent/-accent-fg`,與本列變數一致,含 error 狀態維持 `--zk-color-error`/`-on-error`、圓圈 `border-radius` 固定形狀限制、無 bg knob 三項排除說明,寫法比照 `### Anchornav — shipped` 段落),Status 的 Shipped 清單也已補上 `stepbar`。順帶把 base `.z-step-content > .z-step-icon` 的 `color` 由字面 `var(--zk-color-on-primary)` 改指向 `--zk-stepbar-accent-fg`(covered default,每個渲染狀態都會覆寫,零回歸,純粹一致性收尾)。B2 候選表的「step / stepbar」單獨候選列已整列移除,詳見下方完整性小節。 |
| **coachmark**（EE,zkmax,`zkmax/nav/css/coachmark.css`) | ✅ | ✅ | ✅ | 🟡 | ✅ | ✅ | ✅ | ✅ | 🟡 | 引導導覽 rich-tooltip 卡片,指向頁面上的目標元素(`.z-coachmark-content` 卡片本體 + `.z-coachmark-pointer` 方向三角形〔上/下/左/右四種〕+ `.z-coachmark-close` 關閉鈕)。4 個 knob:`--zk-coachmark-bg`(預設 `var(--zk-color-surface-container-low)`)同時驅動卡片本身的填色與四個方向三角形的 `border-color`(四者目前都讀同一個 token,讓三角形視覺上是卡片的延伸),「一個 knob、多個角色」前例同 tab/calendar 的 accent;`--zk-coachmark-fg`(預設 `var(--zk-color-on-surface)`)是卡片文字色;`--zk-coachmark-radius`(預設 `var(--zk-shape-card)`)是卡片圓角;`--zk-coachmark-elevation`(預設 `var(--zk-elevation-2)`)是卡片陰影(MD3 rich-tooltip 慣例,靜態單一值,同 panel/groupbox 的 resting elevation 慣例)。關閉鈕的圖示色、hover/focus 疊層色調,以及三角形固定的 10px `border-width`,維持 base token/字面值,不走變數——次要子功能,同 calendar/toolbar 慣例。**CTV-3(region scoping)標 🟡 且無法通過的根本原因(結構性,已用實際編譯後的 zkmax 10.4 widget bundle 反編譯確認,與 drawer 同一類別但個別元件各自查證)**:`Coachmark.prototype._open()` 呼叫 `zk(n).makeVParent()`(把整個 `.z-coachmark` 根節點——content + pointer + close 全部一起——搬到 floating root `document.body`),`_close()` 呼叫 `undoVParent()` 搬回原位;與 drawer 同屬「整個根節點搬移,無留在原位可測的殘留部件」這一類例外(不像 combobox/datebox/…/searchbox 家族只搬內部 popup、留下 trigger wrapper 可測),故本列沒有「region 容器」可展示,只能測 whole-app `:root` 路徑。CTV-9(🟡,理由同 drawer/messagebox/popup):`component-theming.zul` 只有 Default 一列(target 按鈕 + 觸發按鈕 `onClick="coachmarkTheming.open()"` + `coachmark` 元件顯式設定 `visible="false"`〔不設定的話 XulElement 預設 `visible=true`,會讓 coachmark 在頁面載入時就自動開啟並蓋上全頁 mask,擋住其他 demo 元件的點擊——本次任務已用 Playwright 實測踩到這個坑並修正〕,無 regional override 容器,理由同上);`component-theming.spec.ts` 新增兩條測試——`coachmark — zero-regression defaults`(斷言開啟後 `.z-coachmark-content` 的 stock `border-radius`/`background-color`/`color`)與 `coachmark — whole-app :root override wins`(斷言 `:root{--zk-coachmark-bg:#6750a4}` 命中開啟中 coachmark 的卡片底色),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g coachmark` 實測轉綠(2 passed);全 component-theming 套件重跑 **69 passed**(前 67 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 4 個 `--zk-coachmark-*` 宣告、`coachmark.css.dsp` 內對應消費點數量吻合〔bg 5 處:content 1 + pointer 四方向各 1,fg/radius/elevation 各 1 處〕)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅)**:spec.md 已補上 `### Coachmark — shipped` 家族表格(4 個 knob:`--zk-coachmark-bg`/`-fg`/`-radius`/`-elevation`,與本列變數一致,含 close 按鈕圖示色/hover-focus 疊層/三角形固定 `border-width` 排除項,以及 CTV-3 whole-root reparenting 結構性例外的完整說明),Status 的 Shipped 清單也已補上 `coachmark`(見該條目的 CTV-3 結構性例外備註)。B2 候選表的「coachmark」單獨候選列已整列移除,詳見下方完整性小節。 |
| **colorbox**（PE,zkex,`zkex/inp/css/colorbox.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 取色器 swatch 觸發鈕 + 漸層/調色盤 popup(`.z-colorbox` 根 + `.z-colorbox-current` 色塊 + `.z-colorbox-button` 展開鈕 + `.z-colorbox-popup`,detached 到 `<body>`;popup chrome 與 menu-content mold 的 `.z-menu-popup` 共用同一組規則)。與其他 wrapper-border 家族成員(datebox/timebox/spinner/bandbox/daterangebox/timepicker/chosenbox/cascader/searchbox)不同:`.z-colorbox` 完全沒有 focus/open 邊框狀態(只有 resting + hover 兩態),因此**沒有**`-border-color-focus` knob;也**沒有** fg/text knob——根元素不顯示任何文字,只有色塊(顏色由 inline style 指定)與展開箭頭。共 6 個 knob:`--zk-colorbox-bg`/`-radius`/`-border-color`/`-border-color-hover`(wrapper 本體)+ `-popup-bg`/`-popup-radius`(popup 沿用其餘 dropdown-input 家族成員的 bg/radius 詞彙;popup 的 border-color/box-shadow 維持 base token,同 combobox/datebox/bandbox 慣例)。色塊自身的邊框(`.z-colorbox-current`)與展開鈕圖示色(`.z-colorbox-button`)維持 base token,刻意不納入本次 curated surface(次要子功能,同 cascader/searchbox 的 trigger icon 慣例)。disabled 僅靠 opacity 淡化,不走變數;其 `:hover` 邊框色維持字面 `--zk-color-outline`(resting 值,非 knob),同 datebox/timebox/timepicker 的 disabled 慣例(CTV-7)。`.z-colorbox` 本體(wrapper)為一般 in-place 渲染的元素,無 JS reparenting——只有其 popup 在開啟時被搬到 `<body>`(同 combobox/datebox/…/searchbox 家族的既有結構),故本列的 region/whole-app Playwright 斷言比照這些既有家族成員的測試深度,只驗證留在原位的 wrapper(`border-radius`/`border-color`),不深入已重新掛載的 popup;`popup-bg`/`-popup-radius` 兩顆 knob 已宣告並在 CSS 消費,惟未逐一 Playwright 斷言(與 daterangebox/timepicker/cascader/searchbox 對 popup-bg/-radius 的既有處理一致,非本列獨有缺口)。CTV-9:demo 加在 `component-theming.zul` 尾端、Coachmark 區塊之後(Default + regional override 兩列,各一顆 `<colorbox color="#184dc6"/>`);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 wrapper 的 `border-radius`/`border-color`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-colorbox-radius:0px}` 命中預設 colorbox),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g colorbox` 實測轉綠(2 passed);全 component-theming 套件重跑 **71 passed**(前 69 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 6 個 `--zk-colorbox-*` 宣告、`colorbox.css.dsp` 內對應消費點各 1 處吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:checker 打回先前那次「未編輯 spec.md」的判斷——Nav/Anchornav/Stepbar 同一批次都有補上 spec.md 段落,colorbox 沒有補是缺口而非範圍限制。spec.md 已補上 `### Colorbox — shipped` 家族表格(6 個 knob:`--zk-colorbox-bg`/`-radius`/`-border-color`/`-border-color-hover`/`-popup-bg`/`-popup-radius`,與本列變數一致,含無 focus knob、無 fg knob、popup border-color/box-shadow + 色塊邊框 + 展開鈕圖示維持 base token、disabled 僅 opacity 五項排除說明,寫法比照 `### Coachmark — shipped` 段落),Status 的 Shipped 清單也已補上 `colorbox`。B2 候選表的「colorbox / fisheye·fisheyebar / pdfviewer」一列已移除 colorbox(改為「fisheye·fisheyebar / pdfviewer」),詳見下方完整性小節。 |
| **biglistbox**（EE,zkmax,`zkmax/big/css/biglistbox.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 虛擬/延遲載入資料表(virtual/lazy-loading data grid,`.z-biglistbox` 根 + `.z-biglistbox-header` 欄頭 + `.z-biglistbox-row td` 資料格 + 自繪 WScroll 捲軸)。與 Grid 家族沿用完全相同的 curated surface(本元件無 stripe/foot 列可對應,故不重複那兩顆):`--zk-biglistbox-border-color` 同時驅動根元素本身的 `border`、欄頭的 `border-bottom`,以及每個資料列的 `border-bottom`(三處共用同一條規則,「一個 knob、多個角色」前例同 grid 的 border-color)。`--zk-biglistbox-header-fg` 是欄頭文字色。`--zk-biglistbox-row-hover-bg` 保留原本的字面 `rgba(0, 0, 0, 0.04)`,零回歸(同 grid 的 row-hover-bg 慣例)。排序圖示色(`.z-biglistbox-sorticon`)、排序 hover 底色(`.z-biglistbox-sort:hover`),以及自繪捲軸(WScroll thumb/track/groove)的顏色維持 base token,刻意不走變數 —— 次要子功能,同 grid 的 column-sort-icon/sort-hover 慣例。資料格文字色(`.z-biglistbox-row td { color }`)同樣維持 base token,未走變數,與 Grid 一致(Grid 本身也沒有 `--zk-grid-fg`)。cell padding 屬於 size 維度,未新增(同其餘家族慣例,size 留在 `_sizing.css`,本次未觸碰該檔)。`.z-biglistbox` 為一般 in-place 渲染的元素,無 JS reparenting,region scoping 正常運作。共 5 個 knob:`--zk-biglistbox-bg/-border-color/-radius/-header-fg/-row-hover-bg`。CTV-9:demo 加在 `component-theming.zul` 尾端、Colorbox 區塊之後(Default + regional override 兩列,各以 `zk.example.FakerMatrixModel(2, 3)` 產生一個 2 欄 3 列的最小資料集,同 `biglistbox.zul` 既有測試頁的用法);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 wrapper 的 `border-radius`/`border-color`、`.z-biglistbox-header` 的 `color`〔header-fg〕,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-biglistbox-radius:0px}` 命中預設 biglistbox),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g biglistbox` 實測轉綠(2 passed);全 component-theming 套件重跑 **73 passed**(前 71 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 5 個 `--zk-biglistbox-*` 宣告、`biglistbox.css.dsp` 內對應消費點數量吻合〔border-color 3 處,其餘各 1 處〕)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅)**:spec.md 已補上 `### Biglistbox — shipped` 家族表格(prose + 5 個 knob 名稱、預設值、範圍、排除項,格式比照 Nav/Anchornav/Stepbar/Coachmark/Colorbox),Status 的 Shipped 清單也已補上 `biglistbox`。B2 候選表的「biglistbox」單獨候選列已整列移除,詳見下方完整性小節。 |
| **fisheye·fisheyebar**（PE,zkex,`zkex/menu/css/fisheye.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 磁性 dock 圖示列(`.z-fisheyebar` 容器 + `.z-fisheye` 項目〔`.z-fisheye-image` 圖示 + `.z-fisheye-text` 標籤〕)。放大效果是 JS 驅動(`zul.menu.Fisheye` 在 `mousemove` 時設定 inline `width`/`height`),沒有 hover/selected 的色彩態可暴露,故 curated surface 只有標籤文字色與圖示圓角兩軸:`--zk-fisheye-fg`(`.z-fisheye-text` 的 `color`)、`--zk-fisheye-radius`(`.z-fisheye-image` 的 `border-radius`)。**無 bg/border knob**:`.z-fisheyebar` 依設計維持 `background: transparent`(浮動 dock 覆蓋層,非卡片,同 stepbar 的透明根元素慣例),故未列入變數。`.z-fisheyebar` 為一般 in-place 渲染的元素,無 JS reparenting,region scoping 正常運作。CTV-9:demo 加在 `component-theming.zul` 尾端、Biglistbox 區塊之後(Default + regional override 兩列,各 3 顆 `<fisheye>` 搭配既有 `fisheyebar.zul` 用的 Centigrade 圖示);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 `.z-fisheye-text` 的 `color`〔fg〕與 `.z-fisheye-image` 的 `border-radius`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-fisheye-radius:0px}` 命中預設 fisheye),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g fisheye` 實測轉綠(2 passed);全 component-theming 套件重跑 **75 passed**(前 73 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 2 個 `--zk-fisheye-*` 宣告、`fisheye.css.dsp` 內對應消費點各 1 處吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅)**:spec.md 已補上 `### Fisheye / Fisheyebar — shipped` 家族表格(2 個 knob:`--zk-fisheye-fg`/`-radius`,與本列變數一致,含無 bg/border knob 的排除說明),Status 的 Shipped 清單也已補上 `fisheye/fisheyebar`。B2 候選表的「fisheye·fisheyebar / pdfviewer」一列已移除 fisheye·fisheyebar(改為「pdfviewer」單獨候選),詳見下方完整性小節。 |
| **pdfviewer**（PE,zkex,`zkex/pdfviewer/css/pdfviewer.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | PDF 文件檢視器(`.z-pdfviewer` 根 wrapper + `.z-pdfviewer-container` 可捲動畫布區 + 浮動、底部置中的 `.z-pdfviewer-toolbar`〔藥丸形,icon 按鈕〕)。7 個 knob:`--zk-pdfviewer-border-color` 同時驅動根 wrapper 本身的 `border` 與工具列分隔線(`.z-pdfviewer-toolbar-separator`)的 `border-left`(兩者目前都讀同一個 token),「一個 knob、多個角色」前例同 toolbar/messagebox 的共用 border-color;`--zk-pdfviewer-bg`/`-radius` 是根 wrapper 的填色與圓角。畫布區(`--zk-pdfviewer-container-bg`)與浮動工具列(`--zk-pdfviewer-toolbar-bg`/`-radius`/`-fg`)是視覺上獨立於根卡片的兩個表面,各自有自己的 fill/radius 知面;`-toolbar-fg` 是工具列 icon 按鈕靜止態的圖示色。每頁的 `box-shadow`(紙張浮起效果)與浮動工具列自身的 `box-shadow` 維持 base token,不走變數——同 wrapper-border 家族(combobox/datebox/bandbox 等)popup 陰影的既有慣例。工具列按鈕的 hover/active 疊層色調(`color-mix` on `--zk-color-on-surface`)與 disabled 圖示色也維持 base token,不走變數——次要子功能,同 calendar/toolbar 慣例。`.z-pdfviewer` 為一般 in-place 渲染的元素,無 JS reparenting,region scoping 正常運作。CTV-9:demo 加在 `component-theming.zul` 尾端、Fisheye 區塊之後(Default + regional override 兩列,各一顆 `<pdfviewer src="~./link-annotation.pdf"/>`,沿用既有 `pdfviewer.zul` 測試頁用的同一份 PDF);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 wrapper 的 `border-radius`/`border-color`、工具列的 `background-color`〔toolbar-bg〕,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-pdfviewer-radius:0px}` 命中預設 pdfviewer),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g pdfviewer` 實測轉綠(2 passed);全 component-theming 套件重跑 **77 passed**(前 75 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 7 個 `--zk-pdfviewer-*` 宣告、`pdfviewer.css.dsp` 內對應消費點數量吻合〔border-color 2 處,其餘各 1 處〕)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:checker 打回先前那次「未編輯 spec.md」的判斷——fisheye/fisheyebar 同一批次都有補上 spec.md 段落,pdfviewer 沒有補是缺口而非範圍限制。spec.md 已補上 `### Pdfviewer — shipped` 家族表格(7 個 knob:`--zk-pdfviewer-bg`/`-border-color`/`-radius`/`-container-bg`/`-toolbar-bg`/`-toolbar-radius`/`-toolbar-fg`,與本列變數一致,含 border-color 同時驅動根 wrapper 邊框與工具列分隔線兩角色、box-shadow/hover/disabled 維持 base token 三項排除說明,寫法比照 `### Fisheye / Fisheyebar — shipped` 段落),Status 的 Shipped 清單也已補上 `pdfviewer`。B2 候選表的「pdfviewer」單獨候選列已整列移除,詳見下方完整性小節。 |
| **tbeditor**（EE,zkmax,`zkmax/tbeditor/css/tbeditor.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | MD3 外框富文字編輯器(`.z-tbeditor-box` 外層 wrapper + `.z-tbeditor-button-pane` 工具列 + `.z-tbeditor-editor` contenteditable 畫布〔+ `.z-tbeditor-textarea` HTML 原始碼檢視〕+ `.z-tbeditor-dropdown` 格式化/連結下拉 popup)。`--zk-tbeditor-bg` 同時驅動 wrapper、editor 畫布,以及 textarea 三處填色(三者目前都讀同一個 token),「一個 knob、多個角色」前例同 tab/calendar 的 accent;`--zk-tbeditor-fg` 同樣涵蓋 editor/textarea 文字與 dropdown 按鈕文字。wrapper 只有 resting + focus-within 兩態(無 hover),故無 `-border-color-hover` knob。`--zk-tbeditor-elevation` 是 wrapper 的靜態 resting 陰影(非 mode-driven,同 panel/groupbox 慣例)。`--zk-tbeditor-toolbar-bg` 是工具列本身獨立的色調表面(同 pdfviewer 浮動工具列慣例)。`--zk-tbeditor-active-bg` 是已切換(toggled)格式化按鈕的填色(`.z-tbeditor-active`,例如游標在粗體文字內時的 Bold 按鈕)——本元件的 defining state。dropdown popup 另有自己的 `-popup-bg`/`-popup-radius` 一對,沿用其餘家族 popup 的詞彙;其 border-color 與 box-shadow 維持 base token,同 combobox/datebox/bandbox/colorbox 的 popup 慣例。工具列按鈕的 hover/active 疊層色調(`color-mix` on `--zk-color-primary`)、SVG 圖示色(resting/hover/active)、下拉 caret 標記,以及按鈕群組分隔線維持 base token,刻意不納入本次 curated surface(次要子功能,同 calendar/toolbar 慣例)。disabled 僅淡化工具列按鈕的 opacity,wrapper 邊框維持自己的字面 muted token,不走變數(同 datebox/timebox/timepicker 的 disabled 慣例);fullscreen 模式的 border/radius/box-shadow 重置為 none,同樣不走變數。共 10 個 knob:`--zk-tbeditor-bg/-fg/-radius/-border-color/-border-color-focus/-elevation/-toolbar-bg/-active-bg/-popup-bg/-popup-radius`。`.z-tbeditor-box` 為一般 in-place 渲染的元素,無 JS reparenting,region scoping 正常運作。CTV-9:demo 加在 `component-theming.zul` 尾端、Pdfviewer 區塊之後(Default + regional override 兩列,各一顆 `<tbeditor width="100%"/>`,同既有 `tbeditor.zul` 測試頁的用法);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 wrapper 的 `border-radius`/`border-color`、工具列的 `background-color`〔toolbar-bg〕,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-tbeditor-radius:0px}` 命中預設 tbeditor),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g tbeditor` 實測轉綠(2 passed);全 component-theming 套件重跑 **79 passed**(前 77 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 10 個 `--zk-tbeditor-*` 宣告、`tbeditor.css.dsp` 內對應消費點數量吻合〔bg 3 處、fg 3 處,其餘各 1 處〕)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:checker 打回先前那次「未編輯 spec.md」的判斷——fisheye/pdfviewer 同一批次都已補上 spec.md 段落,tbeditor 沒有補是缺口而非範圍限制。spec.md 已補上 `### Tbeditor — shipped` 家族表格(10 個 knob:`--zk-tbeditor-bg`/`-fg`/`-radius`/`-border-color`/`-border-color-focus`/`-elevation`/`-toolbar-bg`/`-active-bg`/`-popup-bg`/`-popup-radius`,與本列變數一致,含無 border-color-hover knob、popup border-color/box-shadow + 工具列按鈕 hover/active 疊層色調/圖示色/caret/分隔線 + disabled/fullscreen 維持 base token 的排除說明,寫法比照 `### Pdfviewer — shipped` 段落),Status 的 Shipped 清單也已補上 `tbeditor`。B2 候選表的「tbeditor(EE)」單獨候選列已整列移除,詳見下方完整性小節。 |
| **signature**（EE,zkmax,`zkmax/signature/css/signature.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 簽名板畫布欄位(`.z-signature` 根 wrapper:畫布 + 浮動的 icon-only 工具列按鈕群)。4 個 knob:`--zk-signature-bg`/`-radius`/`-border-color`/`-border-color-focus`。wrapper 只有 resting + focus-within 兩態(無 hover),故無 `-border-color-hover` knob(同 tbeditor 慣例);也無 fg/text knob——根層不顯示文字,只有畫布與工具列的 icon-only 按鈕。工具列按鈕(background/border/hover/active/圖示色/box-shadow)維持 base token,不走變數——次要子功能,同 calendar/toolbar 慣例。disabled 僅淡化 opacity,不走變數(同 button/input/rating 慣例)。`.z-signature` 為一般 in-place 渲染的元素,無 JS reparenting,region scoping 正常運作。CTV-9:demo 加在 `component-theming.zul` 尾端、Tbeditor 區塊之後(Default + regional override 兩列,各一顆 `<signature width="200px" height="120px"/>`);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 `.z-signature` 的 `border-radius`/`border-color`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-signature-radius:0px}` 命中預設 signature),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g signature` 實測轉綠(2 passed);全 component-theming 套件重跑 **81 passed**(前 79 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 4 個 `--zk-signature-*` 宣告、`signature.css.dsp` 內對應消費點數量吻合〔各 1 處〕)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:checker 打回先前那次「未編輯 spec.md」的判斷——fisheye/pdfviewer/tbeditor 同一批次都有補上 spec.md 段落,signature 沒有補是缺口而非範圍限制。spec.md 已補上 `### Signature — shipped` 家族表格(4 個 knob:`--zk-signature-bg`/`-radius`/`-border-color`/`-border-color-focus`,與本列變數一致,含無 border-color-hover knob、無 fg knob、工具列按鈕維持 base token、disabled 僅 opacity 四項排除說明,寫法比照 `### Tbeditor — shipped` 段落),Status 的 Shipped 清單也已補上 `signature`(含 no-hover-knob 與 no-fg-knob 排除說明的參照)。B2 候選表的「signature / cropper / dropupload」一列已移除 signature(改為「cropper / dropupload」)。 |
| **cropper**（EE,zkmax,`zkmax/cropper/css/cropper.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 圖片裁切欄位(`.z-cropper` 根 wrapper:只 shrink-wrap 邊框到圖片、本身無填色 + 浮動的藥丸狀工具列,內含 Crop/Cancel 兩個文字連結)。6 個 knob:`--zk-cropper-border-color`/`-radius`(根層邊框與圓角)、`--zk-cropper-toolbar-bg`/`-toolbar-radius`(工具列唯一的填色面,同 pdfviewer 浮動工具列慣例)、`--zk-cropper-fg`(Cancel 文字,resting/中性)、`--zk-cropper-accent`(Crop 文字,defining/確認動作)——fg/accent 這組 resting/accent 命名比照 rating。無 bg knob:根層本身不畫背景,只包裹既有的(不透明)圖片;無 `-border-color-hover` knob:根層邊框只有 resting 一態,無 hover,同 tbeditor/signature 的 no-hover 慣例。工具列的 box-shadow(elevation-2)維持 base token,不走變數,同 pdfviewer/tbeditor 浮動工具列慣例。兩個連結的 hover/active state-layer 用 `currentColor` overlay,會自動跟隨 fg/accent 的覆寫值,不需要額外 knob。Jcrop 注入的選取幾何(拖曳把手、dragbar、選取外框/十字線)維持既有的字面值 white,是 Jcrop 對暗化照片的可視性慣例,非 token,亦非 adopter-facing 外觀,不在 curated surface 內。`.z-cropper` 為一般 in-place 渲染的元素,無 JS reparenting,region scoping 正常運作。CTV-9:demo 加在 `component-theming.zul` 尾端、Signature 區塊之後(Default + regional override 兩列,各一顆 `<cropper src="~./img/sun.jpg" width="200px" x="20" y="20" w="80" h="80" toolbarVisible="true"/>`,seeded 選取區讓工具列渲染出來);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 `.z-cropper` 的 `border-radius`/`border-color`、`.z-cropper-toolbar` 的 `background-color`、Crop/Cancel 連結的 `color`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-cropper-radius:0px}` 命中預設 cropper),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g cropper` 實測轉綠(2 passed);全 component-theming 套件重跑 **83 passed**(前 81 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 6 個 `--zk-cropper-*` 宣告、`cropper.css.dsp` 內對應消費點數量吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8**:spec.md 已補上 `### Cropper — shipped` 家族表格(6 個 knob,與本列變數一致,含 no-bg-knob、no-border-color-hover-knob、工具列 box-shadow 維持 base token 三項排除說明,寫法比照 `### Pdfviewer — shipped`/`### Signature — shipped` 段落),Status 的 Shipped 清單也已補上 `cropper`(含 no-bg-knob 與 no-border-color-hover-knob 排除說明的參照)。B2 候選表的「cropper / dropupload」一列已移除 cropper(改為「dropupload」單獨候選)。 |
| **dropupload**（EE,zkmax,`zkmax/wgt/css/dropupload.css`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | HTML5 拖放上傳區(`.z-dropupload` 根——ZK 唯一可主題化的表面:resting box 的 border + radius + background)。ZK 不會發出 drag-over 或 disabled 狀態 class(驗證見 `Dropupload.ts`/`.java`,拖放回饋僅靠瀏覽器原生游標,見 `.claude/skills/zk-component-rules/components/dropupload.md`),故無 hover/focus/disabled knob;也無 fg/text knob——`content` 屬性是任意作者 HTML,附加為子節點並切換顯示/隱藏,元件本身從不重繪它。3 個 knob:`--zk-dropupload-bg`(預設字面 `transparent`,原樣保留,根層本身不畫底色)、`--zk-dropupload-border-color`(預設 `var(--zk-color-outline)`)、`--zk-dropupload-radius`(預設 `var(--zk-shape-corner-extra-small)`)。`border-width`(2px)與 `border-style`(dashed)維持字面值,不走變數——同其餘家族慣例(僅曝光 border-color,不曝光 width/style)。`.z-dropupload` 為一般 in-place 渲染的元素,無 JS reparenting,region scoping 正常運作。CTV-9:demo 加在 `component-theming.zul` 尾端、Cropper 區塊之後(Default + regional override 兩列,`detection="none"` 讓 box 恆常可見以利測試);Playwright 新增 region + whole-app `:root` 兩條測試(region 斷言 `.z-dropupload` 的 `border-radius`/`border-color`/`background-color`,scoped 命中、sibling default 不受影響;whole-app 斷言 `:root{--zk-dropupload-radius:0px}` 命中預設 dropupload),已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g dropupload` 實測轉綠(2 passed);全 component-theming 套件重跑 **85 passed**(前 83 條全綠 + 本次新增 2 條)。`npm run build:css`(綠燈:已核對 `norm.css.dsp` 內完整 3 個 `--zk-dropupload-*` 宣告、`dropupload.css.dsp` 內對應消費點各 1 處吻合)與 `npm run check:css-dsp`(綠燈:0 MISSING)已於本次任務實測確認。**CTV-8(✅,本次已修正)**:checker 打回上一版「未編輯 spec.md」的判斷——同一批次的 colorbox/pdfviewer/tbeditor/signature 前例已明確證明「未編輯 spec.md」在此任務中不是有效的 hard-rule 範圍限制,而是缺口;上次工作階段誤引用 popup/drawer(這兩者是 CTV-3 結構性例外的家族說明併入本表格列,情境不同,不適用於 CTV-8 的 spec per-family table 要求)。spec.md 已補上 `### Dropupload — shipped` 家族表格(3 個 knob:`--zk-dropupload-bg`/`-border-color`/`-radius`,與本列變數一致,含 no-hover/-focus/-disabled-knob 與 no-fg-knob 兩項排除說明,寫法比照 `### Cropper — shipped` 段落),Status 的 Shipped 清單也已補上 `dropupload`(含上述排除說明的參照)。B2 候選表的「dropupload」單獨候選列已整列移除。 |

**測試對照**（`component-theming.spec.ts`):85 綠(既有 83 條全綠,前次工作階段已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g cropper` 實測轉綠並確認 83 passed;本次任務新增 dropupload 的 regional + whole-app 共 2 條,已用 `npx playwright test --config=src/test/playwright/playwright.config.ts component-theming -g dropupload` 實測轉綠,並重跑全 component-theming 套件確認 **85 passed**):button(regional/disabled/whole-app 共 3)、
input、window、grid、listbox、tree、panel、groupbox、combobox、datebox、timebox、spinner、
bandbox、daterangebox(regional/whole-app 共 2)、tabbox、menubar、avatar、badge、rating(regional/whole-app 共 2)、progressmeter(regional/whole-app 共 2)、
paging(regional/whole-app 共 2)、combobutton(regional/whole-app 共 2)、selectbox(regional/whole-app 共 2)、inputgroup(regional/whole-app 共 2)、
calendar(regional/whole-app 共 2)、toolbar(regional/whole-app 共 2)、slider(regional/whole-app 共 2)、checkbox(regional/whole-app 共 2)、
messagebox(zero-regression defaults/whole-app 共 2)、popup(zero-regression defaults/whole-app 共 2)、notification(regional/whole-app 共 2)、
toast(regional/whole-app 共 2)、a(regional/whole-app 共 2)、timepicker(regional/whole-app 共 2)、chosenbox(regional/whole-app 共 2)、
cascader(regional/whole-app 共 2)、searchbox(regional/whole-app 共 2)、drawer(zero-regression defaults/whole-app 共 2)、
nav(regional/whole-app 共 2)、anchornav(regional/whole-app 共 2)、stepbar(regional/whole-app 共 2)、
coachmark(zero-regression defaults/whole-app 共 2)、colorbox(regional/whole-app 共 2)、
biglistbox(regional/whole-app 共 2)、fisheye(regional/whole-app 共 2)、pdfviewer(regional/whole-app 共 2)、
tbeditor(regional/whole-app 共 2)、signature(regional/whole-app 共 2)、cropper(regional/whole-app 共 2)、
dropupload(regional/whole-app 共 2,本次新增,已實測轉綠)。
**chip 無專屬斷言**(見上,region 不可展示;可日後補 inline 斷言)。**messagebox、popup、drawer、coachmark 同樣無 region 斷言**(結構性原因見對應列說明,非疏漏)。

**A 家族涵蓋的子部件(sub-parts — 隨父家族一併主題化,不另計、也不算缺口)**:
grid ← column / columns / row / rows / foot / footer;listbox ← listitem / listcell / listhead / listheader / listfoot / listfooter;
tree ← treeitem / treecell / treecol / treecols / treerow / treechildren / treefoot / treefooter;
tabbox ← tab / tabs / tabpanel / tabpanels;combobox ← comboitem;menu ← menubar / menuitem / menupopup / menuseparator;
bandbox ← bandpopup;panel ← panelchildren;input ← textbox / intbox / decimalbox / doublebox / longbox / passwordbox;
spinner ← doublespinner;groupbox ← caption(+ window header)。

---

## B. 候選 / Candidate（有外觀、adopter-facing,尚未宣告變數)

> 依 `zk-edition-components.md` 全表盤點後,「有 theme CSS 但無 `--zk-<comp>-*`」的視覺元件遠多於原列 4 項。
> 依優先序分兩組。「CSS」欄 = 該元件在主題內的樣式檔(相對 `src/main/resources/web/js/`)。新增流程見
> spec〈Recipe — adding a component〉:Audit → `_component-theme.css` 宣告預設 → 元件 CSS 改讀變數 →
> build + `check:css-dsp` → `component-theming.zul` demo + `component-theming.spec.ts` 斷言 → 回填本表。

### B1 — 核心 / CE 高需求（優先)

| Family | 變數? | CSS | 備註 |
|---|---|---|---|
| **radio / radiogroup**（+ checkbox 的 switch/toggle mold） | ⬜ | `zul/wgt/checkbox.css` | checkbox 的 default mold 已移入上方 A 表;radio/radiogroup 為獨立元件(共用同一支 CSS 檔),switch/toggle 是 checkbox 的另兩個 mold,皆未納入本次 curated surface |
| **rangeslider**（PE）/ **multislider**（EE） | ⬜ | `zkex/slider/css/rangeslider.css`,`zkmax/slider/css/multislider.css` | 獨立樣式檔,未隨本次 slider 一起改;共用 slider 的 DOM/知面詞彙(track/thumb/fill),可直接沿用 `--zk-slider-*` 詞彙補上 |
| **toolbarbutton** | ⬜ | `zul/wgt/toolbarbutton.css` | icon/text 按鈕;可併 button 詞彙(toolbar 本體已於上方 A 落地,見 `--zk-toolbar-*`) |
| **breadcrumb / breadcrumbitem** | ⬜ | (無專屬 CSS) | 目前走 base token;要做需先補 CSS |
| **carousel / carouselitem** | ⬜ | (無專屬 CSS) | 同上 |

### B2 — 進階 / PE·EE（低優先,多為 niche)

| Family | CSS | 備註 |
|---|---|---|
| **organigram·orgchildren·orgitem·orgnode / goldenlayout·goldenpanel / portallayout·portalchildren**（EE） | `zkmax/layout/*`,`goldenlayout/*` | 圖 / 進階版面(純版面部分見 C;biglistbox、dropupload 已移入上方 A 表) |

---

## C. 刻意不納入 / Excluded by design（➖,永久 N/A)

無「有意義的外觀變數」——結構/版面靠 flex + 全域 token;內容原子與非視覺元件無外觀面。單一 seed 換色即涵蓋。

| 類別 | 元件 |
|---|---|
| 結構 / 版面容器 | box, div, span, cell, separator, absolutelayout·absolutechildren, anchorlayout·anchorchildren, borderlayout(+ north/south/east/west/center), hlayout/vlayout, hbox/vbox(deprecated), splitter(deprecated — 另有 `_splitter.css` 變數), frozen, auxhead/auxheader, scrollview, columnlayout·columnchildren(PE), cardlayout·rowlayout·rowchildren·linelayout·lineitem·splitlayout·tablelayout·tablechildren(EE) |
| 內容原子 | label, image, imagemap, area, caption(併 groupbox/window), space |
| 非視覺 / 功能 / 巢狀資料 | script, style, timer, include, iframe, html, track, fragment, audio, video, camera, chart, fusionchart, barcode, barcodescanner, jasperreport;detail(grid 子)、group/groupfoot、listgroup/listgroupfoot(sel 子)、sliderbuttons(slider 子) |
| 工具 / 回饋 | scrollbar, loadingbar, errorbox, captcha, misc, fileupload(= button 變體) |

> A 家族的子部件(listcell / treecell / column / tab / comboitem…)已歸「A 涵蓋」,不在此重列。

---

## D. 未決 / Undecided（需一次歸類決定)

| 元件 | 現況 | 待決 |
|---|---|---|
| **confirmpopup**（CE 10.4 新原生,`zul/wgt/confirmpopup.css`,~26 色彩參考) | 走全域 token | 歸 B(dialog-like,補變數)或 C(視為 messagebox 類)?建議與 messagebox / popup 一併決定 |

---

## 完整性斷言 (Completeness)

本表以 [`zk-edition-components.md`](zk-edition-components.md)(ZK 10.4 全元件清單,依 jar 分 CE/PE/EE)為母體逐一盤點,
**共 172 個元件,已逐一核對全數歸類**(每個名稱都在本檔可搜到):
**每個元件都落在 A(已落地 48 家族)/ A-涵蓋(子部件)/ B(候選)/ C(刻意不納入,永久 N/A)/ D(未決)其中之一。**

- 「**所有元件完成**」的定義 = **B 清空 + D 歸類**(A 全綠、C 永久 N/A;完成 ≠ 每個元件都有變數)。
- 目前缺口:**B1(CE 核心)約 5 項**(toolbar 本體已移入 A,B1 剩餘的一項改為 toolbarbutton 單獨候選;slider 本體亦已移入 A,原列拆分後 rangeslider PE / multislider EE 改為獨立候選列,計入本項數;checkbox 的 default mold 本次亦已移入 A,B1 原「checkbox / radio / radiogroup」一列改為「radio / radiogroup(+ checkbox 的 switch/toggle mold)」,項數不變;messagebox 本次亦已移入 A,自 B1 移除,項數 8→7;**popup 本次亦已移入 A**,B1 原「popup / notification / toast」一列改為「notification / toast」,項數不變;**notification 本次亦已移入 A**,B1 原「notification / toast」一列改為「toast」單獨候選,項數不變;**toast 本次亦已移入 A**,B1 原「toast」單獨候選列移除,項數 7→6;**a(anchor)本次亦已移入 A**,自 B1 移除,項數 6→5)+ **B2(PE/EE 進階)約 10+ 項** 待評估補變數(**daterangebox 本次亦已移入 A**,自 B2 移除,項數 20+→19+;**timepicker 本次亦已移入 A**,自 B2 移除,項數 19+→18+;**chosenbox 本次亦已移入 A**,原「chosenbox / cascader / searchbox」一列改為「cascader / searchbox」,項數 18+→17+;**cascader 本次亦已移入 A**,原「cascader / searchbox」一列改為「searchbox」單獨候選,項數 17+→16+;**searchbox 本次亦已移入 A**,自 B2 移除〔原「searchbox」單獨候選列整列刪除〕,項數 16+→15+;**drawer 本次亦已移入 A**,原「nav·navbar·navitem·navseparator / anchornav / drawer / coachmark」一列移除 drawer〔改為「nav·navbar·navitem·navseparator / anchornav / coachmark」〕,項數 15+→14+;**nav·navbar·navitem·navseparator 本次亦已移入 A**,原「nav·navbar·navitem·navseparator / anchornav / coachmark」一列移除 nav 家族〔改為「anchornav / coachmark」〕,項數 14+→13+;**anchornav 本次亦已移入 A**,原「anchornav / coachmark」一列移除 anchornav〔改為「coachmark」單獨候選〕,項數 13+→12+;**stepbar 本次亦已移入 A**,原「step / stepbar」單獨候選列整列移除,項數 12+→11+;**coachmark 本次亦已移入 A**,原「coachmark」單獨候選列整列移除,項數 11+→10+;**colorbox 本次亦已移入 A**,原「colorbox / fisheye·fisheyebar / pdfviewer」一列移除 colorbox〔改為「fisheye·fisheyebar / pdfviewer」〕,項數 10+→9+;**biglistbox 本次亦已移入 A**,原「organigram·orgchildren·orgitem·orgnode / goldenlayout·goldenpanel / portallayout·portalchildren」前的「biglistbox」單獨候選列整列移除,項數 9+→8+;**fisheye·fisheyebar 本次亦已移入 A**,原「fisheye·fisheyebar / pdfviewer」一列移除 fisheye·fisheyebar〔改為「pdfviewer」單獨候選〕,項數 8+→7+;**pdfviewer 本次亦已移入 A**,原「pdfviewer」單獨候選列整列移除,項數 7+→6+;**tbeditor 本次亦已移入 A**,原「tbeditor(EE)」單獨候選列整列移除,項數 6+→5+;**signature 本次亦已移入 A**,原「signature / cropper / dropupload」一列移除 signature〔改為「cropper / dropupload」〕,項數 5+→4+;**cropper 本次亦已移入 A**,原「cropper / dropupload」一列移除 cropper〔改為「dropupload」單獨候選〕,項數 4+→3+;**dropupload 本次亦已移入 A**,原「dropupload」單獨候選列整列移除,項數 3+→2+)；**D** 1 項待歸類。
- 每完成一項 B,把它移入 A 表並補齊 CTV-1…9 + 測試。
