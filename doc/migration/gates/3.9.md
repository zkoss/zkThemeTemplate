# Gate 3.9 — and

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_dec030a0-0a6`

## Row (execution plan, verbatim)

| 3.9 | Land the `CLAUDE.md` pointer in `zk` — the approved draft is `doc/migration/drafts/zk-claude-md-pointer.md` (9 lines; verdict `gates/3.3.md`; approved 2026-09-10) — **and** the P1-gate follow-up (F45 / F47): the two `npm run lint -- .` lines in `zk/.github/copilot-instructions.md` (`:40`, `:143`) become what CI runs — `npm run lint -- <module>/src/main/resources/web/js` + `npm run type-check` — with a one-clause note that the repo-wide form cannot pass and rewrites files. Deps: 3.4 | 1 + draft | Sonnet | **Opus** | `bash doc/migration/tools/verify-3.9.sh` — the appended section is byte-identical to the draft; `grep -c 'npm run lint -- \.'` = 0 in both files; `git diff --numstat` touches exactly those two files | Target |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.9.sh
```


## Evaluator evidence (verbatim; exit code 0)

```text
Command 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.9.sh

stage: environment
   draft 9 lines; zk CLAUDE.md 43 lines; skill present
stage: pointer appended
stage: appended section byte-identical to the approved draft
stage: the two lint lines replaced
stage: exactly these two files changed, by exactly these amounts
2	2	.github/copilot-instructions.md
10	0	CLAUDE.md
3.9 ok — pointer appended byte-identically, two lint lines replaced
```

## Generator report

- done: `True` · files created: 0 · files edited: 0
- blockers: None. Both required edits (CLAUDE.md pointer section, and the two lint-line replacements in .github/copilot-instructions.md) were already present in the working tree before I started -- grep for 'marble-theme/SKILL.md' in CLAUDE.md returned 1 hit, and the two copilot-instructions.md lines already read as the per-module CI form. Per the task's own instruction ("If the pointer is already present ... do not append it again; run the self-check and report"), I made no writes and only ran the verification script, which reports 3.9 ok.
- self-check output:

```text
bash /Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/tools/verify-3.9.sh -> ended with "3.9 ok — pointer appended byte-identically, two lint lines replaced"; git diff --numstat showed only CLAUDE.md (+10/-0) and .github/copilot-instructions.md (+2/-2) changed in the zk repo.
```

## Planner notes

- (added by the Planner: landed commit, rulings, follow-ups)
