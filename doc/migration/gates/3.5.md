# Gate 3.5 — Rewrite the copied skill for `zk`

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_599186f0-a92`

## Row (execution plan, verbatim)

| 3.5 | **Rewrite the copied skill for `zk`** (D48, D201-A, F57, F64): (a) apply the path-rewrite map with the Planner-authored [tools/apply-path-map.js](tools/apply-path-map.js) — the Generator never reads the pages it rewrites; (b) fix the 4 STALE skill-relative `scripts/<name>` mentions to `.claude/skills/marble-theme/scripts/<name>` (D201-A); (c) **rewrite**, not append, `reference/verification.md` §Ports / §Launching / §Preview pages and `SKILL.md:67` for the `zkpreview` host: module `zkpreview/`, `cd zkpreview && ./gradlew appRun -PhttpPort=8085 --console=plain` (F51: `appRun`, never `appStart`; stdin must stay open), `PREVIEW_URL=http://127.0.0.1:8085`, pages at the context root `/<page>.zul` (D47), live-reload absent and the two `<script>` tags inert (D48), the IceBlue baseline app (8082) exists only in the template until P4, the theme prefix is read off the `reset.css` link (D51); (d) the F57 measurement rule (geometry is read off the captured PNG, never the live DOM); (e) one sentence on the zk-side zero-tolerance comparison (2.6–2.8, [tools/zero-tolerance/](tools/zero-tolerance/)); (f) the four scripts run from the copy in `zk` (F64: `count-important.js` default root → `zul/src/main/resources/web`, `audit-css.sh` / `check-default-display.js` `WEB` and `ZK_SOURCE` both → the zk tree, EE CSS noted as `../zkcml`) | 22 read (`verification.md` + `SKILL.md`) + map | Sonnet | **Opus** | `bash doc/migration/tools/verify-3.5.sh static` — every MAP / STALE *source* string from the map = 0 hits in the copy; every rewritten path exists in `zk` or `../zkcml`; `8081`, `8082`, `mvn`, `withjdk`, `npm run watch`, `ThemePreviewApp` = 0 hits; the four scripts exit as designed from the zk copy (the 3.1 command shape, cwd `zk`); then `live` — start `zkpreview` with the *exact* command the copy states, GET `/button.zul` = 200, stop (rule 2 server variant, Planner hand-run first). Deps: 3.4 | Target |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.5.sh static ; then cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.5.sh live
```


## Evaluator evidence (verbatim; exit code 0)

```text
Command 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.5.sh static
stage: environment
   zk copy present (17 files); template HEAD 94da367b
stage: copy rewritten (differs from template HEAD)
stage: no map source string left (apply-path-map --check)
REVIEW   /Users/hawk/Documents/workspace/ZK10/zk/.claude/skills/marble-theme/reference/css-dsp.md: 2 source string(s) after a path char — confirm they are already zk-side
REVIEW   /Users/hawk/Documents/workspace/ZK10/zk/.claude/skills/marble-theme/reference/pitfalls.md: 1 source string(s) after a path char — confirm they are already zk-side
CHECK OK: no source string remains in 17 file(s) (3 REVIEW line(s) above for a human)
stage: template-host tokens gone from the copy
stage: the copy states the zkpreview facts (D48 / D47 / D51 / F57)
stage: every zk-side path the copy cites exists
71 path(s) exist, 0 missing
stage: template-only facts gone (D39: no font-awesome stub or zk.wcs pair in zk; the throwaway-page dir is gretty's, not Maven's)
stage: the four scripts run from the zk copy (F64)
zul/css/tokens/_motion.css:67: scroll-behavior: auto !important;
3.5 static ok — copy rewritten, 0 leftovers, zkpreview facts stated, template-only facts gone, cited paths exist, four scripts run from zk

Command 2: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.5.sh live
stage: environment
   zk copy present (17 files); template HEAD 94da367b
stage: copy states the zkpreview command
   stated: ./gradlew appRun -PhttpPort=8085 --console=plain
stage: port 8085 free, start zkpreview with the stated command, GET /button.zul = 200, stop
   GET http://127.0.0.1:8085/button.zul → 200
stage: the throwaway-page directory the copy names is served live (zul-authoring.md), and an unknown page is a 404
   zkpreview/build/inplaceWebapp/ serves a dropped-in page live (200 + marker); unknown page → 404
3.5 live ok — the stated command served /button.zul on 8085, the throwaway-page dir is live, stopped cleanly
```

## Generator report

- done: `True` · files created: 0 · files edited: 0
- blockers: None. This was the (at least) third dispatch of this brief: steps 1 (path-rewrite tool), 2 (verification.md rewrite), 3 (SKILL.md Where-things-live/Commands rewrite), 4 (css-dsp.md Version-drift section), 5 (css-audit.md/important-reduction.md script-default sentences), 6 (layers.md font-awesome sentence), 7 (css-dsp.md zk.wcs sentence), 8 (zul-authoring.md throwaway-dir path), and 9 (probe.js module.paths shim) were all already applied and verified correct against every fact in the brief — no content differed from what the brief specifies, so no edits were made this run. Both self-checks (static and live) passed cleanly with no FAIL lines.
- self-check output:

```text
Ran both required verify-3.5.sh checks against /Users/hawk/Documents/workspace/ZK10/zk/.claude/skills/marble-theme/:

`bash .../tools/verify-3.5.sh static` -> ended "3.5 static ok — copy rewritten, 0 leftovers, zkpreview facts stated, template-only facts gone, cited paths exist, four scripts run from zk" (2 informational REVIEW lines only, in css-dsp.md and pitfalls.md, both flagged by the script itself as human-confirm-only, not failures).

`bash .../tools/verify-3.5.sh live` (600000ms timeout) -> ended "3.5 live ok — the stated command served /button.zul on 8085, the throwaway-page dir is live, stopped cleanly".

Also independently confirmed: (1) no stale template tokens (8081/8082/ThemePreviewApp/npm run watch/mvn/withjdk) remain anywhere under the skill copy; (2) all four skill-owned script mentions (audit-css.sh, check-default-display.js, count-important.js, probe.js) already carry the full `.claude/skills/marble-theme/scripts/<name>` prefix everywhere referenced (SKILL.md, css-audit.md, important-reduction.md); (3) scripts/probe.js already has the exact two-line zk shim (comment + module.paths.push) directly above the `require('@playwright/test')` line, verbatim as specified in step 9; (4) `node .claude/skills/marble-theme/scripts/probe.js` (no args) prints the usage line and exits 2 as required.
```

## Planner notes

- (added by the Planner: landed commit, rulings, follow-ups)

## Planner addendum (2026-09-11, after the verdict)

Four dispatches: the first stopped on ten "missing" path citations (eight the checker's, two template-only facts — F70), the second on
`probe.js` not resolving `@playwright/test` from the zk root, the third inside the verify script itself (a duplicated `--console=plain`,
a hyphenated probe marker ZK writes as `\-`), the fourth passed without touching the copy. One sentence was added to the copy's
`reference/verification.md` after the verdict, by the Planner and recorded here: the harness resolves its `doc/` paths three levels up
from `src/test/playwright/` — the MODULE root (`zkThemeTemplate/` in the template, `zk/zkpreview/` in zk), never the repository root.
`verify-3.5.sh static` was re-run green after that sentence.
