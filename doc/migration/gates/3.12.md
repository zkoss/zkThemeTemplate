# Gate 3.12 — one subagent each

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_cdecb5b4-9a7`

## Row (execution plan, verbatim)

| 3.10–3.14 ✂ | Copy and re-point **one subagent each** (5 items, 14–60 KB each: `md3-design-verifier`, `zk-spec-author`, `zk-theme-creator`, `zk-theme-evaluator`, `zk-theme-generator`) into `zk/.claude/agents/` via `apply-path-map.js` (measured 2026-09-11: 21 substitutions over the five, `--check` clean, `tasks/` = 0); `tasks/gen-reports/` → `doc/harness/gen-reports/` (the directory + README come from 3.18). **Three agents also carry template-host or jar-hash instructions that the map cannot fix and the briefs name line by line:** `zk-theme-creator` items 5 and 7 (`npm run build:css` / `npm run watch`, `setjdk 17 && mvn test exec:java@preview-app` → the zk build and `zkpreview` commands), `zk-spec-author` §1 (the wrong `ZK10/zkex/` / `ZK10/zkmax/` roots → `ZK10/zkcml/…`) and its hash paragraph, `zk-theme-evaluator` Gate 0b's hash paragraph (both: "hash the jars, never the checkout" → in zk `scripts/js-source-hash.sh` hashes the checkout's widget sources, 3.21). Parallel; deps: 3.4, 3.6, 3.7+3.8, 3.18, 3.21 (the scripts they cite, D204-A) | ≤ 60 each | Sonnet | **Opus** | per agent `bash doc/migration/tools/verify-3.1N.sh` (wrappers over [tools/verify-agent.sh](tools/verify-agent.sh)) — `grep -c 'tasks/'` = 0; source prefixes = 0; template-host tokens (`pom.xml`, `mvn`, `npm run watch`, `setjdk`, `exec:java`, `Hash the jars`, `ZK10/zkex/`) = 0; every cited path exists in `zk` / `../zkcml`; frontmatter opens and closes with `---` and keeps `name:`; the file differs from the original only on lines that held a mapped string or one of the brief-named hand-edit anchors | Target |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.12.sh
```


## Evaluator evidence (verbatim; exit code 0)

```text
Command 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.12.sh
stage: environment
   template HEAD 94da367b; zk-theme-creator 14347 bytes in the template
stage: agent copy present in zk
stage: copy rewritten (differs from template HEAD)
stage: no map source string left (apply-path-map --check)
REVIEW   /Users/hawk/Documents/workspace/ZK10/zk/.claude/agents/zk-theme-creator.md: 1 source string(s) after a path char — confirm they are already zk-side
CHECK OK: no source string remains in 1 file(s) (1 REVIEW line(s) above for a human)
stage: tasks/ gone; template-host tokens gone
stage: frontmatter intact
   frontmatter lines 1–7
stage: every zk-side path the agent cites exists
3 path(s) exist, 0 missing
stage: differs from template HEAD only on lines that held a mapped string or a brief-named hand-edit anchor
2 mapped line(s), 2 hand-edit block(s), 0 unexpected
3.12 ok — zk-theme-creator copied and re-pointed; no template path, no tasks/, no template-host command; frontmatter intact; cited paths exist
(exit code: 0)
```

## Generator report

- done: `True` · files created: 1 · files edited: 0
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/.claude/agents/zk-theme-creator.md`
- self-check output:

```text
Ran `bash /Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/tools/verify-3.12.sh`. Output ended with: "3.12 ok — zk-theme-creator copied and re-pointed; no template path, no tasks/, no template-host command; frontmatter intact; cited paths exist". Intermediate stages all passed: agent copy present, copy rewritten and differs from template HEAD, no map source string left (apply-path-map --check clean, 1 REVIEW line noted as informational per brief), tasks/ and template-host tokens gone, frontmatter intact (lines 1-7), all 3 cited zk-side paths exist, and diff-from-template-HEAD check found exactly 2 mapped lines + 2 hand-edit blocks + 0 unexpected changes.

Steps performed: (1) confirmed the target file did not already exist, so proceeded with fresh copy; (2) `git archive HEAD .claude/agents/zk-theme-creator.md | tar -x` from the template's committed tree into zk; (3) ran apply-path-map.js which printed "rewrote ...: 2 substitution(s)" / "done: 119 rule(s), 1 file(s) changed, 2 substitution(s)" — matches expected "2 substitution(s)"; (4) read only lines 160-189 (the numbered Workflow Rules list) to locate items 5 and 7; (5) made exactly the two named hand edits: item 5 "Build and verify" replaced with the node scripts/build-css.js / gradle compileMarbleCss / restart zkpreview sentence, and item 7 relabeled "Preview app" with the `cd zkpreview && ./gradlew appRun -PhttpPort=8085 --console=plain` / PREVIEW_URL sentence. Item 4's zk-material.css line was left untouched per instructions. No other files were read or written; no git index/branch operations were performed.
```

## Planner notes

- (added by the Planner: landed commit, rulings, follow-ups)
