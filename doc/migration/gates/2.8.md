# Gate 2.8 — tablet family, 30

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_4314cf34-fc6`

## Row (execution plan, verbatim)

| 2.8 ✂ | Zero-tolerance — **tablet family, 30** (mobile UA project) | output-heavy | Sonnet | **Opus** | `bash doc/migration/tools/verify-2.8.sh static` / `live` — the same `verify-family.sh`, family `tablet` (`tablet` project, ledger `ledgers/2.8-tablet.tsv`); same; tablet's two known 2 % opt-ins surface as `OPEN` pairs for the user to rule on, not as ledger explanations (D49, D50). **Planner hand run 2026-09-11 (F67): 30 / 30 identical at the first shot, 53 s of shots; static stops at "ledger file" as designed. Dispatched with 2.7 (brief [drafts/brief-2.8.md](drafts/brief-2.8.md))** |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.8.sh static
```

**Command 2**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.8.sh live
```


## Evaluator evidence (verbatim; exit code 0)

```text
COMMAND 1: ( cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.8.sh static ); echo "EXIT=$?"
stage: oracle: 30 tablet baselines, no orphan left, template pinned to ZK 11 FL.20260909
stage: ledger: 30 rows = the tablet oracle; statuses valid; FIXED commits exist; NOISY = the exception list (0); 0 OPEN row(s) with pairs
2.8 static ok
EXIT=0

COMMAND 2: ( cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.8.sh live ); echo "EXIT=$?"
stage: environment: curl, lsof, npx, preview-server.sh, template node_modules and harness; output under /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.6ipWXTYGy7
stage: port 8085 free before start
stage: gretty appRun serving zkpreview; GET /button.zul → HTTP 200
     52 passed (51.9s)
stage: shots round 1: --project=tablet produced 30 tablet PNGs, every one with a baseline (oracle has 30 after the re-cut)
   --- total 30: IDENTICAL 30
stage: compare round 1 (zero tolerance; exceptions at the template's own: none): 0 of 30 differ
stage: result: /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.6ipWXTYGy7/result.tsv (30 identical or within tolerance within 3 re-shoots, 0 difference(s); pairs under /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.6ipWXTYGy7/pairs/)
stage: server stopped; port 8085 free again
stage: ledger: 30 rows = the tablet oracle; statuses valid; FIXED commits exist; NOISY = the exception list (0); 0 OPEN row(s) with pairs
stage: ledger cross-check: every remaining difference is OPEN, every OPEN row still differs, every FIXED row is identical, every NOISY row within tolerance
2.8 live ok
EXIT=0

(Not truncated; both calls reproduced verbatim.)
```

## Design notes (Opus, non-binding)

Ledger read: /Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/ledgers/2.8-tablet.tsv contains exactly 32 lines of content plus a terminating newline — two comment header lines ("# oracle zkThemeTemplate 40c4ddda", "# zkpreview zk 49a7ebe65c") followed by 30 data rows, each a single tab-separated pair of PNG name and status. There is nothing else in the file: no trailing prose, no blank separators, no extra columns, no commented-out rows.

Agreement with my own live run: every one of the 30 rows is IDENTICAL, and the live stages printed "total 30: IDENTICAL 30" and "compare round 1 (zero tolerance; exceptions at the template's own: none): 0 of 30 differ", with the result line confirming 0 differences. So the statuses agree with the measurement one-for-one — there are no FIXED, NOISY or OPEN rows to reconcile, and the cross-check stage's four invariants are all vacuously satisfied on the OPEN/NOISY side and fully satisfied on the identical side. The 30 filenames are alphabetically ordered and each carries the -tablet.png suffix, matching the 30-baseline oracle the static stage reported after the re-cut.

Non-binding observations for the Planner, none of which affect pass/fail:
1. An all-IDENTICAL ledger with zero FIXED and zero NOISY rows means this item's ledger currently carries no information beyond "everything matched at these two commits". That is the desired end state, but it also means the ledger's FIXED-commit-exists and NOISY-exception-list checks in verify-2.8.sh were exercised only vacuously here; they have no coverage from this item's own data. If those checks matter for later items, worth confirming they were exercised for real somewhere upstream.
2. The two header commits pin the comparison to oracle 40c4ddda (template) and 49a7ebe65c (zk zkpreview). Nothing in the file records the ZK version string ("ZK 11 FL.20260909") that the static stage independently asserts, so the ledger alone is not self-describing about the pinned framework build — a third header line would make the artefact readable without re-running the script.
3. The live run's Playwright line reports 52 tests passed while only 30 tablet shots are compared; the extra 22 are presumably the non-tablet projects or setup specs. Harmless, but the count mismatch between "52 passed" and "30 rows" is the kind of thing a future reader may mistake for a missing-row problem.

## Generator report

- done: `True` · files created: 1 · files edited: 0
  - new: `/Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/ledgers/2.8-tablet.tsv`
- blockers: None. All 30 tablet-family rows were IDENTICAL on the first shot; no OPEN rows, so nothing was copied to zk/tasks/marble-screenshot-diffs/, and no NOISY translation was needed (noisy-exceptions.tsv currently has zero active rows — only delisted comments).
- self-check output:

```text
git status --short -- doc/migration shows only my new ?? doc/migration/ledgers/2.8-tablet.tsv (plus a pre-existing unrelated ?? 2.7-state.tsv and other agents' pending changes I did not touch). wc -l = 32 (2 header + 30 rows). head -3 matches expected header + first row. cut -f2 | sort | uniq -c = 30 IDENTICAL (plus the 2 header lines counted separately since they have no tab-field-2... actually header lines produce their own text as field1, shown as '1 # oracle...' and '1 # zkpreview...' confirming exactly 2 header lines and 30 IDENTICAL rows, 0 NOISY, 0 OPEN). verify-2.8.sh static ended with '2.8 static ok'. Live run printed all expected stage lines (environment, port 8085, gretty, shots round 1: 30/30 baselines, compare round 1: 0 of 30 differ, result, server stopped) then failed as designed at 'ledger file' before I wrote it.
```

## Planner notes

- PASSED on the first dispatch (Opus Evaluator, run `wf_4314cf34-fc6`, after 2.7). Ledger [../ledgers/2.8-tablet.tsv](../ledgers/2.8-tablet.tsv): `# oracle zkThemeTemplate 40c4ddda`, `# zkpreview zk 49a7ebe65c`, 30 rows all `IDENTICAL` at the first shot in the Generator's, the Evaluator's and the Planner's runs alike. The tablet spec's two 2 % `maxDiffPixelRatio` opt-ins never came into play at zero tolerance.
- The ledger is a template file and lands with the next user-approved batch (21).
