# Gate 1.9 — PASS
**Verify command:** `cd /Users/hawk/Documents/workspace/ZK10/zk && npm run lint -- scripts/build-css.js scripts/check-css-dsp.js && echo "stage: eslint clean for both scripts" && test "$(/usr/bin/grep -cF "scripts/*.js" .eslintrc.js)" = 1 && echo "stage: scripts override present once" && T=$(mktemp -d) && node scripts/build-css.js --module zul --out "$T" >/dev/null && n=0 && for f in $(cd "$T" && find . -name '*.css.dsp'); do cmp -s "$T/$f" "zul/codegen/resources/web/$f" && n=$((n+1)); done && test "$n" = 46 && echo "stage: 46 outputs byte-identical to the clean-built codegen" && node scripts/check-css-dsp.js --module zul --theme-dir "$T" | /usr/bin/grep -E 'MISSING *: *0' && echo "1.9 ok: lint clean, behaviour unchanged"`
**Output:**
```text
stage: eslint clean for both scripts
stage: scripts override present once
stage: 46 outputs byte-identical to the clean-built codegen
  MISSING        : 0
1.9 ok: lint clean, behaviour unchanged
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 6)
**Tree state judged:** `.eslintrc.js` gains one override block for `scripts/*.js` (node env, `ecmaVersion` 2020, `no-console` / `one-var` / `zk/noMixedHtml` off — the last a false positive on a CLI string with a bracket placeholder); the two scripts were fixed by `eslint --fix`: 8 × `zk/noNull` (null → undefined at sites whose callers test truthiness), 50 × `key-spacing` alignment in the icon-alias table, 1 × `quotes`. Output byte-identical. **Commit note (D35):** the pre-fix scripts no longer exist in the tree, so commit ① carries the scripts as fixed and commit ④ carries only the override; the Planner chose not to fabricate an intermediate.
