# Gate 2.7 — state family, 55

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_4314cf34-fc6`

## Row (execution plan, verbatim)

| 2.7 ✂ | Zero-tolerance — **state family, 55** (hover 29 / focus 24 / active 2 — the 70 counted 15 orphans, F55) | output-heavy | Sonnet | **Opus** | `bash doc/migration/tools/verify-2.7.sh static` / `live` — the same `verify-family.sh`, family `state` (`chromium` project, ledger `ledgers/2.7-state.tsv`); same ledger shape, re-shoot rule and fix-until-identical rule (D49, D50); the template session's `button-default-hover` alarm was withdrawn (8 sub-pixels of anti-aliasing, passes the harness's own comparator — F57). **Planner hand run 2026-09-11 (F67): 54 identical + `bandbox-focus` on the first re-shoot, 0 differences, 2 m 07 s; static stops at "ledger file" as designed. Dispatched with 2.8 (brief [drafts/brief-2.7.md](drafts/brief-2.7.md))** |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.7.sh static
```

**Command 2**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.7.sh live
```


## Evaluator evidence (verbatim; exit code 0)

```text
COMMAND 1: ( cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.7.sh static ); echo "EXIT=$?"
--- stdout+stderr (verbatim) ---
stage: oracle: 55 state baselines, no orphan left, template pinned to ZK 11 FL.20260909
stage: ledger: 55 rows = the state oracle; statuses valid; FIXED commits exist; NOISY = the exception list (0); 0 OPEN row(s) with pairs
2.7 static ok
EXIT=0

COMMAND 2: ( cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.7.sh live ); echo "EXIT=$?"
--- stdout+stderr (verbatim) ---
stage: environment: curl, lsof, npx, preview-server.sh, template node_modules and harness; output under /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.ofTxMVtYsj
stage: port 8085 free before start
stage: gretty appRun serving zkpreview; GET /button.zul → HTTP 200
     2 failed
     124 passed (1.8m)
stage: shots round 1: --project=chromium produced 55 state PNGs, every one with a baseline (oracle has 55 after the re-cut)
   --- total 55: DIFF 1, IDENTICAL 54
stage: compare round 1 (zero tolerance; exceptions at the template's own: none): 1 of 55 differ
     1 failed
     3 passed (4.5s)
stage: re-shoot 1: 1 now identical, 0 still differ
   bandbox-focus.png	IDENTICAL-ON-RESHOOT 1
stage: result: /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.ofTxMVtYsj/result.tsv (55 identical or within tolerance within 3 re-shoots, 0 difference(s); pairs under /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.ofTxMVtYsj/pairs/)
stage: server stopped; port 8085 free again
stage: ledger: 55 rows = the state oracle; statuses valid; FIXED commits exist; NOISY = the exception list (0); 0 OPEN row(s) with pairs
   flaky rows whose re-shoot count differs from the ledger's (informational):
   bandbox-focus.png	IDENTICAL	IDENTICAL-ON-RESHOOT 1
stage: ledger cross-check: every remaining difference is OPEN, every OPEN row still differs, every FIXED row is identical, every NOISY row within tolerance
2.7 live ok
EXIT=0

(nothing truncated)
```

## Design notes (Opus, non-binding)

Ledger /Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/ledgers/2.7-state.tsv is exactly what the brief describes and nothing more: two comment headers (`# oracle zkThemeTemplate 40c4ddda`, `# zkpreview zk 49a7ebe65c`) followed by 55 tab-separated rows (lines 3-57), alphabetically ordered by PNG name, one trailing newline, no blank lines, no prose, no summary block, no extra columns. Every row carries the same status, IDENTICAL, and that agrees with the live run I just executed: the compare stage settled at "55 identical or within tolerance within 3 re-shoots, 0 difference(s)", with 0 OPEN and 0 NOISY rows and the cross-check stage passing all four invariants. So no status contradicts a measurement.

One thing worth the Planner's attention, non-binding: bandbox-focus.png differed on round 1 and only matched after one re-shoot ("IDENTICAL-ON-RESHOOT 1"), and the script itself flagged that as an informational mismatch against the ledger's plain IDENTICAL. The ledger's two-column shape has nowhere to record that a row is only stable after a re-shoot, so a genuinely flaky state is today indistinguishable from a clean first-pass match, and this discrepancy line will reappear on every live run. Two options: add a third re-shoot-count column (keeping status semantics untouched), or classify bandbox-focus as NOISY with the tolerance/exception entry that goes with it — but note NOISY is currently asserted to equal the exception list of size 0, so the latter would need the exception list updated in the same change. Either way it is a bookkeeping fidelity question, not a correctness one. Also note the live run's Playwright output shows "2 failed / 124 passed" in the harness stage and "1 failed / 3 passed" in the compare stage; both are the expected pre-re-shoot noise the script absorbs, but if the Planner wants a quieter signal, suppressing those inner counts (or labelling them as round-1 pre-re-shoot) would stop them reading as real failures to a human skimming the log.

## Generator report

- done: `True` · files created: 1 · files edited: 0
  - new: `/Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/ledgers/2.7-state.tsv`
- blockers: None. All 55 state-family screenshots were byte-identical on this run (0 OPEN, 0 NOISY), and `verify-2.7.sh static` passed.
- self-check output:

```text
Ran `verify-2.7.sh live` (ZERO_ROOT=/tmp/zero-2.7): all stages printed through `stage: result:` and `stage: server stopped`, ending as designed at `2.7 live FAIL at: ledger file ... (written by the Generator)`. result.tsv/summary1.txt: 55/55 IDENTICAL, 0 differences, no exceptions active in noisy-exceptions.tsv (every line there is a comment). Wrote doc/migration/ledgers/2.7-state.tsv with header lines `# oracle zkThemeTemplate 40c4ddda` and `# zkpreview zk 49a7ebe65c` followed by the 55 result.tsv rows verbatim (all IDENTICAL, no translation needed since none were WITHIN-TOLERANCE/DIFF/SIZE). No prior 2.7-state.tsv existed, so no diff/merge logic was needed. Self-check commands: `git status --short -- doc/migration` shows only `?? doc/migration/ledgers/2.7-state.tsv` as mine (other listed changes are pre-existing Planner/other-agent work I did not touch); `wc -l` = 57 (2 header + 55 rows); `head -n 3` matches; `cut -f2 | sort | uniq -c` = 1 oracle line, 1 zkpreview line, 55 IDENTICAL. `bash doc/migration/tools/verify-2.7.sh static` ended with `2.7 static ok`. No OPEN rows, so no files were copied into zk/tasks/marble-screenshot-diffs/ and none needed. No files under ZK10/zk were touched; no zul/src, zk/src, scripts/, build.gradle, package.json, or .less/.css files were touched; no git index/branch operations were run.
```

## Planner notes

- PASSED on the first dispatch (Opus Evaluator, run `wf_4314cf34-fc6`, together with 2.8; 4 agents, 9.4 m). Ledger [../ledgers/2.7-state.tsv](../ledgers/2.7-state.tsv): `# oracle zkThemeTemplate 40c4ddda`, `# zkpreview zk 49a7ebe65c`, 55 rows all `IDENTICAL` (the Generator's run matched at the first shot; the Evaluator's run needed one re-shoot for `bandbox-focus`, as the Planner's hand run had — reported as informational, by design).
- Opus note, non-binding: the two-column ledger cannot record "stable only after a re-shoot", so a flaky state is indistinguishable from a clean match across runs. Kept as is: the row's statuses `IDENTICAL` / `IDENTICAL-ON-RESHOOT <n>` describe one run's luck (brief rule), and the evidence of flakiness lives in the run logs and the gate files, not in the ledger. A third column is P4 bookkeeping if anyone wants it. The "2 failed / 124 passed" and "1 failed / 3 passed" inner lines are the template's own two structural `screenshot.spec.ts` failures (F55), absorbed by the script.
- The ledger is a template file and lands with the next user-approved batch (21).
