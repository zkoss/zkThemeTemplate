# Gate 3.18b — After the P2 gate (D200-B):

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_f59f4228-2b9`

## Row (execution plan, verbatim)

| 3.18b | **After the P2 gate (D200-B):** copy the rest of the **tracked** `doc/screenshots/` (measured 2026-09-11 post-`40c4ddda`: 291 tracked files = 282 `.png` — 183 baselines + 99 forced-colors captures — + 7 `.gif` review recordings + 2 `.json`; 15.7 MB; **all 291** — `goldenlayout-page.gif` is then present twice in zk, once under `zk/doc/screenshots/` for a root document's link (3.18) and once in the oracle directory, because the oracle must stay `diff -r`-identical to the template's) into **`zk/zkpreview/doc/screenshots/`** — the directory `zkpreview`'s `playwright.config.ts:13` (`snapshotDir: '../../../doc/screenshots'`, resolved from `zkpreview/src/test/playwright/`) actually points at; the plan first said `zk/doc/screenshots/`, one level off (caught by the template session 2026-09-11) — no harness line changes. `coachmark-gallery.gif` / `portallayout-gallery.gif` are recordings, not gallery baselines (`{ext}` resolves to `.png`). One mechanical commit; re-synced the same way if the template re-cuts again before P4 | paths | Sonnet | **Opus** | `bash doc/migration/tools/verify-3.18b.sh` — stage 0 `test -f gates/P2.md`; the comparison is against the template's **committed** state, never its working tree (the forced-colors specs write straight into `doc/screenshots`, so a live checkout can hold a half-written PNG): `git -C <template> archive HEAD doc/screenshots` into a temp dir, then `diff -r` against `zk/zkpreview/doc/screenshots` is empty and the file count is computed from `git ls-files` at run time — no hard-coded total, `.DS_Store` excluded; then `live` — zkpreview up, `--project=gallery -g '^button$'` compares against the copied baseline and passes without writing a file (the first `toHaveScreenshot` comparison possible in zk; rule 2 hand-run first) | Target |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.18b.sh static ; then cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.18b.sh live
```


## Evaluator evidence (verbatim; exit code 0)

```text
Command 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.18b.sh static
stage: 0: the P2 gate has passed
stage: environment
   template HEAD 94da367b tracks 291 files under doc/screenshots
stage: copy present (zk/zkpreview/doc/screenshots)
stage: byte-identical to template HEAD (git archive → diff -r), same file count, no junk
   291 files identical to template HEAD
stage: git sees the copy as new files under zkpreview/doc/screenshots only (or already committed)
3.18b static ok — 291 screenshot files (= template HEAD's tracked set) under zk/zkpreview/doc/screenshots

Command 2: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.18b.sh live
stage: 0: the P2 gate has passed
stage: environment
   template HEAD 94da367b tracks 291 files under doc/screenshots
stage: copy present
stage: start zkpreview, compare ONE gallery shot against the copied baseline (toHaveScreenshot can compare for the first time in zk), stop
   comparing gallery › a against zkpreview/doc/screenshots/a-gallery.png
[1A[2K  1 passed (1.8s)
3.18b live ok — gallery › a compared against the copied baseline and passed; nothing written
```

## Generator report

- done: `True` · files created: 0 · files edited: 0
- blockers: None. Directory /Users/hawk/Documents/workspace/ZK10/zk/zkpreview/doc/screenshots already existed from an earlier run of this brief, so the copy step was skipped as instructed; only the count and static self-check were run this session.
- self-check output:

```text
verify-3.18b.sh static ended with "3.18b static ok — 291 screenshot files (= template HEAD's tracked set) under zk/zkpreview/doc/screenshots"; no FAIL line. File count via find = 291, matching expected.
```

## Planner notes

- Landed: zk commit `cd02d18943` (291 files, no other files touched).
- A concurrent, uncommitted 142-file typography sweep by peer session `zk-05` was present in the
  shared `zk` working tree while this evaluation ran (unrelated to this item; moves dead typography
  utility classes off preview-page wrapper `<div>`s onto explicit `<label>`s, no DOM change). zk-05
  flagged before the live stage finished that it could pollute a screenshot comparison two ways: a
  spurious mismatch against the pre-sweep baselines, or — worse — a re-cut baking the sweep's
  rendering into a commit that would then disagree with `zk` HEAD. Neither happened for `gallery › a`
  (1 passed, file count unchanged before/after, no `--update-snapshots`).
- **Correction (zk-05, verified against the harness source):** the PASS is not because the gallery
  clip excludes the swept elements — it does not. `gallery-scan.spec.ts:61` clips `.z-p-8`, the
  outermost page wrapper, which includes the page heading, section labels and the demo grid, i.e.
  exactly what the sweep touches (confirmed by reading the spec: `const wrapper =
  page.locator('.z-p-8').first();`, and `maxDiffPixelRatio: 0.01` at line 73). The real reason `a`
  passes is that spec's own 1% pixel-difference tolerance: `a-gallery.png` is 1280×289 = 369,920 px
  (confirmed), a ~3,699-pixel budget, and the sweep's text-only change on that page stays under it.
  zk-05 independently ran the full `gallery` project against the swept tree (not committed by them,
  `test-results/` only, `doc/screenshots/` left untouched — confirmed unchanged here) and got 78
  passed / 4 failed: `component-theming`, `dropupload`, `label`, `timepicker` — all four pages the
  sweep touches, each failing outside the 1% budget (`timepicker`: one-line diff, 6,583 px changed at
  identical 1280×478 dimensions, matching the file's own recorded size here — a true positive against
  a now-stale baseline, not a regression; `component-theming` also reflows, 1280×18408 → 1280×18387,
  matching this baseline's own dimensions). The wider point: the 291 PNGs just committed encode
  pre-sweep rendering, and passing 78/82 of them under 1% tolerance does not mean those 78 render
  identically to what the (uncommitted) source now says — green does not mean agreement, only
  within-tolerance.
- **Follow-up: RESOLVED 2026-09-11.** zk-05's typography sweep landed as `zk` `47d26068ed` (142 files
  under `zkpreview/src/main/webapp/web/` only, confirmed by `git log --stat`). Regenerated exactly the
  four flagged baselines against the swept HEAD: `zkpreview` started on port 8085, `--project=gallery
  -g 'gallery (component-theming|dropupload|label|timepicker)$' --update-snapshots` (Playwright 1.59.1
  — a bare `--update-snapshots` presets to `changed`, not `all`, per zk-05's warning; verified `git
  status --porcelain zkpreview/doc/screenshots` showed exactly these 4 files before committing). Full
  `gallery` project re-run afterward: 82 passed. Landed as `43d2a7d4ee`. Ownership: `doc/screenshots/`
  is this item's directory, so this session (not zk-05) regenerated and committed those four — zk-05
  had offered to do it but deferred, matching the "the directory's owning session commits it"
  convention already in force both ways.
- Logged for future live-comparison items generally (rows that run wider than one component, or
  2.6–2.8-style re-runs before P4): check the shared working tree for concurrent uncommitted changes,
  and remember that a within-tolerance PASS is not the same claim as "matches the current source."
