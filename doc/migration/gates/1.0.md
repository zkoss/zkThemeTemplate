# Gate 1.0 — PASS (after one verification-defect FAIL)
**Verify command (as first dispatched, FAIL):** `node -e "require('lightningcss'); console.log('lightningcss', require('lightningcss/package.json').version)" && test -d node_modules/lucide-static/icons && /usr/bin/grep -q '"lightningcss": "1.33.0"' package.json && /usr/bin/grep -q '"lucide-static": "1.11.0"' package.json && echo deps-ok`
**Output (FAIL):**
```text
Error [ERR_PACKAGE_PATH_NOT_EXPORTED]: Package subpath './package.json' is not defined by "exports" in /Users/hawk/Documents/workspace/ZK10/zk/node_modules/lightningcss/package.json
EXIT=1
```
**Cause:** the Planner decorated the row's claim (`require('lightningcss')` succeeds) with a version print through `require('lightningcss/package.json')`, which the package's `exports` map forbids. The install itself was correct. Verification corrected (read the version file with `fs`), Generator result reused from cache, Evaluator re-run.
**Verify command (corrected, PASS):** `cd /Users/hawk/Documents/workspace/ZK10/zk && node -e "require('lightningcss'); console.log('lightningcss', JSON.parse(require('fs').readFileSync('node_modules/lightningcss/package.json','utf8')).version)" && test -d node_modules/lucide-static/icons && /usr/bin/grep -q '"lightningcss": "1.33.0"' package.json && /usr/bin/grep -q '"lucide-static": "1.11.0"' package.json && echo deps-ok`
**Output (PASS):**
```text
lightningcss 1.33.0
deps-ok
EXIT=0
```
**Evaluator:** Sonnet 5, fresh subagent each time · 2026-09-10 · workflow `wf_abfefc3a-44e`
**Tree state judged:** `zk/package.json` + `package-lock.json` modified (uncommitted; `+137 −220 ~184` lockfile churn from exact pins). Follow-up: `@fontsource-variable/inter` was missing from this item's list — see F30 and item 1.0b.
