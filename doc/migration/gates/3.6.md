# Gate 3.6 — by path, not read

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_dec030a0-0a6`

## Row (execution plan, verbatim)

| 3.6 | Copy `doc/spec/` — 26 files, **by path, not read** (F15) — then apply the map with `apply-path-map.js`; STALE rows per D201-A (`doc/forced-colors-review.html`, `doc/mira-reports/framework-gaps.md`, `doc/spec-author-pipeline-plan.md` → the referencing sentence names the file as retired); **three hand-ruled Java rows (F63)**: `MarbleBrand.java` / `MarbleDensity.java` → `zul/src/main/java/org/zkoss/zul/theme/…` (D26 package), `MarbleThemeProvider.java` → a sentence naming `StandardThemeProvider` (D29); **plus two root docs the spec links to that 3.18's list missed (same `../` cause, F68): `doc/component-theme-typography-scope.md`, `doc/zindex-audit.md`, copied verbatim**; two never-existing targets (`marble-compact.css`, `native-severity-naming-proposal.md`) become plain text. Deps: 3.18, 3.7+3.8 | index + map | Sonnet | **Opus** | `bash doc/migration/tools/verify-3.6.sh` — 26 files; **every** relative link in the 26 pages resolves in `zk` (a link walk over all 26, not `index.md` alone — scratch pre-check 2026-09-11: 66 resolve, 6 dead → fixed by two copies + three fix rows); source prefixes = 0; the two root docs verbatim; the three Java targets exist; each page differs from HEAD only on mapped lines | Target |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.6.sh
```


## Evaluator evidence (verbatim; exit code 0)

```text
Command 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.6.sh
stage: environment
   template HEAD 94da367b: 26 spec pages; 3.7 and 3.18 present in zk
stage: zk/doc/spec present
stage: 26 files, no junk
stage: no map source string left (apply-path-map --check)
CHECK OK: no source string remains in 26 file(s)
stage: every relative link in the 26 pages resolves in zk
69 link(s) resolve, 0 dead
stage: the two root docs doc/spec links to are present verbatim (3.18 missed them: ../ links)
stage: the three F63 Java targets exist
stage: each page differs from template HEAD only on lines that held a mapped string
26 pages audited, 0 unexpected change(s)
3.6 ok — 26 pages from template HEAD 94da367b, rewritten, every link resolves in zk
```

## Generator report

- done: `True` · files created: 0 · files edited: 0
- self-check output:

```text
bash /Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/tools/verify-3.6.sh -> ends with '3.6 ok — 26 pages from template HEAD 94da367b, rewritten, every link resolves in zk'; 69 link(s) resolve, 0 dead; 26 pages audited, 0 unexpected change(s); 'CHECK OK: no source string remains in 26 file(s)'. Item was already completed in an earlier run: zk/doc/spec/ (26 files) and the two root docs (doc/component-theme-typography-scope.md, doc/zindex-audit.md) already existed, so per the brief's re-entry rule I ran only the self-check (no re-copy, no re-run of apply-path-map.js) and it passed cleanly on the first try.
```

## Planner notes

- (added by the Planner: landed commit, rulings, follow-ups)
