# Gate 1.10 — drop the `zul/font/font-awesome.css.dsp` stub (E3, chat D45)

**Verdict: PASS** · Evaluator: Sonnet 5 (independent; ran only the two row commands) · Generator: Sonnet 5 · 2026-09-10 · workflow run `wf_2f71a0dd-2dd`

## Row (execution plan, P1 addendum)

Drop the `zul/font/font-awesome.css.dsp` stub: remove it from `stubPaths` in `build-css.js`, remove the checker's two exclusions of it and refresh the header lines that still describe it as a global bundle. Ruling: chat D45 — only IceBlue ever requests that path, so the Marble tree carries no placeholder for it (the Planner had recommended keep-and-document; the user overruled).

## Verification (Evaluator, verbatim commands in the workflow script)

1. Static + lint + fresh build + checker — `EXIT=0`:
   - `font-awesome` survives only in comment lines of the two scripts; the stub path is gone from `build-css.js`
   - `npm run lint -- scripts/build-css.js scripts/check-css-dsp.js` clean
   - `node scripts/build-css.js --module zul --out $T` → **45** `.css.dsp`, no `zul/font/font-awesome.css.dsp`
   - `check-css-dsp.js --module zul`: `required by ZK : 42 (lang css-uri + 2 global bundles)`, `present (real) : 42`, `present (stub) : 0`, `MISSING : 0`, `extra in build : 3` (select, cell, bandpopup — the zk.wcs-aggregated trio), no `font-awesome` wording
2. `./gradlew :zul:clean :zul:assemble` — `EXIT=0`: `zul-11.0.0-SNAPSHOT.jar` holds **45** `.css.dsp`, no `font-awesome` entry, both `reset.css` and `reset-embed.css`.

## Generator report (summary)

Two files edited, `scripts/build-css.js` 4 lines (−3 +1) and `scripts/check-css-dsp.js` 13 lines (−7 +6); nothing else touched. The comment near `build-css.js:755` that names ZK's historical `font-awesome.css.dsp` as the origin of the DSP taglib directive was left as instructed (history, not a reference to the stub). One `rm -rf` of the Generator's own `mktemp` directory was denied by the permission system and left in `/var/folders`; no repository state affected.

## Planner notes

- **Arithmetic closed:** stage 3's 46 becomes **45 = 42 required + 3 aggregated extras**; the checker and the jar now count the same set. `tools/gate-p1.sh` stage 3 expects 45 (edited by the Planner with this item; the P1 gate verdict in [P1.md](P1.md) was taken at 46 and records the change in its amendment section).
- **Known references that stay:** two zktest pages link the default path by hand — `test2/B110-ZK-6024.zul` (`<style src="~./zul/font/font-awesome.css.dsp"/>`, a manual page whose own instructions say to open it with `theme=iceblue_c`) and `test2/B86-ZK-4120.zul` (an `<a href>` to the same path). Under Marble those requests now answer 404 instead of an empty 200; both pages are IceBlue-context manual checks, and `B110_ZK_6024Test` itself is a pure-Java `beforeWidgetCSS` mapping test that never requests the file. Also `.claude/skills/zk-component-rules/reference/zk-core-emitted-selectors.md:146` tells theme authors *not* to rely on such a stub — still correct, now moot for CE.
- **Not re-run:** the full six-stage gate. Stages 1/3/6 are covered for `zul` by command 2 and the checker run above; stages 2, 4 and 5 (lint, zkcml build, zkcml counts) touch nothing this item changed — `build-css.js --module zkex|zkmax` never emitted this stub (`belongsToModule` filters by prefix), so zkex 7 / zkmax 33 are unaffected.
- **Landed 2026-09-10:** `zk` `f4dfea2d0e` "ZK-6112: drop the unused font-awesome stylesheet stub" (two files).
