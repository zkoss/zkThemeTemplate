# Gate 2.2 — | 2.2 | Move the 159 preview / use-case ZULs + the SPA host into the module (pat

**Verdict: PASS** · Evaluator: Sonnet 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-10 · workflow run `wf_6272ff1a-e9d`

## Row (execution plan, verbatim)

| 2.2 | Move the 159 preview / use-case ZULs + the SPA host into the module (path move) | paths | Sonnet | Sonnet | Playwright `smoke` project: every page 200 with a composed body |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.2.sh static
```

**Command 2**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.2.sh live
```


## Evaluator evidence (verbatim; exit code 0)

```text
COMMAND 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.2.sh static
stage: environment: zk 11.0.0-SNAPSHOT; template tree has 159 .zul files
stage: environment: item 2.1's module layout intact
stage: page tree present
stage: page tree: same 379 files as the template (no extra, no missing, no .DS_Store)
stage: page tree: every file byte-identical to the template (159 .zul)
stage: helper classes: 14 present, launchers excluded
stage: BrandSwitcherVM / UseCaseVM: only the Marble import lines changed
stage: ZulListVM: only the directory lookup (and its import) changed
stage: the other helper classes are byte-identical
stage: zk.xml: marble-preview, debug-js, resource.dir=/web, nothing else
stage: build.gradle: +zuti +za11y and nothing else
stage: footprint under zkpreview: build.gradle modified; zk/example, zk.xml and web/ new; nothing else
2.2 static ok
EXIT=0

COMMAND 2: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.2.sh live
stage: environment: curl, lsof, node + playwright, page-smoke.js, render-smoke.spec.ts
stage: page list: 137 pages (115 strict from the template's smoke spec, 22 others incl. the SPA hosts); pv/ fragments excluded
stage: port 8085 free before start
stage: gretty appRun serving; GET web/button.zul → HTTP 200
stage: SPA host preview.zul lists 114 pages
stage: ~./ resolution: /zkau/web/img/ZK-Logo.gif 200, intbox.zul with its pv/matrix.zul template 200
   total 137  passed 137  failed 0  in 27 s
stage: page smoke: all 137 pages HTTP 200 with a composed body; the 115 strict pages show the .z-p-8 wrapper
stage: server stopped; port 8085 free again
2.2 live ok
EXIT=0
```

## Generator report

- done: `True` · files created: 16 · files edited: 1
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/webapp/web/ (159 .zul + assets, 379 files total)`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/webapp/WEB-INF/zk.xml`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/BigList.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/BiglistboxComposer.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/BinaryTreeModel.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/BrandSwitcherVM.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/Comparators.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/FakeListModel.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/FakerMatrixModel.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/LiveGroupRenderer.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/LoadingComposer.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/RowDetailComparator.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/RowLabelComparator.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/SimpleAutoComplete.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/UseCaseVM.java`
  - new: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/main/java/zk/example/ZulListVM.java`
  - edited: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/build.gradle`
- self-check output:

```text
git status --short -- zkpreview shows exactly: M build.gradle (2 insertions only, per diff --stat), and three new untracked paths (src/main/java/zk/, src/main/webapp/WEB-INF/zk.xml, src/main/webapp/web/) — matching the item's exact ownership. verify-2.2.sh static: '2.2 static ok' (159 zul files byte-identical, 379 total files, no .DS_Store, 14 helper classes with only the three specified edits, zk.xml correct, build.gradle +zuti +za11y only). verify-2.2.sh live: '2.2 live ok' — all 137 pages HTTP 200 with composed body (115 strict pages show .z-p-8 wrapper), SPA host preview.zul lists 114 pages, ~./ class-web-resource resolution confirmed for both images and templated pages (intbox.zul + pv/matrix.zul), server started and stopped cleanly, port 8085 freed.
```

## Planner notes

- (added by the Planner: landed commit, rulings, follow-ups)
- Landed in zk as `2e85f09947` (395 paths: `zkpreview/build.gradle` +2 lines, 14 helper classes, `WEB-INF/zk.xml`, the 379-file page tree). First-run PASS; the whole run took 3 m 29 s (Generator + Evaluator), the smoke over 137 pages 27 s with four browser workers.
- Why it passed first time: the Planner had run this exact layout as a throw-away the same day (rule 2, upgraded after 2.1) and the three things that would have failed a first run — missing `zuti`/`za11y`, `ZulListVM`'s class-path lookup, `codeeditor.zul`'s renamed attribute (fixed in the template first, D46) — were in the brief before dispatch. F52 records them.
- Layout is plan D44 (A): pages at `http://127.0.0.1:8085/zkpreview/web/<page>.zul`; `~./` references resolve through `org.zkoss.web.util.resource.dir=/web`. `pv/*.zul` are fragments (the template itself answers 500 for `pv/cascader-content.zul` loaded directly) and are exercised through the 20 pages that apply them.
- Follow-ups for 2.3: every template spec navigates with a leading slash (`page.goto('/button.zul')`), which Playwright resolves against the origin and so drops any path in `baseURL` — the harness cannot simply be re-pointed at `…/zkpreview/web/` (plan D47 / chat D54). Three specs read the page directory and the baselines by `__dirname`-relative paths (`../resources/web`, `../../../doc/screenshots`, `../../../doc/focus-ring-known-clips.json`); the zk checkout has no Playwright dependency yet.
