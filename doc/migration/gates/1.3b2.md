# Gate 1.3b2 — PASS
**Verify command:** `cd /Users/hawk/Documents/workspace/ZK10/zk && T=$(mktemp -d) && node scripts/build-css.js --module zul --out "$T" >/dev/null && test "$(find "$T" -name '*.css.dsp' | wc -l | tr -d ' ')" = 46 && node scripts/check-css-dsp.js --module zul --theme-dir "$T" | /usr/bin/grep -E 'MISSING *: *0' && test -s "$T/zul/font/inter-latin-variable.woff2" && test -s "$T/zul/font/inter-latin-ext-variable.woff2" && test -s "$T/zul/font/inter-LICENSE.txt" && test "$(/usr/bin/grep -rl marble "$T" | wc -l | tr -d ' ')" = 0 && echo "46 outputs, checker clean, fonts vendored, no marble"`
**Output:**
```text
  MISSING        : 0
46 outputs, checker clean, fonts vendored, no marble
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 2)
**Tree state judged:** zk/scripts/build-css.js: copyFonts restored — three Inter files from @fontsource-variable/inter land in zul/font/ of the --module zul output; a fresh build to a temp dir holds 46 .css.dsp and no 'marble' string
