# Gate 1.5b — PASS
**Verify commands (two Bash calls, Opus Evaluator):**
1. `cd /Users/hawk/Documents/workspace/ZK10/zk && test ! -e zul/src/main/java/org/zkoss/zul/theme/MarbleThemeProvider.java && git diff --quiet -- zul/src/main/resources/metainfo/zk/zk.xml && /usr/bin/grep -q '<theme-provider-class>org.zkoss.zul.theme.StandardThemeProvider</theme-provider-class>' zul/src/main/resources/metainfo/zk/zk.xml && /usr/bin/grep -q 'org.zkoss.zul.theme.browserDefault' zul/src/main/java/org/zkoss/zul/theme/StandardThemeProvider.java && /usr/bin/grep -q '~./zul/css/reset-embed.css' … && /usr/bin/grep -q '~./zul/css/reset.css' … && test "$(/usr/bin/grep -rl MarbleThemeProvider zul/src scripts | wc -l | tr -d ' ')" = 0 && ./gradlew :zul:compileJava :zul:checkstyleMain --console=plain -q && echo "zk side: …"` → **EXIT=0**
2. `cd /Users/hawk/Documents/workspace/ZK10/zkcml && git diff --quiet -- zkex/src/main/java/org/zkoss/zkex/theme/StandardThemeProvider.java && test "$(/usr/bin/grep -rl MarbleThemeProvider zkex/src zkmax/src | wc -l | tr -d ' ')" = 0 && ./gradlew :zkex:compileJava :zkmax:compileJava --console=plain -q && echo "zkcml side: …"` → **EXIT=0**
**Output (tail):** `zk side: reset folded into the base provider, wiring restored, compiles, checkstyle clean` · `zkcml side: provider chain at committed text, compiles` · EXIT=0 twice.
**Cause (FAIL only):** —
**Evaluator:** Opus 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 5)
**Opus design notes (non-binding):** (a)–(d) satisfied — same-list mutation, insert before the `zk.wcs` entry with the suffix bypass still applied afterwards, `browserDefault` false → `reset.css` / true → `reset-embed.css` read per call, `resolveThemeURL` applied, property documented. (e) the class Javadoc was reworded (Breeze → Marble) and trailing spaces trimmed — outside the strict fold; called out for the commit message. Maintainer points: (1) `uris.add` is a structural modification — callers must pass a mutable list; **Planner checked:** the only caller, `HtmlPageRenders.getStyleSheets`, builds `new LinkedList<Object>()`. (2) implicit not-found fallback; (3) `@since` prose on a pre-existing method; (4) `embedSafe` inverts the property's vocabulary — **Planner post-edits, same day:** fallback comment added, `@since` tag replaced by a "Since ZK 11.0.0…" sentence, variable renamed `browserDefault`; CRLF preserved; compile + checkstyle re-proven by P1 gate stages 1–2. (5) double resolution through `beforeWidgetCSS` — **not possible:** theme URIs become `<link>` elements directly in `HtmlPageRenders`. (6) no test in scope — F35 (open decision).
**Tree state judged:** `StandardThemeProvider.getThemeURIs` inserts the reset (D29); `MarbleThemeProvider.java` gone; `zul/zk.xml` and `zkex/…/StandardThemeProvider.java` identical to HEAD; the three ported-script comments name the base class. Uncommitted.
