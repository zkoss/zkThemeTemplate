# Gate 3.16 — PASS
**Verify command:** `cd /Users/hawk/Documents/workspace/ZK10/zk && test -L .claude/skills/zul-writer && test -f .claude/skills/zul-writer/SKILL.md && head -1 .claude/skills/zul-writer/SKILL.md | /usr/bin/grep -q '^---$' && /usr/bin/grep -q '"zul-writer"' skills-lock.json && echo "symlink resolves, frontmatter opens, lock entry present"`
**Output:**
```text
symlink resolves, frontmatter opens, lock entry present
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5 (`sonnet`), fresh subagent, given only the item id and the command above · 2026-09-10T13:48+08:00
**Transcribed by:** the Planner from the Evaluator's returned `{pass, exitCode, evidence, cause}`. Same run as 3.15.
**Tree state judged:** `zk/.claude/skills/zul-writer -> ../../.agents/skills/zul-writer`, installed with the skills CLI from `zkoss-demo/agent-skill`; `zk/skills-lock.json` gained the entry. **All three paths are ignored in `zk`** (`.gitignore:37 .agents/`, `:38 .claude/`, `:42 skills-lock.json`) exactly as `show-me` already is, so this install is machine-local by the repository's current policy — F19.
