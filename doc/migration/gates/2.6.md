# Gate 2.6 — gallery family, 98 baselines

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_1a691d81-0e0`

## Row (execution plan, verbatim)

| 2.6 ✂ | Zero-tolerance comparison — **gallery family, 98 baselines** (99 less the orphan `grid-utilities-gallery`, F55) — the template's own harness run against `zkpreview` (D49), with the tracked tools ([tools/zero-tolerance/](tools/zero-tolerance/)) | output-heavy | Sonnet | **Opus** | `bash doc/migration/tools/verify-2.6.sh static` then `live` (a wrapper over [tools/zero-tolerance/verify-family.sh](tools/zero-tolerance/verify-family.sh), family `gallery`). **Waits for the D50-A re-cut as re-scoped by chat D61 A′ / D64 / D66 (all ruled 2026-09-11; the numbers are the template session's):** the template commits the `portallayout.zul` date pin and the 17 orphan deletions now and re-cuts only the deterministic shots (`splitter`, `codeeditor`, pinned `portallayout`); `progressmeter`'s fill is given a fixed value so it does not animate (D64 A, the user's remedy); `avatar-gallery` and `component-theming-gallery` go on the evidenced exception list [ledgers/noisy-exceptions.tsv](ledgers/noisy-exceptions.tsv) and are compared at the template's own per-shot tolerance (D64 B, D66 hybrid) — the measurement of every other shot stays zero tolerance (F57). Ledger `doc/migration/ledgers/2.6-gallery.tsv`, written by the Generator: two header lines (`# oracle zkThemeTemplate <commit>`, `# zkpreview zk <commit>`) and one row per baseline, `<name>.png<TAB>STATUS` with STATUS = `IDENTICAL` / `IDENTICAL-ON-RESHOOT <1..3>` (a differing page is re-shot up to three times, F55) / `FIXED <zk-commit or WORKTREE>` / `NOISY <sub-pixel %>` (only for a name in the exception list; compared at the template's own tolerance, reported `WITHIN-TOLERANCE`) / `OPEN`; **every difference fixed** (chat D57) — a row may read `OPEN` only with the image pair in front of the user under `zk`'s `tasks/marble-screenshot-diffs/` (chat D58), and `live` exits 3 while any row is `OPEN`. static — the oracle is the re-cut one (98 gallery baselines, no orphan PNG, pin `FL.20260909`), the ledger names exactly the oracle's baselines, statuses valid, `FIXED` commits exist in `zk`, `NOISY` rows = the family's names in the exception list, `OPEN` rows have their pair; live — zkpreview started, the template's unchanged specs (`gallery` + `chromium` projects, run from the template checkout through `shots.config.ts`) shot into a scratch directory, every gallery PNG compared with the oracle at zero tolerance (`compare.config.ts`), differing pages re-shot up to 3× (`-g` on the component names, full projects as fallback), a pair written for every remaining difference, then the ledger cross-checked (a remaining difference must be `OPEN`, an `OPEN` row must still differ, a `FIXED` row must be identical, a `NOISY` row must pass at its tolerance). **Planner hand run 2026-09-11 (rule 2):** static stops at "oracle: re-cut landed (99, expected 98)" as designed; live on today's un-re-cut oracle: shots 2.0 m (206 passed, the 2 structural failures of F55), compare 9 s — 94 identical, the four deterministic F55 differences remain (the two flaky ones were identical at the first shot); two script defects found and fixed on the way (F58). **Fourth hand run after the D66 hybrid was built in (exception list active): see F59. Fifth hand run on the re-cut oracle (2026-09-11, F60): 98 / 98 identical at zero tolerance, exception list empty; static stops at "ledger file" as designed — ready to dispatch** |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.6.sh static
```

**Command 2**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.6.sh live
```


## Evaluator evidence (verbatim; exit code 0)

```text
COMMAND 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.6.sh static
---
stage: oracle: 98 gallery baselines, no orphan left, template pinned to ZK 11 FL.20260909
stage: ledger: 98 rows = the gallery oracle; statuses valid; FIXED commits exist; NOISY = the exception list (0); 0 OPEN row(s) with pairs
2.6 static ok
EXIT=0

COMMAND 2: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.6.sh live
---
stage: environment: curl, lsof, npx, preview-server.sh, template node_modules and harness; output under /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.nRzE22UpGk
stage: port 8085 free before start
stage: gretty appRun serving zkpreview; GET /button.zul → HTTP 200
     2 failed
     206 passed (2.0m)
stage: shots round 1: --project=gallery --project=chromium produced 98 gallery PNGs, every one with a baseline (oracle has 98 after the re-cut)
   --- total 98: DIFF 4, IDENTICAL 94
stage: compare round 1 (zero tolerance; exceptions at the template's own: none): 4 of 98 differ
     6 passed (6.9s)
stage: re-shoot 1: 3 now identical, 1 still differ
     3 passed (3.4s)
stage: re-shoot 2: 1 now identical, 0 still differ
   avatar-gallery.png	IDENTICAL-ON-RESHOOT 1
   grid-header-gallery.png	IDENTICAL-ON-RESHOOT 1
   listbox-header-gallery.png	IDENTICAL-ON-RESHOOT 1
   splitter-gallery.png	IDENTICAL-ON-RESHOOT 2
stage: result: /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.nRzE22UpGk/result.tsv (98 identical or within tolerance within 3 re-shoots, 0 difference(s); pairs under /var/folders/sf/_9gcp8w151bfs2jl0d597sbr0000gn/T/tmp.nRzE22UpGk/pairs/)
stage: server stopped; port 8085 free again
stage: ledger: 98 rows = the gallery oracle; statuses valid; FIXED commits exist; NOISY = the exception list (0); 0 OPEN row(s) with pairs
   flaky rows whose re-shoot count differs from the ledger's (informational):
   grid-header-gallery.png	IDENTICAL	IDENTICAL-ON-RESHOOT 1
   listbox-header-gallery.png	IDENTICAL	IDENTICAL-ON-RESHOOT 1
   splitter-gallery.png	IDENTICAL	IDENTICAL-ON-RESHOOT 2
stage: ledger cross-check: every remaining difference is OPEN, every OPEN row still differs, every FIXED row is identical, every NOISY row within tolerance
2.6 live ok
EXIT=0

(Not truncated — both calls are reproduced verbatim in full.)
```

## Design notes (Opus, non-binding)

Ledger inspection (/Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/ledgers/2.6-gallery.tsv), non-binding:

Shape is exactly as specified. 100 lines: two `#` header lines (`# oracle zkThemeTemplate 40c4ddda`, `# zkpreview zk 79ce20577a`) carrying one field each, then 98 two-field rows. No blank lines, no duplicate filenames, rows are in C-locale sorted order, file ends with a single newline, nothing else in the file — no trailing summary, no comments interleaved among the rows. So: nothing besides the header and the rows.

Status agreement: 97 rows are `IDENTICAL`, 1 (`avatar-gallery.png`) is `IDENTICAL-ON-RESHOOT 1`. There are zero OPEN, FIXED and NOISY rows, which is consistent with the static stage line ("NOISY = the exception list (0); 0 OPEN row(s) with pairs") and with the live cross-check passing vacuously on the OPEN/FIXED/NOISY clauses. Against my own live run every status agrees at the level the gate actually asserts: all 98 gallery shots ended identical to their baseline within the 3 re-shoot budget, 0 remaining differences, so no row is mis-classified as clean when it is not, and none is parked as OPEN when it now matches.

The one thing worth the Planner's attention is the flake accounting, which the script itself prints as informational and does not fail on. My run needed re-shoots for four rows — avatar (1), grid-header (1), listbox-header (1), splitter (2) — while the ledger records only avatar as flaky and marks the other three plain `IDENTICAL`. Round 1 reported DIFF 4 / IDENTICAL 94, i.e. 4 % of the gallery was non-deterministic on first shot in this run. That the single flake the ledger does record (avatar, count 1) matched my run exactly is probably coincidence rather than signal: grid-header, listbox-header and splitter are all header-measurement / splitter-position pages, the classic layout-settling shapes, and they look like the same underlying timing class as avatar. Two readings are open and the file cannot distinguish them: either the Generator's capture run genuinely happened to be clean on those three (in which case the `IDENTICAL` entries are honest and the re-shoot column is simply a per-run observation that should not be treated as a stable attribute), or the re-shoot counts were recorded from a run whose settling differed from mine. If the intent is for the second column to be a durable property of the page, three rows are already stale one run later, and the column will keep drifting on every re-verification; if it is meant only as a record of the capture run that produced the baseline pin, it would be worth saying so in a third header line, since the cross-check stage prints the divergence loudly enough that a future reader will read it as a defect. Either way it does not affect the gate: the measurement is 0 differences, and the exit codes decide.

## Generator report

- done: `True` · files created: 1 · files edited: 0
  - new: `/Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/ledgers/2.6-gallery.tsv`
- self-check output:

```text
git status --short -- doc/migration shows only `?? doc/migration/ledgers/2.6-gallery.tsv` as new (plus pre-existing unrelated pending changes from another agent: `A doc/migration/drafts/brief-2.6.md`, `M doc/migration/marble-to-zk-execution-plan.md`, left untouched). Ledger has 100 lines (2 header + 98 rows): header lines are `# oracle zkThemeTemplate 40c4ddda` and `# zkpreview zk 79ce20577a`; status counts are 97 IDENTICAL + 1 IDENTICAL-ON-RESHOOT 1 (avatar-gallery.png, flaky on this run only, reshot once and matched — per the flaky-row rule, written as this run printed it). No OPEN, no NOISY, no FIXED rows, so step 4 (copying diff pairs into zk/tasks/marble-screenshot-diffs/) did not apply. `verify-2.6.sh live` ran to completion with every expected `stage:` line and stopped exactly at the designed `2.6 live FAIL at: ledger file ... (written by the Generator)` before I wrote the file. After writing the ledger, `verify-2.6.sh static` ended with `2.6 static ok`.
```

## Planner notes

- PASSED on the first dispatch (Opus Evaluator, run `wf_1a691d81-0e0`, 8.1 m, 2 agents). The ledger [../ledgers/2.6-gallery.tsv](../ledgers/2.6-gallery.tsv) holds `# oracle zkThemeTemplate 40c4ddda`, `# zkpreview zk 79ce20577a` and 98 rows: 97 `IDENTICAL`, 1 `IDENTICAL-ON-RESHOOT 1` (`avatar-gallery`). No `OPEN`, `NOISY` or `FIXED` row; the exception list is empty (F60).
- The Evaluator's own `live` run saw four first-shot differences (`avatar-gallery`, `grid-header-gallery`, `listbox-header-gallery`, `splitter-gallery`), all rescued within two re-shoots — the template session's second full-suite run was executing on the same machine at that moment (F60/F61: load flakes). The script reported the three rows whose re-shoot count differs from the ledger's as informational, by design.
- The ledger is a template file, so it lands with the next user-approved template batch (20), not on PASS (D49-A covers `zk`/`zkcml` only). No `zk` follow-up: the zkpreview re-sync `79ce20577a` preceded the run.
- Method record: five Planner hand runs preceded the dispatch (F58–F60); the oracle is the D50-A re-cut as re-scoped by D61 A′ / D64 / D66; splitter's rare 156 px geometry (F60) showed up as `IDENTICAL-ON-RESHOOT 2` in the Evaluator's run and is covered by the re-shoot rule, not by an exception.
