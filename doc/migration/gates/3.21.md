# Gate 3.21 — Port the three template scripts the agents and `doc/spec` cite (chat D204-A; MAP identity rows that P1 never copied):

**Verdict: PASS** · Evaluator: Opus 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_50a34aa4-115`

## Row (execution plan, verbatim)

| 3.21 | **Port the three template scripts the agents and `doc/spec` cite (chat D204-A; MAP identity rows that P1 never copied):** `scripts/check-icon-coverage.sh`, `scripts/js-source-hash.sh`, `scripts/render-iceblue-baseline.sh` **plus its dependency `scripts/capture-iceblue.js`** into `zk/scripts/` via `git archive`, then `apply-path-map.js` and the hand rules in the brief: `check-icon-coverage.sh` scans `zkpreview/src/main/webapp/web` and reads the FA / custom keys from `zk/scripts/build-css.js` (`lucide-static` is in `zk`'s `package.json`); `js-source-hash.sh` is **re-pointed, not merely re-pathed** — in `zk` the checkout *is* the artefact, so it hashes `<module>/src/main/resources/web/js/<rel>` (`zul`, then `../zkcml/zkmax` / `zkex`) instead of `~/.m2` jars and `pom.xml`, same concatenation order, same exit codes (0 / 1 missing file / 2 cannot verify); `render-iceblue-baseline.sh` + `capture-iceblue.js` keep port 8082 (the IceBlue host exists only from P4) and run with `zkpreview/node_modules` on `NODE_PATH` (`@playwright/test` lives there, not at the `zk` root). Deps: 3.18; runs before 3.10–3.14 | 4 files | Sonnet | **Opus** | `bash doc/migration/tools/verify-3.21.sh` — the four files tracked under `zk/scripts/`; `bash -n` / `node --check` pass; source prefixes = 0; `check-icon-coverage.sh` exits 0 from `zk` and its "icon references checked" count equals the Planner's pre-computed count over `zkpreview`'s pages; `js-source-hash.sh` exits 1 with no args and 1 for a missing path, and for one contract's `js-source-files:` list prints the `shasum -a 256` of the concatenated checkout files (Planner-computed reference); `render-iceblue-baseline.sh` with `ICEBLUE_URL` at a closed port fails in the node step exactly as the template does under the same condition | Target |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.21.sh
```


## Evaluator evidence (verbatim; exit code 0)

```text
Command 1: cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-3.21.sh
stage: environment
   template HEAD 94da367b; zk deps present
stage: four files present in zk/scripts
stage: syntax
stage: no map source string left; template-only tokens gone
REVIEW   /Users/hawk/Documents/workspace/ZK10/zk/scripts/js-source-hash.sh: 3 source string(s) after a path char — confirm they are already zk-side
CHECK OK: no source string remains in 4 file(s) (3 REVIEW line(s) above for a human)
stage: check-icon-coverage.sh exits 0 from zk and counts what the template's own run counts
   205 icon references in zk == 205 in the template
stage: js-source-hash.sh: exit 1 with no args, exit 1 for a missing file, the checkout hash for a contract's list (zul and zkmax)
   codeeditor: d4b94046a72e6433250872ff9f517f8c032d33329d2b1df9711e5d8df0f3837d == checkout (2 files)
   cropper: c72d379f96cb3f5f3d91f8df64d52c7ea3ae678f37d776d2e478252841a017e5 == checkout (2 files)
stage: render-iceblue-baseline.sh: Playwright loads from zkpreview/node_modules; a closed port fails in the navigation, nothing is written
stage: git sees exactly the four new files under scripts/
3.21 ok — four scripts ported: 205 icon references (== template), js-source-hash matches the checkout for zul and zkmax, iceblue capture reaches the network step

(exit code 0)
```

## Generator report

- done: `True` · files created: 0 · files edited: 0
- blockers: None. This was the second dispatch: the four files already existed in zk/scripts/ from an earlier run. Per the brief's second-dispatch instructions, I re-ran step 2 (apply-path-map.js) only against check-icon-coverage.sh — it reported "0 file(s) changed, 0 substitution(s)" (not the "1/1" the brief anticipated), because inspection showed the ZUL_DIR line was already correctly rewritten to zkpreview/src/main/webapp/web from the earlier run — i.e. idempotent with nothing left to substitute, which is consistent with the brief's note that the tool is idempotent on already-rewritten text. No files were touched by me this dispatch (0 created, 0 edited); the four target files (check-icon-coverage.sh, js-source-hash.sh, render-iceblue-baseline.sh, capture-iceblue.js) already existed from the prior run and needed no further fixes since the self-check passed clean with the terminal "3.21 ok" line.
- self-check output:

```text
Ran `bash /Users/hawk/Documents/workspace/zkThemeTemplate/doc/migration/tools/verify-3.21.sh` — full raw output:\n\nstage: environment\n   template HEAD 94da367b; zk deps present\nstage: four files present in zk/scripts\nstage: syntax\nstage: no map source string left; template-only tokens gone\nREVIEW   /Users/hawk/Documents/workspace/ZK10/zk/scripts/js-source-hash.sh: 3 source string(s) after a path char — confirm they are already zk-side\nCHECK OK: no source string remains in 4 file(s) (3 REVIEW line(s) above for a human)\nstage: check-icon-coverage.sh exits 0 from zk and counts what the template's own run counts\n   205 icon references in zk == 205 in the template\nstage: js-source-hash.sh: exit 1 with no args, exit 1 for a missing file, the checkout hash for a contract's list (zul and zkmax)\n   codeeditor: d4b94046a72e6433250872ff9f517f8c032d33329d2b1df9711e5d8df0f3837d == checkout (2 files)\n   cropper: c72d379f96cb3f5f3d91f8df64d52c7ea3ae678f37d776d2e478252841a017e5 == checkout (2 files)\nstage: render-iceblue-baseline.sh: Playwright loads from zkpreview/node_modules; a closed port fails in the navigation, nothing is written\nstage: git sees exactly the four new files under scripts/\n3.21 ok — four scripts ported: 205 icon references (== template), js-source-hash matches the checkout for zul and zkmax, iceblue capture reaches the network step\n\nManually confirmed the REVIEW line refers to the four legitimate widget-source root path strings (zul/src/..., zk/src/..., ../zkcml/zkmax/src/..., ../zkcml/zkex/src/...) defined in js-source-hash.sh — these are zk-side by design, not leftover template paths. No FAIL line was emitted.
```

## Planner notes

- (added by the Planner: landed commit, rulings, follow-ups)
