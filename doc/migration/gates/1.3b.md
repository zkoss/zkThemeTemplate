# Gate 1.3b — FAIL (verification defect) → re-verified, see below
**Verify command (as dispatched):** `cd /Users/hawk/Documents/workspace/ZK10/zk && T=$(mktemp -d) && node scripts/build-css.js --module zul --out "$T" >/dev/null && test "$(find "$T" -name '*.css.dsp' | wc -l | tr -d ' ')" = 46 && node scripts/check-css-dsp.js --module zul --theme-dir "$T" | tail -3 | /usr/bin/grep -E 'MISSING *: *0' && test "$(/usr/bin/grep -c '/Users/' scripts/build-css.js scripts/check-css-dsp.js | awk -F: '{s+=$2} END {print s}')" = 0 && echo "46 outputs, checker clean, no hardcoded home"`
**Output:**
```text
EXIT=1
```
**Cause (Evaluator, from the output alone):** a silent stage failed; no stage printed anything.
**Planner triage (read-only, stage by stage):** stage 1 exit 0; stage 2 count = 46; **stage 3 failed** — the ported checker prints `MISSING        : 0` correctly but eight lines above the end (the informational "extra in build" block follows it), so `tail -3` never saw it. Stage 4 = 0. The port meets the row's claim; the Planner's `tail -3` is the defect — the same decoration mistake as gate 1.0, now in `tasks/lessons.md`.
**Also surfaced by the Generator (not a verification failure):** it dropped the template's `copyFonts` (Inter woff2 vendoring from `@fontsource-variable/inter`) because F26 did not list it and the package is absent in `zk`. That is a Planner spec gap — F30 — and becomes items 1.0b and 1.3b2; the `~./marble/font/` URLs inside `_fonts.css` become item 1.3c (F31).
**Re-verification:** the corrected command (full-output grep) is run by a fresh Evaluator in the resumed workflow; its result is appended below by the Planner.
