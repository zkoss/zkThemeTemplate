# Gate 1.3c — PASS
**Verify command:** `cd /Users/hawk/Documents/workspace/ZK10/zk && test "$(/usr/bin/grep -rl 'marble/' zul/src/main/resources/web/zul/css zul/src/main/resources/web/js/zul --include='*.css' | wc -l | tr -d ' ')" = 0 && test "$(/usr/bin/grep -c '~./zul/font/' zul/src/main/resources/web/zul/css/tokens/_fonts.css)" = 4 && T=/Users/hawk/Documents/workspace/zkThemeTemplate/src/main/resources/web && d=0 && for f in $(cd "$T" && find zul/css js/zul -name '*.css' | sort); do cmp -s "$T/$f" "zul/src/main/resources/web/$f" || { d=$((d+1)); echo "differs: $f"; }; done && test "$d" = 1 && ! cmp -s "$T/zul/css/tokens/_fonts.css" zul/src/main/resources/web/zul/css/tokens/_fonts.css && echo "only _fonts.css differs, no marble/ left"`
**Output:**
```text
differs: zul/css/tokens/_fonts.css
only _fonts.css differs, no marble/ left
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 2)
**Tree state judged:** zk's copy of zul/css/tokens/_fonts.css: four ~./marble/font/ → ~./zul/font/; the other 86 CE sources stay byte-identical; the template original untouched
