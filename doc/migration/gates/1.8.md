# Gate 1.8 — PASS
**Verify commands (three Bash calls, Sonnet Evaluator):**
1. `./gradlew :zul:checkMarbleCss --console=plain -q && echo "zul: passes on codegen"` → **EXIT=0**
2. `cd /Users/hawk/Documents/workspace/ZK10/zk && T=$(mktemp -d) && node scripts/build-css.js --module zul --out "$T" >/dev/null && T2=$(mktemp -d) && rsync -a --exclude 'button.css.dsp' "$T/" "$T2/" && if ./gradlew :zul:checkMarbleCss -PmarbleCssThemeDir="$T2" --console=plain -q >/dev/null 2>&1; then echo "UNEXPECTED: passed with button.css.dsp missing"; exit 1; else echo "zul: fails on the copy missing button.css.dsp, as required"; fi` → **EXIT=0** (negative control without deleting anything, F38)
3. `cd /Users/hawk/Documents/workspace/ZK10/zkcml && ./gradlew :zkmax:checkMarbleCss :zkex:checkMarbleCss --console=plain -q && echo "zkcml: passes on codegen"` → **EXIT=0**
**Output (excerpt):**
```text
CSS.DSP coverage check  (--module zul)   required by ZK : 42  present (real) : 42  present (stub) : 0  MISSING : 0  extra in build : 4
✓ All css-uri ZK requests are present in the build.
zul: fails on the copy missing button.css.dsp, as required
CSS.DSP coverage check  (--module zkex)  required by ZK : 7   present (real) : 5   present (stub) : 2  MISSING : 0  extra in build : 1
CSS.DSP coverage check  (--module zkmax) required by ZK : 31  present (real) : 27  present (stub) : 4  MISSING : 0  extra in build : 2
zkcml: passes on codegen
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 5)
**Tree state judged:** `checkMarbleCss` task in both build files directly after `compileMarbleCss`, same guard, `dependsOn(compileMarbleCss)`, `node <zk>/scripts/check-css-dsp.js --module <name>` plus `--theme-dir` when `-PmarbleCssThemeDir` is given, `check.dependsOn(checkMarbleCss)`. Uncommitted. **Planner note:** the "extra in build" line for `zul` lists `zul/css/ext.css.dsp` — the stale pre-Marble file of F43, present because codegen was never cleaned; the P1 gate builds from `clean`.
