# Gate 3.2 — PASS
**Verify command:** `cd /Users/hawk/Documents/workspace/zkThemeTemplate && node doc/migration/tools/check-path-map.js`
**Output:**
```text
rows=101 found=101 MAP=84 STALE=11 DEFER=1 DROP=2 OUTPUT=3
PASS: every path string is mapped, every MAP row is mechanical per the ruled families
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5 (`sonnet`), fresh subagent, given only the item id and the command above · 2026-09-10T13:20+08:00
**Transcribed by:** the Planner from the Evaluator's returned `{pass, exitCode, evidence, cause}`. Workflow run `wf_ca998812-091` resumed with `args.items = ["3.3","3.2"]` (3.3 replayed from cache); Generator = a different Sonnet 5 subagent; ≈15 KB read + 19 KB written.
**Checker integrity:** `doc/migration/tools/check-path-map.js` is Planner-authored; its SHA-256 `2f33cd72…0841` was recorded before dispatch and matched after the run — the Generator did not touch the checker.
**Tree state judged:** `doc/migration/path-rewrite-map.md` (129 lines), untracked; the 11 STALE rows are the input to the proposed item 3.17 (repair dead links in the originals).
