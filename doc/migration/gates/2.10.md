# Gate 2.10 — the theme-prefix lookups in `zkpreview`'s `screenshot.spec.ts` follow the `reset.css` link

**Verdict: PASS** · Evaluator: Sonnet 5 (independent; ran only the verify script) · Generator: Sonnet 5 · 2026-09-11 · workflow run `wf_9fa37d06-b64`

## Row (execution plan, verbatim)

| 2.10 | Adapt the two theme-prefix lookups in `zkpreview`'s copy of `screenshot.spec.ts` (D51-A): the prefix is derived from the `reset.css` stylesheet link instead of a `/marble/` path segment, so `combo.css.dsp` is fetched with or without a theme segment | 4 lines | Sonnet | Sonnet | `bash doc/migration/tools/verify-2.10.sh static` then `live` (brief [drafts/brief-2.10.md](drafts/brief-2.10.md)): static — the four lines present, `git diff --numstat` 4/4 on that one file and nothing else under `zkpreview`, the 15 harness files diffed against the template with 2.3's allowlist plus these four lines (`verify-2.3.sh`'s own git-diff checks assume the pre-commit 2.3 tree, so its harness diff is repeated inside `verify-2.10.sh`; its allowlist carries the four lines too); live — the copied harness's `--project=chromium -g "timezone <select>|open-state"` (hyphen: `open state` selects a different, passing test) reports 2 passed against `zkpreview`. |

## Verification commands (the Evaluator typed these and nothing else)

**Command 1**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.10.sh static
```

**Command 2**

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.10.sh live
```


## Evaluator evidence (verbatim; exit code 0)

```text
COMMAND 1 (cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.10.sh static):
stage: environment: spec copy and the template harness present
stage: spec copy: prefix derived from the reset.css link in both tests; nothing else about the fetch changed
stage: footprint: exactly the four replaced lines in screenshot.spec.ts, nothing else under zkpreview
stage: harness: 15 files diffed against the template; only the 2.3 lines and the four 2.10 lines differ
2.10 static ok
EXIT=0

COMMAND 2 (cd /Users/hawk/Documents/workspace/zkThemeTemplate && bash doc/migration/tools/verify-2.10.sh live):
stage: environment: curl, lsof, npx, preview-server.sh, node_modules
stage: port 8085 free before start
stage: gretty appRun serving; GET /datebox.zul → HTTP 200
stage: reset link /zkres/web/86139746/zul/css/reset.css;jsessionid=node06h8bawz8fgtv106wbi2c4x8en1.node0; combo.css.dsp under its prefix → 200
[1/2] hromium] › src/test/playwright/screenshot.spec.ts:296:7 › input focus (no layout shift) › datebox timezone <select>: Mechanism A ring, no border-width transition
[2/2] hromium] › src/test/playwright/screenshot.spec.ts:359:7 › input focus (no layout shift) › datebox & bandbox open-state: inset ring, no border-width bump
  2 passed (2.5s)
stage: copied harness, --project=chromium -g "timezone <select>|open-state" against http://127.0.0.1:8085: 2 passed
stage: server stopped; port 8085 free again
2.10 live ok
EXIT=0
```

## Generator report

- done: `True` · files created: 0 · files edited: 1
  - edited: `/Users/hawk/Documents/workspace/ZK10/zk/zkpreview/src/test/playwright/screenshot.spec.ts`
- self-check output:

```text
git status --short -- zkpreview -> " M zkpreview/src/test/playwright/screenshot.spec.ts" (only file). git diff --numstat -- zkpreview -> "4\t4\tzkpreview/src/test/playwright/screenshot.spec.ts". verify-2.10.sh static -> all stages passed, ended "2.10 static ok". verify-2.10.sh live -> server started, reset link /zkres/web/86139746/zul/css/reset.css;jsessionid=... found, combo.css.dsp under that prefix returned 200, "2 passed (2.5s)" for the two targeted tests, server stopped, ended "2.10 live ok".
```

## Planner notes

- Landed as zk `9236dbe514` — one file, four lines (`git diff --numstat` 4/4), the only content edit ever made to a copied spec (plan D51-A, chat D59). The template original is unchanged: in the template the same slice yields the old `/zkau/web/<v>/marble` prefix.
- Why it passed first run: the Planner had run the identical `sed` on a throw-away copy (2 passed) and then in-tree with `verify-2.10.sh static` + `live` (green, reverted) before dispatch (rule 2, F56). Total run 71 s, two agents.
- Evaluator model: **Sonnet 5** — this run finished minutes before the user's instruction that every Evaluator is Opus 5 from now on; the script default is switched (`evalModel || 'opus'`) and every later item, 2.6–2.9 included, runs with an Opus Evaluator. The user ruled (chat D60, option A, 2026-09-11) that this gate is **not** re-evaluated with Opus: the verify commands are unchanged, the checks are exit-code counts, and the Planner ran them by hand twice before dispatch.
- Trap recorded in F56: the second test is `datebox & bandbox open-state` (hyphen); `-g "open state"` selects a different, passing test, and the pre-2.10 signature is then "1 failed, 1 passed", not two failures.
- Follow-up: none for zk. The template's `verify-2.3.sh` allowlist carries the four lines too, but that script's `git diff` checks only work on the pre-commit 2.3 tree (F56).
