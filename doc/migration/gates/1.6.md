# Gate 1.6 — PASS
**Verify command:** `test "$(git ls-files 'zul/**/*.less' | wc -l | tr -d ' ')" = 0 && test "$(/usr/bin/grep -cE 'compileLess|compileCSS|zklessc|minify-css' build.gradle)" = 0 && test "$(/usr/bin/grep -c 'font-awesome' zul/src/main/resources/web/zul/css/zk.wcs)" = 0 && test "$(/usr/bin/grep -c 'zkless' package.json)" = 0 && test "$(/usr/bin/grep -c 'minify-css' gulpfile.js)" = 0 && M=$(mktemp) && sleep 1 && ./gradlew :zul:processResources --console=plain -q && test "$(find zul/codegen/resources/web/zul/css zul/codegen/resources/web/zul/font zul/codegen/resources/web/js/zul -name '*.css.dsp' -newer "$M" | wc -l | tr -d ' ')" = 46 && test "$(find zul/codegen/resources/web -name '*.css.dsp' | xargs -n1 basename | sort | uniq -d | wc -l | tr -d ' ')" = 0 && echo "LESS gone, pipeline retired, build green with 46 Marble outputs, no duplicate basenames"`
**Output:**
```text
  1 empty stubs (zul)
  zul/css/norm.css.dsp (1947 Lucide icons)
  zul/css/reset.css + zul/css/reset-embed.css
  zul/font/ — Inter latin + latin-ext variable woff2 (+ OFL license) [3 files]
  … (46 per-file lines) …
CSS build complete for --module zul (minified).
LESS gone, pipeline retired, build green with 46 Marble outputs, no duplicate basenames
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 4)
**Tree state judged:** 67 `zul/**/*.less` staged for deletion via `git rm`; `zk/build.gradle` without the `compileCSS` / `compileLess` tasks, `processResources` depending on `compileMarbleCss` only, the `mustRunAfter` block trimmed; `zul/css/zk.wcs` without the `font-awesome.css.dsp` stylesheet line; `package.json` without `zkless-engine`; `gulpfile.js` without `build:minify-css` and its `gulp-postcss` require. Uncommitted.
**Planner notes:** (a) the Generator reported `package.json` churn beyond its edit — the three devDependencies from 1.0 / 1.0b and an npm re-sort of the `dependencies` block caused by `npm uninstall`; the `zk.xml` / `dom.ts` modifications it saw are 1.5's. All expected in a shared checkout; hunks are staged deliberately at commit. (b) The "no duplicate basenames" clause holds for `zul` but is not a real invariant (F42 — it tripped 1.7); the stale-output guard is the P1 gate's `clean` build, whose `clean.doFirst` deletes `codegen/` (F43). (c) `zul/css/ext.css.dsp` (2023-12-22) lingers in codegen as a pre-Marble artefact — gitignored, removed by `clean` (F43).
