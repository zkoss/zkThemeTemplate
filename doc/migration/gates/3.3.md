# Gate 3.3 — PASS
**Verify command:** `cd /Users/hawk/Documents/workspace/zkThemeTemplate && f=doc/migration/drafts/zk-claude-md-pointer.md && test -s "$f" && test "$(wc -l < "$f" | tr -d ' ')" -le 15 && /usr/bin/grep -q 'marble-theme' "$f" && ! ( while IFS= read -r l; do if [ "${#l}" -gt 40 ]; then /usr/bin/grep -qF -- "$l" .claude/skills/marble-theme/SKILL.md CLAUDE.md && { echo "DUPLICATED: $l"; exit 0; }; fi; done < "$f"; exit 1 ) && cat "$f"`
**Output:**
```text
## Marble theme (default look-and-feel from ZK 11.0)

Read `.claude/skills/marble-theme/SKILL.md` before touching any theme CSS, token, or `.css.dsp` registration.

- Layout: tokens/base/utility CSS under `zul/src/main/resources/web/zul/css/`; per-component CSS under `zul/src/main/resources/web/js/zul/<pkg>/css/`; EE/PE component CSS lives in `../zkcml/` (`zkmax`, `zkex`).
- Two frequent mix-ups: you edit `.css`, but the file ZK serves is the generated `.css.dsp`; no stylesheet loads by convention — each one is declared explicitly in `lang.xml` / `lang-addon.xml`.
- Every custom property uses the `--zk-` prefix; no hardcoded color values.
- Verification harness and preview module: documented in the skill's `reference/verification.md`.
- Full token/architecture rules: `doc/spec/index.md`.
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5 (`sonnet`), fresh subagent, given only the item id and the command above · 2026-09-10T12:58+08:00
**Transcribed by:** the Planner from the Evaluator's returned `{pass, exitCode, evidence, cause}`. Workflow run `wf_ca998812-091` (P3 script, `args.items = ["3.3"]`); Generator = a different Sonnet 5 subagent; 2 agents, 10 tool uses, 66 s.
**Tree state judged:** `doc/migration/drafts/zk-claude-md-pointer.md`, untracked (9 lines). The draft becomes "approved" for item 3.9 only when the user says so; a change to it after approval re-opens this gate.
