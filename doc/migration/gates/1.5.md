# Gate 1.5 — PASS (re-verified; the first run FAILed on a verification defect, F41)
**Verify commands (as dispatched, three Bash calls; Opus Evaluator):**
1. `./gradlew :zweb:compileJava :zweb:checkstyleMain :zul:compileJava :zul:checkstyleMain --console=plain -q && test -f zul/build/classes/java/main/org/zkoss/zul/theme/MarbleThemeProvider.class && test -f zul/build/classes/java/main/org/zkoss/zul/theme/MarbleBrand.class && test -f zul/build/classes/java/main/org/zkoss/zul/theme/MarbleDensity.class && /usr/bin/grep -q '<theme-provider-class>org.zkoss.zul.theme.MarbleThemeProvider</theme-provider-class>' zul/src/main/resources/metainfo/zk/zk.xml && /usr/bin/grep -q 'DEFAULT_NAME = "marble"' zweb/src/main/java/org/zkoss/web/theme/StandardTheme.java && /usr/bin/grep -q 'DEFAULT_DISPLAY = "Marble"' zweb/src/main/java/org/zkoss/web/theme/StandardTheme.java && /usr/bin/grep -q "tname = 'marble'" zul/src/main/resources/web/js/zul/dom.ts && echo "zk side: compiles, checkstyle clean, wiring present"` → **EXIT=0**
2. `cd /Users/hawk/Documents/workspace/ZK10/zkcml && ./gradlew :zkex:compileJava :zkmax:compileJava --console=plain -q && /usr/bin/grep -q 'extends org.zkoss.zul.theme.MarbleThemeProvider' zkex/src/main/java/org/zkoss/zkex/theme/StandardThemeProvider.java && echo "zkcml side: compiles against the new base"` → **EXIT=0**
3. `… node scripts/build-css.js --module zul --out "$T" … grep -- '--zk-color-primary' … no 'marble/' … && npm run lint -- zul/src/main/resources/web/js/zul/dom.ts >/dev/null 2>&1 && echo …` → **EXIT=2**, no output
**Cause (Evaluator, from the output alone):** command 3 failed silently at or after the token check.
**Planner triage:** stages 1–3 of command 3 pass by hand; the `npm run lint -- <file>` stage exits 2 with an ESLint *configuration* error (`zk/noMixedHtml` options rejected: "should NOT have more than 0 items"). The same error occurs for `zul/` and `zk/` directory-level lint and is unrelated to 1.5: `eslint-plugin-zk/dist` is a gitignored build output last built 2024-09-26 while its `src/` changed 2026-07-15 (F41). Lint is the P1 gate's pre-commit requirement, not this row's claim; removed from command 3 and the Evaluator re-run.
**Opus design notes (non-binding, verbatim gist):** (a) super-first — yes, and an improvement over the jar variant; (b) `browserDefault` honoured identically; (c) inserts before the `zk.wcs` entry, `contains()` deliberate; (d) no jar-only behaviour carried; FA suppression not reproduced in Java (handled by 1.6's `zk.wcs` edit); `@since 11.0.0` matches `gradle.properties` (11.0.0-SNAPSHOT). **Two maintainer-level concerns for the Planner:** the zkex class `StandardThemeProvider extends org.zkoss.zul.theme.MarbleThemeProvider` reads oddly and needs a release note; and **any customer provider that extends `StandardThemeProvider` directly keeps compiling but silently loses the reset stylesheet** — a quiet upgrade regression. Raised to the user as chat D34 (F39).
**Re-verification result (fresh Opus Evaluator, command 3 without the lint stage):** commands 1, 2, 3 all **EXIT=0** — `zk side: compiles, checkstyle clean, wiring present` / `zkcml side: compiles against the new base` / `token layer present, no marble/ segment`. **PASS.**
**Opus design notes on re-verification (non-binding):** (a)–(d) all confirmed faithful: super-first keeps the suffix bypass; `browserDefault` byte-equivalent to the jar variant; positional insert before the `zk.wcs` entry with the index-0 fallback; jar-only behaviour correctly shed. Two maintainer observations: (1) the Font Awesome suppression left no replacement — `zk.wcs` still listed `font-awesome.css.dsp` and LESS still generated it at judging time; **resolved by 1.6** (the `<stylesheet>` line is gone, `font-awesome.less` is deleted, the builder now emits a 0-byte stub). (2) Under a non-default theme, `Aide.injectURI` may reshape the `zk.wcs` URI so the `contains()` scan misses and the index-0 fallback carries the load; **moot after 1.5b** (D29 folds the insertion into `StandardThemeProvider` and inserts before the bypass runs). Smaller: the subclass returned a fresh list where the base returns the caller's list — 1.5b restores the same-list contract; the zkex `StandardThemeProvider extends …MarbleThemeProvider` naming oddity — removed by 1.5b.
**Procedural note (Evaluator-disclosed):** commands 1 and 3 were typed without their leading `cd` because the shell already sat in that directory, and command 1 was run three times (all EXIT=0). Verdict unaffected; recorded as F40 follow-up.
**Tree state judged:** `MarbleThemeProvider` subclass variant (D26). **Superseded by 1.5b (D29)** — this verdict certifies the tree before the fold.
