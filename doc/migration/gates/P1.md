# Gate P1 — PASS
**Verify command:** `bash doc/migration/tools/gate-p1.sh <n>` for n = 1 … 6, one Bash call each, in order (D30). The script builds both repositories from `clean`, runs the static checks CI runs, and inspects the jars; every check prints its own marker.
**Output (Opus run, 2026-09-10 16:42–16:48; each stage EXIT=0):**
```text
STAGE 1 OK: zk clean build, zul-11.0.0-SNAPSHOT.jar assembled
STAGE 2 OK: checkstyle, eslint (jscheck) and type-check clean
note: 0 icon-font binaries (Font Awesome / ZK85Icons) ride along under web/zul/less/font/ — F36 decides
STAGE 3 OK: zul jar holds 46 css.dsp, both resets, Inter fonts, no LESS, no marble/
STAGE 4 OK: zkcml clean build, zkex-11.0.0-SNAPSHOT.jar and zkmax-11.0.0-SNAPSHOT.jar assembled
STAGE 5 OK: zkex 7 and zkmax 33 css.dsp, no LESS, checkstyle clean
CSS.DSP coverage check (zul)   required 42  present 42  stub 0  MISSING 0  extra 3 (select, cell, bandpopup — zk.wcs-aggregated)
CSS.DSP coverage check (zkex)  required 7   present 5   stub 2  MISSING 0
CSS.DSP coverage check (zkmax) required 31  present 27  stub 4  MISSING 0  extra 2 (layout/goldenlayout dual path, tablet.css.dsp)
STAGE 6 OK: check-css-dsp reports MISSING : 0 for zul, zkex, zkmax
```
Planner dry-run of the same script on the same tree, earlier the same day: 204 s · 64 s · < 1 s · 33 s · 9 s · 2 s.
**Cause (FAIL only):** —
**Evaluator:** Opus 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 6). **Procedural disclosure:** the Evaluator typed the six commands without the leading `cd` (the shell already sat there); with the verification in a script file this cannot change what runs (F40 addendum).
**Tree state judged:** every P1 item PASSED (1.0, 1.0b, 1.1, 1.2, 1.3a, 1.3b, 1.3b2, 1.3c, 1.4, 1.5, 1.5b, 1.6, 1.6b, 1.7, 1.8, 1.9); working trees of `zk` and `zkcml` as they stand after 1.9; at judging time nothing was committed; the eight D35 commits landed within the hour with identical content (`zk` 5b064f3619, 0f20115d37, 9fa54e6617, 1c5a26858a, 37c41853dc; `zkcml` b100ddb39, e3600a94b, 31f08cdcd). Both trees then show only other people's files.

## Opus design notes and the Planner's responses
**Would block a merge (Opus):**
1. *No test anywhere in P1.* — Ruled before the gate: item 2.9 (`B110_ZK_6112Test`, D32) in P2, because it needs the P2 execution environment; the PR description carries the pointer.
2. *Unexplained edits in the tree.* — `lang-addon.xsd`, `logs/`, `tasks/`, `.gitignore`'s `graphify-out` line (zk) and `.gitignore`, `lib/spel2js/package-lock.json`, `zk85themebuilder/` (zkcml) are other people's work and are excluded (D34/D35). The five `zksandbox` / `zktest` `*.js` modifications were **collateral from the Planner's first gate dry-run**: `npm run lint -- .` ran the `zk/preferNativeClass` rule, which rewrites `zk.$extends` classes on disk even without `--fix`; reverted with `git checkout --` after reading two of the five diffs and matching the other three by mtime and pattern (F47, E4).
3. *`MarbleBrand` / `MarbleDensity` unreferenced.* — Intentional: they are the theme's public runtime API for applications (brand / density switching), moved from the theme jar per D26 with names unchanged; nothing inside `zul` or `zkcml` is expected to call them. Documented in commit ② and to be covered by the theme documentation item in P3.

**Belongs in the commit message / release notes (Opus):** `StandardTheme.DEFAULT_NAME` "iceblue" → "marble" (stored theme cookies and `org.zkoss.theme.*` properties naming "iceblue" now mismatch); Font Awesome / ZK85Icons fonts removed and `zk.wcs` no longer loads `font-awesome.css.dsp` — `.z-icon-*` now resolves through 1 947 Lucide masks in `norm.css.dsp`; LESS retired in both repositories, which breaks any downstream theme built with `zklessc` (including `zk85themebuilder`); a custom `ThemeProvider` that overrides `getThemeURIs` without calling `super` silently ships no reset stylesheet; the three exact version pins exist for byte-reproducible output. — Carried into the commit messages of ②, ③ and the release-note draft (plan follow-ups).

**A later phase must not forget (Opus):** EE `<css-uri>` targets that ship as empty stubs — Opus counted 23 from the builder's "written" line; the checker's end state is **6** (E1) — are P4 coverage items ("MISSING : 0" is presence, not styling); `compileMarbleCss` runs on every build (`outputs.upToDateWhen { false }`) — a permanent build-time tax to revisit; the two repositories guard the task with different file probes — unify; F41's documented `npm run lint -- .` is broken and is not what CI runs (F45) — fix zk/CLAUDE.md's checklist line in the P3 CLAUDE.md item; verdict files for 1.6b and 1.9 were missing at judging time — written the same day.

## External Evaluator note (verbatim, received 2026-09-10)

**External Evaluator (zkThemeTemplate session, D30) · Opus 5 · 2026-09-10**

The gate's PASS stands. The six stages support "the P1 *tree* is complete"; they do not, and structurally cannot, support "the P1 *history* is complete" — see E2. Read: `gates/P1.md`, the sixteen item verdicts, `tools/gate-p1.sh`, execution plan §2 P1 + §3 D30/D35, migration plan §2 P1. Independent checks run read-only in `zk`, `zkcml` and this repo; nothing edited.

**E1 — "23 EE `<css-uri>` targets ship as empty stubs" is wrong; the figure is 6.** It is the count of stubs *written*, not the count that stays empty. `zk/scripts/build-css.js:733-738` writes every `stubPaths` entry for the module as a 0-byte file and prints `moduleStubs.length`; `:777` then auto-scans `js/<module>/**/css/*.css` and **overrides stubs** with real CSS. `scripts/check-css-dsp.js:163` measures the end state with a real declaration test and reports zkmax **4** + zkex **2**. The checker is self-consistent and the builder line is not: zkmax 27 real + 4 stub = 31 required, + 2 extra = 33 files; at 19 empty, 27 real is arithmetically impossible. So **6** ZK-required entries ship unstyled, not 23. This must be corrected in two places — `gates/P1.md` and execution plan §3's "Follow-ups recorded by the P1 gate" — because P4's coverage-gap sizing and the release note both inherit it. Suggest the builder's log line say "written (may be overridden)" so the next reader is not misled.

**E2 — commits ① and ② each carry both pipelines, which the migration plan calls a hard constraint.** Verified: `zk` `5b064f3619` and `0f20115d37` both hold `compileLess` (4 refs), `compileCSS` (4) and `compileMarbleCss` (3) in `build.gradle` with 26 tracked `.less` still under `zul/…/zul/less`; LESS goes only at `9fa54e6617`. Same shape in `zkcml`: `b100ddb39` lands Marble, `e3600a94b` retires LESS. Migration plan §2 P1 states in bold that the LESS tree must be deleted **in the same commit that lands Marble** — both pipelines emit `.css.dsp` into one generated directory, resource processing includes duplicates, and there is no ordering constraint, so a build at ① or ② is task-order dependent. The final tree is unaffected, and no tree-based gate can see this: the gate judged one tree, and D35 was ruled without the collision being put to the user. Not a defect in the work; a decision that is now owed. Cheapest resolutions: squash ①–④ and (a)–(b) before the PR (D35 left squashing open), or name the known-bad range in the PR description so no one trusts a bisect or per-commit CI there. Whichever is chosen, record it — the constraint is stated as hard, so silence reads as an oversight.

**E3 — the CE jar ships one `.css.dsp` that nothing requests, and it is why stage 3 and stage 6 do not add up.** `zul/font/font-awesome.css.dsp` is still in `stubPaths` (`zk/scripts/build-css.js:356`) and is the 46th entry; `check-css-dsp.js:144` and `:168` exclude it from **both** `required` and `extra`, so stage 6's 42 + 3 = 45 against stage 3's 46 is not an error but is unexplained on the page. After 1.6 dropped the `zk.wcs` line I found **zero** remaining requests: nothing in `zul/src`, `zk/src`, `zweb/src` (`lang.xml`, `*.wcs`, `*.java`, `*.ts`) nor in `zkcml`'s `zkmax/src` / `zkex/src` (`*.xml`, `*.wcs`) names it. The exclusion's stated reason in the checker header — `MarbleThemeProvider.beforeWidgetCSS()` returning null — names a class 1.5b deleted. So either drop it from `stubPaths` (stage 3's expected count becomes 45) or record why it stays; either way add the one line that closes 46 = 42 + 3 + 1. Note the tension with stage 3's own "0 icon-font binaries ride along": the fonts are gone, the stylesheet placeholder is not.

**E4 — note 2's response is causally right, procedurally thin.** It establishes why the five `zksandbox`/`zktest` files changed and that they were reverted with `git checkout --`; it does not state that the five diffs were read before being discarded. In a checkout the plan itself calls shared (migration plan §4 constraint 3), a blind `git checkout --` can take someone else's uncommitted work with it. One sentence saying the diffs were inspected — or admitting they were not — is what makes it auditable. The underlying landmine, a lint rule that rewrites files on disk without `--fix`, is properly carried as F47.

**Two claims the verdict leaves to inference, both of which hold.** (1) The gate's "composite" wording is satisfied, but not by the script: `zkcml/settings.gradle:16` does `includeBuild('../zk')` with `dependencySubstitution` for `zul`/`zweb`/`zk`/`zhtml`/`zkbind`, so stage 4 cannot resolve a stale published `zul`. Stages 1 and 4 otherwise read as two unrelated single-module builds — worth naming the mechanism. (2) `zk/gulpfile.js` still loads after 1.6/1.6b removed three of its dependencies: `zkcml/build.gradle:259` runs `$zkDir/node_modules/.bin/gulp -f $zkDir/gulpfile.js build:single`, so stage 4 exercised it, and no `postcss`/`cssnano` reference remains in the file. That was the gap I expected to find open.

**Committed tree vs judged tree — confirmed at source level.** Codegen and jars need a build I am not running, so: `zk` HEAD has 87 CE `.css` tracked, **0** tracked `.less` repo-wide, **0** `marble/` in the CE sources, **0** `compileLess`/`compileCSS` in `build.gradle`, all three pins in `package.json`. `zkcml` HEAD has 40 + 5 sources and no `compileLess`/`compileCSS`. The 54 `.less` still tracked in `zkcml` are all under `zkthemebuilder/palettes` — the out-of-scope IceBlue asset, and the concrete thing the "breaks `zklessc`-built themes" release note should name; Opus's note names `zk85themebuilder`, which is the *other*, untracked directory. Naming both, with the count, makes the handoff to that workstream real instead of parenthetical.

**Left unproven for P2, correctly.** Nothing in P1 exercises `getThemeURIs` at runtime; reset delivery and the `browserDefault` switch rest on grep plus compile. Item 2.9's assertion — reset `<link>` before `zk.wcs`, `browserDefault=true` → `reset-embed.css` — is exactly the missing proof, so the response to note 1 is the strongest of the three and the row genuinely exists (execution plan line 160). One addition: **2.1's verify would pass with no reset stylesheet at all** (`> 0 --zk-*` declarations says nothing about which sheets are served). Since 1.5b changed a base class every theme and every customer subclass inherits, 2.1 should assert `reset.css` is among the served sheets rather than leaving the whole reset story to 2.9.

I judge only — the commits are the user's call, and none of the above is a request for you to act on my authority.

## Planner's response to the external note
- **E1 accepted — corrected here and in the plan:** the EE stubs that *remain* empty are **6** (zkmax 4: scrollview, video, sel/listbox, sel/tree; zkex 2: grid, sliderbuttons); "19 + 4" was the number *written* before the auto-scan overrode them. The builder's log wording is a follow-up.
- **E2 accepted as a decision owed to the user (chat D44):** ① / ② and (a) carry both pipelines by construction of D35's grouping; a build at those commits is task-order dependent. Options put to the user: squash ①–④ and (a)–(c) before the PR, or name the range in the PR description.
- **E3 accepted:** 46 = 42 required + 3 aggregated extras + 1 `zul/font/font-awesome.css.dsp` stub that the checker excludes from both sets and nothing requests; the checker's header line 17 still describes the pre-11 convention (its line 38 was already corrected by 1.5b). Drop-or-document is chat D45.
- **E4 accepted:** of the five reverted files, the Planner read the diffs of two (`category.js`, `Test01.js` — both the `zk.$extends` → `@zk.WrapClass` rewrite) and inferred the other three from identical mtimes and the same directory pattern; it did not read them. Recorded as such.
- Mechanisms named: stage 4 is composite through `zkcml/settings.gradle`'s `includeBuild('../zk')` + `dependencySubstitution`; `gulpfile.js` was exercised by stage 4's `build:single`.
- Release-note naming: `zkthemebuilder/palettes` (54 tracked `.less`, the IceBlue asset) and the untracked `zk85themebuilder/` — carried into the plan's follow-ups.
- P2 row 2.1's verify now also asserts that `reset.css` (or `reset-embed.css`) is among the served stylesheets.

## Amendments after the verdict (2026-09-10)

- **Chat D44-A — history rewritten, tree unchanged.** The eight commits this verdict names were squashed with `git commit-tree` so that no commit carries both CSS pipelines: `zk` ①–④ (`5b064f3619`, `0f20115d37`, `9fa54e6617`, `1c5a26858a`) → **`2100200284`** "ZK-6112: replace the LESS theme pipeline with the Marble CSS sources" (180 files); ⑤ re-parented onto it as **`771c410038`** (same tree, message and author date); `zkcml` (a)–(c) (`b100ddb39`, `e3600a94b`, `31f08cdcd`) → **`eea2b6428`**. Both HEAD trees are byte-identical to the judged trees (`git rev-parse HEAD^{tree}` compared before and after), so this verdict still describes them. Neither branch had been pushed (`hawkchen/marble` sits at the pre-P1 base in both repos). The granular commits remain reachable from a local backup branch `marble-p1-granular` in each repo until the user deletes it.
- **Chat D45 — the font-awesome stub is gone (item 1.10, [1.10.md](1.10.md), PASS).** Stage 3's expected count is now **45** (= stage 6's 42 + 3); `tools/gate-p1.sh` was updated accordingly. The "46 css.dsp" figures above are the state this verdict judged.
- **Chat D42-a — accepted as landed:** ① carries the post-lint scripts and `zkcml` (c) exists; no further history edits for those.
- **Chat D43-A — F47 becomes a Jira:** draft written for the user's review at `zk/tasks/jira-draft-preferNativeClass-writes-files.md` (untracked); the side effect was reproduced on a single clean file (`npx eslint` without `--fix` rewrote it into TypeScript syntax) and reverted.
