# Gate 2.0 — IceBlue-only tests tagged and skipped (user instruction 2026-09-10; F48)

**Verdict: PASS** · Evaluator: Sonnet 5 (independent; ran only the four row commands) · Generator: Sonnet 5 · 2026-09-10 · workflow run `wf_ed34e9c1-8b9` (Generator cached from the first launch; the Evaluator re-ran after the session window was closed mid-run)

## Row (execution plan, P2)

`@Tag("IceBlueOnly")` on `B86_ZK_4102Test` (class) and on `F96_ZK_4783Test.testIceblue()` (method); `zktest/build.gradle` excludes the tag in `test` and `testGroupForkJVMTestOnly` and adds `testIceBlueOnly`; project rule `.claude/rules/iceblue-tests.md`; one line in `.github/copilot-instructions.md`. Nothing deleted, no assertion changed.

## Verification (Evaluator, verbatim commands in the P2 workflow script)

1. Static — `EXIT=0`: tag quoted 5× in `build.gradle` (4 excludes + 1 include); `task testIceBlueOnly(type: Test)` present; class-level tag on 4102; method-level tag on `testIceblue` only (one occurrence in the file); exactly two test files carry the tag; rule file has frontmatter; run-tests doc names the task; exactly four tracked files modified under `zktest/`, `.claude/`, `.github/`; the Java diffs contain only the import and annotation lines.
2. `./gradlew test --tests B86_ZK_4102Test` — Gradle exit 1 with `No tests found for given includes: … [org.zkoss.zktest.zats.test2.B86_ZK_4102Test](--tests filter)`; no STARTED event. **The tag is honoured by `test`.** (1 m 23 s.)
3. `./gradlew testIceBlueOnly --tests B86_ZK_4102Test` — `B86_ZK_4102Test > test() STARTED` then `FAILED`. The opt-in task runs it; it fails under Marble because it asserts the IceBlue 32 px slider width — which is exactly why it is tagged.
4. `./gradlew test --tests F96_ZK_4783Test` — Gradle exit 0; `testSapphire`, `testAtlantic`, `testBreeze`, `testSilvertail` STARTED and **PASSED**; `testIceblue` never appears. **Method-level exclusion works, and the four ZK 10 theme-pack jars (10.0.1.1-Eval) still pass against the ZK 11 SNAPSHOT.**

## Generator report (summary)

Five files: `zktest/build.gradle` +38/−4 (two excludeTags lines per task, the new task modelled on `test` without the A11Y branching, jacoco and reports lines), each Java file +2 (import + annotation), `.claude/rules/iceblue-tests.md` new, `.github/copilot-instructions.md` +5/−1. `./gradlew tasks --all` lists `testIceBlueOnly` next to `testGroupForkJVMTestOnly` and `testWCAGOnly`.

## Planner notes

- **Scope (chat D46):** 30 zktest pages mention `iceblue`; 15 have Java tests; an independent read-only classification found only these two assertions to be IceBlue-calibrated. The other 13 mention IceBlue in an unused theme switcher, a label or a comment and assert theme-independent relations (alignment, equality of computed styles, class transitions). They stay untagged on purpose: tagging them would hide real Marble regressions.
- **Environment facts learned:** Chrome/WebDriver works in this environment; a zktest Gradle run costs ~1.5 min warm (5 min cold, composite build of zk + zkcml). The four theme-pack tests passing means those jars remain usable on the Marble branch — relevant to F48 / chat D47 (which theme zktest actually serves).
- **Landed 2026-09-10:** `zk` `5cfc315c9d` "ZK-6112: tag IceBlue-only tests and skip them while Marble is the default theme" (six files, including the D41 `zk.xml` line).
- **Planner post-edit (chat D47-a, plan D41):** `zktest/src/main/webapp/WEB-INF/zk.xml:712` `org.zkoss.theme.preferred` changed from `iceblue` to `marble` (1 line; XML well-formed; the file's only `iceblue` mention). Static check only — under ZK 11 `iceblue` is not a registered theme, so the previous value fell through `ThemeFns.getCurrentTheme` to the highest-priority registered theme among the four theme-pack jars; `marble` is `StandardTheme.DEFAULT_NAME` and is always registered. Runtime proof is 2.9's. Chat D46-a confirmed the two-test scope above.
