# Gate 1.4 — PASS
**Verify command:** `cd /Users/hawk/Documents/workspace/ZK10/zkcml && T=/Users/hawk/Documents/workspace/zkThemeTemplate/src/main/resources/web && a=0 && for f in $(cd "$T" && find js/zkmax zkmax/css/tablet -name '*.css' | sort); do cmp -s "$T/$f" "zkmax/src/main/resources/web/$f" && a=$((a+1)); done && test "$a" = 40 && b=0 && for f in $(cd "$T" && find js/zkex -name '*.css' | sort); do cmp -s "$T/$f" "zkex/src/main/resources/web/$f" && b=$((b+1)); done && test "$b" = 5 && O1=$(mktemp -d) && node /Users/hawk/Documents/workspace/ZK10/zk/scripts/build-css.js --module zkmax --out "$O1" >/dev/null && test "$(find "$O1" -name '*.css.dsp' | wc -l | tr -d ' ')" = 33 && node /Users/hawk/Documents/workspace/ZK10/zk/scripts/check-css-dsp.js --module zkmax --theme-dir "$O1" | /usr/bin/grep -E 'MISSING *: *0' && O2=$(mktemp -d) && node /Users/hawk/Documents/workspace/ZK10/zk/scripts/build-css.js --module zkex --out "$O2" >/dev/null && test "$(find "$O2" -name '*.css.dsp' | wc -l | tr -d ' ')" = 7 && node /Users/hawk/Documents/workspace/ZK10/zk/scripts/check-css-dsp.js --module zkex --theme-dir "$O2" | /usr/bin/grep -E 'MISSING *: *0' && echo "40 + 5 sources identical; zkmax 33, zkex 7 outputs; checkers clean"`
**Output:**
```text
  MISSING        : 0
  MISSING        : 0
40 + 5 sources identical; zkmax 33, zkex 7 outputs; checkers clean
EXIT=0
```
**Cause (FAIL only):** —
**Evaluator:** Sonnet 5, fresh subagent · 2026-09-10 · workflow `wf_abfefc3a-44e` (P1 batch 3)
**Tree state judged:** 40 untracked `.css` under `zkcml/zkmax/src/main/resources/web/{js/zkmax,zkmax/css/tablet}` and 5 under `zkcml/zkex/src/main/resources/web/js/zkex`, byte-identical to the template. The ported builder's `--module zkmax` / `--module zkex` partitions were exercised for the first time here and produced the F28 counts (33 = 32 + `tablet.css.dsp`; 7).
