{
  "//": "Marble theme — focused CSS hygiene config. Intentionally does NOT extend stylelint-config-standard: the full standard set produces ~989 mostly-stylistic findings (class-pattern flags every .z-*/.lm_*, vendor-prefix flags required -webkit-, descending-specificity flags intentional cascade) that bury the real signal. These rules target only duplication / redundancy / dead CSS — the concerns this theme actually cares about. See doc/css-audit-report.md.",
  "//fix-warning": "Run `npm run lint:css` to REPORT only. Do NOT blindly run stylelint --fix on this repo: --fix escapes `<!--` to `\\3c !--` inside /* */ comments that document DOM structure (corrupts docs). Apply --fix to a narrow glob and review the diff.",
  "rules": {
    "no-duplicate-selectors": [true, { "severity": "warning" }],
    "declaration-block-no-duplicate-properties": [true, { "ignore": ["consecutive-duplicates-with-different-values"] }],
    "declaration-block-no-duplicate-custom-properties": true,
    "declaration-block-no-redundant-longhand-properties": true,
    "shorthand-property-no-redundant-values": true,
    "no-duplicate-at-import-rules": true,
    "no-invalid-position-at-import-rule": true,
    "block-no-empty": true,
    "comment-no-empty": true,
    "no-empty-source": true
  }
}
