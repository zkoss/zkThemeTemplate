> **本檔語言為 zh-TW**,與 `doc/iceblue-drop-less-*.md`、`tasks/l4-density-mechanism.md` 一致。

# 現況查核與下一步(2026-08-14)

> **本檔是一次性的查核紀錄,不是規範也不是進度文件。** 規範看
> `doc/iceblue-drop-less-execution-plan.md`,狀態看 `doc/iceblue-drop-less-progress.md`。
> 本檔只回答三個問題:**現在的程式碼真的是文件說的樣子嗎 · 下一步是什麼 · 還有什麼沒決定。**

---

## L1 一句話結論

> **←2026-08-14 當日已裁示,本節的「擋著」已經消失。** 兩項裁示:
> **① L-4 拍板採用 ⇒ P7 解除 BLOCKED**(記為 **S57**);
> **② `git merge -s ours` 先不動**(稽核維持有效,只是不現在做)。
> ⇒ **本案自此沒有任何階段被決策擋著,下一步是直接開工 P7。**
> 以下 L1/L2 保留裁示前的判讀原文,作為「當時憑什麼這樣建議」的紀錄。

~~**唯一擋住整案的是一個決策,不是一件工作** —— **L-4 的整體採納裁示**。
裁完 P7 就解除 BLOCKED,而 P7 是最後一個工程階段(P8 是收尾)。
在那之前,**repo 裡沒有任何「可以現在自己動手」的本案實作**。~~

---

## L2.1 實測現況(今天跑的,不是引用文件)

| 項目 | 實測 | 與文件是否一致 |
|---|---|---|
| `npm run check:gate` | **exit 0** | ✅ |
| `check:baseline` | **86 檔**全部 match `doc/baseline-manifest.sha256` | ✅ |
| `check:p4a` | 移除 **731** 條 / `-webkit-` **314 = 314** / tablet **60** 條 deferred | ⚠️ 見 L2.4 |
| `check:p4b` | 移除 **14** / 新增 **7** / **9** 檔;tablet **1** 條 orphan deferred | ✅ |
| `check:density-css` | 產生器 up to date —— **333 seeds → 350 declarations(2 rounds)、512 omitted** | ✅ |
| 來源樹剩餘 `.less` | **58 個**,全部在 `less/` 目錄下 = **57 partial + 1 entry**(`zkmax/less/tablet.less`) | ✅ holdout 剩 1 |
| `check:var-table` / `check:mixin-table` | **仍然 exit 1** | ✅ 已裁示留到 P8(S14 + S37) |
| 未 commit | 3 個 untracked:`tasks/iceblue-resync-with-master.md`、`tasks/theme-pack-palette-mechanism.md`、`tasks/theme-pack-productization-options.md` | ✅ 依 `task-doc-tracking-policy.md` 刻意不追蹤 |
| 分支位置 | `iceblue` @ `19d45549`;`origin/master` 領先 **6 個 commit**,merge-base 仍在 `a89d44e0` | ⚠️ 見 L2.3 第 2 項 |

**結論:程式碼與文件一致,閘門是綠的,沒有隱藏的紅燈。**

---

## L2.2 下一步(嚴格依序)

```
① 拍板 L-4  ──►  ② P7 (tablet)  ──►  ③ D4 → D5  ──►  ④ P8
   決策            工程主體            密度平板層      收尾
```

### ① 拍板 L-4 —— 唯一該做的下一件事

- **它是決策,不是工作。** `doc/iceblue-drop-less-plan-appendix.md:1593` 的決策紀錄仍寫
  「**L-4 仍未定,不要順勢假設**」;而 `tasks/l4-density-mechanism.md` L3.4 的
  **五項子未決已於 2026-08-13 全數裁示**(commit `19d45549`)⇒ 只剩最上層那一句「採用 / 不採用」。
- **解除條件在事實層面早已滿足**:D1–D3 已完工,並對 `iceblue_c 11.0.0` oracle 逐 token
  比對 **350 條值全同、缺 0 條**。缺的只是把它寫成裁示。
- **裁示的內容**:採用 `data-density` 屬性 + 350 條 runtime 覆寫,取代 build 期換 jar
  (14.4 KB 取代 282.8 KB 的 `iceblue_c`)。
- **裁完要順手改的**:進度文件 L2.2 的 BLOCKED 表移除 P7 那一列、L1〈下一步〉第 5 項結案、
  附錄 L3-F 的 L-4 那一列改寫。

### ② P7 —— tablet,而且它一次背四段 delta

P7 不是「把一個檔轉成 CSS」,它是**整案唯一一個把四件事綁在同一段 delta 裡**的階段:

| # | 內容 | 出處 |
|---|---|---|
| 1 | `tablet.less` → `.css`(輸出端 **681** 條) | P7 本體 |
| 2 | P4a 讓出的 **60** 條前綴 | 計畫書 §P4a |
| 3 | P4b 讓出的 **1** 條 orphan(`-moz-appearance`) | 計畫書 §P4b |
| 4 | **D4** —— compact 平板規則加屬性前綴 | `l4-density-mechanism.md` D4 |

**第 4 項是真正的風險,不是第 1 項。** 桌面 compact 是純 token 值替換;平板**完全不是** ——
`var()` 用量兩側一模一樣(95 = 95),差異全是字面值與規則增減,所以
**token 覆寫對 tablet 一點作用都沒有**,兩套規則都得出貨。
而且有 **67 條只在 default 側存在**的規則,改成屬性覆寫後會**繼續生效**,
必須在 compact 區塊裡**逐條中和**。驗收條件是那 67 條**全部**對 `iceblue_c` 做
mobile UA computed-style 比對全綠 —— **不是抽驗**。

**另有兩個已記在案、P7 會再撞到的東西**:
- **S52 第 (2) 項** —— `tablet.css.dsp` 的 `${".z-page "}*{-webkit-user-select:none}`
  特異性 (0,1,0),P4b 改動後與 `user-select: text` **打平**,勝負取決於樣式表順序。
- **S36** —— P7 驗收要順手把 `readme.md` 的兩個 compact 旋鈕收成一個。

**P7 的一項前置已經悄悄解除,但文件沒跟上**:計畫書 §P7 寫「tablet 需要 mobile UA 的
Playwright 專案」,而 `tasks/zk11-jar-baseline-visual-ab.md` 顯示 `sync-mobile` / `jar-mobile`
兩個 project 已存在並實測過(**0 頁差異**,fingerprint DIFFERENT ⇒ 非空轉)。
⇒ **裁示 L-4 之後,P7 沒有任何剩餘前置。**

### ③ D4 → D5 · ④ P8

D4 併在 P7 那一顆;D5 是收尾(移除 build 期旋鈕、改寫 `readme.md:45` 那一章、正式關閉 S36)。
P8 的 G-zero 核帳 = ~~**P4 + P5 + P7 三段**~~ **`P4a + P4b + D1/D2 + D4` 四段**已核准 delta
的總和,不多不少(**←2026-08-14 已更正,見下方 L2.6 缺口 1 與計畫書附錄 C21**)。

---

## L2.3 未決事項 —— 分成「擋著」與「不擋」

### 擋著的:只有 1 項

| # | 未決 | 誰要裁 | 影響 |
|---|---|---|---|
| **1** | **L-4 整體採納** | 使用者 | **擋住 P7 → D4 → D5 → P8,亦即整案剩餘全部** |

### 不擋,但沒有排程也沒有人裁的:8 項

| # | 未決 | 性質 | 現況 |
|---|---|---|---|
| 2 | **`git merge -s ours origin/master`** | 版控 | `tasks/iceblue-resync-with-master.md`(今天寫)已做完逐 commit 稽核並附指令與 commit message 範本,**尚未執行**(merge-base 仍是 `a89d44e0`)。等 go/no-go |
| 3 | **FA 裸別名 `.fas/.fass/.far/.fab` 要不要跟著 upstream 刪** | G-delta | upstream 已刪,本分支的 `baseline/`、ZK 11 jar baseline、輸出**三者都還有**。要跟進得改 `gen-fa-css.js` **並重新 freeze `baseline/`**(會改變 byte gate 期望值)⇒ 自己走一次 G-delta。**沒有排程** |
| 4 | **L2.4 第 5 項**(`combo` 586 → **191**,L-8 的 B 案) | G-delta | 原本寫「等 P4」,**P4 已於 08-10 DONE** ⇒ 現在可動,但**沒排程、沒核准 delta**。要不要在 P7 之前做、還是併進 P8,是個決定 |
| 5 | **L2.4 第 7 項**(14 處重複/矛盾宣告,含 `tabbox` 的 `float` 死碼) | G-delta | 同上 |
| 6 | ~~**S53 —— cropper `touch-action` 的 ZK 產品端開單**~~ **←已結案** | 對外動作 | **2026-08-14 已開單:[ZK-6147](https://zkoss.atlassian.net/browse/ZK-6147)**(`ZK` / Bug / Affects **11.0.0**) |
| 7 | **L2.5 三個產品面問題** | 產品 | (a) 別的主題會不會填 `#footer().append-style()` hook;(b) 4 個死路徑輸出要不要刪(輸出檔 85 → 81,G-delta);(c) `tbeditor` 版本對齊(已證明是 (b) 的子集)。**都排在 P8,都還沒答案** |
| 8 | **S31 —— 版號四處一致性沒有腳本守著** | 工程債 | 建議排 P8,未排 |
| 9 | **Theme Pack 產品定位** | 產品 | `tasks/theme-pack-productization-options.md` 的 **D1–D5 待裁示**。**不屬本案**,但它的結論會改寫 `theme-pack-palette-mechanism.md` 的交付物歸屬 |
| 10 | **M-1 —— Marble `COMFORTABLE` → `DEFAULT`** | 跨 worktree | 已裁示「要改,但不是現在」。觸發條件 (a) 已於 08-13 排除 ⇒ **只剩 (b) Marble 首次公開發行前**。不在本 worktree 執行 |

---

## L2.4 順手撿到的一件文件債(與 S55 同類)—— **已於 2026-08-14 修正,記為 S56**

> **修正結果**:主線兩份文件描述**現況**的 5 處全部補上更正標記;
> **L3-A 閘門紀錄與 S 表共 13 處的 728 一律未動**(附加式紀律);
> 新增 **S56** 到 L3-I;順帶修掉三個過期的索引計數(L3-A 55/57 → **58**、L3-I 53 → **56**)。
> `npm run check:doc-refs` 通過(41 檔 / 139 條連結)。
>
> **量到的比原本以為的更多**:除了 728 → 731,還有 `-webkit-` 313 → **314**,
> 以及儀器原始差異 **749 → 1104 筆** —— 後者是因為 **D1/D2 成為第三個已核准 delta**
> 而不是前綴政策,`check:cssdiff` 不做判斷,所以密度覆寫塊也算進原始差異。

**`check:p4a` 現在實測 731 條,但主計畫書與進度文件仍有 23 處寫 728**
(進度 6 · 計畫書 4 · 進度附錄 13)。

- **數字本身沒有錯,理由也已經記下來** —— `tasks/zk11-jar-baseline-visual-ab.md:267`:
  ZK 11.0.0 同步讓 `calendar` 多一個區塊,其 `.borderRadius()` 展開**正好貢獻 3 條**
  新符合資格的移除,兩處獨立宣告**各自**算出 731。**這是 tripwire 正常運作,不是遷就。**
  檔案數維持 45。
- **但主線文件沒有任何更新標記** ⇒ 讀 L1/L2 的人會拿到 728,跑閘門會看到 731。
  這與 08-13 剛修掉的 **S55**(里程碑 2/5 vs 3/5 自相矛盾 6 天)**是同一類**:敘述過期,不是量測改變。
- **正確做法不是全域取代**:L3-A 閘門紀錄裡的 728 是 **P4a 收工當時**跑出來的數字,
  依該表「附加式不覆寫」的規則**不能改**。要改的只有 L1/L2 描述**現況**的那幾處,
  外加一條 Change Log(建議編 **S56**)。

---

## L2.5 P4 的延後項與未排程項目(2026-08-14 補列)

> 本節回答「**P4 有沒有延後沒做的事**」。分成四類:**已移交 P7(有主、有帳)**、
> **明確不做但沒有階段歸屬**、**因 P4 完工而解鎖卻沒排程**、**已裁示但未執行的對外動作**。
> 第五類「刻意保留」列出來是為了**避免被誤當成待辦**。

### (A) 已移交 P7 —— 有主、有帳、有腳本擋著

| # | 內容 | 量 | 由誰擋住 | 驗收 |
|---|---|---|---|---|
| **A1** | P4a 讓出的前綴(`border-radius` 24 / `box-shadow` 12 / `box-orient` 12 / `box-flex` 9 / `background-size` 3) | **60** 條 | `scripts/p4a-delta.js:63` 的 `DEFERRED` set;`check:p4a` 印 `deferred to P7: 60` | 同 P4a —— 移除後每條在同 rule 內都要有無前綴同伴,`-webkit-` 不動 |
| **A2** | P4b 讓出的 orphan `-moz-appearance: none` | **1** 條 | `check:p4b` 印 `deferred to P7: 1 orphan` | 同 P4b —— 純移除 or 改名,擇「現代瀏覽器行為不變」的一邊 |
| **A3** | **S52 第 (2) 項** —— `tablet.css.dsp` 的 `${".z-page "}*{-webkit-user-select:none}` 特異性 (0,1,0),P4b 改名後與 `user-select: text` **打平**,勝負取決於樣式表順序 | 1 處 | **沒有腳本擋**,只寫在 S52 | P7 動 tablet 時必須**顯式**決定順序,不能靠現況碰巧成立 |

**帳目已結清**:P4b 第 4 層驗證算出「可移除前綴 **803 → 61**,剩下 61 條**全部**在 P7 holdout」,
而 **A1 + A2 = 61** ✓。**A3 不在那 61 條裡**(它是特異性問題,不是移除標的),所以它是
三項裡**唯一沒有任何自動化盯著**的一項。

### (B) 明確「不做」,但**沒有排進任何階段** —— 需要一個決定

P4 的普查是按「**屬性**帶不帶前綴」做的,所以以下三類它**結構上看不到**。
計畫書 §P4b 只寫「列為後續議題」「明確不做」,**沒有階段歸屬、沒有人裁、沒有核准 delta**:

| # | 殘留 | 量 |
|---|---|---|
| **B1** | 無前綴的 `zoom: 1` —— 同樣是死掉的 IE hasLayout hack,只是沒有前綴 | **22** 條 |
| **B2** | 前綴在**值**上:`display: -ms-flexbox` / `-webkit-box` / `-moz-box` / `-ms-inline-flexbox` | **13** 條 |
| **B3** | 前綴 **pseudo selector**:`::-moz-placeholder`、`:-ms-input-placeholder`、`::-ms-check` … | 約 **79** 處 |

**兩個選項,現況是兩個都沒做**:① 併進 P8 走一次自己的 G-delta;② 明文寫成 won't-do 並進 migration guide。
它目前只存在於 §P4b 的一則引言裡,**不在任何待辦表上**。

> **B2 已經有一個「該一起處理」的理由被記下來了**:S52 第 (3) 項指出 P4b 拿掉 `-ms-flex-align`
> 之後,同 block 仍留 `display: -ms-flexbox`,是**半套 IE10 fallback**,「應與 B2 一起收而不是分兩次」。

### (C) 因 P4 完工而解鎖、但沒排程的兩項舊待辦(進度文件 L2.4)

| # | 內容 | 閘門 | 現況 |
|---|---|---|---|
| **C1** | 第 5 項 —— `combo` 的 6× 重複宣告併成原生選擇器清單,**586 → 191** 條(−67%) | G-delta | 原寫「等 P4」;**P4 已於 08-10 DONE** ⇒ 可動,但**沒排程、沒核准 delta** |
| **C2** | 第 7 項 —— 重複/矛盾宣告 **14 處**(含 `tabbox.css` 的 `float: left` 死碼) | G-delta | 同上 |

> **C1 與 P7 有耦合,建議在 P7 開工前一起決定。** `tablet/compact/_combo.less:27,36` 與 `combo`
> **同形狀**,已拍板歸在同一項(**L-8**)。**若 P7 先轉完 tablet 再回頭做 C1,同一個決策會被切成
> 兩段 delta**,P8 核帳要對兩次 —— 這是可以現在花零成本避免的。

### (D) 已裁示但未執行的對外動作

| # | 內容 | 現況 |
|---|---|---|
| **D-a** | **S53** —— cropper `touch-action` 交 ZK 產品端開單 | ~~已裁示「P4b 維持純移除、該問題另開議題交 cropper 負責人」,但**單還沒開**~~ **←2026-08-14 已開單並結案:[ZK-6147](https://zkoss.atlassian.net/browse/ZK-6147)**(`ZK` / Bug / Affects **11.0.0**)。**本案自此對 P4 沒有任何未執行的動作** |

### (E) 刻意保留,**不是欠帳**(列出以免被誤讀成待辦)

- **B 群 44 條** —— `-webkit-font-smoothing` 16、`-moz-osx-font-smoothing` 16、
  `-webkit-touch-callout` 6、`-webkit-tap-highlight-color` 4、`-webkit-user-drag` 1、
  `-webkit-user-modify` 1。**從來沒有標準化過**,移除等於刪功能。
- **`-webkit-` 314 條** —— L-2 選項 C 拍板全數保留,閘門逐次斷言 `314 = 314`。

---

## L2.6 本次查核新發現的兩個帳目缺口(都不在 L2.3 的 10 項裡)

### 缺口 1:**P8 的核帳公式漏掉 D1/D2 的 352 條** —— 照現在的寫法,P8 過不了

> **✅ 已於 2026-08-14 修完(拍板後執行)。** 兩半都做了:
> ① **公式**改為 **`P4a + P4b + D1/D2 + D4`**,9 處活的敘述全部更新(原文以刪除線保留),
> 歷史列 **S48** 與 `tasks/l2-browser-support-analysis.md:168` 依附加式紀律**不動**;
> ② **L3-A 補上缺的 D1/D2 那一列 = 紀錄 #59**(`1` 檔 / `0 移除 / 352 新增`),
> 日期寫**補記日 2026-08-14**、量測當天重跑,**不回填**。
> 規範層紀錄:計畫書附錄 **C21**。數字是重量的:`check:gate` **exit 0**、
> `cssdiff --max-diff 5000` = 48 檔 / 1104 筆 = **745 移除 + 359 新增**,
> 按檔拆 `+` 得 norm **355** / slider **2** / pdfviewer **2**,扣掉 P4b 的 7 條改名 ⇒
> norm 剩 **352 = 350**(`data-density`,實測 grep)**+ 2**(DSP `<c:if>` 開關)。

- **公式現況**:**8 處**文件都寫「差異必須**完全等於 P4 + P5 + P7 三階段**已核准的 delta 總和,不多不少」
  —— 計畫書 `:198` / `:509` / `:549`、附錄 `:1433` / `:1764`、進度 `:264`、進度附錄 `:53` / `:992`,
  另加 `tasks/utility-css-vs-p7-ordering.md:33`。
- **與實測不符**:**S56(今天才記的)自己就量出**儀器原始差異 = **1104 筆 = 745 移除 + 359 新增**,
  其中 **D1 的 350 + D2 的 2 = 352 條新增**,而 S56 明寫 D1/D2 是「**第三個已核准 delta**」。
- **同時 P5 貢獻 0** —— 它不採 `@scope`,收在 **G-zero**(S35)。
  **公式點名了一個貢獻零的階段,卻漏掉一個貢獻 352 的。**
- **核帳的資料來源也缺**:計畫書 §P8 寫「核帳資料來自進度文件 **L3-A〈閘門紀錄〉**」,
  但 L3-A 目前 **58 列,最後一列 #58 是 2026-08-12 的改名**,
  **沒有任何一列記 D1/D2 的 +350 / +2**。D1–D3 的驗收證據在
  `tasks/l4-density-mechanism.md` L3.5,**不在 L3-A**。
- **正確公式**應為 **P4a + P4b + D1/D2 + P7(含 D4、A1、A2)**,其中 **P5 = 0**。
- **為什麼現在就要修**:**P7 會加上第四段 delta**。公式錯著不改,P8 核帳會**恰好差 352 條新增**,
  而那個時點最容易的解釋是「P7 做錯了」—— 這正是本案一路在防的那種誤判。

### 缺口 2:S56 的更正只掃了 `doc/`,`scripts/` 的註解還停在 728

`scripts/*.js` 有 **8 處**把 **728** 當**現值**敘述(散在 6 個檔)。最該修的是
`scripts/p4a-delta.js:34,36` —— 整段小標寫「**WHY 728 IS DECLARED TWICE**」、內文寫
「`--expect 728` on `check:p4a` in package.json」,而 **`package.json:30` 實際是 `--expect 731`**、
`p4a-delta.js:74` 的 `EXPECTED_REMOVALS` 也是 **731**。

| 檔 | 行 | 備註 |
|---|---|---|
| `scripts/p4a-delta.js` | 8、21、34、36 | 4 處;**`:65` 是唯一已更新的一處**(「728 until the ZK 11 sync (2026-08-13), then 731」) |
| `scripts/check-p4a-delta.js` | 10 | 1 處 |
| `scripts/density-delta.js` | 9、18 | 2 處(`:18` 是史實敘述,依附加式紀律可留) |
| `scripts/check-bytes.js` | 21 | 1 處 |
| `scripts/check-build-css.js` | 64 | 1 處 |
| `scripts/p4b-delta.js` | 9 | 1 處 |

**分類與 S56 完全相同**:**敘述過期,不是量測改變**。修法也應相同 ——
只改描述**現況**的句子,史實敘述(「P4a 當時移除了 728 條」)不動。
**這一則值得單獨記一條 Change Log**,因為它證明 S55 / S56 那條反覆出現的病
(散文與量測脫節)**在 `scripts/` 註解裡有一個沒人掃過的第二現場**。

---

## L2.7 下一步(更新後)

```
① P7(tablet + @themeProfile + D4 + A1 + A2 + A3)  ──►  ② D5  ──►  ③ P8
```

- **P7 沒有任何剩餘前置**:L-4 已於今日拍板(**S57**);mobile UA 的 Playwright 專案
  (`sync-mobile` / `jar-mobile`)已存在並實測 **0 頁差異且兩側 fingerprint DIFFERENT**。
- ~~**開工前建議先做三件便宜的事**~~ **←三件都已於 2026-08-14 結案**(都沒動 CSS,`check:gate` 仍 exit 0):
  1. ✅ 修 **缺口 1** 的核帳公式 + 補紀錄 **#59**(規範層 **C21**);
  2. ✅ 修 **缺口 2** 的 `scripts/` 註解(9 處 `728` → `731`);
  3. ✅ **C1 不與 P7 同批** —— 隨第一期範圍封閉延後(**C20**),P7 走 L-8 的 A 案逐字轉,
     **這個決策問題本身消失了**,不需要再決定一次。
- **P7 的真正風險是 D4 的 67 條中和**,不是 tablet 轉檔本身。驗收是那 67 條**全部**對
  `iceblue_c 11.0.0` 做 mobile UA computed-style 比對全綠,**不接受抽驗**。

---

## L2.8 「第一期只證明轉換,CSS 移除一律只記錄不做」——**已於 2026-08-14 拍板**

> **提案原文**:「P4 如果是要移除 CSS 部分,我建議先都不做,只記錄下來。因為我們目前第一期的
> 目標,是先確定 CSS 內容能夠正確從 LESS 被轉換。其他的變動都儘量減少,等下一階段或未來再做。」
>
> **裁示(2026-08-14,user)**:
> **① 採用本提案** —— A、B、C 三項一律「只記錄,不做」。
> **② keep-vs-revert 選 A** —— 「**已經移除了就維持,其他的就不要再改了**」⇒
> P4a / P4b 維持現狀**不回退**,且第一期**不再新增任何移除**。
> **③ 承重項不受影響** —— D1 / D2 / D4 與 `@themeProfile` 退場仍在第一期(見下方〈承重 vs 選配〉)。
>
> **⇒ 第一期的 CSS 變更範圍自此封閉:P4a + P4b(已完工)+ D1/D2(已完工)+ D4(隨 P7)。**
> **除此之外不再有任何一條宣告被增刪。**
>
> 本節以下的評估是拍板前寫的,原文保留作為「當時憑什麼這樣建議」的紀錄。

### 先劃一條線:哪些 delta 是**承重**的,哪些是**選配清理**

提案的精神是「第一期 = 只做為了脫離 LESS 而**非做不可**的事」。照這條線分類,結論很乾淨:

| 類別 | 內容 | 能不能延後 |
|---|---|---|
| **承重**(不做就脫不了 LESS) | **D1 / D2**(`data-density` runtime 覆寫)、**D4**(tablet 密度層)、P7 的 `@themeProfile` 退場 | **不能** —— `@themeProfile` 是 LESS 變數,P8 讓 `.less` 歸零之後那個旋鈕就不存在了。L-4 已拍板 `iceblue_c` 不再出貨,所以替代機制必須同期到位 |
| **選配清理**(不做也能脫離 LESS) | **P4a / P4b**(已完工)、**B1–B3**、**C1 / C2** | **能** —— 這正是提案要延後的那一類 |

> **這條線要寫進文件,否則會被誤讀。** 「其他的變動都儘量減少」若被外推到 D1/D2/D4,
> 第一期就交付不出可用的 compact,而那是 L-4 已經拍板的對外承諾。

### 提案對 A / B / C / D 四項的實際效果

| 項 | 是否被關掉 | 說明 |
|---|---|---|
| **A1**(tablet 60 條前綴) | ✅ **關掉** | 不移除,只記錄。`scripts/p4a-delta.js:63` 的 `DEFERRED` 由「暫緩到 P7」變成「第一期不做」,`EXPECTED_REMOVALS` 維持 **731** 不動 —— **腳本一行都不用改**,只要改註解的措辭 |
| **A2**(1 條 orphan `-moz-appearance`) | ✅ **關掉** | 同上,`check:p4b` 的 `deferred to P7: 1 orphan` 改為「第一期不做」 |
| **A3**(S52 的特異性打平) | ✅ **關掉,但理由不是「不做移除」** | **實測**:`.z-focus-a` 在 `zul/css/norm.css:539`,競爭者 `${".z-page "}*{-webkit-user-select:none}` 來自 `zkmax/less/tablet/default/_norm.less:7` ⇒ **兩者在不同的輸出檔**,勝負由 `norm.css.dsp` 與 `tablet.css.dsp` 的**載入順序**決定,不是單檔內的來源順序。P7 若是 G-zero 純轉換,**tablet 輸出逐 byte 不變 ⇒ 載入順序不動 ⇒ 這個打平維持現狀**。它是 P4b 收工當下就已經出貨的既成狀態,**不是 P7 會製造的新風險** ⇒ 降級為「記錄」 |
| **B1–B3**(~114 處殘留) | ✅ **完全關掉** | 本來就是「明確不做」,缺的只是一個歸屬。提案給了它歸屬:**第一期 won't-do**,寫進 migration guide |
| **C1**(`combo` 586 → 191) | ✅ **關掉,並順帶消掉一個排程決策** | C1 不是瀏覽器支援問題,是**來源可讀性重構**。延後之後,P7 的 `tablet/compact/_combo.less` 就照 **L-8 的 A 案逐字轉**(與 P3 對 `combo` 的做法一致)⇒ 上一輪提的「C1 要不要與 P7 同批」**這個問題本身消失了** |
| **C2**(14 處重複/矛盾宣告) | ✅ **關掉** | 同 C1,延後 |
| **D-a**(S53 cropper 開單) | ❌ **沒有關掉** | 它**不是本 repo 的 CSS 變更**,是對 ZK 產品端的**對外動作**。做或不做都不影響第一期的任何 byte ⇒ 提案管不到它,仍需一個 yes/no |

**小計:A、B、C 三項全部關掉(A3 是以「既成狀態」而非「延後」的方式關掉);D 不受影響。**

### 提案**強制**帶出的一個新決策:已完工的 P4a / P4b 要不要回退?

**P4a(2026-08-07)與 P4b(2026-08-10)已經完工並進版控**,所以「先都不做」對它們而言
不是「不做」,而是「**要不要回退**」。這是提案沒有明講、但邏輯上躲不掉的一題:

| 選項 | 作法 | 代價 / 得失 |
|---|---|---|
| **【A】保留(建議)** | P4a / P4b 維持現狀,提案只適用於**尚未動手**的 A、B、C | **代價 ≈ 0**。兩階都已通過 G-delta 閘門、第 4 層獨立驗證與負向控制,帳目已結清。第一期的說法變成「**除了兩段已核准且逐條可推導的 delta 之外,全樹零差異**」—— 仍然是強主張,而且 `check:p4a` / `check:p4b` 逐次證明它 |
| **【B】回退** | revert 745 移除 + 7 新增(48 檔),退掉 `check:p4a` / `check:p4b`,閘門回到 `files differing: 0` | 第一期的說法變成「**全樹逐 byte 零差異**」,最強。**但代價很實在**:(1) 回退本身是動 48 檔的高風險變更;(2) 丟掉已驗證的 delta-aware 機制 —— 而 **D1/D2 仍然需要它**(`density-delta.js` 走的是同一套技法),所以機制不會因此消失,只是少一個使用者;(3) P4b 的 7 條改名是**經證明行為中性**的修正(Firefox 今天確實吃 `-moz-appearance`),回退等於把它們放回去 |

**建議 A 的理由一句話**:提案要保護的是「**轉換正確性**」這個命題,而 P4a / P4b **不在那個命題上** ——
它們的差異是**獨立記帳、逐條可推導、有專屬閘門**的,不會污染「LESS → CSS 轉換是否正確」的訊號。
回退換來的是敘述更漂亮,不是證據更強。

### 照提案(選項 A)執行之後,P7 長什麼樣

| # | 原本 | 提案後 |
|---|---|---|
| 1 | `tablet.less` → `.css`(681 條) | **不變,但閘門從 G-delta 變 G-zero** —— 逐 byte 相同,和 P3 的 74 檔同一種驗收 |
| 2 | + A1 的 60 條移除 | **移除** |
| 3 | + A2 的 1 條 orphan | **移除** |
| 4 | + `_combo.less` 走 L-8 B 案 | **改走 A 案逐字轉** |
| 5 | + **D4**(compact 平板層,67 條逐條中和) | **不變 —— 這是承重的,留在 P7** |

⇒ **P7 從「一次背四段 delta」縮成「一段 G-zero 轉換 + 一段 D4 的 G-delta」**,
而且那段 G-zero 轉換與 P3 已經做過 74 次的事情**完全同形**。

> **提案沒有降低 P7 的真正風險。** 上一輪已經指出:P7 的風險是 **D4 的 67 條中和**,不是 tablet 轉檔。
> D4 是承重項,不在提案的延後範圍內,驗收條件(67 條**全部**對 `iceblue_c 11.0.0` 做
> mobile UA computed-style 比對全綠,不接受抽驗)**一個字都不變**。

### 對 L2.6 兩個缺口的影響

- **缺口 1(P8 核帳公式)**:**沒有被修掉,但正確答案變簡單了**。照選項 A,公式應為
  **`P4a + P4b + D1/D2 + D4`**(P5 = 0、P7 的轉換 = 0);照選項 B 則是 **`D1/D2 + D4`**。
  **兩種都不是現在文件寫的「P4 + P5 + P7」** ⇒ 這一項仍要修,而且**要等本提案拍板才知道改成哪個**。
- **缺口 2(`scripts/` 註解的 728)**:**完全不受影響**,可以立刻修。

---

## 附:本次查核用到的指令

```bash
cd /Users/hawk/Documents/workspace/zkThemeTemplate-iceblue
npm run check:gate                       # exit 0
git merge-base HEAD origin/master        # a89d44e0(尚未 sync)
git log --oneline HEAD..origin/master    # 6 commits
find src/main/resources/web -name '*.less' | wc -l   # 58
```

### 第二輪(L2.5–L2.7 的依據,同日稍晚)

```bash
npm run check:gate                       # exit 0 —— P4a 731 / -webkit- 314 = 314 /
                                         #          P4b 14 移除 · 7 新增 · 9 檔 /
                                         #          deferred to P7: 60 + 1 orphan
grep -rn "728" scripts/*.js package.json # 8 處註解仍寫 728;package.json:30 實際是 --expect 731
grep -rn "P4 + P5 + P7" doc/ tasks/      # 8 處核帳公式,均未含 D1/D2
grep -n "紀錄 #5[5-9]" doc/iceblue-drop-less-progress-appendix.md   # L3-A 最後一列是 #58(2026-08-12 改名)
```
